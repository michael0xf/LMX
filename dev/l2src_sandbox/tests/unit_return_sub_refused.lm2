# `return: s` with s a sub: a callable without a result has no value to return -- refused.  (It
# emitted a call line at column 0, which L1 rejected.)
int: y 0

sub: s ()
    y: 3
end: s

fn: w () int
    int: k 1
    return: s
end: w

return: w()
