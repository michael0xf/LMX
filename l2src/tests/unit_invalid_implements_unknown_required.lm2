# Adapted from lingvamyxa_prev/tests/trans_invalid_implements_unknown_required.lm2.
# Trap: consumer is MissingProtocol too, so diagnosis is unknown-consumer first.

KnownCandidate:
    size_t: knownField 0U
end: KnownCandidate

if: implements(KnownCandidate, MissingProtocol, MissingProtocol) != 0
    return: 1
return: 0
