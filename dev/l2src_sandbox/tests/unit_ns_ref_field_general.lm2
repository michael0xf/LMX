# FABLE-SONNET-SEND-REF-20260925-172 commit 1: the general reference field in
# a named Structure body, `@: T name` -- the same pointee vocabulary a formal
# or local pointer accepts (a core word or another named Structure), not a
# Message-only kind. Holder declares one of each: @: int p (a core word) and
# @: Model q (another named Structure), proving both spellings are ADMITTED.
# p round-trips a live address at runtime (non-null after assignment,
# pointer-identical to the address taken).
# D-53: q round-trips too.  An own Structure field is a slot holding the
# Structure itself (-189 c3b-3), so `@mo` is that reference, the slot's value:
# h\q takes it, is non-null and identical to `@mo`.  (A field read THROUGH
# h\q, `h\q\value`, is D-76, not this witness.)  Success is 7.
Model:
    size_t: value 1U
end: Model

Holder:
    @: int p
    @: Model q
end: Holder

fn: check () int
    Holder: h
    Model: mo
    int: n
    @: int pn
    n: 5
    pn: @n
    h\p: pn
    if: h\p = 0
        return: 1
    if: h\p != pn
        return: 3
    h\q: @mo
    if: h\q = 0
        return: 4
    if: h\q != @mo
        return: 5
    if: h\q != mo
        return: 6
return: 7

sendMessage: exit(exit_code: check(); stdout: ""; stderr: "")
return
