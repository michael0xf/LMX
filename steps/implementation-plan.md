# Общий план Codex и Grok

Актуальное поручение автора от 2026-09-20. Этот файл — единый план дальнейшей работы; отчёты о выполненных исследованиях остаются отдельными.

## Принятое устройство

Единственный механизм допуска кандидатов: аналитический `implements` по используемым именованным путям дерева принимающего выражения, затем выполнение его юнит-тестов интерпретатором графа. Иного самостоятельного механизма семантической валидации не вводить. Позиционный `lmx_runtime_implements` признан автором невалидным и подлежит полной замене. Классификация адресов по таблице арены — инфраструктура типов, которую эта замена должна сохранить.

Нормативное описание: [L3 §7](../docs/LMX_semantics.ru.md#admission). Исследование старого кода и мест использования: [отчёт Codex](l3-semantics.md). Перенос исходников и L1/L2: [отчёт Grok](l1-l2-migration.md).

## Этапы и критерии завершения

Последовательность реализации: аналитический `implements` по дереву → **интерпретатор уже построенного графа** → валидация юнит-тестами через этот интерпретатор. Интерпретация исходного текста не является промежуточным этапом. Парсер строит граф; интерпретатор исполняет его структуры и связи.

| Этап | Ответственный | Состояние | Результат и проверка |
| --- | --- | --- | --- |
| 1. Перенос исходников и первое описание L1/L2 | Grok | Коммит df42882; полная конформность не заявлена | 16 файлов l1src и 511 файлов sandbox l2src; происхождение зафиксировано; расхождение двух исходных копий остаётся явным |
| 2. Единая норма валидации RU/EN | Codex | Выполняется | Все актуальные параграфы допуска L1/L2/L3 ссылаются на один механизм; старый обход описан только как дефект реализации |
| 3. Контракт входов аналитического implements | Grok; Codex сверяет семантику | Назначение подготовлено, доставка Grok пока блокируется его активным ходом | Найти дерево Consumer и доступное представление именованных путей кандидата; определить подключение к транслятору без выдуманных runtime-таблиц и новых полей узла |
| 4. Полная замена аналитического implements | Grok после этапа 3 | Ожидает | Обход дерева и вложенных именованных путей; перестановка различных полей не влияет; отсутствие нужного вложенного поля отклоняется; неиспользуемые поля не требуются; нет возврата к сравнению всего плоского дескриптора |
| 5. Интерпретатор графа | Распределить после контракта исполнения | Запланирован | Исполняет тела на графе с согласованными правилами вызова и состояния; интерфейс тестов уточняется по модели, не угадывается |
| 6. Юнит-тесты как runtime-валидация | Поверх интерпретатора этапа 5 | Запланирован | Тесты заданы принимающим выражением и исполняются над кандидатом; успешный аналитический ответ сам по себе не завершает допуск |
| 7. Подключение допуска и регрессия | Grok + Codex | После этапов 4–6 | Проверки реальных мест допуска; сохранена классификация арены для почты/вызовов; старые self-tests позиционного алгоритма не считаются проверкой новой семантики |

## Координация

- Codex редактирует норму L3, согласует параграфы допуска в L1/L2 и фиксирует решения автора. Grok ведёт реализацию после принятия назначения, собственные тесты и состояние переноса.
- Обновлять статус этапа только по фактическому результату. Перед редактированием одних файлов согласовать владение через существующие каналы из [work_chat](../work_chat/README.md).
- Не перезаписывать чужой индекс Git или незакоммиченные изменения. Коммитить свои точные пути; после проверки документации делать push.
- При противоречии модели и кода фиксировать факт и задавать автору конкретный вопрос. Ограничения исторической реализации не превращать в требования языка.

## Codex: текущий результат

Исследован `lingvamyxa_prev/implementsTree.txt` и код `lm_trans_uses_*`. Старый P0-обход — основа исследования, но его fallback на плоский required-дескриптор не восстанавливать. Нынешний позиционный обход вызывается только self-test в проверенных деревьях. Обёртка `lmx_domain_kind` использует общий индекс арены; её инфраструктурное назначение необходимо сохранить.

## Grok: текущий результат

Перенос и первая редакция L1/L2 сохранены в df42882. Новое назначение LMX-PLAN-20260920 ещё не подтверждено: мост отказал в отправке во время незавершённого хода. Наличие этого плана само по себе не означает, что Grok уже получил новое поручение.
