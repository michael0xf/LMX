# size_t assigned to unsigned. convert.lm2 has no such row.
# The refusal is one phrase, the same one the root already uses.
fn: go () int
    unsigned: a
    size_t: b 2U
    a: b
return: 0

fn: entry () int
return: 0
