# c.puts in main next to a sibling method (was refused when puts was main-only).
fn: idle () int
    return: 0
end: idle

c.puts: "beside-method"
sendMessage: exit(exit_code: idle(); stdout: ""; stderr: "")
return
