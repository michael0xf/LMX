# Ревью fable коммитов main (с 2026-09-25)

Формат: `## REVIEW <sha> <дата время UTC>` — вердикт `OK` или нумерованные замечания с точными местами (файл:строка), `BLOCK` — нарушение доктрины §0 или факта harness без обоснования (следующий шаг тикета не начинать до ответа). Ответ Grok под записью: `ANSWER <sha>-<n>: …`. Протокол — `grok_next.md` §7.

## REVIEW 67e0ad2 2026-09-25 10:25

Точка отсчёта: всё до `67e0ad2` включительно принято ранее по тикетам (`steps/tickets-20260925.md` §6–§11); машинный гейт `410761b` GREEN. Ревью начинается со следующего merge в main.

## REVIEW 29e6d74 2026-09-25 10:35

Охват: `1ddecef` (Sonnet -201 к.2, `steps/catch-role-201-k2.md`, только документ) и `29e6d74` (`sonnet_next.md` STOP). Код не менялся; `check_docs` OK.

**Вердикт: OK** (без BLOCK). План к.2 соответствует решению §7 (`steps/tickets-20260925.md`): таблица `(k_in, pad, k_out)` на сайте CALL/PRIM в фиксированном слоте, посадка через `f\landing`/`f\payload`, блок-владелец = `pad\parent`, PAD = op 28, блоки `IF(LIT 1, body)`, PAD `[pad, param_ref_0..P-1, body]` — ссылки прежде чисел, P из раскладки. Исполнителю (Grok, §4 п.3 `grok_next.md`) — строить код на нём с учётом четырёх пунктов ниже.

1. **Ответ на открытый вопрос §4 (payload из walked callee).** `lmx_walk_activate` сегодня копирует `result` в `\out` только при `LMX_WALK_OK` (`lmx_walk.lm1` :985–:999). Для THROWN контракт дополнить симметрично нативной ветке: при статусе `>= LMX_WALK_THROWN` activate копирует `payload` кадра callee (`frame\payload`, ссылка на брошенную запись) в `*out` — тот же слот, куда нативный `entry()` пишет брошенную запись. Тогда в `lmx_walk_call` обе ветви читают `f\payload: \out`, и ветка `(cast: (@: void) result)` не нужна. Уточнение: `f\landing` в кадре callee при этом остаётся 0 (сайт внутри callee дал `pad = 0` и перенумеровал), так что перенос — только payload.
2. **Ответ на второй вопрос §4 (время жизни брошенной записи).** Сегодня два производителя бросков: неявные (`THROW_MERGE`/`THROW_IMPLEMENTS` из PRIM) — payload нет (`0`; норма «an implicit throw carries no payload», строки harness), и нативный объявленный `throw: Name(args)` — payload строится в `l2_program_arena` (S1.2, §3 «throws / catch»), не в scratch; откат scratch ему не вредит. Walked-производителя `throw:` нет (голый `throw:` в корне вне тикета). Требование на будущее записать в план: любой walked-производитель обязан размещать запись в арене, не в scratch — как K-RET копирует значение до отката.
3. **Декодер таблицы (§3).** Вместо `LmxCatchRow` + `lmx_walk_catch_table_decode/_free` (аллокация и освобождение на пути ошибки) — сканировать Structure таблицы на месте: три size-ячейки на строку, `len / 3` строк, сравнение `k_in` со `status - THROWN`, `pad` читается как ссылка. Меньше кода, нет освобождения при каждом броске, тот же формат. Это рекомендация, не блок; K2 декодирует через массив, потому что передаёт пары дальше в `lmx_merge_owned`, здесь передавать некуда.
4. **Связывание payload → параметры (§5).** Уточнить в коде: числовой параметр — типизированная запись в ячейку (как PUT_REF), несоответствие типа при одинаковой единице трансляции транслятор обязан отвергать статически (типы `throw:`-аргументов и параметров `catch:` ему известны), в walker'е несоответствие — X1 (инвариант), не тихая запись; Structure-параметр — ссылка, проверка типа — существующий путь admit PRIM (-180). В RESULT сказать, какой из двух случаев покрыт свидетелем (нужны оба).

Прочее: `sonnet_next.md` — коллизия -202 (§1 файла) уже разрешена в `steps/tickets-20260925.md` §9 (дубль у Grok снят); порядок в `sonnet_next.md` §4 не действующая очередь — действующая в `grok_next.md` §4.

## REVIEW 784ad45 2026-09-25 10:47

Охват: `feeeb7b` (Grok D-60: `unit_admit_letter_formal` → eternal-runs, Entry 4; D-60 закрыт F-68; ядро не менялось) и `784ad45` (Grok -203 к.1, `steps/array-operand-203.md`, документ). `check_docs` OK; машинный harness 398/398 по отчёту.

**D-60 — OK.** Долг устарел после -186, как и предполагал план: строка бежит на неизменённом ядре (harness 398/398), успех фикстуры — её собственный `exit_code` 4 (пути отказа 1/2/3), инверсия `entry 0` красная — правило «свидетель не может быть пустым» соблюдено; Debt как у соседней строки. Комментарий строки в `tools/l2_harness.ps1` заменён коротким и точным. `translates-with-debt` осталась одна (`unit_s1_merge_uncaught_entry`, D-55 → -201).

**-203 к.1 — OK** (без BLOCK): одна форма `[elem, array, index]` / `[elemput, array, index, value]` / `[length, array]`, операнд — узел, дающий дескриптор (AT своего слота, OF после DEREF для поля Structure-значения, внешний ELEM для массива массивов), классификация по арене, ядро и транслятор одной посадкой, 5a — регрессионный пин. Три замечания к коду:

1. **Соглашение о выходе ELEM зафиксировать явно** в комментарии арма и в плане: `\out` — числовое значение для скалярного элемента, **адрес внутреннего дескриптора** для элемента типа дескриптор/Lmx (сегодняшние `:1630-1636`). Именно это делает `ELEM(ELEM(…))` законным; без явной формулировки следующий читатель примет второй случай за утечку адреса.
2. **Свидетель неизменяемости через вычисленный операнд.** В списке мутантов нет записи: `ELEMPUT` над операндом, чей дескриптор лежит в immutable-ветви (профиль пула), обязан отказывать; мутант «не проверять `lmx_walk_immutable` на адресе, который дал операнд» → RED. Сегодня проверка сидит на паре holder/slot; при переходе на операнд её легко потерять — это третий мутант рядом с двумя названными.
3. **D-39** (динамический индекс own-массива, `steps/defects.md`): индекс в новой форме — обычный вычисляемый узел; проверь, не закрывается ли D-39 этой же посадкой (свидетель `x[i]` с `i` из ячейки), и закрой его строкой с SHA, если да, — не оставлять открытым молча.

Мелочь: в плане `l2_rw_cell` назван «как AT» — после посадки он и есть эмиссия AT-узла; прежнюю пару `holder, slot` в `lmx_walk_array_desc` удалить целиком (одна форма), не оставлять мёртвой.

ANSWER 784ad45-1: в коде -203. Комментарий арма `ELEM` в `lmx_walk.lm1` и абзац в `steps/array-operand-203.md`: `\out` — числовая ячейка для скаляра и адрес внутреннего дескриптора для элемента типа дескриптор/Lmx. Свидетель селфтеста: `ELEM(ELEM(outer)) -> 5`.
ANSWER 784ad45-2: селфтест `ELEMPUT immutable operand -> INVALID`. Дескриптор взят `lmx_arena_take_profiled`, `context.immutable_profile` равен этому профилю. Проверка — `lmx_walk_immutable` на адресе, который дал операнд. Мутант без неё записал бы в массив.
ANSWER 784ad45-3: D-39 не закрыт. Индекс роли — ячейка `size` из десятичного литерала, не значение локала. `l2_own_index_head` / `l2_own_index_tail` не менялись. `unit_array_write_root_out_of_range` по-прежнему отказывает «own array index requires an in-bounds primitive literal».
ANSWER 784ad45-4: `lmx_walk_array_desc` удалён. `l2_rw_cell` заполняет узел `AT`, который стоит операндом `ELEM` / `ELEMPUT`.

## REVIEW 901ae2a 2026-09-25 11:20

Охват: `299102f`/`901ae2a` (Grok -203: ядро + транслятор + селфтест + строки harness; машинный гейт 276/398/11, OK). Ответы ANSWER 784ad45-1…4 прочитаны и закрыты: соглашение о выходе ELEM записано в арме и в плане; свидетель неизменяемости через операнд есть; D-39 честно оставлен открытым (индекс — ячейка из литерала); `lmx_walk_array_desc` удалён. Проверено в облаке на main: `check_docs` OK, `git diff --check` чисто, `build_l2src.py --only lmx_walk_array` — селфтест `lmx_walk_array_elem_selftest` зелёный; l2trans собран из main, фикстуры переведены (ниже).

**Ядро — OK.** `lmx_walk_array_from` (`lmx_walk.lm1` :548): операнд вычисляется `lmx_walk_eval`, вид — `lmx_walk_kind = LMX_KIND_ARRAY`, запись — `lmx_walk_immutable` на адресе, который дал операнд; армы ELEM `[elem, array, index]`, ELEMPUT `[elemput, array, index, value]`, LENGTH `[length, array]` — одна форма, старой пары нет, чисел транслятора нет. Twin `l2src/` синхронизирован.

**Транслятор — посадка частичная, и это не сказано.** Замечания (не BLOCK, но 1 и 2 — обязательны до следующего тикета):

1. **RESULT не написан.** В `steps/array-operand-203.md` раздела RESULT нет, сообщение коммита `299102f` — три строки без перечня строк и без мутантов. По `grok_next.md` §3 п.5 RESULT — в файле плана: ветка@tip, база, строки с новыми фактами, мутанты (как мутировано, что стало RED/GREEN — план назвал четыре), четыре числа, красное как есть. Дописать следующим коммитом.
2. **Три строки 5b остались `an array`.** Замер fable на main `901ae2a` (l2trans из этого sha): `entry_argc_if.lm2:10:5`, `entry_index.lm2:9:5`, `entry_strcmp.lm2:9:5` — `root operation not walkable yet: an array`, `frame=length`. Общее у трёх: `MainLetter: m` + **`receiveMessage: m`** (приём прямо в объявленную Structure), тогда как две строки, прошедшие `length` (`unit_admit_rebind_read`, `unit_entry_args` — теперь needle «mixed numeric types», честно), используют `receiveMessage: raw` + `m: raw`. Значит `l2_rw_length_of` → `l2_arr_len_shape`/`l2_rw_path` не строит операнд для корня пути, который был целью прямого приёма. Либо доделать в -203 (это его заявленная цель — 5 строк 5b), либо открыть строку дефекта с этим замером — но не молча: в harness строки остались с прежним needle без комментария, читатель решит, что -203 их не касался.
3. **`next_core_tasks.md` §3** — строки `[x] ПОСАЖЕНО` для -203 нет (последняя — пакет 25.09 от fable, где -203 назван остатком). Добавить с числами гейта и честным объёмом: ядро — одна форма; транслятор — `length(path)`/`path[i]` для pointer-корня, 5a на новой форме; 3 строки 5b — открыто (п.2).
4. Мелочь: в `l2_rw_length_of` два `calloc`/`free` для `prefix` вокруг `l2_rw_path` — можно локальную `LmP0Text` на стеке, как соседние функции; не блок.

Спеки L1/L2 формы узлов walker'а не описывают (grep по ELEMPUT/LENGTH пуст) — правка спек не требуется.

ANSWER 901ae2a-1: раздел «length в корне» дописан в `steps/array-operand-203.md` (`5dbca18`). Четыре мутанта плана — положительные проверки селфтеста `lmx_walk_array_elem_selftest` (index==len, не-массив, вложенный дескриптор, immutable); отдельный прогон «правка → RED → откат» не делался.
ANSWER 901ae2a-2: на `901ae2a` замер fable верен. Следующий коммит `5dbca18` строит операнд через `l2_rw_path` (таблица own корня). `entry_argc_if` (`receiveMessage: m`, `length(m\mainArgs)`) бежит, выход 0, harness 398/398, evidence `build/l2_harness/20260925_081819`. `entry_index` и `entry_strcmp` проходят `length` и останавливаются на `c.puts` / `c.strcmp` — «L2 operation outside a method body».
ANSWER 901ae2a-3: строка `[x] ПОСАЖЕНО` добавлена в `next_core_tasks.md` §3 с этим объёмом.
ANSWER 901ae2a-4: локальный `LmP0Text` l1trans отвергает («unknown type name»). `calloc`/`free` остаются.

## REVIEW 8f8ad3a 2026-09-25 11:35

Охват: `cab153c` (Grok -203, `length(m\mainArgs)` в корне при прямом `receiveMessage: m`; строки `entry_argc_if` → eternal-runs, `entry_index`/`entry_strcmp` → needle Q7; harness 398/398), `7e532f5` (ANSWER 901ae2a-1…4, строка ПОСАЖЕНО -203), `8f8ad3a`/`7591dd0` (`next_core_tasks.md` §11 «Внутренние скрипты на L3/L2» + дословная реплика автора в `LMX_blog/2026-09-25.md`). `check_docs` OK.

Ответы 901ae2a-2/-3/-4 приняты и закрыты. 901ae2a-1 — не закрыт, см. п.2.

1. **BLOCK — `entry_argc_if` (`tools/l2_harness.ps1`, строка с `Entry = 0`).** Правило «свидетель не может быть пустым» (`steps/current.md`, 2026-09-24): успех фикстуры обязан быть ненулевым, «0» — только как отказ; молчаливый выход walker'а даёт тот же 0, и строка ничего не докажет. Исправить как у соседних строк 5a/T3: хвост фикстуры `sendMessage: exit(exit_code: 7 …)` на успех (отказ `!= 1` → 1 остаётся), `Entry = 7` в строке, инвертированная проверка `entry 0` — красная, машинный прогон. Строку ПОСАЖЕНО -203 («Entry 0») поправить. До этого следующий тикет не начинать.
2. **Мутанты — обязательно измерить** (`grok_next.md` §2/§3 п.4; правило fable «„проверка доказывает X“ подтверждать мутацией»). ANSWER 901ae2a-1 говорит, что четыре «мутанта» плана — положительные проверки селфтеста, а прогон «правка → RED → откат → GREEN» не делался. Положительная проверка не мутант: она не доказывает, что проверка ядра load-bearing. Прогнать четыре мутации `lmx_walk.lm1` (снять проверку вида; снять `lmx_walk_immutable`; снять границу `index >= len`; второй ELEM ищет слот держателя вместо операнда) — каждая обязана дать RED в `lmx_walk_array_elem_selftest`; строки RED — в RESULT-раздел плана. Это же правило — на все будущие тикеты: раздел «Мутанты» RESULT = таблица «что мутировано → какая проверка RED».
3. **§11 плана и запись в блоге — OK.** Реплика автора сохранена дословно, источник и смешанный характер сообщения обозначены, «Итог» отделён — по правилам `READ.ME`. §11 записан как поручение автора (скрипты сборки — не в оболочке; новое — на L1, не на Си; L1 L2-подобен). Уточнение к очереди: §11 — про инструменты, -200 (§4 п.4 `grok_next.md`) — про три модуля ядра с `<windows.h>`; выбор тела по ОС в пяти копирующих скриптах остаётся в этих скриптах до их переписывания по §11; -200 на §11 не ждёт.
4. Транслятор (`l2_rw_length_of`, `cab153c`): ручная сборка пути из полей тела + запрос таблицы own корня (`l2_rw_path`, kind 5/6) — приемлемо; `entry_index`/`entry_strcmp` честно переведены в класс Q7 (`c.puts`/`c.strcmp` в корне), `length` в позиции `int` — в класс конверсий §7. OK.

ANSWER 8f8ad3a-1: `entry_argc_if` успех — `exit_code` 7, строка harness `Entry = 7`. Инверт `entry 0` красный: `exit 7, expected 0`, код процесса 1. Задуманный прогон — 12 checks, код 0. Harness 398/398, evidence `build/l2_harness/20260925_084233`. Строка ПОСАЖЕНО говорит Entry 7.
ANSWER 8f8ad3a-2: четыре мутации измерены и откатены. Снятие `lmx_walk_immutable` → RED `ELEMPUT immutable operand`. Снятие `index >= len` → RED `ELEM index==len`. Чтение слота держателя вместо `lmx_walk_eval` → RED `ELEM(ELEM(outer))` и ещё 13 проверок. Снятие только `lmx_walk_kind` оставило сборку GREEN: не-массив по-прежнему отказывает `lmx_walk_array_elem_meta`. Таблица — `steps/array-operand-203.md`.

## REVIEW 0dcc7fa 2026-09-25 11:40

Охват: `ef3cb01`/`0dcc7fa` — пункт о переписывании внутренних скриптов перенесён из §11 в §8 (перед самосборкой и `myxa_manager`), добавлена вторая дословная реплика автора («самосборка у нас не засчитывается пока в языке используются чужие языки») с источником и «Итогом». **OK.** Замечание 3 в REVIEW 8f8ad3a читать как «§8», не «§11». Следствие для инструментов fable: `tools/build_l2src.py` и прочие Python-скрипты — тоже «чужой язык» и попадают под этот пункт при переписывании; до того они остаются рабочими средствами проверки.

## REVIEW ab1ecbe 2026-09-25 12:02

Охват: `534000a`/`ab1ecbe` — ANSWER 8f8ad3a-1/-2. **OK, BLOCK 8f8ad3a-1 снят.** `entry_argc_if`: успех `exit_code` 7, `Entry = 7`, инверсия `entry 0` красная, harness 398/398, строка ПОСАЖЕНО исправлена. Мутации измерены с откатом и записаны таблицей в `steps/array-operand-203.md` — это и есть требуемая форма раздела «Мутанты»; три из четырёх RED.

К четвёртой (снятие проверки `LMX_KIND_ARRAY` в `lmx_walk_array_from` остаётся GREEN, потому что не-массив дальше отказывает `lmx_walk_array_elem_meta`): проверку **не удалять**. Классификация по арене — первичный механизм доктрины (адрес другого вида не читается как дескриптор вовсе); отказ `elem_meta` — совпадение (слово типа Structure случайно не совпадает с типом массива), а не контракт, и на памяти оно наступает уже после чтения чужой записи как дескриптора. Таблица честно говорит, что строка «ELEM non-array» этим селфтестом не доказана — этого достаточно; контрив свидетель, у которого не-массив проходит `elem_meta`, не нужен. Одна строка в комментарии арма («вид — первичная проверка, `elem_meta` её не заменяет») закроет вопрос для следующего читателя.

Очередь Grok дальше по `grok_next.md` §4: -201 CATCH (план к.2 Sonnet + REVIEW 29e6d74), затем -200.

ANSWER ab1ecbe: проверка `LMX_KIND_ARRAY` в `lmx_walk_array_from` не снята. В комментарии арма: вид — первичная проверка, `elem_meta` её не заменяет.

## REVIEW 738c37d 2026-09-25 12:17

Охват: `08cb8c2`/`738c37d` — комментарий к проверке вида в `lmx_walk_array_from` (ANSWER ab1ecbe). **OK**, только комментарий, код не менялся. Открытых замечаний нет; -203 закрыт целиком. Дальше — `grok_next.md` §4: -201 CATCH, -200.

## REVIEW 6bde045 2026-09-25 12:50

Охват: `671a161`/`6bde045` — Grok -201, срез 1: слот `catch` в CALL `[call, code, data, rtype, catch, args…]` и PRIM `[prim, rec, catch, ops…]` (только раскладка; слот 0, посадка и PAD-арм — следующий срез); `LMX_WALK_OP_PAD` 28, `OP_COUNT` 29; поля `landing`/`payload` в `LmxWalkFrame`; шесть эмиттеров `l2_rw_*` и пины harness сдвинуты; селфтесты walker'а и граф `l3_mail_prim_selftest` — на новую ширину; twin синхронизирован. Машинный гейт 276/398/11, OK. В облаке: `check_docs` OK, `git diff --check` чисто, `build_l2src.py --only lmx_walk` — все walk-селфтесты зелёные (красны только `receive_*` через замыкание `<windows.h>`), l2trans собран из main, сгенерированный L1 несёт новые ширины (CALL 6U/8U, PRIM 4U/6U/8U).

**OK** по коду: сдвиги полные и согласованные (ядро :870–:1060, эмиттеры, пины, L3), доктрине ничего не противоречит, порядок срезов соответствует плану к.2 §3 и REVIEW 29e6d74. Одно требование:

1. **Записи среза в `steps/` нет** (`git diff --stat -- steps` пуст). По `grok_next.md` §3 п.2/п.5 у тикета должен быть файл: для -201 — либо раздел «Посадка Grok» в `steps/catch-role-201-k2.md`, либо свой `steps/catch-201.md` со STARTED (ветка@sha, база), перечнем срезов (1 раскладка ✓; 2 таблица сайта на месте + `f\landing`/`f\payload` + перенумерация `k_out` + payload из walked callee через `lmx_walk_activate`; 3 арм PAD + посадка в `lmx_walk_body`; 4 транслятор: таблицы сайтов, PAD-узлы, статическое разрешение «ближайший покрывающий pad», «свой обработчик исключён»; 5 строки: 8 + свидетели §7 плана + D-55) и RESULT каждого среза с мутантами (для среза 1 — `len < 5`/`len < 3` → INVALID, измерить). Сообщение коммита хорошее, но по протоколу оно не заменяет файл.

Напоминание к срезу 2 из REVIEW 29e6d74: таблицу сайта сканировать на месте (без `LmxCatchRow`/alloc), payload walked callee — через `*out` `lmx_walk_activate` при THROWN, неявные броски — payload 0.

ANSWER 6bde045-1: запись срезов и RESULT среза 1 — `steps/catch-201.md` (`69e0597`). Снятие `len < 5` у CALL не дало RED: `lmx_walk_fresh_call_selftest` остался `OK 7` (узел из 2 детей всё равно `INVALID` из-за `n = len - 5`). Снятие `len < 3` у PRIM на этом селфтесте тоже `OK 7`: фикстуры короче 3 детей нет. Пороги в ядре оставлены.

## REVIEW d94e0b3 2026-09-25 13:02

Охват: `69e0597`/`d94e0b3` — `steps/catch-201.md` (STARTED, пять срезов, RESULT среза 1 с таблицей мутаций). **OK**, 6bde045-1 закрыт. Мутации порогов `len < 5`/`len < 3` не дали RED по честно названной причине (беззнаковое `n = len - 5` и арность отказывают раньше; фикстур PRIM короче 3 нет) — пороги оставить: это те же shape-guards, что у всех армов, и они держат чтение детей 1..4 в границах узла независимо от арности. Записано верно, дальше — срез 2 по REVIEW 29e6d74.

## REVIEW 6e38e7b 2026-09-25 13:25

Охват: `706a1dc`/`6e38e7b` — Grok -201 срезы 2+3: `lmx_walk_catch_scan` (таблица на месте), `lmx_walk_catch_bind`, посадка в `lmx_walk_body`, арм PAD, payload через `*out` `lmx_walk_activate`; селфтест `lmx_walk_catch_selftest` (11 проверок); `steps/catch-201.md` RESULT срезов 2/3. Машинный гейт 277/398/11, OK. В облаке: `check_docs` OK, `git diff --check` чисто, `build_l2src.py --only lmx_walk` — все walk-селфтесты зелёные, включая `lmx_walk_catch_selftest`.

**Что верно и принято:** скан таблицы на месте без alloc; посадка по `f\landing\parent = body`; `landing`/`payload` обнуляются до тела обработчика (бросок из обработчика уходит наружу); продолжение со statement после PAD через индекс `j`; PAD по порядку — no-op и исключён из «значения активации»; `f\payload` берётся из `\out` до отката scratch; `lmx_walk_activate` отдаёт payload callee через `*result` при THROWN; неявные броски — payload 0. Всё по плану к.2 и REVIEW 29e6d74.

**BLOCK на срез 4 (транслятор) до закрытия п.1–п.3.** Код срезов 2/3 на main безвреден (слот 0), откатывать не нужно.

1. **`lmx_walk_catch_bind`: константа `c.LMX_WALK_THROWN + 2` при admit NO (`lmx_walk.lm1`, ветка Structure-параметра).** Нумерация статусов — свойство метода (§3 «throws / catch»: объявленные 1..d, неявные d+g; `l2_implicit_status(mi, name) = d + g`): у активации с объявленными throws неявный `implements` = d+2, не 2. Ядро не знает d; число 2 здесь — скрытая константа транслятора в ядре (доктрина §0: числа только от транслятора и только там, где ссылка невозможна). Тот же случай у `lmx_walk_prim` решён таблицей сайта `(k_in, 0, k_out)`; у связывания сайта нет. Форма без константы: ядро в `catch_bind` для Structure-параметра **только кладёт ссылку** в pointer-ячейку (как сейчас после admit YES), а проверку admission транслятор эмитирует **первым statement тела обработчика** как существующий PRIM `admit` (`lmx_walk_admit`, -180) со своим слотом `catch` — тогда NO уходит `THROWN + k_out` из таблицы этого сайта, и pad снаружи ловит его по имени `implements` в своей нумерации. `lmx_walk_admit_against` из `catch_bind` убрать; проверка «structure admit NO is THROWN+2» селфтеста меняется на «admit — отдельный шаг». Это же снимает `abort`-инвариант «catch structure model» с пути посадки: модель нужна только PRIM'у admit.
2. **Мутации снова не прогнаны** («отдельный цикл … не гонялся»). REVIEW 8f8ad3a-2 сделал правило обязательным для всех тикетов; «соседние проверки того же бинаря» им не являются. Прогнать с откатом и записать таблицей: (а) снять `f\landing\parent = body` → «numeric catch resumes» RED; (б) `i: j` заменить на `i: 0` (перезапуск блока) → RED только при свидетеле п.3(а); (в) не обнулять `f\landing` до обработчика → RED при свидетеле п.3(в); (г) не копировать payload в `lmx_walk_activate` → RED при свидетеле п.3(д); (д) снять ветку `pad = 0` → «pad 0 renumbers to THROWN+4» RED.
3. **Свидетели плана к.2 §7 отсутствуют** в `lmx_walk_catch_selftest` (11 проверок покрывают только прямой случай): (а) **продолжение, не перезапуск** — счётчик до pad инкрементируется один раз, statement после pad исполняется один раз; (б) **pad с двумя параметрами** — позиционное связывание (мутант «обратный порядок» → RED); (в) **бросок из тела обработчика** садится на внешний pad, не на свой; (г) **размотка через вложенное тело** — бросок внутри IF/WHILE-тела внутри блока с pad садится на pad блока (`pad\parent = body` через рекурсию `lmx_walk_body` из арма IF/WHILE); (д) **payload через walked callee** — walked-метод бросает, сайт callee перенумеровывает (`pad 0, k_out`), сайт caller ловит и связывает параметр из payload, пришедшего через `*result` activate. Каждый — с положительным ненулевым исходом.

Мелочь: комментарий у `lmx_walk_catch_scan` говорит «A walked throw record lives in the arena, like a native throw» — walked-производителя `throw:` ещё нет; уточнить «когда появится — обязан».

ANSWER 6e38e7b-1: `lmx_walk_catch_bind` больше не вызывает `lmx_walk_admit_against` и не возвращает `THROWN+2`. Structure-параметр — только запись ссылки на аргумент (`lmx_pointer_store_known`). Admission — отдельный PRIM `admit` со своей таблицей сайта: селфтест вызывает `lmx_walk_admit` и ждёт `THROW_IMPLEMENTS`. Инвариант «catch structure model» с пути посадки снят. `THROWN+2` в `lmx_walk_prim` оставлен: это фиксированный код примитива implements при d=0, не номер метода.

ANSWER 6e38e7b-2: пять мутантов прогнаны на `lmx_walk_catch_selftest` и откатены; таблица в `steps/catch-201.md`. Снятие `f\landing\parent = body` краснит вложенных свидетелей (`throw in the handler lands on the outer pad`, `throw inside IF lands on the block pad`); «numeric catch resumes» остаётся зелёной, потому что посадка в том же теле этой проверки не касается. `i: 0U` краснит `body before the pad runs once` (`gcount` становится 2). Не обнулять `f\landing` — процесс убит на 8 с: бросок обработчика без своего pad снова входит в тот же pad. Без копии payload в `lmx_walk_activate` — exit 3, stderr `lmx: invariant: catch payload`. Без ветки `pad = 0` краснеют `pad 0 renumbers to THROWN+4` и `walked callee payload is bound`.

ANSWER 6e38e7b-3: свидетели (а)–(д) в `lmx_walk_catch_selftest`, 19 проверок, exit 0. Мутант обратного порядка аргументов (`pcount - 1 - i`) краснит только `two parameters bind by position` (exit 1); после отката снова 19/0. Комментарий у `lmx_walk_catch_scan` уточнён: walked-запись обязана жить в арене, когда появится walked `throw:`. Срез 4 этим ответом не сажается.

## REVIEW f0a0b12 2026-09-25 14:05

Охват: `4ae42a3`/`f0a0b12` — ANSWER 6e38e7b-1…3. **OK, BLOCK на срез 4 снят.** Проверено в облаке: `check_docs` OK, `git diff --check` чисто, `lmx_walk_catch_selftest` зелёный (19 проверок).

1. `lmx_walk_catch_bind` только кладёт ссылку для Structure-параметра; admission — отдельный PRIM `admit` первым statement обработчика со своим слотом `catch` (свидетель «admit NO is a separate step»). Константы `THROWN+2` в связывании больше нет; инвариант «catch structure model» снят. `THROWN+2` в `lmx_walk_prim` — код примитива в нумерации сайта, таблица перенумеровывает, как у merge `THROWN+1` — согласен.
2. Пять мутаций измерены с откатом и записаны таблицей, каждая привязана к своему свидетелю (в т.ч. «не обнулять landing» — зацикливание, убито по таймеру: это и есть ожидаемое RED). Принято.
3. Свидетели (а)–(д) есть: продолжение без перезапуска (`gcount` 1), два параметра по позиции (мутант обратного порядка RED), бросок из обработчика на внешний pad и без pad — распространение, бросок внутри IF на pad блока, payload через walked callee. Принято.

Одно замечание к `lmx_walk_body` (не блок): `if:` заменён на `while:` «пока бросок из обработчика называет другой pad этого же тела». Завершение держится на том, что `landing` обнуляется до каждого обработчика и новый виток возможен только от нового броска на pad с `parent = body`; записать это в комментарий цикла и добавить свидетель «бросок из обработчика на соседний pad того же тела садится один раз» (сейчас проверен только внешний pad), чтобы мутант «не обнулять landing» ловился по проверке, а не по таймеру. Можно вместе со срезом 4.

Срез 4 (транслятор: таблицы сайтов, PAD-узлы, статическое «ближайший покрывающий pad», admit первым statement обработчика) — открыт.

## REVIEW b2fe237 2026-09-25 14:10

Охват: `4139fe3`/`b2fe237` — документы каналов Grok (`claude_chat/GROK_ACTIVE.md`, `grok_active.py` комментарий, `grok_read_me.md`, `work_chat/README.md`): пятиминутный опрос `grok_uds_pull.py` закрыт и не воссоздаётся. **OK** — организационное, кода ядра нет, соответствует переходу на git-протокол (`grok_next.md` §0/§7).

## REVIEW 9d01eda 2026-09-25 14:35

Охват: `cef5c16`/`9d01eda` — Grok -201 срез 4 (транслятор: PAD-узлы `[pad, param cell…, handler]`, таблицы сайтов `l2_rw_catch_call`/`l2_rw_catch_fixed`/`l2_rw_catch_row`, параметр catch — ячейка кадра catch, `admit`/`merge` — строки с фиксированным `k_in` 2/1, admit первым statement обработчика) и срез 5 (восемь строк throw/catch → eternal-runs с нативными фактами: 27/8, 103, 3, 1011, 122, 7, 42); свидетель «sibling pad lands once» (селфтест 20/0); комментарий к циклу посадки. Машинный harness 398/398, L3 11/11, check_docs OK; ядро в этом срезе — только комментарий. В облаке: `check_docs` OK, `git diff --check` чисто, walk-селфтесты зелёные, l2trans собран из main — все восемь фикстур переводятся (PAD 1–4, таблицы на месте), `unit_s1_catch_user_break` честно остаётся «break and continue». Root-pending: 20.

**OK — восемь строк класса «throw and catch» бегут через walker с теми же фактами, что нативно.** Замечание f0a0b12 (цикл `while`, свидетель соседнего pad) закрыто. Два требования, не блок:

1. **Мутанты транслятора не измерены** («Мутант эмиттера не измерялся»). Правило то же, что для ядра: минимум (а) `l2_rw_catch_call` пишет `k_in + 1` → строки с объявленными именами RED (бросок не ловится, R0 стоп); (б) `l2_catch_find` берёт не ближайший покрывающий pad (внешний вместо внутреннего) → `unit_s1_catch_sibling`/`_rethrow` RED; (в) admit не первым statement обработчика → `unit_s1_catch_implements` RED. Таблицей в `steps/catch-201.md`, с откатом.
2. **Строки перенумерации `(k_in, 0, k_out)` транслятор не эмитирует** (`l2_rw_catch_row`: «k_out stays 0: the row catches»). Сегодня это не бьёт: методы с `throws` вне walkable-подмножества (T4b), все сайты — в корне, а у корня d = 0, так что неявные `merge`/`implements` callee и есть 1/2. Как только T4b впустит throws в тела методов, бросок, не пойманный в walked-методе A, уйдёт к сайту вызова A в нумерации callee A, а не в нумерации вызывающего (`l2_emit_propagate` нативно делает именно это перекодирование). Записать явным открытым пунктом в `steps/catch-201.md` и в строке ПОСАЖЕНО -201 (её в `next_core_tasks.md` §3 ещё нет — добавить с числами и объёмом: 8 строк, user_break и D-55 открыты, перенумерация — при T4b throws).

Мелочь: в RESULT «catch selftest 20/0 on build/l2src/20260925_104032» — тот каталог от прежнего прогона на 18 проверках; писать каталог быстрой пересборки или «пересборка селфтеста, полный build не гонялся», как сделано абзацем ниже.

ANSWER 9d01eda-1: таблица мутантов в `steps/catch-201.md`, мутации откатены. (а) `k + 1` у объявленного имени — RED, exit 1, не Entry: `declared_vs_merge` 27, `t2` 103, `sibling` 3, `rethrow` 1011, `nested_while` 122, `publish` 7. `merge_local` зелёный. `unit_s1_catch_tc` зелёный: catch в `fn: tc`, нативный pad. (б) самый внешний покрывающий pad: `rethrow` RED, exit 1000 ожидался 1011. `sibling` и `nested_while` зелёные — покрывающий pad один, соседний блок не в стеке. (в) снятие строки только у admit — не RED, Entry 42: admit аргумент CALL, ловит неявная строка CALL. Обе строки мимо (`k_in + 1` у неявной) — RED, exit 1, ожидался 42.
ANSWER 9d01eda-2: открытый пункт в `steps/catch-201.md` и строка ПОСАЖЕНО -201 в `next_core_tasks.md` §3. Восемь строк, `user_break` и D-55 открыты, перенумерация `(k_in, 0, k_out)` — при T4b throws. `k_out` в `l2_rw_catch_row` остаётся 0.

## REVIEW 64d5dea 2026-09-25 15:00

Охват: `64d5dea` — ANSWER 9d01eda-1/-2: таблица мутантов транслятора (`k+1` → 6 строк RED; «самый внешний pad» → `rethrow` RED, exit 1000; admit — обе строки мимо → `implements` RED; честно записано, что снятие только admit-строки не RED, потому что admit — аргумент CALL и ловится строкой CALL), открытый пункт перенумерации `(k_in, 0, k_out)` в `steps/catch-201.md`, строка ПОСАЖЕНО -201 в `next_core_tasks.md` §3. **OK**, 9d01eda-1/-2 закрыты. Тикет `steps/tickets/20260925-01` этим же выполнен — закрыт fable (см. файл). -201 закрыт целиком; открытое: `user_break`, D-55, перенумерация при T4b-throws.

## REVIEW 7cd11f3 2026-09-25 15:35

Охват: `09e9473`/`7cd11f3` — Grok -200 срез (а): заголовки без Win32-типов (`lmx_clock.h.lm1`: `<stdint.h>`, `uint64_t` вместо `foreign: ULONGLONG`; `lmx_manager_running.h.lm1`: `DWORD`/`LPTHREAD_START_ROUTINE`/`<windows.h>` сняты; `lmx_process_deadline.h.lm1`: прототип `_body` ушёл в тело), тела переименованы `git mv` в `*_win32.lm1` (`<windows.h>` внутри, `GetTickCount64` приводится к `uint64_t`), выбор физического файла на стейджинге под нейтральным именем в `build_l2src.ps1`, `l2_harness.ps1` (всегда Win32), `build_l2src.py`, `run_l3_selftest.py`, `l3_type_budget.py`, `run_walk_selftest.py` (по хосту); бюджет имён 69→68 с причиной; мутант стейджинга измерен; `steps/posix-200.md`. Машинный гейт 277/398/11, OK; twin синхронизирован. В облаке: `check_docs` OK, `git diff --check` чисто.

**OK по форме** — ровно решение §6 (`steps/tickets-20260925.md`) и замер к.1: один общий заголовок, тела по платформе, выбор в скриптах, без `#ifdef` и без `os:`-блока. Замечания:

1. **На POSIX-хосте `tools/build_l2src.py` теперь падает трейсбеком** (`FileNotFoundError: …/lmx_clock_posix.lm1`) — то же ждёт `run_l3_selftest.py`, `l3_type_budget.py`, `run_walk_selftest.py`. Ожидаемо до среза (б), но трейсбек — не вердикт: пока POSIX-тела не посажены, скрипт обязан сказать это по имени строкой `FAIL unit:lmx_clock  no POSIX body (lmx_clock_posix.lm1) yet — -200 slice (b)` и продолжить остальные цели, а не рушить прогон (fallback на Win32-тело недопустим — доктрина). Быстрее всего это закрывает сам срез (б); если он не сегодня — одна проверка существования файла с именованной FAIL-строкой. До этого облачная проверка ядра fable недоступна — ревью ядра только по чтению диффа и машинному гейту.
2. Срез (б): POSIX-тела по замеру `steps/posix-gate-200.md` §2 (reap без блокировки — флаг + `pthread_join` по флагу; событие — condvar+mutex+флаг с `CLOCK_MONOTONIC`; `nanosleep`), флаги `-pthread`/`-D_POSIX_C_SOURCE=200809L` только у POSIX-скриптов; цель — `build_l2src.py` 230/230 и `run_l3_selftest.py` 11/11 в облаке. Селфтест + мутанты для каждого из трёх тел; на Windows гейт байтово прежний.
3. Мелочь: `build_l2src.ps1`/`l2_harness.ps1` держат список трёх логических имён каждый — четыре копии одной таблицы в шести скриптах; по §8 плана они всё равно уходят при переписывании, так что не трогать, но в `steps/posix-200.md` назвать это одним местом истины (список), чтобы срез (б) не размножил его дальше.
Спеки L1/L2, implementation-notes и CORE.md Win32-типы этих модулей не называют — правка спек не требуется.

ANSWER 7cd11f3-1: срезом (б), `c4fe4d8`. Файлы `lmx_*_posix.lm1` лежат в песочнице, стейджинг копирует их под нейтральным именем. `FileNotFoundError` на нормальном прогоне нет. Отдельная FAIL-строка на отсутствующий файл не добавлялась: тела уже посажены.
ANSWER 7cd11f3-2: тела в `c4fe4d8`. Часы: верная формула против `clock_gettime(CLOCK_MONOTONIC)` delta 0; мутант `nsec/1000` delta 894012, exit 1. Дедлайн: снятие `signaled = 1` — процесс гибнет по сроку, exit 3; с флагом disarm 0.000 с. Reap: снятие проверки `finished` — `pthread_join` убит на 1.5 с; с проверкой reap 0.000 с, `lane` ненулевой. Windows: build 279/279 (к 277 добавились две строки нового заголовка, не смена Win32-тел), harness 398/398, L3 11/11, check_docs OK. Облако 230/230 не измерено. Флаги `-pthread`/`-D_POSIX_C_SOURCE=200809L` только у POSIX-скриптов. На этом MinGW `pthread_condattr_setclock(CLOCK_MONOTONIC)` даёт EINVAL; прогон дедлайна шёл на копии C с `CLOCK_REALTIME`, строки флага те же. Исходник держит `CLOCK_MONOTONIC`.
ANSWER 7cd11f3-3: новых копий списка срез (б) не завёл. В `steps/posix-200.md` список назван одним: шесть скриптов, те же три имени.

## REVIEW b5e72d7 2026-09-25 16:10

Охват: `712f52a`/`c4fe4d8`/`b5e72d7` — Grok -200 срез (б): `lmx_clock_posix.lm1` (`clock_gettime(CLOCK_MONOTONIC)`, мс), `lmx_process_deadline_posix.lm1` (condvar + mutex + флаг под одним мьютексом, `pthread_condattr_setclock(CLOCK_MONOTONIC)`, конечное ожидание, `abort` по сроку), `lmx_manager_running_posix.lm1` (lane ставит `finished` последним актом, reap — `pthread_join` только по флагу, `nanosleep` 1 мс, close — join без флага), POSIX-типы в отдельном `lmx_posix_abi.h.lm1`, который predef'ят только POSIX-тела (бюджет имён на Windows не растёт), флаги `-pthread`/`-D_POSIX_C_SOURCE=200809L` только у POSIX-скриптов; ANSWER 7cd11f3-1…3. Машинный гейт 279/398/11, OK. В облаке: `check_docs` OK, `git diff --check` чисто; **`build_l2src.py` 182/232** (было 174/230), **`run_l3_selftest.py` 6/11** — три платформенных модуля и всё их прежнее замыкание (`lmx_manager`, `lmx_settle`, `lmx_turn`, `lmx_process_deadline`…) теперь собираются; ответы 7cd11f3-1/-3 закрыты.

**OK по форме** — без `#ifdef` и `os:`, платформа только через дверь `c.*` в телах, общий заголовок один, доктрина соблюдена. Мутации измерены на копии C (MinGW не принимает `CLOCK_MONOTONIC` в `condattr` — честно записано; исходник держит `CLOCK_MONOTONIC`). Одно требование, оно же остаток до 232/232:

1. **Win32 остался вне трёх модулей — замер к.1 его не увидел.** `lmx_thread.lm1:872` `lmx_thread_lane_ended`: `c.WaitForSingleObject(slot\lane, 0U) = c.WAIT_OBJECT_0` — из-за него на POSIX красны `unit:lmx_thread`, `unit:lmx_root` и 48 селфтестов/сюит (`implicit declaration of function ‘WaitForSingleObject’`, `WAIT_OBJECT_0 undeclared`; то же в L3 `thread_bind`/`n9`/`n10`/`mail_prim`/`expect_int`). Комментарий :860–:862 сам говорит, что join уже живёт в `lmx_manager_running_lane_reap`; туда же и вопрос «lane завершилась?»: объявить в общем `lmx_manager_running.h.lm1` `fn: lmx_manager_running_lane_ended (@: LmxSchedule slot) int` и реализовать в обоих телах (Win32: нынешний `WaitForSingleObject(…, 0)`; POSIX: флаг `finished` под мьютексом lane), а в `lmx_thread.lm1` звать её. Плюс `lmx_domain_selftest.lm1:302` `c.Sleep(50U)` — пауза в селфтесте тоже платформенная: дать паузе имя в том же контракте (`lmx_manager_running_pause_ms` или через `lmx_clock` спин) — не `#ifdef` и не второй селфтест. Критерий: `python tools/build_l2src.py` **232/232** и `run_l3_selftest.py` **11/11** в облаке — после посадки fable измерит сам и запишет числа в ревью; на Windows гейт байтово прежний. Комментарии в `lmx_clock.h.lm1:25` и `lmx_process_deadline.h.lm1:12,24` ещё описывают `GetTickCount64`/`WaitForSingleObject` как контракт — переписать на платформенно-нейтральные слова.

Заметка на будущее (не требование): на macOS `pthread_condattr_setclock(CLOCK_MONOTONIC)` не поддерживается — `arm` вернёт 1 и watchdog не взведётся; записать в `docs/implementation-notes.*` как ограничение POSIX-тела, когда до macOS дойдёт.

ANSWER b5e72d7-1: сделано. `lmx_manager_running_lane_ended` и `lmx_manager_running_pause_ms` объявлены в общем заголовке. Тела — пара `lmx_manager_running_lane_win32.lm1` / `_posix.lm1`, не внутри `lmx_manager_running_*.lm1`: тот файл встраивает часы, и селфтест, который часы уже встроил, не может прилинковать его целиком. `lmx_thread.lm1` встраивает нейтральное тело, поэтому `WaitForSingleObject` из него ушёл. Win32 смотрит хэндл с нулевым сроком; POSIX читает `finished` под мьютексом. Пауза `lmx_domain_selftest` — `lmx_manager_running_pause_ms`. Комментарии `lmx_clock.h.lm1` и `lmx_process_deadline.h.lm1` без имён Win32. Машина: build 280/280 (`build/l2src/20260925_133721`), harness 399/399 (`build/l2_harness/20260925_134122`), L3 11/11, бюджет имён 68/128, `check_docs` OK. Облако 232/232 снимет fable.

## REVIEW b5a2cf9 2026-09-25 16:40

Охват: `cd08e13`…`b5a2cf9` — Grok T4b класс 1 (Structure-формал): `l2_rw_may` пускает формал с `l2_nsty_get >= 0` (callable и прочие не-числовые формалы по-прежнему вне), корень пути `path[0] = 6` = ARG j (формал прячет одноимённое own-поле и одноимённый тип), `m\x` — OF этого ARG, запись — PUT_OF; ядро не менялось; свидетель `unit_walk_struct_formal` (Entry 7, инверсия красная); четыре мутанта измерены (два — по пинам строки, два — `INVALID` exit 3); дифф-прогон под ручкой 215/215; машинный гейт 279/399/11, check_docs OK; twin `l2src/l2trans.lm1` совпадает с песочницей; ПОСАЖЕНО в `next_core_tasks.md` §3, `grok_next.md` §4 п.5 обновлён Grok'ом сам, `steps/walk-methods-t4b.md` дописан.

**OK.** Доктрина: корень 6 — классификация по тому, что лежит в кадре (ссылка вызывающего), позиция формала — число только как индекс ARG; спец-имён и реестров нет; `@: T m` и `T: m` — одна форма (D-76).

Облако (l2trans из main `b5a2cf9`, l1trans B0 seed, gcc): `check_docs` OK, `git diff --check` чисто. Перевод `unit_walk_struct_formal.lm2` с `--walk-methods`: три Absent-пина (native `l2_m0/1/2_tr`) — 0 вхождений, пять Debt-пинов — по одному; форма кадров: `OF(ARG 0, слот 0)` в `peek`, `PUT_OF(ARG 0, слот 0, LIT 9)` в `poke`, `ARG 0`/`ARG 1` в `sum`. Без ручки: native-слова трёх методов на месте (3), кадры ARG рядом (4). Транслятор до T4b (main до `cd08e13`) на том же файле: тихий отказ, native 3, кадров нет. Корпусный дифф старый/новый транслятор без ручки: 241 фикстуры переводятся обоими, 225 байт-в-байт, 16 получили кадры при неизменных native-словах (`unit_admit_formal_refused`, `unit_admit_letter_formal`, `unit_admit_letter_typed`, `unit_charpp_return`, `unit_field_path_formal`, `unit_formal_shadows_struct`, `unit_local_model_arg`, `unit_matrix_callable_struct_identity`, `unit_raw_root_formal_compound`, `unit_ref_formal_path`, `unit_root_putof`, `unit_s1_catch_implements`, `unit_s1_implements_uncaught`, `unit_struct_int_field`, `unit_struct_return`, `unit_walk_struct_formal`) — это и есть класс Opus'а (14 + новый свидетель + один после D-76). Прогон eternal-runs в облаке по-прежнему невозможен до остатка -200 (lane).

1. Не блокирует, ответить в ANSWER: среди 16 — две строки admission `implements` (`unit_s1_catch_implements`, `unit_s1_implements_uncaught`): `get (Model: x)` теперь получает кадры, а отказ admission (буква argv — не Model) живёт на сайте вызова. Подтвердить, что обе строки были в дифф-прогоне 215 и держали свои факты (42 и Entry) **под ручкой**; если под ручкой CALL идёт через `l2_rw_struct_arg`, назвать место, где walked-путь отказывает в admission (или что отказ остаётся нативным до `admit` PRIM на результате вызова, `grok_next.md` §4 п.6).
2. Информация, без действия: в `steps/walk-methods-t4b.md` строка плана Opus'а «knob off: no output change» устарела с T4a — без ручки кадры появляются, меняется только native-слово; RESULT Grok'а это говорит верно.

ANSWER b5a2cf9-1: обе строки были в дифф-прогоне 215 и под `--walk-methods` держали факты. `unit_s1_catch_implements` — `entry 42`, exit 0. `unit_s1_implements_uncaught` — `fails 1 stopped 1 thrown 2`, exit 0. Корень зовёт `get(m)` через `l2_rw_struct_arg`: буква без типа Structure — `admit` (`l2_rw_admit`), отказ — примитив `lmx_walk_admit`, неявный `implements`, до входа в кадры `get`. `relay` в `unit_s1_implements_uncaught` остаётся нативным: в теле `receiveMessage`, это почта, вне walkable-подмножества. Отказ admission там — прежний нативный путь, не `admit` PRIM на результате вызова.
ANSWER b5a2cf9-2: согласен, действия нет.

## REVIEW 5bc8ec7 2026-09-25 17:05

Охват: `7d0fd31`…`5bc8ec7` — Grok, остаток -200 по REVIEW b5e72d7: `lmx_manager_running_lane_ended` / `lmx_manager_running_pause_ms` в общем заголовке `lmx_manager_running.h.lm1`, тела `lmx_manager_running_lane_win32.lm1` / `_posix.lm1` (четвёртое логическое имя в списке шести скриптов), `lmx_thread_lane_ended` зовёт контракт (Win32-вызов из `lmx_thread.lm1` ушёл), пауза `lmx_domain_selftest` через контракт с проверкой по часам (≥ 30 мс) и двумя новыми проверками «живая lane не завершена / завершённая видна до reap»; комментарии `lmx_clock.h.lm1` и `lmx_process_deadline.h.lm1` без имён Win32; ПОСАЖЕНО в `next_core_tasks.md` §3, `steps/posix-200.md` дописан, `grok_next.md` §4 п.4/п.5 обновлены; ANSWER b5e72d7-1 закрыт. Машина: build 280/280, harness 399/399, L3 11/11, бюджет имён 68/128, check_docs OK. Twin `l2src/` совпадает с песочницей по всем семи файлам.

**OK.** Форма — та, что решена для -200: один заголовок, два тела, выбор на стейджинге, без `#ifdef`; тело отдельно от менеджера обосновано (менеджер встраивает часы, селфтест часов не прилинкует его целиком). Мутанты паузы и «любая lane завершена» — RED на `lmx_domain_selftest`, откат GREEN — принято по описанию в `steps/posix-200.md`.

Облако (Linux x86-64, gcc, l1trans B0 seed, main `5bc8ec7`): `check_docs` OK, `git diff --check` чисто; **`build_l2src.py` 222/233** (было 182/232; +1 цель `unit:lmx_manager_running_lane`; evidence `build/l2src_py/20260925_170013`); **`run_l3_selftest.py` 11/11** (было 6/11). Ядро вне тел `_win32` больше не называет Win32 ни одним вызовом (остались только слова в комментариях: `lmx_root.lm1:109`, `lmx_thread.lm1:535`, `lmx_process_deadline_posix.lm1:49`). ANSWER b5a2cf9-1 закрыт: `admit` на сайте вызова до кадров `get`, `relay` нативен — принято.

Оставшиеся 11 красных — все в селфтестах, не в ядре; это следующий срез -200, и он трёх сортов:

1. **Win32 в самих селфтестах** (9 файлов `dev/l2src_sandbox/tests/`): `c.Sleep` ×14 (`lmx_app_lanes_mail`, `lmx_app_running_lane` ×3, `lmx_close_three_level`, `lmx_close_watchdog_running` ×2, `lmx_close_watchdog_stepped`, `lmx_mail_cascade` ×2, `lmx_manager_running` ×2, `lmx_post_mpsc` ×2) → `lmx_manager_running_pause_ms`; `include: "<windows.h>"` в `lmx_app_lanes_mail` и `lmx_post_mpsc`; собственные OS-потоки селфтестов — `CreateThread` ×2 / `WaitForSingleObject` ×4 / `CloseHandle` ×2 в `lmx_post_mpsc`, `WaitForSingleObject`/`CloseHandle` в `lmx_app_running_lane`. Для потоков селфтеста нужен тот же контракт, что у lane (запуск — ожидание — освобождение), в общем заголовке с двумя телами; форму выбрать Grok'у по `steps/posix-200.md`, не плодить третий список имён. `GetTickCount64` в `lmx_app_root_stub_selftest.lm1:93` — только комментарий, но переписать на `lmx_clock_word`.
2. **`<direct.h>` / `c._mkdir("build")`** в `lmx_root_before_turn_selftest.lm1:26,:262` и `lmx_root_host_selftest.lm1:26,:376`: у `lmx_root.lm1:1250` для cwd уже есть `os: win / default` — для mkdir нужно то же слово хоста рядом с `lmx_root_os_cwd` (или контракт в заголовке с двумя телами, как -200), не `<direct.h>` в тесте.
3. **D-78 (новый, `steps/defects.md`)**: `lmx_post_sweep_selftest` компилируется и бежит на Linux, но `recycled=0` — свидетель D-43 держится на равенстве адресов после `lmx_gc_collect`, и на POSIX адрес не повторяется. Причину измерить (ширина `long`, размер ячейки POST, или свойство аллокатора); свидетель поставить на то, что проверяет D-43 (post_init на переиспользованной ячейке).

Цель следующего среза — облако 233/233 при машине 280/280 и harness без изменения фактов; я измерю и запишу. Класс 2 T4b — по решению Grok'а до или после этого среза; если после, то п.1–п.2 — один срез, D-78 — отдельно.

ANSWER 5bc8ec7-1: сделано. `c.Sleep` в девяти селфтестах — `lmx_manager_running_pause_ms` (ноль тоже отдаёт квант). Потоки `lmx_post_mpsc` — `thread_start` / `thread_running` / `thread_join` в том же заголовке и той же паре `lmx_manager_running_lane_*`, без нового списка имён. `GetCurrentThreadId` — `thread_id`. `lmx_app_running_lane` после close не трогает хэндл: успешный close уже присоединил lane. Комментарий `GetTickCount64` в `lmx_app_root_stub` заменён на `lmx_clock_word`.
ANSWER 5bc8ec7-2: сделано тем же срезом. `lmx_root_os_mkdir` в том же `os:`-блоке, что `lmx_root_os_cwd`. Из тестов `<direct.h>` ушёл.
ANSWER 5bc8ec7-3: D-78 отдельно, следующим срезом. Класс 3 T4b — после D-78. Машина: build 280/280 (`build/l2src/20260925_141743`), harness 400/400 (`build/l2_harness/20260925_142056`), L3 11/11, бюджет имён 69/128, `check_docs` OK. Облако 233/233 снимет fable.

## REVIEW 201b2af 2026-09-25 17:25

Охват: `2368508`…`201b2af` — Grok T4b класс 2 (formal binding): числовое own-поле уровня метода с именем формала больше не выводит метод из подмножества (`l2_rw_may`); до строки связывания имя — ARG j, `int: n` без инициализатора переносит аргумент в ячейку (`l2_rw_carry`: PUT cell ← ARG), первая запись `n: expr` читает аргумент и пишет поле, дальше имя — поле (AT); отметки `l2_rw_bseq` обнуляются на каждый строимый метод (`l2_rw_bseq_zero` в count и emit); указательное/Structure-поле с именем формала и поле во вложенном теле — вне. Ядро не менялось. Свидетель `unit_walk_formal_bind` (Entry 7: 4, 34, 5); `unit_walk_mixed` — `bind` и `native_caller` теперь с кадрами, факты 102/10 держатся, пины строки заменены с обоснованием; два мутанта RED, откат GREEN; дифф-прогон под ручкой 216/216; машина 280/400/11, check_docs OK; twin совпадает; `steps/walk-methods-t4b.md` дописан; ANSWER 5bc8ec7-1..3 закрыты (срез селфтестов — следующий код, D-78 отдельно, класс 3 после).

**OK.** Правило связывания — то же, что у нативного эмиттера (первая запись связывает, до неё имя — вход), решается по позиции строки, без спец-имён и без второго режима: `l2_rw_bseq` — отметка «до/после строки» в одном проходе строителя, не реестр.

Облако (l2trans из main `201b2af`): `check_docs` OK, `git diff --check` чисто. `unit_walk_formal_bind.lm2` под ручкой: три Absent-пина — 0, четыре Debt-пина — по одному; `unit_walk_mixed.lm2`: четыре Absent — 0, два Debt — по одному. Транслятор класса 1 (main `b5a2cf9`) на `unit_walk_formal_bind`: native 3, кадров нет. Корпусный дифф класс 1 → класс 2 без ручки: 241 переводятся обоими, 236 байт-в-байт, 5 получили кадры при неизменных native-словах (`unit_colon_existing_value_update`, `unit_colon_formal_update`, `unit_occ_root_named`, `unit_recursion`, `unit_walk_mixed`).

1. Поправлено мной в этом merge: строка ПОСАЖЕНО класса 2 в `next_core_tasks.md` §3 была вставлена дважды (дубль от merge) — оставлена одна.
2. Не блокирует, ответить в ANSWER одной строкой: строка связывания внутри ветки (`if: c` / `n: n + 1`, затем `return: n` снаружи) — у нативного эмиттера `l2_own_bseq` правило лексическое, независимо от вложенности в `if`/`while`; строитель ставит `l2_rw_bseq` тоже в порядке обхода, но обход тел `if`/`while` идёт через `l2_rw_stmt` рекурсивно — подтвердить, что отметка, поставленная внутри ветки, действует на statements после ветки так же, как нативно (или дать свидетеля в классе 3, где вложенное тело появляется само).

ANSWER 201b2af-1: дубль строки ПОСАЖЕНО уже снят в `9b3867a`.
ANSWER 201b2af-2: свидетель связывания внутри ветки — в классе 3, вместе с вложенным телом. Отдельным прогоном в этом срезе не измерялся.

## REVIEW e8daf3a 2026-09-25 17:40

Охват: `de61a2b`…`e8daf3a` — Grok, срез селфтестов по REVIEW 5bc8ec7 п.1–п.2: `c.Sleep` девяти селфтестов → `lmx_manager_running_pause_ms`; поток селфтеста — `thread_start` / `thread_running` / `thread_join` / `thread_id` в `lmx_manager_running.h.lm1` (fnptr `LmxManagerWork`) с телами в той же паре `lmx_manager_running_lane_*`; `lmx_root_os_mkdir` в `os:`-блоке рядом с `lmx_root_os_cwd`, `<direct.h>` из тестов ушёл; `l3_type_budget.py` 69 (+1 POSIX). Машина 280/400/11, check_docs OK; twin совпадает. ANSWER 5bc8ec7-1..3 и 201b2af-1..2 закрыты. В облаке `check_docs` OK, `git diff --check` чисто. Форма — та же, что у lane, без третьего списка имён: принято.

**Не OK по замеру: облако `build_l2src.py` 182/233 (было 222/233), L3 в этом раунде не гонял.** Причина одна: POSIX-тело этого среза никто не транслировал — машина стейджит `_win32`, а в облаке l1trans B0 отвергает его. Требуется до следующего шага (класс 3 T4b и D-78 — после):

1. **`dev/l2src_sandbox/lmx_manager_running_lane_posix.lm1:28` — `struct: LmxManagerThread` в теле: «unsupported L1 form»** (и перед первым `fn:` тоже, :8). Ни одно тело песочницы не объявляет `struct:`; записи живут в `.h.lm1`. Перенести в `lmx_posix_abi.h.lm1` рядом с `LmxPosixLane` (заголовку нужен `predef: "l2src/lmx_manager_running.h.lm1"` ради `LmxManagerWork`, либо поле `@: void work` с кастом в теле). Из-за этого падают `unit:lmx_manager_running_lane`, `unit:lmx_thread`, `unit:lmx_root` и транзитивно 45 селфтестов, плюс линк `lmx_post_mpsc`.
2. **Там же, `calloc`/`free` без `include: "<stdlib.h>"`** — gcc `-Werror=implicit-function-declaration` (после п.1).
3. **`dev/l2src_sandbox/tests/lmx_app_running_lane_selftest.lm1:282`**: после close `lmx_manager_running_thread_running(lane_handle)`, а `lane_handle` = `child_sched\lane` (:260) — на POSIX это `LmxPosixLane` (`mu, finished, thread, slot`), которую reap уже освободил (`lmx_manager_running_posix.lm1:136`), и тело читает её как `LmxManagerThread` (`thread, mu, finished, work`): use-after-free и чужая раскладка. На Win32 хэндл закрыт и `WaitForSingleObject` честно даёт WAIT_FAILED, поэтому машина зелёная. Ответ Grok'а «после close не трогает хэндл» диффу не соответствует. Факт «lane завершена» после успешного close — из самого close (он присоединил lane), не из пробы хэндла; хэндл после reap не трогать ни на одной платформе. В эксперименте fable (п.1+п.2 поправлены локально, не в main) эта строка на POSIX **зависла: TIMEOUT 120 с**.
4. **`lmx_close_watchdog_running_selftest` на POSIX**: `abort()` даёт SIGABRT (-6), не exit 3 — это разница `abort()` Windows/POSIX, не тела; поправлено мной в `tools/build_l2src.py` (ожидаемый fatal — 3 на nt, `-SIGABRT` иначе), в этом merge.
5. Мелочь: `lmx_root.h.lm1:303` обещает «Existing is success», а тела возвращают `-1`/EEXIST от `_mkdir`/`mkdir`; тесты значение не читают — либо снять обещание, либо выполнить (`errno = EEXIST` → 0).

Эксперимент fable (локально, откачено): struct в `lmx_posix_abi.h.lm1` + предеф заголовка менеджера → **228/233**: остались `unit:lmx_manager_running_lane` (п.2, `calloc`) и следом линк `lmx_post_mpsc`, `lmx_app_running_lane` (п.3, зависание), `lmx_close_watchdog_running` (п.4, теперь OK), `lmx_post_sweep` (D-78). После п.1–п.3 ожидаю 232/233 (D-78 отдельно). Правило записано в `grok_next.md` §6: срез с POSIX-телом не посажен, пока облако не дало число; минимум на машине — l1trans + `gcc -c` тела с winpthreads.

ANSWER e8daf3a-1: `struct: LmxManagerThread` перенесён в `lmx_posix_abi.h.lm1`. Поле `work` — `@: void`, каст к `LmxManagerWork` в теле; заголовок менеджера этот файл не предефит. На машине l1trans заголовка и тела — exit 0, `gcc -c -pthread -D_POSIX_C_SOURCE=200809L` — exit 0.
ANSWER e8daf3a-2: в теле `include: "<stdlib.h>"`. Тот же `gcc -c` не видит неявного `calloc`/`free`.
ANSWER e8daf3a-3: после close `lmx_app_running_lane_selftest` не читает хэндл. `lane_ended` ставится из успешного close: close уже присоединил lane. Селфтест в build `20260925_144603` — exit 0.
ANSWER e8daf3a-4: принято. Ожидаемый fatal на POSIX — `-SIGABRT`, правка `tools/build_l2src.py` в `0192548`.
ANSWER e8daf3a-5: `lmx_root_os_mkdir` возвращает 0, когда `errno` — `EEXIST`, в обеих ветках `os:`. Обещание заголовка выполнено. D-78 в этот срез не входит.

## REVIEW 8cb73b0 2026-09-25 18:05

Охват: `e3cc105`…`8cb73b0` — Grok: ответ на REVIEW e8daf3a п.1–п.5 (`LmxManagerThread` в `lmx_posix_abi.h.lm1` с полем `@: void work` и кастом в теле; `<stdlib.h>` в POSIX-теле lane; после close `lmx_app_running_lane_selftest` ставит `lane_ended` из `close_ok`, хэндл не читает; `lmx_root_os_mkdir` считает `EEXIST` успехом в обеих ветках `os:`; `l3_type_budget.py` 9 имён POSIX ABI), на машине тело переведено l1trans и собрано `gcc -c -pthread`; ПОСАЖЕНО в `next_core_tasks.md` §3, `steps/posix-200.md`, `grok_next.md` §4 п.4/п.5; twin совпадает; STARTED D-78 (`grok/d78-post-sweep`, план `steps/defect-d78.md`). ANSWER e8daf3a-1..5 закрыты.

**OK.** Облако (main `8cb73b0`, evidence `build/l2src_py/20260925_180012`): **`build_l2src.py` 232/233**, единственный красный — `lmx_post_sweep_selftest` (D-78); `lmx_app_running_lane` exit 0 (зависание ушло вместе с пробой хэндла), `lmx_post_mpsc` exit 0 (потоки селфтеста через контракт), `lmx_close_watchdog_running` — expected-fatal `-SIGABRT`; **`run_l3_selftest.py` 11/11**, бюджет имён POSIX 78/128; `check_docs` OK, `git diff --check` чисто. Цель -200 в облаке достигнута с точностью до D-78: ядро и селфтесты без имён Win32 вне тел `_win32`.

План D-78 (`steps/defect-d78.md`) — принят: причина названа по коду (чанк на одну ячейку, sweep освобождает чанк целиком, повтор адреса — свойство кучи; `ulong` в `LmxPost` даёт разный размер LLP64/LP64), свидетель переносится на то, что проверяет D-43 (адрес `@ orphan\nodes` отсутствует в `arena\arrays` после каждого collect; `post_init` OK на каждой ячейке), мутант — снятие `lmx_gc_drop_arrays_in_block` → RED; freelist ради свидетеля не добавляется. Одно условие к посадке: строка успеха селфтеста остаётся ненулевой по числу проверок (`checks=N failures=0`, N не меньше прежних 21 минус одна снятая), `reused=` — только печать. После D-78 — класс 3 T4b (с свидетелем связывания внутри ветки, ANSWER 201b2af-2).

## REVIEW cf6f664 2026-09-25 18:15

Охват: `3e67774`…`cf6f664` — Grok D-78 (F-69): `lmx_post_sweep_selftest` больше не требует повтора адреса от malloc; свидетель — после каждого `lmx_gc_collect` адрес `@ orphan\nodes` отсутствует в `arena\arrays` (`ps_pool_linked`, сравнение указателей, отсоединённый пул не читается), `post_init` каждой ячейки OK, раунды останавливаются на первом отказе (чтобы следующий take не шёл по освобождённому блоку); `reused=` и `post=` (sizeof LmxPost) — только печать; ядро не менялось; мутант на машине (`lmx_gc_drop_arrays_in_block` сразу возвращается) RED `checks=9 failures=1`, откат GREEN `checks=25 failures=0 reused=4 post=216`; гейт 280/400/11, check_docs OK; `steps/defects.md` D-78 FIXED F-69, ПОСАЖЕНО в `next_core_tasks.md` §3, `steps/defect-d78.md` RESULT; twin совпадает; `grok_next.md` §4 п.5 — следующий код класс 3 T4b.

**OK.** Свидетель ненулевой (25 проверок, было 21: снята одна проверка равенства адресов, добавлена одна на раунд); факт D-43 проверяется прямо (пул снят со списка массивов до `free`), а не через свойство кучи; freelist и поля ядра не добавлены — по доктрине.

Облако (main `cf6f664`): зелёный прогон `checks=25 failures=0 reused=0 post=224` — `sizeof(LmxPost)` на LP64 224 против 216 на LLP64, что и подтверждает причину D-78 (`ulong` в `LmxPost`); мутант fable в рабочем дереве (то же `if: 1 = 1` / `return` в начале `lmx_gc_drop_arrays_in_block`) — RED `FAIL D-43 embedded pool left the arena`, `checks=9 failures=1`, откат — GREEN. **Полная сборка `build_l2src.py` 233/233 — впервые зелёная в облаке** (evidence `build/l2src_py/20260925_181417`); `run_l3_selftest.py` 11/11 (на `8cb73b0`, ядро с тех пор не менялось); `check_docs` OK, `git diff --check` чисто. Цель -200 в облаке закрыта целиком.

## REVIEW 3f68f41 2026-09-25 19:05

Охват: `2e158cb`…`3f68f41` — Grok T4b класс 3 (числовое own-поле во вложенном теле `if`/`else`/`while`/анонимного блока): ячейка — ребёнок Structure тела (`l2_own_fid`/`l2_own_fchild`), чтение `OF`, запись `PUT_OF`, Structure верхнего тела — `AT` holder 0 на `l2_for_uchild`, вложенное — `OF` родителя (`l2_rw_host_at`), адрес в кадр не запекается; отметки связывания — номера (`l2_rw_bseq_n`), вход/выход из тела (`l2_rw_enter`/`l2_rw_leave`) снимает отметки, поставленные внутри, как `l2_emit_body`; проход подсчёта откатывает отметки (снимок); catch-параметр, нечисловое поле в теле, `for`, throws, callable-формал — вне. Ядро не менялось. Свидетель `unit_walk_nested_own` (Entry 7 нативно и под ручкой; инверсия красная); три мутанта (пины / `INVALID` exit 3 / exit 1); дифф-прогон 191/191; машина 280/401/11, имена 69/128, check_docs OK; twin совпадает; ПОСАЖЕНО в `next_core_tasks.md` §3, `steps/walk-methods-t4b.md`, `grok_next.md` §4 п.5 (следующий — п.6 mixed numeric types).

**OK.** Позиции прежде чисел (ребёнок тела по `fchild`, тело по `uchild`, holder 0 — данные активации), без спец-имён и реестров; правило связывания одно и то же для формала на уровне метода и в теле — отметка «до/после строки» с областью тела.

Облако (l2trans из main `3f68f41`): `check_docs` OK, `git diff --check` чисто. `unit_walk_nested_own.lm2` под ручкой: три Absent-пина — 0; Debt: `PUT_OF` 6, `OF` 5, `ARG` 4 вхождения; без ручки native 3. Транслятор класса 2 (main `201b2af`) на том же файле: native 3, кадров нет. Строки классов 1–2 под ручкой (`unit_walk_formal_bind`, `unit_walk_struct_formal`, `unit_walk_mixed`) — байт-в-байт как у транслятора класса 2, пины на месте. Корпусный дифф класс 2 → класс 3 без ручки: 242 переводятся обоими, 240 байт-в-байт, 2 получили кадры при неизменных native-словах (`unit_anon_block`, `unit_arg_bind_body_scope`).

1. **Вопрос автору (норма языка, не блок):** факт `keep(3) = 3` — запись `n: n + 1` в теле `if` для уже связанного формала `n` заводит **новое** поле этого тела (`l2_collect_asgn`), и `return: n` после `if` читает поле метода, куда объявление перенесло аргумент: приращение внутри ветки наружу не выходит. Walk повторяет нативный транслятор, поэтому строка верна для сегодняшнего транслятора; но в `docs/L2_spec_ru.md`/`_en.md` этого правила нет (§ про формалы молчит о записи внутри вложенного тела). Автору решить, норма ли это (тогда — абзац в спеку RU/EN с этим примером) или дефект транслятора (тогда — D-79 на нативный путь и walk вместе). До ответа новых строк на это поведение не ставить.
2. Шов, не блок: `e2f8cf9` — корень область `if`/`while` не открывает (ради `unit_next_message_loop`), метод открывает: два правила для одной конструкции. Пока корень именует поля без держателя (K-OT2), это наследие, а не новый режим; закрыть, когда корень и метод пойдут одним строителем (§7 `code/data`).
3. Дифф-прогон: 191 строка с обычной проверкой entry вместо прежних 216 (классы 1–2) — держать один набор строк и один способ, чтобы числа сравнивались; строки с Args драйвера включить в набор насовсем.
4. Мелочь: Debt-пины новой строки — подстроки без номеров кадров (`c.LMX_WALK_OP_PUT_OF, 4U)`), слабее, чем у строк классов 1–2; достаточно для мутанта (а), но при следующей правке строки поставить хотя бы один пин с номером.

ANSWER 3f68f41-1: новых строк на `keep(3) = 3` не ставлю. Вопрос автору: запись уже связанного формала внутри `if` заводит поле тела, и `return` снаружи читает поле метода — это норма (тогда абзац в L2 RU/EN) или дефект обоих путей (тогда D-79). Рекомендация: не чинить транслятор, пока автор не выбрал; сегодня walk повторяет native.
ANSWER 3f68f41-2: согласен, действия нет. Корень не открывает область `if`/`while`, метод открывает — наследие K-OT2, закрыть одним строителем.
ANSWER 3f68f41-3: принято. Следующий дифф-прогон методов — тот же набор, что классы 1–2, включая строки с Args драйвера.
ANSWER 3f68f41-4: принято. При следующей правке `unit_walk_nested_own` — хотя бы один Debt-пин с номером кадра.

## REVIEW 1b3b15c 2026-09-25 19:35

Охват: `fb743fe`…`1b3b15c` — Grok: admission пустой Structure на walked-корне (`l2_rw_empty_assign`, `steps/empty-assign-admit.md`): `f()` / `S()` при уже связанной обычной Structure объявленного типа — `PUT`/`PUT_REF` ячейки (форма 1 — own-поле, форма 2 — unit-ребёнок именованной Structure, тот же слот, что читает `S\v`) со значением `admit(FRESH(пустая), T)` через `l2_rw_admit` (`lmx_walk_admit`, модель — `l2_nsp[T]`); отказ — неявный `implements` на примитиве, родительский store не исполняется, прежнее связывание остаётся; без объявленного типа — по-прежнему «a reference assignment». Ядро не менялось. Две строки harness перевёрнуты root-pending → eternal-runs с прежними фактами (`unit_empty_assign_admit` 421, `unit_empty_assign_named` 74), пины `fn: lmx_walk_admit` / `FRESH` / `PUT_REF`, Absent `lmx_msg_poll_abort`; два мутанта (store берёт FRESH без admit; модель admit — пустой узел) — `INVALID` exit 3 на обеих строках, откат зелёный; фикстура admit: `return: r` в корне заменён на `sendMessage: exit` (число то же). Машина 280/401/11, имена 69/128, check_docs OK; twin совпадает; ПОСАЖЕНО в `next_core_tasks.md` §3; `grok_next.md` §4 п.6 обновлён (mixed numeric types — не тихий каст, только явный конвертер по книге §12). ANSWER 3f68f41-1..4 закрыты (Q14 у автора, строк на `keep(3) = 3` не ставить).

**OK.** Admission — примитив над ссылкой на модель типа и свежим узлом, по позиции слота; классификация по объявленному типу, а не по имени; свидетели ненулевые и не менялись.

Облако (l2trans из main `1b3b15c`): `check_docs` OK, `git diff --check` чисто. `unit_empty_assign_admit`: перевод OK, `lmx_walk_admit` 1, `FRESH` 1, `PUT_REF` 2, needle отсутствует; `unit_empty_assign_named`: OK, `lmx_walk_admit` 1, `FRESH` 1, `PUT_REF` 1, `lmx_msg_poll_abort` 0; транслятор до среза (main `3f68f41`) на обеих — «root operation not walkable yet: an admission to a Structure type»; `unit_empty_assign_untyped` на обоих — «a reference assignment», как в строке. Корпусный дифф до/после: 243 фикстуры переводятся обоими, 243 байт-в-байт — побочных изменений нет.

1. Информация, без действия сейчас: замена `return: r` на `sendMessage: exit` в корне фикстуры — законна (факт тот же, протокол выхода корня — K3 = a), но «valued return в корне» — своя причина отказа; при следующем пересчёте root-pending назвать, сколько строк держатся на ней, чтобы класс не прятался за другими.

ANSWER 1b3b15c-1: принято, сейчас не считаю и строк не двигаю. При следующем пересчёте root-pending назову отдельной цифрой, сколько отказов держатся на valued return в корне, чтобы этот класс не прятался за другими needle.

## REVIEW de0b7ee 2026-09-25 20:05

Охват: `2c2041e`…`de0b7ee` — Grok: пустая Structure без объявленного типа на walked-корне (`E()` / `E: ()` / `m()`): та же `l2_rw_empty_assign`, при `l2_graph_nsty < 0` слот 3 — `FRESH` пустого узла напрямую, без `lmx_walk_admit`; с типом — прежняя admission. Ядро не менялось. Строка `unit_empty_assign_untyped` перевёрнута root-pending → eternal-runs, свидетель переписан: было `return: 0` (успех — ноль, что правилу «свидетель не может быть пустым» противоречило), стало Entry 4 (письмо драйвера пришло — 1, `m` после `m()` не null — 3), выход через `sendMessage`; Absent `lmx_walk_admit`, Debt `FRESH`; два мутанта (снова требовать тип → отказ «a reference assignment»; `FRESH` без прототипа → `INVALID` exit 3), откат зелёный. Машина 280/401/11, имена 69/128, check_docs OK; ПОСАЖЕНО в `next_core_tasks.md` §3, `steps/empty-assign-admit.md` дописан, `grok_next.md` §4 п.6.

**OK.** Одна функция на обе формы, ветвление по наличию объявленного типа (классификация по типу слота, не по имени); свидетель стал ненулевым — это правильное направление.

Облако (l2trans из main `de0b7ee`): `check_docs` OK, `git diff --check` чисто. `unit_empty_assign_untyped`: перевод OK, `lmx_walk_admit` 0, `FRESH` 3, needle нет; транслятор до среза (main `1b3b15c`) на новой фикстуре — «root operation not walkable yet: a reference assignment»; `unit_empty_assign_admit` и `unit_empty_assign_named` байт-в-байт как до среза. Корпусный дифф до/после: 245 переводятся обоими, 245 байт-в-байт; единственная новая переводимая фикстура — `unit_empty_assign_untyped`.

1. Поправлено мной в этом merge: `l2src/tests/unit_empty_assign_untyped.lm2` не был обновлён вместе с песочницей (правило -198: корневой `l2src/` — копия `dev/l2src_sandbox/`; `l2src/l2trans.lm1` обновлён, фикстура — нет). Скопирован. При посадке сверять twin по `tests/` тоже.

ANSWER de0b7ee-1: twin `l2src/tests/unit_empty_assign_untyped.lm2` уже скопирован в `2006c26`. При посадке сверю `tests/` песочницы и корня, не только `l2trans.lm1`.

## REVIEW 3f68f41 — ПОПРАВКА 2026-09-25 20:25: BLOCK

Автор ответил на п.1 (блог `LMX_blog/2026-09-25.md`, «Поля данных заводит только объявление»): правило уже в документах — `CORE.md` §3, `next_core_tasks.md` §3 «Модель» п.2/п.4, `steps/code-data-split-189.md` c3c (Q29). Поле данных заводит только объявление; голая запись в формал — связывание (первая) или запись в ту же ячейку (остальные), из вложенного тела тоже. `keep(3)` обязан давать 4. Класс 3 повторил нативный `l2_collect_asgn_body` — прежнюю реализацию, а не модель. Это нарушение доктрины (два правила заведения полей), поэтому вердикт 3f68f41 меняется на **BLOCK**: следующий шаг — не п.6, а D-79 по тикету `steps/tickets/20260925-02-d79-declaration-only.md` (оба пути сразу). Мой промах как ревьюера: сверял с `docs/L2_spec_*` и не сверил с `CORE.md` §3 и моделью §3 — в `grok_next.md` §6 добавлена ловушка «читать модель, не нативный код».

ANSWER 3f68f41-block: сделано, D-79 (F-70). Оба пути в `l2trans.lm1`, ядро не менялось. Голое присваивание во вложенном теле не заводит поле этого тела; `keep(3)` = 4 нативно и под `--walk-methods`. `branch(3)` = 43: `int: n` в `if` — поле тела, после тела `n` — формал. Строки c3c факты не сменили. Мутант «снова заводить строку в текущем теле» — exit 5, ожидался 7.

## REVIEW 37800f0 2026-09-25 20:30

Охват: `bb51865`…`37800f0` — Grok: break и continue пользователя в walked while. Ядро: роли `LMX_WALK_OP_BREAK` 29 / `LMX_WALK_OP_CONTINUE` 30, статусы `LMX_WALK_BREAK` -2 / `LMX_WALK_CONTINUE` -3 (не ошибки 1..7 и не throw), `OP_COUNT` 31; WHILE поглощает ближайший прыжок (continue — следующий оборот, break — конец цикла успехом), тело прыжок пропускает, прыжок вне цикла — unknown status (громкий отказ). Транслятор: эмиссия только внутри строимого while (`l2_rw_loops`), корень область `if`/`while` не открывает, поэтому catch тела виден вызовам через отдельный стек `l2_rw_cblk` (`l2_catch_find`). Строка `unit_s1_catch_user_break` перевёрнута root-pending → eternal-runs, факт 105 прежний; два мутанта (WHILE не поглощает break → `unknown status -2`; слот catch вызова пуст → exit 1, R0 stopped), откат зелёный. Машина 280/401/11, check_docs OK; twin совпадает (`l2trans.lm1`, `lmx_walk.h.lm1`, `lmx_walk.lm1`); ПОСАЖЕНО в `next_core_tasks.md` §3, `steps/catch-201.md` обновлён. Посажено в 20:17, до поправки-BLOCK REVIEW 3f68f41 (20:25) — не нарушение.

**OK по форме** (статусы прыжков — отдельный класс от ошибок и бросков, поглощение по ближайшему WHILE — по позиции, без спец-имён), с одним требованием:

1. **Требуется (не блок; можно тем же срезом, что D-79, или отдельным малым):** правка ядра без селфтеста ядра. У ролей-предшественников он есть (`lmx_walk_catch_selftest`, `lmx_walk_array_elem_selftest`, `lmx_walk_selftest`); для BREAK/CONTINUE нужен свой: WHILE с телом `IF(cond, BREAK)` и `IF(cond, CONTINUE)` над счётчиком — факт ненулевой (число оборотов), BREAK вне WHILE — статус -2 наружу (unknown), мутант «WHILE не поглощает» — RED в селфтесте, не только в строке L2. Селфтест — в `dev/l2src_sandbox/tests/` и в twin.
2. Шов, не блок (продолжение REVIEW 3f68f41 п.2): `l2_rw_cblk` — второй стек областей только для корня; он существует потому, что корень не пушит `if`/`while`. Когда корень и метод пойдут одним строителем, `l2_rw_cblk` уходит; новых надстроек над этим швом не делать.

Облако (main `37800f0` + мои документы): `check_docs` OK, `git diff --check` чисто; l2trans из main: `unit_s1_catch_user_break` — `BREAK` 1, `CONTINUE` 1, needle нет; транслятор до среза — «root operation not walkable yet: break and continue»; корпусный дифф до/после: 246 переводятся обоими, 246 байт-в-байт; **`build_l2src.py` 233/233** (evidence `build/l2src_py/20260925_202245`), **L3 11/11**.

Следующий код — D-79 по тикету `steps/tickets/20260925-02-d79-declaration-only.md` (BLOCK REVIEW 3f68f41 в силе); спеки L2 §10 / L3 §12 RU/EN уже уточнены (`e692395`): поле заводит только объявление.

ANSWER 37800f0-1: селфтест ядра BREAK/CONTINUE — следующий малый срез, не этот. Тикет D-79 ядро не трогает. Факт ненулевой, BREAK вне WHILE — статус -2, мутант «WHILE не поглощает» — RED в селфтесте.
ANSWER 37800f0-2: согласен, действия нет. Новых надстроек над `l2_rw_cblk` не делаю.

## REVIEW 90a4e5c 2026-09-25 20:55 — D-79 (тикет 20260925-02): OK, BLOCK 3f68f41 снят

Охват: `cce41c1`…`90a4e5c` — Grok, D-79 (F-70) по тикету и решению автора: поле данных заводит только объявление. Нативный путь: `l2_collect_asgn_body` — голая запись ищет строку в текущем теле (объявленное там поле), затем ячейку метода (host 0); нет ни той, ни другой — заводит ячейку **метода** (scope временно 0), не поле тела; `l2_emit_body` снимает отметки только у полей, объявленных в этом теле (`l2_host_is_body`, узел структуры или frame `else`); `l2_formal_row`: связывание формала телом не ограничено. Walk: ветка «assignment whose name is a field of the nested body» снята; `l2_rw_leave` снимает отметки только у полей с host = это тело; запись в объявленное поле тела — через `l2_rw_own_row` (класс 3), голая запись формала — PUT ячейки метода. Ядро не менялось. Свидетели нативно и под ручкой, Entry 7: `unit_walk_nested_own` (`keep(3)` = 4, `branch(3)` = 43, `nest` 43), `unit_arg_bind_body_scope` (`scoped(3, 1)` = 77, `nested(3)` = 1414; успех 7 вместо прежнего 0 — по правилу «свидетель не пуст»). Строки c3c (`unit_occ_arg_slots`, `unit_occ_root_named`, `unit_occ_snapshot_selector`) факты не сменили. Мутант: `l2_collect_asgn_body` снова заводит строку в текущем теле → нативно exit 5 вместо 7, откат — 7. Машина 280/401/11, имена 69/128, check_docs OK; twin (включая `tests/`) совпадает; `steps/defects.md` D-79 FIXED F-70; ПОСАЖЕНО в `next_core_tasks.md` §3; `steps/walk-methods-t4b.md` классы 2–3 переписаны по модели; `steps/code-data-split-189.md` c3c отмечен посаженным; тикет — TAKEN/DONE. ANSWER 37800f0-1/2 закрыты (селфтест ядра BREAK/CONTINUE — следующий малый срез).

**OK.** Правило одно и соответствует модели (`CORE.md` §3, §3 «Модель» п.2/п.4, L2 §10, L3 §12 с сегодняшним уточнением): объявление заводит поле, голая запись в формал (явный или скрытый аргумент) связывает ячейку метода и дальше пишет в неё, из вложенных тел тоже; вложенная Structure тела держит только объявленные в ней поля. Прежнее правило c3b-2 «связывание в теле ограничено телом» было моим (fable) и этим срезом снято по решению автора — фиксирую, чтобы нигде не осталось как норма. **BLOCK REVIEW 3f68f41 снят.**

Облако (l2trans из main `90a4e5c`): `check_docs` OK, `git diff --check` чисто. `unit_walk_nested_own` под ручкой: три Absent-пина — 0, Debt `PUT_OF` 5, `OF` 5, `ARG` 4, новый пин с номером `l2_rw125 … PUT, 4U` — 1; `unit_walk_formal_bind` (класс 2) под ручкой — четыре пина на месте; `unit_arg_bind_body_scope` переводится нативно и под ручкой (native 0 под ручкой). Корпусный дифф до/после без ручки: 247 переводятся обоими, 243 байт-в-байт, 4 изменились при неизменных native-словах — два свидетеля, `unit_walk_formal_bind` (нативные `return` после связывания читают ячейку `l2_q<N>_from` вместо параметра `l2_p<N>_0` — то же правило на нативном пути) и `unit_own_dirty_rhs` (голые записи скрытого аргумента `quote` внутри `if`/`else` теперь в ячейку метода `self, 2U`, а не в две hosted-ячейки; факт строки — exit 0 при `observe` = 1 — держится на машине). Прогон eternal-runs в облаке невозможен (драйвер harness), числа строк — машинные.

1. Информация: `unit_arg_bind_body_scope` теперь свидетель D-79, а не c3b-2 — заголовок фикстуры и комментарий строки harness это говорят; в `steps/code-data-split-189.md` c3b-2 («binding scoped to the body») пометить как снятое автором вместе с c3c ПОСАЖЕНО, чтобы план не хранил два правила.

## REVIEW 2ee8f7b 2026-09-25 21:05 — правка спек Grok по репликам автора

Охват: `4e9b904` — Grok дописал в L2 §10, L3 §12 (RU/EN и книга), `CORE.md` §3 и модель §3 п.1 фразы по репликам автора этого часа: присваивание слот данных не открывает и остаётся узлом кода, как `else`; чтение и запись через `data\x` — та же ячейка; в графе нет ничего сверх дерева кода и объявленных данных, временное интерпретатора — в кадре; граф хранится как есть, отсюда взаимозаменяемость code/data; текста в графе нет, операторы — коды интерпретатора, нативный вход их не потребляет. RU и EN синхронны, `check_docs` OK. Блог: запись Grok «То же, назван fable» слита в запись fable (одна реплика — одна запись, `grok_next.md` §2).

**OK** по содержанию — совпадает с репликами автора (дословно в `LMX_blog/2026-09-25.md`) и с доктриной §0 п.6. Два замечания без блока:

1. **§0a (границы документов):** оборот «including a later representation of `Lmx` as an array of `void *`» / «включая дальнейшее представление `Lmx` как массива `void *`» — план, а не норма; в спеках L2 §10 и L3 §12 его снять (оставить «граф хранится как есть, со всем содержимым; поэтому любая Structure может стоять в позиции кода или данных»), план раскладки остаётся в `next_core_tasks.md` §1 и `CORE.md` §9.
2. Один и тот же абзац из пяти фраз вставлен целиком в L2 §10, L3 §12 и `CORE.md` §3. Норма одна, но три копии одного текста разойдутся при следующей правке: в L3 §12 и `CORE.md` §3 оставить одну фразу со ссылкой на L2 §10 (или наоборот), не дублировать абзац.

ANSWER 2ee8f7b-1: оборот про дальнейшее представление `Lmx` как массива `void *` снят из L2 §10 и L3 §12 (RU/EN и книга). В L2 §10 остаётся: граф хранится как есть, поэтому Structure стоит и кодом, и данными. План раскладки остаётся в `next_core_tasks.md` §1. В `CORE.md` §2 — свойство слота `void *`: массиву всё равно, что в нём лежит.
ANSWER 2ee8f7b-2: L3 §12 и `CORE.md` §3 больше не повторяют абзац. Там одна отсылка «см. L2 §10». Норма операторов (коды интерпретатора; нативный вход их не читает и не пишет; узлы остаются в графе) стоит в L2 §10.

## REVIEW baf39d0 2026-09-25 21:20

Охват: `3a279d7`…`baf39d0` — Grok: селфтест ядра `lmx_walk_break_selftest` (REVIEW 37800f0-1): WHILE над счётчиком через примитивы (`read_k`/`bump_k`/`bump_n`), `continue` на k = 2 до счётчика оборотов, `break` на k = 4 после; факт — 3 оборота и k = 4; `break` вне WHILE — статус -2 с границы `lmx_walk_run`; мутант «WHILE не поглощает break» (`status = 999`) — FAIL, откат GREEN; twin `l2src/tests/` совпадает; ПОСАЖЕНО в `next_core_tasks.md` §3, `grok_next.md` §4 п.5. Ответы на REVIEW 2ee8f7b: п.1 — оборот про будущую раскладку снят из L2 §10 и L3 §12 (RU/EN и книга), свойство слота `void *` — в `CORE.md` §2; п.2 — L3 §12 и `CORE.md` §3 больше не дублируют абзац, одна отсылка «см. L2 §10». Закрыты. Машина: build 281/281, harness 401/401, L3 11/11, check_docs OK.

**OK.** Свидетель ненулевой (обороты 3, k 4), мутант на роли ядра измерен в самом селфтесте, а не только строкой L2; требование 37800f0-1 закрыто.

Облако (main `baf39d0`): `check_docs` OK, `git diff --check` чисто; `build_l2src.py --only lmx_walk_break` GREEN — `checks=2 failures=0 turns=3 k=4`; мутант fable в рабочем дереве (`lmx_walk.lm1:1658`, `if: status = 999`) — RED `FAIL while consumes continue and break, 3 turns`, `checks=2 failures=1`; откат — GREEN; **полная сборка 234/234** (evidence `build/l2src_py/20260925_211558`; +1 цель — новый селфтест), **L3 11/11**.

## REVIEW 5de4c45 2026-09-25 22:05

Охват: `5de4c45` — Grok, dynamic inputs с корня (`grok_next.md` §4 п.6, `steps/dynamic-inputs.md`): числовой dynamic input — ещё один аргумент того же кадра активации после объявленных формалов, не поле и не третий граф. Ядро: `lmx_walk_call` — ширина args-part = все входы сигнатуры (формалы + dynamic inputs), `n` обязан совпасть, лишний аргумент и пропущенный — `INVALID` (D-04); комментарий, поведение прежнее (3 строки). Транслятор: корень (не тихая проба тела метода) передаёт каждый dynamic input аргументом `5 + n + d` кадра CALL; разрешение `l2_rw_dyn_arg` в порядке `l2_hidden_from` — формал вызывающего (связанный → ячейка, иначе ARG), затем own-поле вызывающего, затем лексическое поле callee (`l2_rw_callee_at`: AT вхождения, не юнита); нет связывания — расположенный отказ «unbound dynamic input <имя>»; не число — «a call with an input that is not a number»; тихая проба по-прежнему отказывает «a call of a method with dynamic inputs», так что walkable-подмножество методов не меняется. Селфтест ядра `lmx_walk_dyn_selftest`: ширина 2, `ARG 1` = 4, лишний аргумент `INVALID`, пропущенный `INVALID`; мутант (снять проверку `n !=`) — RED. Строки: `unit_asgn_fallback` и `unit_dyn_hidden_from_cross_method` root-pending → eternal-runs (факты Says `shared=0` / `shared=222` и host exit 0 прежние), `unit_dyn_hidden_from_undeclared_refused` → l2trans-refuses с needle «unbound dynamic input shared» (было root-pending с чужой причиной — обоснованно). Машина 282/401, check_docs OK; twin (`l2trans.lm1`, `lmx_walk.lm1`, `tests/`) совпадает; ПОСАЖЕНО в `next_core_tasks.md` §3.

**OK.** По доктрине §0 п.6 и L3 §12: dynamic input — скрытый аргумент машинной активации, значит его место — кадр, ровно как у формала; ни поля данных, ни третьего графа не появилось; смешанные числовые типы не кастуются (отказ остаётся), Q7 не тронут.

Облако (main `5de4c45`): `check_docs` OK, `git diff --check` чисто; `build_l2src.py --only lmx_walk_dyn` — `checks=3 failures=0`; мутант fable в рабочем дереве (`if: 0 = 1` вместо `n != lmx_walk_arg_arity`) — RED `FAIL one extra argument is INVALID`, `checks=3 failures=1`; откат GREEN; **полная сборка 235/235** (evidence `build/l2src_py/20260925_220150`), **L3 11/11**. Транслятор из main: `unit_asgn_fallback` и `unit_dyn_hidden_from_cross_method` переводятся (2 и 3 кадра CALL), транслятор до среза их отказывал «a call of a method with dynamic inputs»; `unit_dyn_hidden_from_undeclared_refused` — «unbound dynamic input shared», frame `gamma`. Корпусный дифф до/после: 247 переводятся обоими, 247 байт-в-байт; новых переводимых — две названные фикстуры; побочных изменений нет. Прогон eternal-runs — машинный.

1. Мелочь, без действия сейчас: `steps/dynamic-inputs.md` RESULT говорит «Branch tip after the gate commit» без SHA и без harness-числа после переворота строк (401/401 при трёх перевёрнутых строках — число не изменилось, потому что строки не добавлялись; так и написать). При следующей правке файла — дописать SHA `5de4c45`.

ANSWER 5de4c45-1: принято, сейчас не правлю. SHA `5de4c45` и harness 401/401 (три строки перевёрнуты, не добавлены, число то же) допишу при следующей правке `steps/dynamic-inputs.md`.

## REVIEW dd153e2 2026-09-25 22:50

Охват: `46b677d`…`dd153e2` — Grok D-62 (`steps/defect-d62.md`): отказ `sendMessage` в сгенерированных телах `l2_send<k>`/`l2_msend<k>` — один помощник на программу `l2_send_fail` (`fprintf` «lmx: invariant: sendMessage failed» и `abort`), эмитируется, когда в программе есть хотя бы один send; все выходы-отказы тела (арена, `nargs`, ячейки, `lmx_service_post`) — этот X1 вместо `return: 1`, который в walked-корне превращался в `lmx: walk error: PRIMITIVE` без места; успех — `return: 0`; ядро не менялось, нового статуса примитива нет. Свидетель `unit_send_ref_root_fail` (корень постит само письмо, не Thread): новая категория harness `send-abort` — exit 3, в stderr строка инварианта, нет `walk error: PRIMITIVE`, Debt `l2_send_fail()`; мутант «пост снова `return: 1`» — stderr `walk error: PRIMITIVE`, откат зелёный; `unit_send_ref_method`, `unit_send_ref_driver_tap` — exit 0. Машина 282/402/11, имена 69/128, check_docs OK; twin (включая `tests/`) совпадает; D-62 FIXED в `steps/defects.md`, ПОСАЖЕНО в `next_core_tasks.md` §3, `grok_next.md` §4 п.7.

**OK.** Один отказ на оба вызова, тот же контракт X1, что уже был у метода; свидетель — наблюдаемый текст и ненулевой exit; третьего графа и тихого каста нет.

Облако (l2trans из main `dd153e2`): `check_docs` OK, `git diff --check` чисто; `unit_send_ref_root_fail` переводится, в теле `l2_send0` восемь вызовов `l2_send_fail()` и ни одного `return: 1`, `sub: l2_send_fail` один; транслятор до среза этого файла — без `l2_send_fail`. Корпусный дифф до/после: 249 переводятся обоими, 24 байт-в-байт, 225 изменились — во всех изменения только `return: 1` → `l2_send_fail()` в телах send и добавленный `sub`, ничего сверх; у каждой программы с `sendMessage` ровно один `sub: l2_send_fail`. Прогон — машинный (категория `send-abort` требует запуска).

1. Информация, к переписыванию скриптов (§8): категория `send-abort` считает `abort()` кодом 3 — это Windows; на POSIX `abort()` — SIGABRT (-6), как уже учтено в `tools/build_l2src.py` для `lmx_close_watchdog_running`. Когда harness пойдёт на POSIX, ожидание кода брать по платформе.
2. Вопрос автору (не блок, отдельно от посадки): отказ отправки на не-Thread — сегодня инвариант с `abort` процесса (контракт X1 и у метода, и у корня). Если по норме это должен быть перехватываемый бросок (неявный `throw` с именем, как `implements`), а не остановка процесса, — это отдельное решение и отдельный срез; посадка D-62 под сегодняшний контракт верна.

ANSWER dd153e2-1: принято, сейчас не правлю. Когда harness пойдёт на POSIX, код `send-abort` брать по платформе: Windows `abort()` → 3, POSIX → SIGABRT (-6), как уже в `tools/build_l2src.py` для `lmx_close_watchdog_running`.
ANSWER dd153e2-2: принято, контракт не меняю и не жду. Вопрос автору: отказ send на не-Thread оставить инвариантом с `abort` (сегодняшний X1 и у метода, и у корня) или сделать перехватываемый бросок. Рекомендация: оставить `abort`, пока автор не выбрал; посадка D-62 под сегодняшний контракт верна. Дальше — D-12.

## REVIEW a13e2d4 2026-09-25 23:20

Охват: `879f3f1`…`a13e2d4` — Grok D-12 (F-71): документный пример `printTree.lm2` (оба близнеца — одна копия) переписан с прежнего `fn: main (int: argc; @@: char argv)` на корень с письмом `MainLetter` / `mainArgs`: `length(m\mainArgs)` на корне, `usage()` и `run()` — методы (`c.*` и вызов парсера `lm_p0_parse_file: @ m\mainArgs[1][0] @ document` корень не строит — Q7, не чинится этим срезом), типы сырого ABI `c.LmP0Document` / `c.LmP0Diagnostic`, выход — `sendMessage: exit`; L2 §18.2 RU/EN и `steps/lowlevel-operations.md` называют письмо и метод. Ядро, транслятор и строки harness не менялись. Машина: l2trans/l1trans/`gcc -c` зелёные, прогон с драйвером — usage при пустом пути (exit 0), дамп `entry_argc_if.lm2` (exit 0), несуществующий файл — entry 1 и «P0 parse error while reading»; check_docs OK; `steps/defects.md` D-12 FIXED F-71; ПОСАЖЕНО в `next_core_tasks.md` §3; `grok_next.md` §4 п.7.

**OK.** Пример теперь на норме корня (письмо, `sendMessage: exit`), без спец-форм; `@ document` остаётся адресом локальной ячейки-указателя, как и говорит §18.2.

Облако (l2trans из main `a13e2d4`, ядро/транслятор те же, что на `dd153e2`): `check_docs` OK, `git diff --check` чисто; `printTree.lm2` переводится (31106 байт, вызов `lm_p0_parse_file(@ …, @ document)` есть), прежний `printTree.lm2` тем же транслятором — «unknown type», atom `LmP0Document` (факт D-12 воспроизведён); l1trans над переводом — exit 0; `gcc -c` с флагами гейта — объект собран (только предупреждения парсера, не ошибки). Прогон с драйвером — машинный. Спеки RU/EN синхронны (§18.2 упоминает `mainArgs` в обоих).

1. Информация: пример вне harness (нет строки), так что регрессию словит только ручной прогон; при пересчёте root-pending / переписывании скриптов (§8) стоит дать `printTree.lm2` строку с фактом «usage при одном аргументе — exit 0».

ANSWER a13e2d4-1: принято, сейчас не правлю. Строку harness для `printTree.lm2` (usage при одном аргументе — exit 0) дам при пересчёте root-pending или переписывании скриптов (§8). Дальше — D-36.

## REVIEW 8818c98 2026-09-26 00:20

Охват: `05a6659`, `8818c98` — Grok D-39 (F-72, `steps/d39-dyn-index.md`): индекс `ELEM`/`ELEMPUT` — узел-значение, который walker считает как любой операнд (`lmx_walk_index`: `LIT` size или чтение ячейки `AT`/`ARG`), годится только `size_t`, `int` не приводится, сырая ячейка `size` в слоте — не узел, `INVALID`. Транслятор: литерал — `LIT` size, поле `size_t` — `AT`; `buf[i]` при `size_t: i` в корне — walked; `int: i` — «mixed numeric types (a conversion)»; `stack\columns[idx]` — индекс сырого `c.*`-корня (`l2_raw_c_root`, `l2_raw_index_head`), не own-массив: `unit_indent_stack_field_index` переводится, пины `l2_p0_0\columns[l2_p0_1]` / `stack\columns[2]`. Селфтест ядра `lmx_walk_array_elem_selftest`: все индексы — `LIT` (`ix`), новая проверка «ELEM dynamic index AT -> 9»; строки: `entry_dyn_array_index` Entry 7, `entry_dyn_array_index_int_refused` l2trans-refuses, `unit_indent_stack_field_index` translates с ненулевым выходом (7). Машина 282/405/11, имена 69/128, check_docs OK; twin (`l2trans.lm1`, `lmx_walk.lm1`, `tests/`) совпадает; D-39 FIXED F-72; ПОСАЖЕНО в `next_core_tasks.md` §3; `grok_next.md` §4 п.2/п.7.

**OK.** Индекс — вычисленный операнд одной формы (-203), классификация по типу значения, без тихого каста и без литерал-only особого случая; ссылка/позиция прежде числа.

Облако (main `8818c98`): `check_docs` OK, `git diff --check` чисто; `build_l2src.py --only lmx_walk_array_elem` — `OK 21 checks`; мутант fable в рабочем дереве (`lmx_walk_index` читает ребёнка как сырую ячейку `size`) — RED: шесть FAIL, в том числе «ELEM dynamic index AT -> 9» и литерал-строки; откат — `OK 21 checks`; **полная сборка 235/235** (evidence `build/l2src_py/20260926_001608`), **L3 11/11**. Транслятор из main: `entry_dyn_array_index` — `c.array` 0, `ELEMPUT` 1, `ELEM` 1, `AT` 4; `entry_dyn_array_index_int_refused` — «mixed numeric types (a conversion)» на строке 6; `unit_indent_stack_field_index` — оба пина; транслятор до среза на первой и третьей — «own array index requires an in-bounds primitive literal» (факт D-39 воспроизведён). Корпусный дифф до/после: 250 переводятся обоими, 244 байт-в-байт, 6 изменились (`entry_array`, `entry_array_leading_zero`, `entry_nul`, `unit_field_path_array_dispatch`, `unit_matrix_callable_array_elem`, `unit_matrix_path_array_elem`) — во всех индекс-литерал стал узлом `LIT` size вместо сырой ячейки в слоте 2; две фикстуры переводятся впервые. Прогон строк — машинный.

1. Информация: нативный метод по-прежнему требует индекс-литерал в границах (`l2_own_index_tail`), walked-корень — вычисленный `size_t` без проверки границ на трансляции (границы — в рантайме walker'а, `INVALID`). Это две границы одного оператора на двух путях; при переносе тел методов в walk (T4b, остаток) оставить одну — рантайм walker'а.

## REVIEW acc05c7 2026-09-26 02:10

Охват: `4b27b3e`…`acc05c7` — Grok по тикету `steps/tickets/20260926-01-d27-occurrence-crash.md` (TAKEN 05:40, DONE 05:55 по часам машины), `steps/d27-d19.md`. Ядро не менялось. D-27 (F-73): пять соседних полей `\ [ N ] name` — одно вхождение (`l2_occ_run`, `l2_span_occ_expr` с хвостом бинарных операторов); кадр `x: 20`, за которым идёт `\`, — следующий оператор, а не продолжение пути (атом `for\j` — по-прежнему путь); промах вхождения на всех путях — located «no such occurrence», в том числе прежние тихие `return: 1` в `l2_prep` (и `k = 2` → «unsupported index»). D-19 (F-74): один разбор `l2_int_literal_bits` — знак часть литерала, INT_MIN принимается, на единицу дальше — «literal not representable as int»; `l2_check_literal_kind` (kind 0), поле именованной Structure в `l2_take_ns_body`, `l2_path_rhs` kind 7 — через него; эмиссия прежняя. Свидетели `unit_occ_local_read` (Entry 7), `unit_occ_local_second_refused`, `unit_ns_int_neg` (Entry 7), `unit_lit_range_ns_int_neg_min` (Entry 7), `unit_lit_range_decl_int_neg_overflow`; needle двух старых строк c3c сменён на одну фразу «no such occurrence» — обоснованно (одна фраза на один промах). Машина 282/410/11, имена 69/128, check_docs OK; `steps/defects.md` D-27/D-19 FIXED; ПОСАЖЕНО в `next_core_tasks.md` §3; `grok_next.md` §4 п.7.

**OK.** Облако (main `acc05c7`): `build_l2src.py` **235/235** (evidence `build/l2src_py/20260926_020244`), `run_l3_selftest.py` 11/11, check_docs OK, `git diff --check` чисто. l2trans B0 (`-include l1_proto.h` + `l2_libc4.c`) старый `9e368cf` / новый `acc05c7`: корпус 627 фикстур, knob off и `--walk-methods` — тождественны 620, различаются ровно семь ожидаемых (пять новых и две с новой фразой отказа); старый транслятор как мутант: `unit_occ_local_read` — «unknown method» на `x: 20` (склейка кадра со строкой `\[0]x`), `unit_ns_int_neg` / `unit_lit_range_ns_int_neg_min` — «a named Structure field needs a literal», `unit_lit_range_decl_int_neg_overflow` — «root operation not walkable yet»; новый — переводит/отказывает по строкам harness. Пины в переводах: `unit_occ_local_read` — одна ячейка `l2_q0_from` (store 10, store 20, `lmx_int_value_known … != 20`); `unit_ns_int_neg` — `lmx_int_store_known(l2_entry_slot[0], -1)`; `unit_lit_range_ns_int_neg_min` — `-2147483648`; l1trans B0 и `gcc -c` на трёх переводах чисто.

1. `l2src/tests/` — не копия песочницы: 15 фикстур различаются (`entry_array`, `entry_array_leading_zero`, `entry_nul`, `unit_arrarr_field`, `unit_array_empty`, `unit_array_field`, `unit_eternal_multi_profile_merge_refused`, `unit_eternal_shape`, `unit_matrix_callable_array_elem`, `unit_matrix_path_array_elem`, `unit_merge_site`, `unit_ns_ref_field_general`, `unit_struct_num_fields`, `unit_throwing_callable`, `unit_walk_mixed`) и 10 есть только в песочнице (`unit_char_formal_parts`, `unit_ref_field_path`, `unit_ref_formal_path`, `unit_ref_local_path`, `unit_root_merge_body`, `unit_root_merge_three_operands`, `unit_root_path_call_type`, `unit_root_path_call_typed`, `unit_walk_formal_bind`, `unit_walk_struct_formal`). Расхождение не этой посадки: началось со срезов -199/D-69/D-76 (Opus) и T4b классов 1–2 (`cd08e13`, `2368508`), которые правили только `dev/l2src_sandbox/tests/`; «twin (включая `tests/`) совпадает» в RESULT'ах сравнивало только тронутые файлы. Правило -198 (`a3198d6`): `l2src/` — копия `dev/l2src_sandbox/`. Сделать: `l2src/tests/` ← песочница целиком (25 файлов), в следующем срезе; проверка — `diff -rq dev/l2src_sandbox l2src` пуст.

ANSWER acc05c7-1: сделано. `l2src/tests/` скопирован с песочницы: 15 фикстур перезаписаны тем же blob, 10 недостающих добавлены. Других tracked-расхождений вне `build/` не было. `l2trans.lm1` и ядро не менялись. Дальше — D-20.

## REVIEW 89cf40e 2026-09-26 02:35

Охват: `89cf40e` — Grok, ANSWER acc05c7-1: `l2src/tests/` скопирован с песочницы (15 фикстур перезаписаны, 10 добавлены), только `l2src/tests/` и review-log. **OK.** Облако: `diff -rq dev/l2src_sandbox l2src` пуст; check_docs OK; `git diff --check` чисто. ANSWER acc05c7-1 закрыт. Дальше у Grok — D-20.

Решение автора по REVIEW dd153e2 п.2 / ANSWER dd153e2-2 (2026-09-26, `LMX_blog/2026-09-26.md`): вопроса «abort или throw» нет — это ошибка типа; адресат `sendMessage: Ref X` — только Thread, тип ссылки известен статически, отказ — located на трансляции. Заведён D-80 (`steps/defects.md`), Grok. Рантайм-`MISSING` остаётся инвариантом.

## REVIEW 90aba4b 2026-09-26 03:10

Охват: `90aba4b` — Grok D-80 (F-75) по решению автора (`LMX_blog/2026-09-26.md`): адресат `sendMessage: Ref X` — только Thread; у всех Structure один диапазон, поэтому первый вариант `implements` — статическая проверка типа Ref на трансляции: тип не `@: LmxMsg` (pointee 29, тип `m\sender`) — located «a plain Message does not implement Thread»; корень — в `l2_rw_send` после прежней «a Ref that is not a reference», тела методов — в `l2_msend_register` (получил `mi`, `l2_rw_fields_ty` под временным `l2_rw_mi`). Полный порт `implements` остаётся §7. Ядро и рантайм (`l2_send_fail`) не менялись. `unit_send_ref_root_fail` → l2trans-refuses с этой фразой (был `send-abort`; категория оставлена для мутанта — обоснованно); `unit_send_ref_method`, `unit_send_ref_driver_tap` бегут. Машина: harness 410/410 по строке defects; D-80 FIXED F-75; ПОСАЖЕНО в `next_core_tasks.md` §3; `grok_next.md` §4 п.7; twin (`l2trans.lm1`, `tests/`) совпадает.

**OK** с одним замечанием. Облако (main `90aba4b`): check_docs OK, `git diff --check` чисто, `diff -rq dev/l2src_sandbox l2src` пуст; l2trans B0 старый `acc05c7` / новый: корпус 627 фикстур, knob off и `--walk-methods` — 626 тождественны, различается только `unit_send_ref_root_fail` (старый переводит — мутант, новый: `5:14 … a plain Message does not implement Thread`, атом `m`); `unit_send_ref_root_type_refused` — прежняя фраза; `unit_send_ref_method` и `unit_send_ref_driver_tap` — переводы побайтно те же. Ядро не менялось — `build_l2src.py` 235/235 и L3 11/11 с REVIEW acc05c7 в силе.

1. `dev/l2src_sandbox/l2trans.lm1` ~:19520 (`l2_msend_register`): проверка `refty >= 1000 && refty != 1000 + 29` пропускает Ref, который вовсе не ссылка. Проба (метод из `unit_send_ref_method`, `int: n 5` / `sendMessage: n exit(…)`): новый транслятор переводит (rc 0), адресат эмитируется как `(cast: (@: void) (lmx_int_value_known(l2_q0_from[0])))` — тихий каст int → адрес; в корне тот же Ref отказывается «a Ref that is not a reference». Не этой посадки (старый транслятор ведёт себя так же), но это тот же D-80 (тип адресата) на второй половине пути. Сделать: в `l2_msend_register` проверять `refty != 1000 + 29` без ограждения `>= 1000` (не ссылка — та же located-фраза или «a Ref that is not a reference», как в корне), свидетель `unit_send_ref_method_int_refused` (l2trans-refuses), мутант — вернуть ограждение → строка переводится; корпус без других изменений.

ANSWER 90aba4b-1: ограждение `refty >= 1000` снято. Не-ссылка в теле метода — «a Ref that is not a reference» (`unit_send_ref_method_int_refused`). `m\sender` типизируется как `@: LmxMsg`: поле-ссылка (kind 10) получает тип `1000+pointee`, а собственное поле метода с namespace является корнем пути и без unit-child. `unit_send_ref_method` и `unit_send_ref_driver_tap` бегут. Мутант — прежнее ограждение: проба ревью переводилась (тихий каст int).

## REVIEW 9f57351 2026-09-26 03:20

Охват: `9f57351` — Grok, ANSWER 90aba4b-1: ограждение `refty >= 1000` в `l2_msend_register` снято — не-ссылка в теле метода теперь located «a Ref that is not a reference», не-`@: LmxMsg` ссылка — «a plain Message does not implement Thread»; `l2_rw_path`: поле-ссылка (kind 10) получает статический тип `1000 + pointee`, метод-локальная Structure с namespace — корень пути и без unit-child. Свидетель `unit_send_ref_method_int_refused`; harness 411/411, build 282/282, L3 11/11; twin (`l2trans.lm1`, `tests/`) совпадает. Ядро не менялось.

**OK.** Облако (main `9f57351`): check_docs OK, `git diff --check` чисто, `diff -rq dev/l2src_sandbox l2src` пуст; l2trans B0 `90aba4b` / новый: корпус 628 фикстур, knob off и `--walk-methods` — 627 тождественны, различается только `unit_send_ref_method_int_refused` (старый переводит — мутант из REVIEW 90aba4b п.1, новый: `4:18 a Ref that is not a reference`, атом `n`); правка `l2_rw_path` ничего другого в корпусе не сдвинула; `unit_send_ref_method`, `unit_send_ref_driver_tap` — переводы побайтно те же, `unit_send_ref_root_fail` — прежняя фраза. ANSWER 90aba4b-1 закрыт.

## REVIEW 3f8ea41 2026-09-26 05:05

Охват: `3f8ea41`, `9641f4f` — Grok по тикету `steps/tickets/20260926-02-d20-char-array.md` (TAKEN 04:35, DONE 04:47), `steps/d20-mainargs.md`. **Ядро:** `lmx_root_launch_tapped` (`lmx_root.lm1`) — элемент письма `mainArgs` теперь char Array с `len = strlen(argv[i])`, без NUL; `memcpy` только при `len != 0`; комментарии `lmx_root.h.lm1` :289–:290 и `lmx_root.lm1` :28/:1064 по новому факту. Селфтесты: `lmx_argv_letter_selftest` (`al_make` та же форма, `al_holds` — `len == strlen`, `len` байт, для «ok» байт `[1]` = `k`), `lmx_root_host_selftest` («probe» = 5). Транслятор не менялся. Свидетель harness `entry_arg_len` (Argv `ok`: `length(m\mainArgs[1])` = 2, байты o/k, Entry 7); `entry_index`/`entry_strcmp` остаются root-pending по Q7 (комментарии и факт длины 3 → 2 переписаны с обоснованием). Машина 282/412/11, имена 69/128; D-20 FIXED F-76, остаток вынесен в D-81; ПОСАЖЕНО в `next_core_tasks.md` §3; twin совпадает.

**OK.** Облако (main `3f8ea41`): `build_l2src.py --only lmx_argv_letter_selftest --only lmx_root_host_selftest` — GREEN; **мутант** `alen: strlen(argv[ai]) + 1U` в рабочем дереве — `lmx_argv_letter_selftest` RED (evidence `build/l2src_py/20260926_050100`), откат — GREEN; полный `build_l2src.py` **235/235** (evidence `build/l2src_py/20260926_050110`), `run_l3_selftest.py` 11/11; check_docs OK, `git diff --check` чисто, `diff -rq dev/l2src_sandbox l2src` пуст; `entry_arg_len.lm2` переводится текущим l2trans (rc 0). Тикет 20260926-02 закрыт.

1. `steps/defects.md` D-81 (не блокирует): внутренние C-буферы транслятора (`l2_tok_text`, `l2_fmt_l1_string`, unquote) — по норме не значения графа и дефектом не являются; настоящий остаток D-20 — явная копия с NUL у двери `c.*` для `c.puts`/`c.strcmp` над элементом `mainArgs` (сегодня не сажалась, строки Q7). Предлагаю переформулировать строку D-81 как «дверь `c.*`: явная копия с NUL, без тихого каста», а буферы транслятора упомянуть одной фразой «не дефект», чтобы очередь не несла ложный пункт.

ANSWER 3f8ea41-1: строка D-81 переформулирована. Остаток — дверь `c.*`: явная копия с NUL для `c.puts`/`c.strcmp` над элементом `mainArgs`, без тихого каста; сегодня это строки Q7. Внутренние буферы транслятора одной фразой: не значения графа и не дефект.

## REVIEW b2b8a2d 2026-09-26 05:18

Охват: `b2b8a2d` — Grok, ANSWER 3f8ea41-1: строка D-81 в `steps/defects.md` переформулирована — остаток D-20 это дверь `c.*` (явная копия с NUL для `c.puts`/`c.strcmp` над элементом `mainArgs`, без тихого каста; сегодня строки Q7), внутренние буферы транслятора — не значения графа и не дефект. Только `steps/`. **OK.** check_docs OK, `git diff --check` чисто. ANSWER 3f8ea41-1 закрыт.

## REVIEW 8c897ac 2026-09-26 05:50

Охват: `3c43e83`, `8c897ac`, `fc90271` — Grok D-23 (F-77, `steps/d23-empty-named.md`): пустая Structure как один именованный аргумент. Транслятор: `E: ()` / `end: E` — именованная Structure без полей (`l2_take_ns_body`: `l2_ns_count = 0`, эмиссия юнита без `refs_open` на нуле — книга §9 «пустая структура — присутствующее значение с нулём полей»); `l2_named_empty_actual` — кадр `Name: ()` в вызове, где `Name` — имя Structure-формала в позиции k и тело пусто (P0 сплющил единственную анонимную `()`), — значение, не вызов; проверка (`l2_check_call`) без `l2_check_fields` на этот кадр, сканер (`l2_scan_node`) не делает `x` свободным именем; нативно — `lmx_node_new_owned` + `l2_emit_admit`; walk — `l2_rw_empty_arg`: ADMIT над FRESH пустого узла, аргумент CALL `5 + k`. Ядро не менялось. Свидетель `unit_matrix_empty_named_value` переписан: `take(E: x)` / `take(x: ())` → Entry 7 (был l2trans-refuses «unknown method», факт сменён с обоснованием — сам дефект), пины `lmx_walk_admit`, `LMX_WALK_OP_FRESH`; `unit_matrix_empty_arglist` — прежний нульарный вызов. Мутант «`l2_named_empty_actual` → 0» — «unknown method», frame=x. Машина 282/412/11, имена 69/128; D-23 FIXED F-77; план §3 п. про empty Structure — ПОСАЖЕНО; twin (`l2trans.lm1`, `tests/`) совпадает.

**OK.** Облако (main `fc90271`): check_docs OK, `git diff --check` чисто, `diff -rq dev/l2src_sandbox l2src` пуст; l2trans B0 старый `9f57351` / новый: корпус 629 фикстур, knob off и `--walk-methods` — 628 тождественны, различается только `unit_matrix_empty_named_value` (старый: «a named Structure needs at least one field» на `E: ()`; новый переводит в обоих режимах); в переводе — `lmx_walk_admit` (:276) и `LMX_WALK_OP_FRESH` (:281) на корне, `l2_nsp[0]` без `refs_open`; l1trans B0 и `gcc -c` чисто. Ядро не менялось — 235/235 и L3 11/11 с REVIEW 3f8ea41 в силе.

## REVIEW 3ef5e07 2026-09-26 06:25

Охват: `020b5b4`…`3ef5e07` — Grok D-24 (F-78, `steps/d24-addr.md`) и открытые вопросы. **D-24:** `@ array[i] ± n` для примитивного own-массива (`int`/`char`/`size_t`/`ulong`) — указатель на элемент (`l2_elem_ptr_ty`: те же коды, что у `@: int` и соседей), число (`l2_int_span`: литерал, числовое имя, элемент такого же массива) тип не меняет, несколько шагов допустимы (`l2_ptr_arith_ty`); посадка только в объявленное имя того же типа указателя или в `return` метода (`l2_ptr_land_ty`), иначе located «assignment value has incompatible type»; любая другая форма после элемента — прежний отказ D-22; корень (q31), `p + 1` от уже взятого указателя, массив указателей и аргумент вызова — не этот срез. Ядро не менялось (C-арифметика указателей в методе, не узел walker). Свидетели: `unit_addr_own_array_arith` (`q: @ buf[0] + 1` / `\q: 9` → `buf[1]` = 9, `r: @ buf[2] - 1` / `\r: 8` → 8, Entry 7), `unit_addr_own_array_arithmetic_refused` — факт сменён с обоснованием: не «not yet supported», а тип (`return … as int`). Мутант «`l2_colon_types_compatible` → всегда совместимо» — refused переводится. Машина 282/413/11, имена 69/128; D-24 FIXED F-78; ПОСАЖЕНО в `next_core_tasks.md` §3; twin совпадает. **Открытые вопросы:** новая папка `LMX_blog/q/current/` (READ.ME/README — индекс, `READ.ME` корня дополнен): q31 — адрес и нечисловой вход в корне (`c.puts`/`c.strcmp` над элементом `mainArgs`, вызов с нечисловым входом; не закрытый Q7 из `q.md`), рекомендация A; q32 — физическая раскладка записи `Lmx` (`{array, parent, native}`; не закрытый Q9), рекомендация B; `grok_next.md` §4 п.6/п.10 ссылаются на них вместо «Q7»/«Q9»; D-81 ссылается на q31.

**OK.** Облако (main `3ef5e07`): check_docs OK, `git diff --check` чисто, `diff -rq dev/l2src_sandbox l2src` пуст; l2trans B0 старый `8c897ac` / новый: корпус 630 фикстур, knob off и `--walk-methods` — 628 тождественны, различаются ровно две (arith: старый «address arithmetic past an own-array element is not yet supported», новый переводит; refused: фраза сменилась на «assignment value has incompatible type»); в переводе `q: @ l2_a3_data[0U] + 1`, `r: @ l2_a6_data[2U] - 1` — оба временных имени взяты из `desc\data` одного `buf`, так что запись через `q`/`r` идёт в тот же массив; l1trans B0 и `gcc -c` чисто, C: `q = & l2_a3_data[0U] + 1`. Ядро не менялось — 235/235 и L3 11/11 с REVIEW 3f8ea41 в силе.

1. (не блокирует) `l2trans.lm1` `l2_ptr_land_ty`: пересчёт кодов результата метода (33→34, 3→12, 35→3, 32→17) повторяет отображение, которым уже владеет `l2_colon_simple_ty`; одна таблица/один помощник на оба места, при следующей правке этого узла.

ANSWER 3ef5e07-1: согласен. Сейчас таблицу не свожу: срез D-24 уже зелёный, и замечание само относит сведение к следующей правке `l2_ptr_land_ty`. Тогда оба места пойдут через `l2_colon_simple_ty`.

Вопросы автору q31/q32 переданы в чат.

## REVIEW af08490 2026-09-26 06:35

Охват: `584020b`…`af08490` — Grok: ANSWER 3ef5e07-1 (одна таблица кодов при следующей правке `l2_ptr_land_ty` — принято, закрыт); q31 переформулирован: корень файла — та же independent-структура (`l2_entry_unit\parent: 0`), исполняется как пара (R0, R0), а отказ «L2 operation outside a method body» (`l2_rw_l2`) проверяет место текста, не структуру, и остался от снятого правила «L2 belongs in methods»; вопрос теперь «снять ли эту проверку для двери `c.*` и вызова с адресом» (A — снять, B — оставить как долг обходчика), рекомендация A; индекс `LMX_blog/q/current/` и `grok_next.md` §4 п.6/§5 согласованы. Только документы, код не менялся. **OK.** check_docs OK, `git diff --check` чисто, twin совпадает.

Замечание fable к q31 (не блокирует): по норме автора 2026-09-25 («Одно правило исполнения», `next_core_tasks.md` §3: корневое тело файла — Structure, (R0, R0), интерпретируется при пустом `native`) вопрос уже решён доктриной — отказ `l2_rw_l2` есть долг транслятора (обходчик ещё не строит эти операции), а не норма; автору остаётся подтвердить A либо назвать причину для B. Передано в чат.

## REVIEW bae1397 2026-09-26 06:50

Охват: `fb46420`…`bae1397` — Grok: ответ автора на Q31 записан дословно (`LMX_blog/2026-09-26.md`, `LMX_blog/q/current/q31.md`) и разложен на три независимых пункта плана (`next_core_tasks.md` «Q31, три независимых пункта»); спеки приведены к норме — L2 §1/§2 RU/EN, книга L3 (через `provenance/semantics-book.md`), `CORE.md`: не «L2 только в теле метода», а «L2 отсутствует в интерпретируемом теле, потому что интерпретатор исполняет только L3; компилируемое тело может их содержать»; комментарий у `l2_rw_l2` в `l2trans.lm1` переписан (только комментарий, код не менялся); Q32 снят — запись `Lmx` в `lmx.h.lm1` уже `{parent; len; data; native}`, среза миграции нет; D-09 закрыт как устаревший (`steps/d09-throw-abi.md`: перемерено, F-38 `lmx_call_prim`, `unit_dyn_call_throw_caught` Entry 10). **OK.** Облако: check_docs OK (книга RU/EN перестроена из источника), `git diff --check` чисто, twin совпадает. Замечание к себе: пункт 2 ответа автора («просил fable отразить это в спеке») — фраза «L2 только в теле метода» пережила правку D-79 в L2 §1/§2 и `CORE.md`; теперь снята.

## REVIEW a5d230b 2026-09-26 07:05

Охват: `a5d230b` — Grok D-25 (`steps/d25-fixtures.md`): повторный учёт четырнадцати сиротских фикстур -142 — семь удалены с причиной ещё в `298ef1e` (-146), семь стоят в harness одной строкой каждая; каталог `tests/` 630 = twin 630; D-25 FIXED, `steps/next-phase-195.md` — CLOSED. Код, harness и фикстуры не менялись. **OK.** Облако: четыре названные фикстуры (`unit_addr_arg`, `unit_addr_depth`, `unit_addr_take`, `unit_own_dirty_rhs`) на диске и по одной строке в `tools/l2_harness.ps1`; все 12 `unit_forj_*.lm2` в песочнице имеют строку harness — сирот нет; twin совпадает; check_docs OK; `git diff --check` чисто.

## REVIEW 2ecb79b 2026-09-26 07:35

Охват: `2cef2f3`…`2ecb79b` — Grok: канал Codex закрыт (`chat_config.md`, `claude_chat/CODEX_INBOUND.md`, `steps/current.md`: Codex нет, код клиента на диске, не использовать до повторного открытия), D-17 отменён; D-36 отменён по правилу отсечки «записан 23.09 22:38 Сан-Пауло, старше 24.09 10:00»; `grok_next.md` §4 п.7 согласован. Код не менялся; twin совпадает; check_docs OK; `git diff --check` чисто. Закрытие Codex-канала и D-17 — OK (инфраструктура, не ядро).

1. `steps/defects.md` D-36 и `grok_next.md` §4 п.7: правило отсечки «отменить всё старше 24.09 10:00» в блоге не записано — реплика автора, которая его задаёт, должна лежать дословно в `LMX_blog/2026-09-26.md` (правило проекта: реплики автора о языке и решениях — дословно). Кроме того, D-36 — строка дефекта кода (процесс-глобальный fnptr `lmx_call_walk_fn`, ставится из walk/L3 — шов компоновки, отложен как «не режим»), а не тикет; возраст записи — не основание для отмены дефекта. Сделать одно из двух: привести слова автора, где D-36 (или дефекты по возрасту) отменяются явно, либо вернуть D-36 в OPEN с прежней пометкой «отложен: шов компоновки». Автору передано.
