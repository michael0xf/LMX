# c.puts in a method body via the raw c.* door (not main-only specials).
fn: say () int
    c.puts: "from-method"
    return: 0
end: say

sendMessage: exit(exit_code: say(); stdout: ""; stderr: "")
return
