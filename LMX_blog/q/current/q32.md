# Q32. Физическая раскладка записи `Lmx` — вопроса нет

Состояние: 2026-09-26, снят. Вопрос «менять ли запись сейчас» поставлен по ошибке: в `dev/l2src_sandbox/lmx.h.lm1` запись уже такая.

```text
struct: Lmx
    @: Lmx parent
    int: len
    @: void data
    LmxEntry: native
end: Lmx
```

`len` и `data` — массив `void *`. `parent` и `native` уже поля этой записи. Отдельного среза миграции нет. Это не закрытый Q9 из `q.md` и не новое задание.
