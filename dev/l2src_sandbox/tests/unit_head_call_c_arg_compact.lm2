# Control opposite of unresolved empty: compact c.bogus with an argument still
# translates (is_known door). Not a method call; emission keeps the c. spelling.
fn: main () int
    c.bogus(1)
    return: 0
end: main
