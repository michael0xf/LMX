# Distinct diagnostic: known candidate/required, unknown consumer.

KnownCandidate:
    size_t: knownField 0U
end: KnownCandidate

fn: main () int
    if: implements(KnownCandidate, KnownCandidate, MissingConsumer) != 0
        return: 1
    return: 0
end: main
