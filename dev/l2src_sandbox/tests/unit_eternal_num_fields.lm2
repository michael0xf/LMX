independent:
    const:
        immutable:
            (): E
                int: i 4
                unsigned: u 5U
                ulong: l 6U
                size_t: s 3U
            end: E
        end: immutable
    end: const
end: independent

# FABLE-OPUS-DISCARD-20260924-147 (D-15 reopened): the numeric fields of an
# eternal branch -- int, unsigned, ulong, size_t -- are cells of their own
# primitive type in the branch's exact sealed profile, holding their literals
# (the driver's `int`/`unsigned`/`ulong`/`size` root facts).  Success is 7.
return: 7
