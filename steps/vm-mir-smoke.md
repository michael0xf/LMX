# VM MIR build/load smoke (Path A §1)

- request_id: `LMX-VM-MIR-SMOKE-20260920-39B`
- author: Grok Bot
- utc: 2026-09-20T15:47:00Z
- tree: `C:\\Nyasha_Planet\\LMX` HEAD `573baa1`
- scope: evidence report only. Third-party MIR checkout/binaries stay under ignored `build/vm/`. No `lmx_walk` / `l3_interp` / mixa edits.

Author override of the idle hold in `steps/vm-porting.md`: after 38B inventory, proceed with earliest Path A item only (MIR).

## Prerequisites found (no installs)

| Tool | Where | Version / note |
|---|---|---|
| Windows git | host PATH | 2.37.0.windows.1 |
| Windows cmake | host PATH | 3.30.5 |
| Windows gcc | `C:/Qt/Tools/mingw1310_64/bin/gcc.exe` | 13.1.0 (MinGW) |
| WSL | Ubuntu, WSL2 | kernel `6.18.33.2-microsoft-standard-WSL2`; distro was Stopped, starts on demand |
| WSL git | `/usr/bin/git` | 2.34.1 |
| WSL cmake | `/usr/bin/cmake` | present |
| WSL gcc/g++/make | `/usr/bin/*` | gcc 11.4.0 (Ubuntu 22.04) |

Chosen path: **WSL Ubuntu** (MIR documents Linux/macOS; do not treat Win32 MinGW as supported for MIR itself).

## Upstream MIR

- URL: https://github.com/vnmakarov/mir
- Clone: `build/vm/mir` (gitignored under `build/`)
- Commit: `a8ab7c31cd5f9b23b77d84c60b3d83e62d9d304c` (`a8ab7c3` — *Use the last callee-saved reg in jcall.c for aarch64.*)
- Depth-1 clone; build dir: `build/vm/mir-build`
- Build:

```
cd build/vm/mir-build
make SRC_DIR=../mir -f ../mir/GNUmakefile -j$(nproc)
```

- Build exit: **0**. Produced local `c2m`, `m2b`, `b2m`, `mir-bin-run` (not installed system-wide).

## Fixture

- Tracked L1 C99 seed (smallest tracked `lm1/build/*.c` that has `main`): `lm1/build/make.lm1.c`
- Bytes: 7443
- SHA-256: `1e8e04e23850657fe0729096ec6ac1c428033888f76da4b5d5b13a3590165213`
- git blob: `b33f7a9e8b331561ff71283d91f083fcaea69a4d`
- Provenance: generated by `l1trans` (header comment); tracked under `lm1/build/` (self-build C seed).

CLI note: `c2m` treats `-ei` / `-eg` / `-eb` as *execute with remaining argv*; source file must precede `-ei` (`c2m FILE -ei [args…]`). `m2b` / `b2m` use stdin/stdout, not path arguments.

## Commands and exits

Working directory for artifacts: `build/vm/smoke/` (ignored).

| Step | Command | Exit | Result |
|---|---|---|---|
| Interpret+run | `c2m lm1/build/make.lm1.c -ei` | 1 | Program printed its own usage (no subcommand); **interpreter load OK** |
| Interpret+run useful | `c2m …/make.lm1.c -ei mkdir …/smoke/scratch/from_mir` | **0** | Directory created; stdout: `cmake -E make_directory …` |
| Text MIR | `c2m …/make.lm1.c -S -o …/smoke/make.mir` | **0** | 16062-byte text MIR |
| Binary MIR | `m2b < make.mir > make.bmir` | **0** | 4647-byte `.bmir` |
| Round-trip | `b2m < make.bmir > make.round.mir` | **0** | Text module header matches |
| Direct `.bmir` | `c2m …/make.lm1.c -o …/make_direct.bmir` | **0** | 4644-byte binary |
| Load/run binary | `mir-bin-run make.bmir make.lm0 mkdir …/from_binrun` | **0** | Directory created |

## Verdict

**PASS** — MIR built on existing WSL; tracked `make.lm1.c` compiles to MIR text/binary and loads/runs via `c2m -ei` and `mir-bin-run`.

Not claimed: full L1 P0/own suite, libc ABI matrix, or Windows-native MIR. Next Path A item when authorized: WASM linear (VM-02), not started here.

## Untracked only

`build/vm/mir`, `build/vm/mir-build`, `build/vm/smoke`, helper `*.sh` under `build/vm/`. This report is the only intended Git path.
