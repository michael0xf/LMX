# Adapted from lingvamyxa_prev/tests/trans_invalid_implements_unknown_candidate.lm2.
# Consumer is known so the miss lands on the candidate.

KnownConsumer:
    size_t: knownField 0U
end: KnownConsumer

if: implements(Missing, KnownConsumer, KnownConsumer) != 0
    return: 1
return: 0
