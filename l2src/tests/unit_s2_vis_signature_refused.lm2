# S2 BACKWARD VISIBILITY: a method signature names a named Structure declared BELOW the method.
# The unit is read in one lexical pass, so the type does not exist yet where the header stands.
fn: take (Point: p) int
return: 0
Point:
    size_t: x 1U
end: Point
return: 0
