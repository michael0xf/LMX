# A bare own field or formal is a value: evaluated and discarded, not called.  y stays 4, and g's
# bare formal a leaves a as it was: 4 * 10 + 9 = 49.
int: y 4

fn: g (int: a) int
    a
    return: a
end: g

y
int: r 0
r: g(9)
sendMessage: exit(exit_code: y * 10 + r; stdout: ""; stderr: "")
return
