# Технические уточнения автора — 2026-09-23

## Ответы на Q6–Q10 (граф ребёнка, поля C-структур, порядок объявления, `node\x`, API почты для аргументов)

Источник: реплика автора в сессии `fable` 2026-09-23 (~05:20), вписана автором прямо в `q.md` под вопросом Q6 и продублирована в чат с пометкой «Q6-10 ответил и поскольку это требует обсуждения давай обсудим и дальше будем обсуждать». Вопросы — в `q.md` (Q6: R1/R2/R3 — кто делает граф ребёнка independent; Q7: P1/P2 — доступ к полям C-структур; Q8: U1/U2/U3 — вызов метода выше объявления переменной; Q9: N1/N2 — значение `node\x`; Q10: (A)/(B)/(C) — чтение аргументов из письма). Дословно:

```text
Q6 "файл lm3 lm3 по определению independent (это ты имел в виду под R1 "source"?)
Q7 -- P1
Q8 -- U2
Q9: "убрать N1" правка должна была быть прямо в 
next_core_tasks.md но  в твоем примереq.md```
Model:
    int: x 1
end: Model
Model: m
fn: f () int
    int: x 2
    return: node\x
end: f
```
нет никакого "x" у лексического родителя f(). Так как x объявлен внутри Model! То есть и не N2 тоже. 
N3 -- это ошибка 

Q10: вообще не понимаю вопрос. Синтаксис это про парсер. Откуда в задаче написания API возьмется новый синтаксис?!
Вам нужно только добавить общее API _для почты_ . Очевидно новым ресивером (это семантикаа не синтаксис). Я 
предлжил простейший nextMessage: до 0. Вы не письмо кладете внутрь аргументова аргументы внутрь писма!!!! 
Положите их в массив строк "mainArgs" -- прямо как пришли в Си и дело с концом. Я непонимаю ни одного из твоих трех 
предложений 
```

## Q11: merge бросает throw; для операторов без видимого `throws` отлов не обязателен; неотловленный — в корень с `running = 0`

Источник: реплика автора в сессии `fable` 2026-09-23 (~05:50), ответ на Q11 из `q.md` (провал merge при объявленных throws: abort-класс или объявленный throw). Дословно:

```text
Q11 -- вроде же обсуждали уже. merge бросает throw _но для всех опреаторов в которых программист не видит глазами явное перечисление throws_ не обязательно его отлавливать и тогда улетает в корень с runnable=0. Если нашел (там кстати нет обертки как таковой -- точка обработки может бытть дальше в методе на том же уровне, почитай спеку или если в спеке нет значит из старой спеки и их старых примеров надо перенести из проекта lingvamyxa_prev) -- обрабатывается стандартно и никуда не улетает
```

## Уточнение к Q11: размещение `catch` — до, после или с обёрткой; два одноимённых `catch` не спорят на одном уровне

Источник: реплика автора в сессии `fable` 2026-09-23 (~06:05), поправка к формулировке fable «точка обработки дальше в том же методе на том же уровне». Дословно:

```text
catch не обязательно в том же методе на том же уровне дальше -- там есть пример и с оберткой и с catch сзади и с catch спереди, просто два catch не могут друг с другом спорить на одном уровне. Если в новой спеке нет -- скопируй спеку и примеры из старог проекта
```

## Q12: payload `throw` → `catch` — обычное присваивание через runtime `implements`

Источник: реплика автора в сессии `fable` 2026-09-23 (~06:20), ответ на Q12 из `q.md` (реплика начинается с выдержки из вопроса fable — пункт «согласуют префикс, который потребляет `catch:`»). Дословно:

```text
" согласуют префикс, который потребляет `catch:`; " общий механизм верификации присваивания стандартным runtime implements, и поэтому при трансляции его можно вызывать только частично и транслятор может оптимизировать только ограниченно. Если ошибка типа -- стандартный путь
```

## Q13: `throws:` в шапке метода; явно не отловленное — ошибка трансляции; L1 — не L2, автор пишет только L2

Источник: реплика автора в сессии `fable` 2026-09-23 (~06:35), ответ на Q13 из `q.md`; начинается с выдержки из вопроса fable (ссылка на `l1.txt ~10205` и пример t2). Дословно:

```text
Q13: " (l1.txt ~10205): метод «может объявить имена, которые могут покинуть его тело»; t2 ставит `throws: " -- L1 это не L2, L1 спек пишите вы, я только стараюсь объяснить что это промеэточный язык трансляции и поэтому этоне совсем Си и должен быть L2-подобен. Я пишу только L2. Пожтверждаю "throws:" в шапке метода и если он там и явно не отлавливается то ошибка трансляции
```

## Q14: обработчик покрывает вложенные операторные блоки — общее свойство видимости callable

Источник: реплика автора в сессии `fable` 2026-09-23 (~06:50), ответ на Q14 из `q14-15.md` (регион обработчика: T строго по тексту / K как в старом коде). Дословно:

```text
Q14 -- K и поправь спеку если там неясно написано. Это не какое-то уникальное поведение а общее свойство видимости callable  как мы обсуждали выше
```

## Стиль: `end: <name>` — для длинных тел; `return:` как trailer; закрывается только лестница целиком

Источник: реплика автора в сессии `fable` 2026-09-23 (~07:05), по поводу формы примера в `q14-15.md`. Внесено в `steps/current.md` (раздел «Стиль записи тел»). Дословно:

```text
Отдельно: объясни всем агентам и напиши куда-нибудь в видный всем и сразу документ что 1) формы типа "
fn: firstArg (MainLetter: m) @: char
    return: @ m\mainArgs[1][0]
end: firstArg
" допустимы по правилам парсера но являются плохим стилем так как end:<name> служит для закрытия длинных тел. "return:" в даннмо случае может быть также и trailer то есть стоять вместо "end:".2) Кроме того парсер не требует последовательного закрытия каждого уровня, требуется только финальное закрытие лестницы -- только в этом отличие от Python
```

## Q15: `T: []: []: x` — два уровня самостоятельных одномерных массивов; `[][]:` — прямоугольный

Источник: реплика автора в сессии `fable` 2026-09-23 (~07:15), ответ на Q15 из `q14-15.md`. Собственные слова автора дословно:

```text
Q15: многомерные массивы ровные и неотличимы от включающего их одномерного (как в Си). Одномерный массив одномерных массивов это не двумерный массив даже в Си!!!
```

Вместе с репликой автор вставил текст ответа (помечен как вставка; автор его принял словом «Да»), сохраняется как выдержка:

````text
Да. Ответ на Q15 — **(a), как общее правило для любого `T`**, а не специальный тип письма:

```yaml
MainLetter:
    char: []: []: mainArgs
end: MainLetter
```

Здесь два применения `[]:` создают два уровня **самостоятельных одномерных массивов**: внешний хранит ссылки на дескрипторы внутренних массивов `char`. Это не `[][]:` — один прямоугольный двумерный массив. И это не C `char **` без длин:

```text
mainArgs       = {len: argc,            data: LmxArrayDesc **}
mainArgs[i]    = {len: strlen(argv[i])+1, data: char *}
```

Завершающий NUL входит в элементы внутреннего массива. Оба уровня создаются в арене письма. `MainLetter` остаётся обычной явно объявленной Structure; никакого контекстного `String` и таблицы исключений для `mainArgs` не нужно.

Физическая основа **уже есть**: базовый дескриптор [`{len, data}`](C:/Nyasha_Planet/LMX/dev/l2src_sandbox/lmx.h.lm1:204), конструктор массива `char` и [`lmx_array_ref_new_owned(LMX_TYPE_ARRAY_OF_DESC, ...)`](C:/Nyasha_Planet/LMX/dev/l2src_sandbox/lmx_array_ref_owned.lm1:10). Внешний адресный тип говорит «ссылки на дескрипторы массивов»; что каждый внутренний массив именно `char`, задаёт явное составное объявление и проверяет общий механизм типизации/admission, а не имя поля или контекст письма.

Остаётся зафиксировать семантику записи `T: []: []: x` в спецификации и проверить её понижение транслятором. Отсутствие этой строки в документации — пробел реализации/описания, **не** основание выбирать (b) или временное исключение (d).
````

## Q16: `sizeof:` для массива — «как вам удобно» (рекомендованный вариант A, байты)

Источник: реплика автора в сессии `fable` 2026-09-23 (~08:05), ответ на Q16 из `q16.md` (A — байты / B — число элементов; рекомендация A). Дословно:

```text
"Что возвращает `sizeof:` для массива — байты или число элементов"-- то что вам удобно (видимо рекомендуемый ответ)
```

## К Q16: `c.*` — общие двери; исключение для `c.sizeof` было безосновательным; новые ресиверы добавлять можно

Источник: реплика автора в сессии `fable` 2026-09-23 (~08:10), сразу после ответа на Q16. Дословно:

```text
эту конструкцию ввели только потому что вы безосновательно сделали синтаксическое исключение для c.sizeof (котороя вообще не понимаю). c.* -- общие двери , а новые ресиверы вы можете добавлять в разумном пределе сколько вам надо
```

## `node` — лексический callable-родитель, точнее его подструктура-потомок, где стоит occurrence; никогда не «моё»

Источник: три реплики автора в сессии `fable` 2026-09-23 (~08:20), уточнение к Q9 после отчёта fable о переписке фикстур sticky/dirty («node\x как моя own-ячейка напрямую»); вторая и третья реплики исправляют опечатку первой («parent-потоком» → «parent-потомком»). Дословно, в порядке поступления:

```text
node\x это вообще не "моя" а лексического callable-родителя (то есть его подструктуры если она есть -- то есть node это не обязательно прямой parent лексического метода-родителя, а может быть его parent-потоком)
```

```text
parent-помком
```

```text
parent-потомком
```

```text
Thread тут вообще не при чём
```

## Единая P0-форма пустого и единственного структурного аргумента

Источник: реплика автора в этой сессии 2026-09-23. Технический фрагмент сообщения, дословно:

```text
`f()`   `f: ()` &#x20;
f:
\---
должны быть синонимами ещё в парсере. Я писал это явно. Объяснение которое ты перенес из старой спеки о том что пустая структура для трансдятора неотличима от отсутствия аргументов было !!!ОБЩЕЕКАК И ВСЕ ОСТАЛЬНОЕ ЧЕРТ ПОБЕРИ!!!!  Именно поэтому "f:" запрещено. Для голого "f" "исключение" на уровне парсера которое на исключение вовсе а явное видимое глазу отсутствие пустой структуры. Для трансдятора все четыре формы -- синонимы просто потому что он не может отлчить один аргумент-структуру от структуры аргументов (это истинно не только для пустых структур но также для одной структуры-аргумента соержащей тело).&#x20;
```

## Скобки не определяют вызов; отсутствующая голова объявляет пустую структуру

Источник: технический фрагмент реплики автора в этой сессии 2026-09-23, дословно:

```text
В семантике отдельно объясни что f() сам по себе не означает вызов, вызов получается только если f явно определено как callable. То есть если встреачется f() и callablt неопределено то включается правила для 2) объявления 3) присваивания
f() #при неопределнном f означает объявить пустую структуру f и это синоним "f: ()"
```
