# Тикет 20260925-01: закрыть -201 CATCH по REVIEW 9d01eda

Статус: OPEN (fable, 2026-09-25 14:50 UTC).

Задача — три пункта из `steps/review-log.md` REVIEW 9d01eda, без нового кода ядра:

1. **Мутанты транслятора** (`dev/l2src_sandbox/l2trans.lm1`), с откатом и таблицей в `steps/catch-201.md`: (а) `l2_rw_catch_call` пишет `k_in + 1` → строки с объявленными именами RED; (б) `l2_catch_find` берёт не ближайший покрывающий pad → `unit_s1_catch_sibling`/`_rethrow` RED; (в) admit не первым statement обработчика → `unit_s1_catch_implements` RED. Прогон — машинный harness на затронутых строках.
2. **Открытый пункт «строки перенумерации `(k_in, 0, k_out)` не эмитируются»** — записать в `steps/catch-201.md` (раздел «Открыто») и в строку ПОСАЖЕНО: сегодня не бьёт (методы с throws вне walkable-подмножества, у корня d = 0); при T4b-throws транслятор обязан эмитировать их по образцу `l2_emit_propagate`.
3. **Строка `[x] ПОСАЖЕНО` за -201 в `next_core_tasks.md` §3** (после строки -203): форма (таблица сайта, посадка через кадр, PAD 28), объём (8 строк throw/catch бегут, root-pending 20), числа гейта, открытое: `unit_s1_catch_user_break` (break/continue), D-55, перенумерация при T4b.

Готово, когда: таблица мутантов в `steps/catch-201.md`, строка ПОСАЖЕНО на main, `check_docs` OK, `DONE <sha>` в этом файле.

Дальше — продолжать `next_core_tasks.md` (очередь `grok_next.md` §4: -200 POSIX-двойник, T4b).

CLOSED fable 2026-09-25 15:00 UTC: выполнено до выдачи тикета — `64d5dea` (ANSWER 9d01eda-1/-2: мутанты транслятора, открытый пункт перенумерации, строка ПОСАЖЕНО -201). Брать не нужно.
