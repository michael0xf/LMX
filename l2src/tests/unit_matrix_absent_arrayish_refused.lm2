# FABLE-GROKBOT-MATRIX-20260924-143 cell: absent x Array-ish paren body.
# `buf(3)` with absent buf is not an array declaration; REFUSAL.
fn: probe () int
    buf(3)
    return: 0
end: probe

return: probe()
