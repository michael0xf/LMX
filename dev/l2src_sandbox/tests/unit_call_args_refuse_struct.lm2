# Originally FABLE-GROKBOT-CALL-ARGS-20260922-92 (l2_call_args sole-Structure
# unwrap): pinned `mystruct: ()` as a refused call, since "mystruct" named no
# callable. Superseded by FABLE-SONNET-EMPTY-STRUCT-20260923-132 (book SS10/
# SS12): after -127's P0 parity, `mystruct: ()` is the same Frame as `mystruct()`
# and the closed empty vertical -- an absent name with an empty Structure body
# is a declaration, not a failed call, so this now measures the new correct
# outcome instead of the old refusal. The positive checklist fixture below
# witnesses the declared variable itself; this one only needs to keep
# translating clean.
mystruct: ()
return: 0
