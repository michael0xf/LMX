# CONVERSION.md — mixa_manager C → L1 (ticket 20260917-073447)

## Status (2026-09-17 ~07:55 -03)

**PARTIAL.** Foundation headers translated; mechanical `include:`→`predef:` done (117 tokens / 74 files); `tools\build_mixa.ps1` exists; first `-Run` RED (70/282). Original `.c`/`.h` not yet deleted. ABI probes not yet converted. Sync to `L1\mixa_manager\` not done (await greener build).

## Baseline

- `tools\build_l2src.ps1 -Run` BEFORE: **GREEN: 115 targets**; evidence `L1\build\l2src\20260917_073736`

## L1 construct added

- **`struct:` may complete `type: Foo struct Foo` (kind 97 → kind 115)** in `l1src\l1trans.lm1` (`l1_hdr_type_add`): needed for opaque/complete pairs and for `MixaBackend` forward+body.
- Self-build PASS evidence `build\self_build\20260917_075202_514`
- New pin: `32F3018CD0261E4CAC97B3E6D02C84673A2E9D17F46C8498D0148572E11B2576`

## Headers written (new `*.h.lm1`)

mixa_core, mixa_overlay (+ unit mixa_overlay.lm1), mixa_file, mixa_file_win32, mixa_selection, mixa_draw, mixa_tiles, mixa_buttons, mixa_backend, mixa_event_fifo, mixa_pump, mixa_backend_headless, mixa_backend_win32, mixa_pointer, mixa_highlight, mixa_help, mixa_process, mixa_process_win32, mixa_cmdline, mixa_cmdline_dispatch, mixa_cmdline_dispatch_impl, mixa_app_path, mixa_app_controller, mixa_app_controller_impl, mixa_app_fmpanel, mixa_app_fmpanel_impl.

Translate check (focus set): 26/26 OK after pin + return-type spelling fixes.

## Conscious C kept

| path | why |
| --- | --- |
| `vendor\lmx_msg_host_ingress_v0\` | archive (ticket) |
| `recovery_20260910_222730\` | archive (ticket) |
| `tests\mixa_win32_smoke_harness.c` | Win32 CreateProcess/PostMessage/`windows.h` out-of-process harness |
| `tests\mixa_ingress_host_harness.c` | Win32 host-ingress demo; vendor LmxMsg + `windows.h` |
| `mixa_backend_tableref.h` | L1 open 10.13 — no `type:` spelling for `typedef const T *Alias`; forward-decl + typedef |
| `mixa_file_win32_l2_win.h` | L1 cannot emit `#define WIN32_LEAN_AND_MEAN` before `#include <windows.h>` |
| `tests\l1_gaps\vt.h` | same 10.13 bridge fixture (`VTRef`) |
| `mixa_overlay.h` | still present; body moved to `mixa_overlay.lm1` — pending delete after callers green |
| remaining `*.h` / unit `*.c` | pending delete after build green + ABI probes |

## test.c

Generated-looking draft (`/* generated by l1trans */`) defining `mixa_console_read_window` + a small `main` that opens a file. Treat as dead draft probe; **SKIP** in build_mixa; do not translate as production.

## Defects / contradictions (file:line)

1. **`MixaButton` name collision (C):** `mixa_backend.h:126` `typedef enum MixaButton` vs `mixa_buttons.h:16` `typedef struct MixaButton`. Confirmed `gcc` error when both included. Documented in `mixa_app_fmpanel.h` comments. L1 renames mouse enum to **`MixaMouseButton`** (enumerators `MIXA_BUTTON_*` unchanged).
2. **Import / duplicate types across `*_l2.h.lm1` and new real `.h.lm1`:** e.g. both declare `MixaButton` / `MixaFm` — units that predef both fail (`duplicate type name`). Needs a second pass to drop redundant L2 mirror decls or stop predef'ing both.
3. **`mixa_file_win32.lm1` vs opaque+complete:** compile errors on incomplete `MixaFile` field access when only API header is visible — verify predef of `mixa_file_win32.h.lm1` in that unit.
4. **Still-C units (20260918-063608):** `mixa_console_window` / `mixa_event_source` now `.lm1` source of truth; residual `.c` renamed to `*.c.superseded_by_lm1`. go.sh BUILD OK after share_action.h.lm1 + ctors exclude + vendor LmxMsg + L1ROOT cwd for l2src translate.

## build_mixa first run

- Evidence: `dev\mixa_migration\build\20260917_075522`
- Verdict: **RED 70 of 282** (many headers OK; many core units OK; backend-family and controller-family mostly FAIL pending tableref forward-decl fix + duplicate-type cleanup)
- After tableref forward-decl: backend.lm1.h + probe compiles (`gcc exit=0`)

## Mechanical include pass

117 `include: "mixa_manager/*.h"` → `predef: "mixa_manager/*.h.lm1"` across 74 files. Left alone: system `<...>`, `mixa_backend_tableref.h`, `mixa_file_win32_l2_win.h`, `tests/l1_gaps/vt.h`.

## ABI probes

Not converted yet (`tests\*_abi_probe_*.c` still C). Plan: translate like l2src adapters; run original vs translated stdout byte-for-byte.

## Next for peers

1. Re-run `build_mixa.ps1 -Run` after tableref + controller MixaFm fix.
2. Resolve `*_l2.h.lm1` vs real `.h.lm1` duplicate structs (prefer real headers; thin L2 mirrors).
3. Finish `mixa_console_window.c` / `mixa_event_source.c` / ABI probes → `.lm1`.
4. Delete residual `.c`/`.h` (except conscious list); sync tree to `L1\mixa_manager\`.
## Update — ticket 20260917-081239 (2026-09-17 08:20:46)

- Mikhail decision: **no** 47-conflict diff; migration wins; `conflicts_wtb5_snapshot\` archive only (see sandbox `REPORT.md`).
- `go.sh` retargeted from lingvamyxa `wtb5_l2trans_cand` → `L1\dev\mixa_sandbox` + `L1\l2src`.
- `build_mixa.ps1`: skip handC residual `.c` when `.lm1` twin exists; skip `mixa_event_source.c` until `.lm1` (no plain `lmx_message.h` under `L1\l2src`).
- Publish to `L1\mixa_manager\` still blocked until build green.
