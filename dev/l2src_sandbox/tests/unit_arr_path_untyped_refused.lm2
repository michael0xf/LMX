# The letter as nextMessage gives it is a graph with no declared type: a path
# through it has no fields to walk.  A program declares `MainLetter: m` first.
nextMessage: m
if: m\mainArgs[1][0] != 0
    return: 1
return: 0
