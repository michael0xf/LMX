# Тикет fable 2026-09-26-03 — §7: используемый путь проверяется целиком, не первым сегментом (полчаса молчания)

Статус: OPEN. Написан по правилу `steps/tickets/README.md`: последний коммит ведущего на main — `0e00053` (S7 `l2_admit_paths`) в 12:38 UTC, к 13:15 UTC ни коммита, ни ANSWER, ни TAKEN. Срез 2 плана (`steps/implements-s7.md`, таблица преобразований) ждёт формы от автора — очередь не ждёт, берётся следующий пункт §7 без вопроса к автору: ANSWER dfee18c-2 сам назвал его («первый сегмент пути; вложенное имя и тип поля не сравниваются — закрывает порт receiving-expression admission»), `next_core_tasks.md` §7 :448.

## Норма

Книга L3 (`docs/LMX_semantics.en.md` :447, RU то же место): «following explicit description fields continues a thick path and makes those fields requirements»; не сравнивается только неиспользованное (:450). Значит для прочитанного `slot\a\b` требованиями становятся и `a`, и `b` внутри `a`; для прочитанного листа — его домен (kind), не только имя. Consumer — список путей (`45e58c7`, `0e00053`); эта норма про то, что в нём сверяется.

## Где сегодня грубость

- `dev/l2src_sandbox/l2trans.lm1` `l2_descriptor_used` :10194–:10228: обрезает путь по первому `\` (`cut`), проверяет только `l2_ns_has_field(req, buf)` → `l2_ns_has_field(cand, buf)`; вложенный сегмент и `l2_nsf_kind`/`l2_nsf_ref` (:235–:237) не читаются.
- `l2_ns_has_field` :9692 — присутствие по имени, без вида.
- `l2_uses_scan_follow` :10107 уже пишет путь целиком (`a\b\c`, разделитель 92) — данных хватает, менять обход не нужно.

## Сделать (один срез транслятора, ядро не трогается)

1. `l2_descriptor_used`: идти по сегментам. Для сегмента `s` на текущей паре (cand_i, req_i): поле `s` есть у req_i → обязано быть у cand_i (иначе `compatible = 0`); если у req_i поле `s` — ссылка на Structure (`l2_nsf_ref`), спускаться в пару (ref поля cand_i, ref поля req_i) и продолжать; последний сегмент — лист: `l2_nsf_kind` совпадает, иначе `compatible = 0`. Имени, которого нет у req на своём уровне, в Consumer нет (как сегодня). Никаких имён-исключений, реестров, второго пути для walk/native.
2. Свидетели (в `dev/l2src_sandbox/tests/` и `l2src/tests/`, строки `tools/l2_harness.ps1`):
   - `unit_s7_nested_ok.lm2`: `Outer{Inner: in}`/`Inner{size_t: x}`, кандидат с той же формой, тело читает `slot\in\x` → Entry 7.
   - `unit_s7_nested_missing.lm2`: у кандидата `in` есть, но без `x` → отказ «implements is false in function argument» (сегодня — измерить: ожидаемо ложный допуск, записать в план).
   - `unit_s7_leaf_kind.lm2`: лист прочитан, у кандидата поле того же имени другого вида (`int` против `size_t`) → отказ (сегодня — измерить).
   - Прежние `unit_s7_used`, `unit_s7_empty`, `unit_s7_identity`, `unit_s7_uses64/65`, `unit_invalid_implements_used_field` — без изменений результата.
3. Мутант: спуск во вложенный сегмент отключён (ветка `cut` как сегодня) — `unit_s7_nested_missing` переводится (RED); откат — отказ. Второй мутант: сравнение kind снято — `unit_s7_leaf_kind` переводится.
4. Учёт: `steps/implements-s7.md` — раздел среза с числами гейта; `grok_next.md` §4 п.9; `next_core_tasks.md` §7 :448 — отметить, что транслятор закрыл вложенные сегменты и лист (ядерный обход `lmx_runtime_implements` внутрь `ARRAY_OF_DESC` — остаток, отдельной строкой, если не сделан). Близнецы `l2src/` (включая `tests/`) равны песочнице.

## Критерий готовности

Прочитанный `slot\in\x` без `x` у кандидата отказывается; прочитанный лист другого вида отказывается; непрочитанное не требуется; корпус прежних фикстур не сдвигается кроме перечисленных; harness зелёный; мутанты RED.

Дальше — продолжать `next_core_tasks.md`.

TAKEN grok/s7-nested@f2873b4 2026-09-26 13:25 UTC
