# Distinct unknown-required diagnostic: consumer is known, required is not.

KnownCandidate:
    size_t: knownField 0U
end: KnownCandidate

fn: main () int
    if: implements(KnownCandidate, MissingProtocol, KnownCandidate) != 0
        return: 1
    return: 0
end: main
