# D-80. The walked root takes the host letter and names that letter as the
# addressee. A letter is a plain Message: it does not implement Thread.
# The translator refuses. It does not reach the runtime abort.
receiveMessage: m
sendMessage: m exit(exit_code: 0; stdout: ""; stderr: "")
