***ВСЕ ПРАВИЛА ЯЗЫКА УНИВЕРСАЛЬНЫ. НИКАКИХ ИСКЛЮЧЕНИЙ НИ ДЛЯ ЧЕГО НЕ ДЕЛАЕТСЯ. ЕСЛИ ТРЕБУЕТСЯ ИСКЛЮЧЕНИЕ ИЛИ НАЙДЕНО ПРОТИВОРЕЧИЕ — ОБСУЖДАТЬ ЯВНО.***

# Следующие задачи ядра LMX

Состояние списка: 2026-09-22. Это рабочая очередь ещё не завершённых изменений, обсуждавшихся сегодня. Она не заменяет нормативные спецификации и не объявляет реализацию готовой. При расхождении сначала фиксируются обе нормы и запрашивается решение автора; обходные ветки, shim и «похожее» поведение не добавляются.

## 0. Порядок и границы работы

- [x] **СЕЙЧАС: удалить защитный close-пакет, добавленный коммитом `4da4658`, затем поставить эту галочку и продолжить очередь ниже.** Не откатывать коммит целиком и не затрагивать его остальные полезные изменения. Хирургически удалить из ядра: `LmxRoot.close_stage`, сохранённый в `LmxRoot` `close_deadline`, состояния `LMX_ROOT_CLOSING` / `LMX_ROOT_POISONED`, контракт повторного входа и продолжения частично выполненного close, `lmx_root_arena_blocks_valid`, защитно-диагностический контракт `lmx_root_release_final` и добавленную в `lmx_root_poison_selftest.lm1` проверку искусственно закольцованной арены. Вернуть один линейный синхронный алгоритм над корректными по построению объектами: локальный deadline → освобождение orphan-политики → settle детей → закрытие почты R0 → снятие маршрута → удаление R0 у родительской заглушки → закрытие её почты → освобождение service и arena → очистка handle-полей → `CLOSED`. Не добавлять взамен preflight/validator, recovery/poison state, retry/stage, shim или новые проверки внутренних инвариантов. Сохранить исходную часть `lmx_root_poison_selftest.lm1`, проверяющую инициализацию владельцем памяти, предварительно заполненной `0xAA`: она тестирует конструктор, а не защиту от повреждения живого ядра. Готовность: focused root tests, полный `build_l2src`, L3 runner, generated harness, `check_docs` и `git diff --check` зелёные; только после этого поставить `[x]` и брать следующий пункт.
- [x] Аварийную коррекцию базовых массивов сохранить отдельным зелёным checkpoint `7419470`.
- [x] До нового colon/occurrence/callable writer закрыть два оставшихся comment-only файла Array и отдельный документационный checkpoint; tracked tree должен иметь однозначное ownership.
- [ ] Не запускать двух writer/build одновременно; учитывать `LOCKED` и `OWNED_BY`.
- [ ] Каждый этап заканчивать свежими целевыми тестами, `python tools/check_docs.py`, `git diff --check`, точным коммитом и проверкой расхождения с upstream.
- [ ] Любое обнаруженное логическое противоречие немедленно выносить на обсуждение с минимальным примером и точными местами кода; не выбирать правило по удобству реализации.


<a id="doc-boundary"></a>
## 0a. Document boundary (AUTHOR-DOC-BOUNDARY-20260922-21)

Specifications must not contain conversations or plans. Current plan lives **only** in `next_core_tasks.md` and `next_core_tasks_dictionary.md`. Conversation/decision chronology belongs in historical documents such as `LMX_blog`, not normative specs. Normative specs may state tersely that `c.*` is the raw C door — with no ticket provenance, cleanup plan, or planned `sizeof:` feature text.

<a id="architectural-doctrine"></a>
## 0. Architectural doctrine (AUTHOR-ARCH-DOCTRINE-20260922-17)

**Mandatory for the current docset and all later implementation.** Every decision must be architectural, not a local patch. When a required contract is missing, **STOP and ask the author directly** — never fill the gap with a name-special parser branch, allowlist, header scanner, hidden registry, shim, fallback, or an extra field in a base kernel type.

Explicit anti-patterns and replacements:

1. **`c.*` is a raw door into C, classified per TOKEN.** Every token spelled `c.*` is a raw C name; every non-`c.*` argument inside the construct stays an ordinary L2 expression under general L2 lowering. So `c.printf("%d", x)` has a raw head and an ordinary L2 argument, and `c.sizeof(c.LmxArena)` passes both tokens raw for C to interpret. Classification is never by concrete name. The translator must not separately recognize `c.sizeof` / `c.puts` / other names, parse `*.h`, or construct an "allowed C" dictionary. Invalid raw C is diagnosed by the C toolchain. Lmx needs use ordinary language receivers/libraries, not `c.*` special semantics.
2. **Base Array** keeps its minimal invariant (`{len, data}`). Dynamic growth belongs to a separate List/ArrayList implementation. Never add capacity or policy fields to Array for one consumer.
3. **Base Message** stays minimal kernel graph/scheduling/message machinery. Win32 UI and application state belong in adapters/application structures referenced through ordinary composition — never embedded into Message.
4. **Similar future demand:** add a separate type/receiver/library/adapter at the correct layer; do not mutate the lower abstraction.

Dictionary stable-ids: [`architectural-placement`](next_core_tasks_dictionary.md#architectural-placement), [`clean-kernel-acceptance`](next_core_tasks_dictionary.md#clean-kernel-acceptance). Existing violations are **cleanup debt** blocking the clean-kernel checkpoint before self-build.

Dictionary: [
o-defensive-kernel](next_core_tasks_dictionary.md#no-defensive-kernel) — kernel invariants by construction/tests; no defensive close state machines in base kernel types.

**`sizeof:`:** **введён** L2 language receiver-operator (AUTHOR-SIZEOF-DESIGN-20260922-20 / FABLE-GROKBOT-INCLUDE-AND-SIZEOF-20260923-123 part2); not a callable; no global-contract HOLD/OPEN. **Семантика (автор, Q16, 2026-09-23):** размер хранилища значения/типа в БАЙТАХ (для own-массива — все элементы), операнд — L2-значение или тип, разрешаемый транслятором; `length` — число элементов; `c.sizeof(...)` — сырой C через дверь `c.*`.


## 1. Аварийная коррекция базовых Array и Lmx

### Неподвижный контракт

- [x] Базовый `LmxArrayDesc` оставить ровно `{len, data}`. *(Уточнено автором 2026-09-23: тот же контракт «размер + данные, без capacity» в представлении `VoidArray {size_t size; void *data}`, имя `LmxArrayDesc` в целевом ABI исчезает — см. «Представление `VoidArray`» ниже; смысл не изменён, меняются имя типа/поля и вложенность.)*
- [x] Базовый `Lmx` оставить ровно `{parent, len, data}`. *(Уточнено автором 2026-09-23: `Lmx {VoidArray array; Lmx *parent}` — те же два смысла, дескриптор детей вложен как одно поле и стоит первым (смещение 0), `parent` вторым; четвёртого поля по-прежнему нет.)*
- [x] Удалить из базового Array понятия `capacity`, `reserve`, `resize`, `append`, `grow` и переключения backing storage.
- [x] Не добавлять wrapper, inheritance или ABI shim, скрывающий прежнюю growable-семантику.
- [x] Динамический контейнер держать отдельным механизмом со своим состоянием, операциями и инвариантами. В checkpoint `7419470` List имеет `KIND_LIST`, использует открытую пару `{len,data}`, хранит capacity во внутреннем префиксе backing, а `LmxListDesc` не экспортируется в headers. Перенос работающей L1-реализации на L2 выполняется позже.

### Представление `VoidArray` (автор через Codex, CODEX-FABLE-VOIDARRAY-PLAN-20260923-01 + уточнение CODEX-FABLE-VOIDARRAY-ORDER-20260923-02; документация и план, НЕ реализация сейчас)

Принято автором (2026-09-23, с уточнением порядка полей в тот же день; дословно — `LMX_blog/2026-09-23.md`, архивирует Codex): `typedef struct { size_t size; void *data; } VoidArray; typedef struct Lmx { VoidArray array; struct Lmx *parent; } Lmx;` — **`array` первым полем (смещение 0), `parent` вторым**; псевдонима `LmxArrayDesc` в целевом ABI **нет** (он был лишь переходным именем без семантической и физической роли — удалить/переименовать все употребления). `VoidArray`, вложенный в `Lmx`, — настоящий дескриптор массива ссылок на детей; его backing — зарегистрированный в арене диапазон адресов (сам дескриптор — не `LmxRange`; классификация диапазонов остаётся раздельной: узел `Lmx` — `LMX_KIND_STRUCT` 6, backing слотов детей — `LMX_KIND_CHILDREN`/`LMX_TYPE_REFS` (`lmx_arena_refs.lm1` :35), отдельный дескриптор Array — `LMX_KIND_ARRAY` 3 (`lmx_array_owned.lm1` :56); `lmx.h.lm1` :158/:164). Все отдельные базовые Array и вложенный `Lmx.array` — ровно один C-тип `VoidArray`. Capacity/рост в базе не появляются; `List` остаётся отдельным механизмом (§1 выше). Сегодня в коде: `Lmx {parent; int len; void *data}` (`dev/l2src_sandbox/lmx.h.lm1` :65–:69; stable `l2src/lmx.h.lm1` :60), `LmxArrayDesc {size_t len; void *data}` (:206–:209; stable :175); `LmxArrayDesc` упоминается в 94 отслеживаемых файлах вне build-каталогов (dev 22 + tests 20, stable l2src 15, tmp_unitroot_test 17, docs 8, steps 3, tools 1, provenance 1, to_fable 1). Зависимости от «`parent` первым» по целевому чтению не найдены (все `cast: (@: Lmx)` в ядре — приведения целой записи, адресных трюков `&node\parent` нет); окончательный ответ даёт инвентарь ниже — найденная зависимость сообщается автору, а не обходится.

Порядок: один писатель ядра; после S1/S3 2b/-127 (не пересекать с писателями транслятора и парсера) и до GATE (представление ядра фиксируется до самосборки §8); документация и гейты — ДО кода. Ни один пункт не отмечать выполненным без свидетельства.

- [ ] Инвентарь (read-only, число мест по файлам): все употребления имени `LmxArrayDesc` (к удалению/переименованию в `VoidArray`, включая stable, seeds, tests, `tmp_unitroot_test`), любое место, читающее `Lmx` как «`parent` первым» (приведения, арифметика адресов, `c.offsetof`, ручные раскладки в эмиттере/seeds), чтения/записи `\len`/`\data` у `Lmx` и `LmxArrayDesc` (dev `~300` строк с `LmxArrayDesc`/`\len`), структурные смещения в эмиттере транслятора (аудит G2: 26 мест), `c.INT_MAX`/отрицательные сентинелы (`lmx_arena_refs.lm1` :27/:47, `lmx_merge_owned.lm1` :20/:32, `lmx_merge_owned.h.lm1` :63, `l2trans.lm1` — 32 упоминания), классификация диапазонов арены (`lmx_arena*`), копирование/слияние графа (`lmx_copy_process`, `lmx_merge_owned`), `dev/l3_interp` (34 обращения), stable `l2src/` и generated seeds `lm1/build`.
- [ ] Документация RU/EN синхронно (L1/L2 спеки, `provenance/semantics-book.md` → `docs/LMX_semantics.*`, `docs/implementation-notes.*`, `dev/l2src_sandbox/LMX_ARRAY_OWNED.txt`, комментарий в `lmx.h.lm1`): `VoidArray` — единственный C-тип дескриптора; `Lmx = {parent, array}`; `size` — `size_t`, логическое число элементов, backing ровно `size` ячеек; дескриптор ≠ диапазон арены; без capacity/grow; List отдельно. (Ведёт Codex; `check_docs` зелёный.)
- [ ] Гейты до кода: selftest раскладки (размер/смещения `Lmx`, `VoidArray`; **свидетель смещения 0: `c.offsetof(Lmx, array) = 0`**, `parent` следом; имени `LmxArrayDesc` нет в активных исходниках, seeds и тестах — проверка `rg` в гейте по `dev/l2src_sandbox`, `l2src`, `lm1/build`, `dev/l3_interp`, `tools`; исторические `LMX_blog/`, `provenance/` и архивные копии намеренно сохраняют старое имя и в гейт не входят), свидетель раздельной идентичности диапазонов **по измеренной сегодня классификации** (поправка Codex, CODEX-FABLE-VOIDARRAY-ORDER-20260923-02): backing `Lmx.array.data` (слоты детей) регистрируется как `LMX_KIND_CHILDREN`/`LMX_TYPE_REFS` (`lmx_arena_refs.lm1` :35), отдельный дескриптор Array — как `LMX_KIND_ARRAY` (`lmx_array_owned.lm1` :56; вид его backing — по типу элемента), сам узел `Lmx` — `LMX_KIND_STRUCT`; при «array первым» адрес `&node\array` совпадает с адресом узла и классифицируется как STRUCT, не ARRAY — три различия (CHILDREN / STRUCT / отдельный ARRAY) сохраняются после миграции; мутант «дескриптор как LmxRange» — RED), строки harness, пиннящие чтения `node\array\size`/`\data` в сгенерированном C, self-build 8/8 на seeds; мутанты (лишнее поле в `Lmx`; `int` вместо `size_t`; чтение `len` мимо вложенности) — RED.
- [ ] Миграция кода одним коммитом-серией на одном SHA: `Lmx {VoidArray array; Lmx *parent}` (array первым), удаление/переименование всех употреблений `LmxArrayDesc` → `VoidArray` (без переходного псевдонима в целевом дереве), `int len` → `size_t size` с вложенным доступом `node\array\size`/`node\array\data`; сентинелы `INT_MAX`/отрицательные длины → явные проверки размера без отрицательных значений; arena refs и классификация диапазонов; copy/merge графа; stable `l2src/` + dev + `lm1/build` seeds синхронно; эмиттер транслятора и `dev/l3_interp` — по инвентарю. Полный набор гейтов (harness, build_l2src -Run, L3, run_parser, self-build, check_docs, diff --check) на одном SHA. Новых полей в `Lmx` нет.
- [ ] Логические конфликты, обнаруженные при инвентаре (например, место, которому нужен отрицательный `len` как состояние), — автору минимальной программой, без обходного пути.

### Дескрипторы динамических массивов (автор, Q18, 2026-09-23; `LMX_blog/2026-09-23.md`, `q18.md`)

Общее правило автора: базовый дескриптор массива — только `{size_t len; T *data}` без capacity; **любая динамическая реализация, которой нужна capacity, заводит новый описанный тип** `{<T>Array array; size_t capacity}` (пример автора: `Array {size_t len; void *data}`, `DynamicArray {Array array; size_t capacity}`); в C — отдельный дескриптор на каждый тип элемента одной формы, дескрипторы одного типа лежат в арене в своём массиве однотипных дескрипторов. **Уточнение автора (тот же день):** каждый дескриптор динамического массива заводится единообразно по этому правилу **с использованием уже существующего дескриптора фиксированного массива** данного типа как поля `array` — не новый параллельный тип; для void — тот же тип, что вложен в `Lmx` (сегодня `LmxArrayDesc`, единое имя после переименования; сама раскладка `Lmx` — отдельная работа, не эта); по коду не плодить дублёров — всё унифицировать: если типизированный фиксированный дескриптор для `T` нужен и его нет, он заводится один раз и используется и фиксированными массивами `T`. **Второе уточнение автора:** `void` в примере — просто один из типов элементов: дескриптор массива `int` — `{size_t len; int *data}`, никогда не void-дескриптор с приведением; void-дескриптор — только для массивов элементов `void *` (включая вложенный в `Lmx`). Режим и очерёдность — координатор.

- [ ] Инвентарь ядра (`dev/l2src_sandbox`, на `edd3f8f`): шесть мест с capacity — (1) L1 List `lmx_list_owned.lm1` (capacity спрятана в префиксе backing, `LmxListDesc` не экспортируется); (2) индекс арены `lmx_arena.h.lm1` :19–:24 (`index`/`count`/`capacity` вразбивку); (3) сервис `lmx_service.h.lm1` :57–:61 (`entries`/`count`/`capacity`); (4) ящик Thread `lmx_post.h.lm1` :127–:131 (`inbox_slots`/`inbox_capacity` + `head`/`tail`, кольцо); (5) чанк пула `lmx.h.lm1` :260–:266 (`LmxChunk {base; capacity; count; next; mark}`); (6) копир графа `lmx_graph_copy_owned.lm1` :7–:150 (приватные hash-map и стек `keys/vals/n/cap` через `malloc`). Проверить и зафиксировать по каждому месту, лежит ли дескриптор в арене в массиве однотипных дескрипторов (автор: «надеюсь, так и есть») — где нет, назвать место, не чинить молча.
- [ ] Ввести типы по правилу: на каждый тип элемента `Lmx<T>Array {size_t len; <T> *data}` и `Lmx<T>DynamicArray {Lmx<T>Array array; size_t capacity}` (имена — по образцу типов ядра, единообразно; void-вариант один); перевести все шесть мест: List — публичный `DynamicArray` вместо скрытого префикса; индекс арены и сервис — одно поле-дескриптор вместо троек; ящик — `DynamicArray` + `head`/`tail`; чанк — `DynamicArray` + `next` + `mark`; копир — свои типы того же правила. Ни у одного базового дескриптора capacity нет; никаких wrapper/shim.
- [ ] Гейты: selftest раскладки (`offsetof(<T>DynamicArray, array) = 0`, размер = базовый + `size_t`), `rg`-гейт «capacity встречается только в полях типов `*DynamicArray`/чанков» по активным исходникам ядра; существующие selftests List/pool/post/service/arena зелёные; build_l2src -Run, harness, L3, self-build 8/8, check_docs, diff --check на одном SHA; мутанты: capacity, возвращённая в базовый дескриптор, → RED; List без `DynamicArray` → RED.
- [ ] Документы RU/EN синхронно (L1/L2 спеки, книга → generated, implementation-notes, `LMX_ARRAY_OWNED.txt`): общее правило и список типов; убрать любое утверждение, что базовый Array или почта несут capacity.
- [ ] **Решение координатора по BLOCKER -134 (чанк пула):** чанк — stride-generic сырое хранилище, тип элемента известен только во время исполнения (kind/type/stride), поэтому его настоящий статический тип — байт: `LmxByteArray {size_t len; unsigned char *data}` (один раз, годится любому фиксированному байтовому буферу) и `LmxByteDynamicArray {LmxByteArray array; size_t capacity}`, `len`/`capacity` в байтах; `LmxChunk {LmxByteDynamicArray cells; next; mark}`; арифметика ячеек (len/stride, capacity/stride) — одним помощником; пул остаётся runtime-generic (не делить на семейства по T), без void-приведений и wrapper/union. **Подтверждено автором (2026-09-23):** «если не замедляет, то оставляйте наиболее общий код»; обобщённый код пула горячие пути не замедляет (чтение ячеек — прямое, stride только при выделении; стоимость арены — классификация адреса).
- [ ] **Решение координатора по бюджету типов (-134, 2026-09-23 ~00:20):** композиция дескрипторов добавляет 8 именованных типов заголовков (4 фиксированных + 4 динамических), а таблица имён типов заголовков в НАШЕМ l1trans (`l1src/l1trans.lm1` :5520–:5545 `l1_hdr_type_add`: 64 имени / 4096 байт) была на пределе у L3 Thread-единиц; Grok «сбросил» 8 типизированных `fnptr:` (LmxPostApi и LmxThread mail API) в `@: void` — отвергнуто как стирание типов/подгонка. Решение: поднять таблицу до 128 имён / 8192 байт в l1trans, пересобрать и перепинить транслятор по процедуре self-build (fixed point 8/8, seeds `lm1/build`, `L1_PIN.txt`, `bin/l1trans.exe`), все гейты на новом пине ДО дескрипторных коммитов; `tools/l3_type_budget.py` — EXPECT с причиной, измеряет ответ транслятора. Автор может отменить одной строкой (альтернатива — сузить predef-замыкание L3 Thread).
- [ ] **Измерено Grok -134 (факт для автора, не чинится в -134):** дескрипторы индекса арены, сервиса, кольца ящика, записей чанков и копира лежат в `malloc`/`calloc`-памяти, а не в типизированных массивах арены (в арене — только дескрипторы List). Отдельный пункт: перенести записи в типизированные массивы арены там, где это не ломает владение; где ломает — назвать и спросить.
- [ ] Вне ядра — по тому же правилу, отдельными писателями и позже: таблицы транслятора `l2trans.lm1` (~107 упоминаний capacity/`_cap`), парсер `l1src` (`lm_own_*`), `dev/l3_interp` (3).

### Оставшиеся исправления и gate

- [x] Восстановить прямое подключение реализаций `lmx_array_new_owned`, `lmx_array_ref_new_owned`, `lmx_array_ref_new_positive_owned` в фактических потребителях, не возвращая динамику в базовый Array.
- [x] Удалить устаревшие `capacity/grow` утверждения из L1/L2-спецификаций и нормативной документации: закрыто writer-слотом `GROK-BOT-ARRAY-DOC-GATE-20260922-02` (L1/L2 specs, `provenance/semantics-book.md` → regenerated `LMX_semantics.*`, implementation-notes; base Array `{len,data}`, динамика на List/`KIND_LIST`). Comment-only `LMX_ARRAY_OWNED.txt` / `lmx_pool.lm1` в `dev/l2src_sandbox` остаются отдельным пунктом ниже и вне того docs-gate.
- [x] Разделить употребление `Array` и нового динамического `List` в dev-потребителях, включая eternal driver и состав детей Thread.
- [x] Вернуть L3 type budget к доказанному `62/64`, `818/4096` архитектурно, без подгонки ожидаемого числа.
- [x] Получить зелёные gates checkpoint `7419470`: L2 harness `60/60` без FAIL (`build/l2_harness/20260922_073250`), все 11 L3 suites exit 0, type budget `62/64`, `check_docs`, `git diff --check`.
- [x] Сохранить и отправить точный checkpoint `7419470`; проверить `HEAD == upstream`.
- [x] Закрыть два правильных, но не вошедших в `7419470`, comment-only изменения отдельным точным commit/push, не смешивая документацию Codex:
  - `dev/l2src_sandbox/LMX_ARRAY_OWNED.txt`: заменить старое описание `{len, capacity, data}`/`capacity = len` на базовый `{len,data}` с backing ровно `len` ячеек и отдельно назвать динамический List;
  - `dev/l2src_sandbox/lmx_pool.lm1`: исправить комментарий о пуле дескрипторов Array с `{len, capacity, data}` на `{len,data}`.
- [x] После этого повторить `git diff --check`, `python tools/check_docs.py`, подтвердить `HEAD == upstream` и отсутствие чужих tracked-изменений перед следующим writer.
- [x] Не считать ошибочное предложение из FINAL о «pre-existing check_docs desync» известным дефектом: независимые свежие прогоны `check_docs` зелёные.

### Остатки аудита checkpoint, которые нельзя потерять

- [x] G4: свидетельствующий тест отказа `attach` при `take` (пара `[target, source_arena]` остаётся в очереди). REVERIFY 2026-09-22 stage1: `5a8c444` ancestor of `055454e0`; green `lmx_post_take_refusal_selftest` checks=30 failures=0; private mutants of `lmx_post_take_inbox` fail 8 / 11 / 12 checks (clear source before attach / drop pair on refusal / ignore attach result); evidence `build/stage1_l2src_20260922_114359` (rebaseline RED only on migrated `lmx_list_grow_selftest` (was `lmx_list_grow_selftest`) — see gate note). No reimplement.
- [x] G2: whole-occurrence conclusion (G2-WHOLE-OCCURRENCE-CONCLUSION-20260922-40; supersedes G2-GENERIC-RANGE-CORRECTION-20260922-36 gap framing; VOID impl GROKBOT-G2-VOID-20260922-32; freeze G2-NO-PREMATURE-BIND-ACT-20260922-38). DeepSeek trace DEEPSEEK-G2-DYNAMIC-OCCURRENCE-TRACE-20260922-37: dynamic route / merge / explicit argument pass or copy the whole callable `@: Lmx` Structure occurrence and preserve child[0]; direct callable-field assignment forbidden; no binding act missing (`lmx_call_bind` withdrawn). METHOD and shared `LmxCallable` remain immutable after construction; method array is catalog/retention root. METHOD already classifies via generic arena typed-range — do not invent method-admit / method-ranges / selection-rebinding acts. Construction-time generated `callable\method: rec` vs checked plan publication is measured asymmetry only — not a non-const overload gap. Existing DEBT comment at `l2_emit_unit` ~16505 left unchanged until final author/Stage-1 decision. No G2 code change.
- [x] T-B: fixture `unit_node_path_nested_own.lm2` + harness row. Green focused harness `build/stage1_harness_tb_20260922_124333` (3 targets). Generated lowering writes `lmx_arena_ref_cell(node, 0U)` (host-less node path, not self/host). `unit_node_formal` untouched. Private `l2_path_own` for-path-equivalent mutant still greens (identical gen) — emission witness is the control proof for this Stage-1 bound.
- [x] List grow witness migrated (OPENROUTER-RETIRED-ARRAY-GROW-20260922-31 / RELEASE-RESUME-47): lmx_array_grow_selftest renamed to lmx_list_grow_selftest; element reads via lmx_list_data(); capacity-prefix invariant asserted. Green in uild/stage1_release_20260922_123932 (list checks=20 failures=0). Private noprefix mutant (raw descriptor\data indexing) fails checks=20 failures=2. List implementation / base Array untouched.
- [x] G1: AUTHOR-G1-R0-STANDARD-20260922-33 / G1-REFUSAL-STANDARD-20260922-35 / G1-ORPHAN-UNREGISTER-GUARD-20260922-39 — R0 closure absolutely standard; ordinary per-record mailbox-first (S2 own mail close, S3 unregister); S4→S5 parent-stub handshake preserved; no dual-mailbox-first invention; exact RU quote archived in LMX_blog/2026-09-22.md. Also AUTHOR-K1-G1-DECISION-20260922-27 UNIFY for settle/orphan_reclaim terminals with mailbox close.
- [ ] Не переоткрывать G3 без нового evidence: merge sealed-ветви уже проверен как сохранение физической ссылки.
- [x] Повторно сверить код с техническим журналом о единственном источнике состава детей: не допустить второго root-slot, отдельной manager membership queue или другой параллельной модели. Evidence CONTINUE-NEXT-CORE-20260922-54 / DEEPSEEK-CHILD-SOURCE-AUDIT-20260922-55 PASS + Codex verify on HEAD 0292ecb: `lmx_thread_children_bind_graph` refuses a second children List; production manager/settle/orphan use `lmx_thread_child_count`/`at` over the sole parent children List; no second root slot / manager membership queue / shadow child registry. Precision: `service_count`/`at`/`find` remain public (tests call them) — proof is production traversal + structural single-list bind, not API inaccessibility. Residual comment-only debt: a few membership comments still say Array although the container is List (do not expand code scope merely to fix nouns).

## 2. Единая модель трёх поверхностных форм

### Норма

- [ ] Блочная, короткая/colon и скобочная формы должны быть семантически равноценны во всём языке.
- [ ] Только P0 различает скобочную, short/colon и вертикальную исходные записи. Все допустимые эквивалентные записи он обязан свести к одной нормальной Structure/Frame до передачи транслятору. Транслятор получает готовую нормальную структуру и вообще не рассматривает исходный синтаксис, `---`, скобки или разновидность записи.
- [ ] `LM_P0_FRAME_COLON` и `LM_P0_FRAME_COMPACT`, если сохраняются ради spans/форматирования, не являются семантическим входом транслятора. Resolution, checking и emission не могут читать их для выбора операции.
- [ ] `f(a b)`, `f: a b` и соответствующая вертикальная форма должны после P0 быть одной и той же последовательностью полей, а не тремя случаями транслятора.
- [x] `f()`, `f: ()` и явно пустая вертикальная форма `f:` с обязательным `---` должны иметь **одинаковое P0 дерево**: Frame с пустым Structure-body. Общая §4.0.1 действует и для непустой единственной анонимной обёртки всего списка: `f(a b)` = `f: (a b)` уже после P0. Незавершённое `f:` P0 отвергает. Голый `f` остаётся atom/expression, но для callable даёт тот же нульарный эффект. Полный аудит и маршрут — `next_parser_fix.md`. **Свидетельство (Grok Bot, FABLE-GROKBOT-P0-SOLE-CONTAINER-20260923-127):** P0 PART1 `bc22881` — одно дерево `body fields=0` для трёх пустых форм и одно дерево для `f(a b)`/`f: (a b)`/вертикали (goldens `tests/parser/current`, `run_parser.py --profile current` 27/27; parity `nextMessage(m)`/`nextMessage: (m)`/вертикаль); голое `f:` — отказ 32; мутант (вернуть материализацию 9c166ff) → `empty_paren_f` RED. Остаток: Mix-узел `{...}` не должен разворачиваться (PART1b, тот же ID).
- [ ] Единственная анонимная Structure всего списка аргументов прозрачна **в P0 для всех голов**, а не только после CALL-resolution. GROKBOT-CALLABLE-INTERP-STAGEC-20260922-65 доказал лишь частичную позднюю CALL-нормализацию; это измеренное состояние кода, не норма. `mystruct()` и `mystruct: ()` подчиняются одному правилу объявления из нормального Structure-body.
- [ ] Пустая Structure/unit при этом остаётся настоящим значением. Чтобы передать её как один аргумент, нужна непрозрачная позиция, например именованное поле.
- [ ] Голый `f` в исполняемой позиции — одноатомное выражение (не P0-исключение и не sugar): после успешного разрешения `f` как callable вызывается с нулём потреблённых аргументов по универсальному callable-first. P0 может сохранить атом; транслятор не различает его по синтаксическим флагам форм. Голый data-атом вычисляется как выражение, его результат отбрасывается (discard); он не вызывается.
- [x] **IMPLEMENT (post-Stage1; AUTHOR-CALLABLE-DESCRIPTOR-IMPLEMENT-20260922-43; supersedes witness-only AUTHOR-CALLABLE-FORMAL-WITNESS-20260922-42 for plan wording). Status: NATIVE/PARSER DONE under CONTINUE-NEXT-CORE-20260922-54 Stage B; INTERPRETER DONE under GROKBOT-CALLABLE-INTERP-STAGEC-20260922-65.** Routes a-f + descriptor-only -44/46 in `l2trans.lm1` (cf table, typed_formal method-head, bodiless keep addr=0, head_is_call+call_sel=2, sig-compatible admission, nullary `return: f` / `lmx_call0`, emit_formal recovers dt-encoded `@: Lmx`). Fixtures: `unit_callable_formal_descriptor`, `unit_callable_descriptor_direct_refused`, `unit_callable_formal_sig_refused` + harness rows. **Interpreter** (`dev/l2src_sandbox` walk/interp): `LMX_WALK_NOT_CALLABLE` / `LMX_INTERP_NOT_CALLABLE`; enter descriptor+no-plan+no-steps ⇒ NOT_CALLABLE; RET of call_ready ⇒ nullary activate; CALL-only bag unwrap (FABLE-56); `lmx_walk_selftest` Stage C fixtures (test/test2/test3/test4 + empty/wrap bags). Never METHOD.addr bridge. No new registry/type/`lmx_call_bind`/use-prescan.

Exact source cases:

```text
fn: test () int
    return: 1

fn: test2 (test: f) int
    return: f

# descriptor-only contract:
fn: test3 () int

fn: test4 (test3: f) int
    return: f
```

Acceptance:
1. `test: f` / `test3: f` are callable formals; runtime value is a whole callable Lmx occurrence; bare `f` in return invokes nullarily and yields `int`.
2. A bodiless `fn: test3 () int` is **always** a valid descriptor-only callable contract (author intent) — not conditionally retained only if a later use scan finds it. Do **not** add a use-dependent exception or prescan. A stray bodiless fn is a descriptor declaration, not “unsupported body”. Compatibility via existing METHOD/CALLABLE address-range + matching signature (structural sig equality); do not block on a final high-level type system.
3. E2E: `test2(test) == 1` and `test4(test) == 1`; direct invocation of descriptor-only `test3` must fail with language/runtime NOT_CALLABLE (addr=0) — never invent a body and never fall through to an undefined-symbol linker failure.
4. Cover parser/P0, native L2 lowering, and interpreter. Callable-formal discrimination = resolved binding KIND. Forbidden: METHOD/catalog mutation, new descriptor registry, `lmx_call_bind`, direct callable-field assignment, duplicate type mechanism. Use whole-occurrence passing, typed address ranges, signature comparison (aligns G2-WHOLE-OCCURRENCE-CONCLUSION-20260922-40).
5. Cross-ref §3 Вызов / Expression statement. Author quote archived `LMX_blog/2026-09-22.md`.

**Measured current defect (DEEPSEEK-CALLABLE-FORMAL-MIN-20260922-41 evidence addendum):**
- P0 accepts `test: f` because the formal head is opaque.
- `l2_typed_formal` ~10730 must resolve method-head formals before foreign fallback; foreign-by-value fallthrough ~10892–10902 currently silently mis-types as by-value C type `test` (wrong diagnostic later). Method-head formal must use existing `l2_colon_graph_ty` while retaining required method/signature metadata.
- Formals can already carry the required whole occurrence via existing graph type: `l2_colon_graph_ty()` interns `Lmx` pointer depth 1 (same existing type-code route; no new type code).
- `l2_head_is_call` ~10203 sees unit methods and callable paths but cannot classify formal `f`; current formal remains VALUE.
- Call emission ~13055–13062 has lexical/selected occurrence self paths but no callable-formal self path.

**Minimal route after Stage1 (bounded; no new type mechanism):**
(a) `l2_typed_formal`: when formal type head resolves by `l2_find_method`, record graph type via `l2_colon_graph_ty` and retain required method/signature identity in translator metadata; do not fall through foreign-by-value.
(b) Make call-vs-value resolution context-aware by method `mi`, so a formal recorded as callable KIND resolves as CALLABLE. Do **not** infer callable merely from all `Lmx`-typed formals — ordinary Structure values remain VALUE. Method/signature constraint metadata is necessary; no name special case.
(c) Call emission: third self source = formal's `@: Lmx` C parameter; capture it before actual evaluation like existing occurrence paths.
(d) Signature/admission against required `test` descriptor before call; reuse existing `l2_descriptor_implements` / `l2_check_implements` / `l2_prep_implements` and generic typed address-range + sig (exact hookup left to the implementation ticket writer).
(e) `return: f` needs no special return rule once `f` is callable KIND — ordinary expression evaluation invokes it nullarily.
(f) Tests: old branch must no longer emit by-value C type `test`; positive `test2(test)==1`; incompatible-signature negative; cover parser/native/interpreter.

**Exact measured anchors (DEEPSEEK-CALLABLE-FORMAL-MIN-20260922-41 HAS returned — no FILL/unavailable/wait-41 placeholders):** `dev/l2src_sandbox/l2trans.lm1`
- `l2_typed_formal` ~10730 (method-head resolve before foreign fallback) and foreign-by-value fallthrough ~10892–10902 → use `l2_colon_graph_ty` + retain method/signature metadata.
- `l2_head_is_call` ~10203 → context-aware (`mi`); method-constrained formal = CALLABLE KIND; do not classify every `Lmx` formal callable.
- call emission ~13055–13062 → add callable-formal C parameter as third captured self source.
- `l2_emit_formal` ~13520–13584 / `l2_emit_sig` ~13586 → existing graph type already emits `@: Lmx`; no new type code.
- admission: existing `l2_descriptor_implements` / `l2_check_implements` / `l2_prep_implements` + generic typed address range + sig.
- `return: f`: no return special case once KIND is callable.
**Descriptor-only `test3` measured route (DEEPSEEK-DESCRIPTOR-ONLY-TEST3-20260922-44 / CALLABLE-DESCRIPTOR-ONLY-ANCHORS-20260922-46 — pending placeholder REMOVED):**
1. `l2_collect_method` ~11359–11368 currently frees an unbound bodiless row and errors `unsupported body`. Preserve/fill the row with `l2_m_body=0` instead. Existing body passes already guard null.
2. Signature is already structural: `l2_m_sig[i]=l2_intern_mi(i)` via `l2_intern_store_mi`; structurally identical `test()` and `test3()` share sig. Use sig equality for the bounded contract.
3. Record the method/signature anchor per callable formal with a translator metadata table parallel to `l2_nsty_*` (exact precedent near ~9516); do **not** create a new runtime registry or foreign type. Graph physical type remains existing `@: Lmx`.
4. Emission for declaration-only row: catalog METHOD with `rec\sig` and `rec\addr: 0`; emit no body/native symbol. Existing runtime `lmx_call.lm1` ~73–80 returns `LMX_CALL_NOT_CALLABLE` when selected descriptor has `addr=0`.
5. Native direct-call lowering must diagnose invocation of a resolved bodiless method **before** emitting an undefined C symbol; keep value/contract use legal. This is call-site validation, not `l2_head_is_call` classification and not a linker fallback.
6. Tests: descriptor row exists/classifies by address range; `test`/`test3` sig equal; `test4(test)==1`; incompatible signature rejected; direct `test3` invocation → language/runtime NOT_CALLABLE (never invented body / undefined-symbol linker failure); cover parser/native/interpreter.

### Исправления P0

- [x] Исправить завершённость colon Frame отдельно от числа полей. GROKBOT-P0-EMPTY-FORMS-20260922-74 временно материализовал фиктивную пустую Structure в `lm_p0_stream_resolve_pending_delimiter`, чтобы пройти `lm_p0_validate_nonempty_colon_frames*`; это **не** каноническое дерево. `LM_P0_FRAME_SEPARATOR_CLOSED` обозначает `;`/`)` и не является признаком допустимого `---`. После исправления `f:` без аргументов/закрытия остаётся отказом 32; см. `next_parser_fix.md`. **Свидетельство (Grok Bot, FABLE-GROKBOT-P0-SOLE-CONTAINER-20260923-127):** `bc22881`: явное закрытие хранится отдельно (`LM_P0_*_DELIMITER_CLOSED`, не `SEPARATOR_CLOSED`), материализация в `lm_p0_stream_resolve_pending_delimiter` удалена, `lm_p0_validate_nonempty_colon_frames*` отвергает только висячее `f:`; три копии парсера + seeds `lm1/build`, self-build 8/8.
- [x] (маршрут и измеренное расхождение: `next_parser_fix.md`) Нормализовать в P0 единственную анонимную Structure всего Frame body в её поля, ровно один уровень и независимо от головы, формы и пустоты. `f()`, `f: ()`, `f:` + `---` должны давать один P0 dump с `body fields=0`; аналогично непустое тело. Текущие `tests/parser/current` goldens, утверждающие `fields=1`, являются свидетелями **старой реализации**, их нужно обновить после изменения кода, не импортированные исторические goldens. Голое `f` — atom, но исполняется нульарно после callable-resolution. **Свидетельство (Grok Bot, FABLE-GROKBOT-P0-SOLE-CONTAINER-20260923-127):** `bc22881` `lm_p0_normalize_sole_anonymous_container` (один уровень, любая голова/форма/пустота; opaque/trailer-bearing Structure остаётся значением); goldens current обновлены; исторический профиль — карта расхождений `tests/parser/previous-l1/divergences.json` (8 честных расхождений + 1 дефект Mix → PART1b), импортированные байты неизменны, `check_docs` зелёный.
- [x] После P0 parity убрать конкурирующую CALL-only распаковку: `dev/l2src_sandbox/l2trans.lm1:l2_call_args`, `lmx_walk.lm1:lmx_walk_call`/prepare scan; проверить count/check/admission/emit и общий declaration-path. `mystruct()`/`mystruct: ()` обязаны обрабатывать одинаковый пустой Structure-body, а не требовать сохранённого анонимного дочернего узла. Предыдущее доказательство interpreter CALL-only и отказ native из FABLE-P0-LANDED-VERIFY-20260922-77/-85 остаются историческим evidence, не завершённой задачей. **Свидетельство (Grok Bot, FABLE-GROKBOT-P0-SOLE-CONTAINER-20260923-127):** транслятор — PART2 `6ca54c9`: `l2_call_args` и его пять мест удалены, потребители читают `body\first_field`; `unit_call_args_refuse_nested` (`add: ((1 2))`) отказан; мутант (приватный P0 с сохранённым контейнером) → транслятор отказывает Structure-аргументу; harness 187/187. **Остаток:** `lmx_walk.lm1` по-прежнему CALL-only разворачивает bag (prepare ~:288–:318, `lmx_walk_call` ~:497–:540 — одна живая Structure после callee ⇒ её поля как аргументы) — удаление у Opus после S3 2b; `mystruct()`/`mystruct: ()` объявление пустой Structure — Sonnet -132. **Opus, FABLE-OPUS-S1-STATUS-20260923-133 (`a3bc1c6`/`1abafa4`, harness 222/222, build_l2src 250, L3 11/11):** `lmx_walk_scan_node`/`lmx_walk_call` берут детей вызова с индекса 2 как аргументы по одному (Structure/ячейка — ссылкой, frame — вычисляется); unwrap-локалы удалены; `lmx_walk_selftest` 124 проверки (`host()` без детей → 7; `test2(pair)` с `pair = (1 2)` после P0-нормализации — та самая Structure с полями 1 и 2); мутант (walker cb85dc2) → RED «a sole Structure argument reaches the formal unchanged». Остаток (не -133): walker не проверяет число аргументов на входе — лишний Structure-аргумент нульарному callee интерпретатор не отвергает (parity с native — строка §6).
- [x] Обновить только те parser expectations/goldens, которые ошибочно отвергают явно закрытую пустую форму или Frame с настоящими вертикальными детьми. Ожидание REJECT для незавершённого одиночного `receiver:` сохранить. Generated/self-build mirrors должны остаться синхронны. (GROKBOT-P0-EMPTY-FORMS-20260922-74: focused f()/f:()/vertical/lone + mystruct fixtures; existing REJECT kept)
- [ ] Не задавать смысл вложенной записи неоднозначной фикстурой: принадлежность следующей строки определяется общей структурой/отступом. Поле, вложенное в body `ping`, означает аргумент `ping`; поле на уровне body внешнего `c` означает соседний аргумент `c`. Если текущий P0 не различает эти два дерева, остановиться с минимальным противоречием, а не закреплять один вариант golden-файлом.
- [x] Синхронно исправить все нормативные/рабочие копии parser и generated/self-build источники, не оставляя различного поведения stable/dev. (GROKBOT-P0-EMPTY-FORMS-20260922-74: l1src + l2src_sandbox/l1src + lm1/build mirrors)
- [ ] Не превращать bare atom в Frame только ради вызова; добавить разрешение атома в executable consumer.
- [ ] Проверить старый алгоритм `Anonymous Argument Container Transparency` из `lingvamyxa_prev/Lingvamyxa_spec.txt` §4.0.1 и перенести его без новой параллельной нормализации.

### Известные места, где форма сейчас меняет смысл

- [x] В `l2_head_is_call` раннее решение по `COMPACT` удалено коммитом `f858cea`: `l2_find_method` выполняется до path-kind fallback, а тело больше не читает `LM_P0_FRAME_COMPACT`. Сохранять это как regression-инвариант и не переоткрывать без нового evidence.
- [x] Аудировать `COMPACT`-ветки function-pointer, raw-field и C/predef call checking/emission по тому же принципу: удалить только form-based precedence, не удаляя правильную проверку разрешённой callable-головы. (GROKBOT-COMPACT-RAWFIELD-BATCHA-72 + COMPACT-FNPTR-BATCHC-76: `rg LM_P0_FRAME_COMPACT` на `dev/l2src_sandbox/l2trans.lm1` = 0 semantic reads; ty40 call = resolved KIND/type 40 in all forms; raw_fld=5 и c.sizeof carve-outs сохранены.)
- [x] Удалить `COMPACT`-исключения из распознавания локальных объявлений и неподдержанных численных типов. (GROKBOT-COMPACT-DECL-BATCHB-75: `l2_struct_local` + `l2_unimpl_numeric`.)
- [ ] Удалить `COLON` как признак assignment/unknown-type routing в `l1trans.lm1`, если правило равноценности подтверждено и для L1.
- [ ] Добавить parity-проверки stable `l2src`, `dev/l2src_sandbox` и self-build mirrors.

## 3. Разрешение объявления, присваивания и вызова

- [ ] **Порядок объявлений уровня единицы (автор, Q8 = U2, 2026-09-23):** объявление — оператор точки входа на своём месте; ячейка own-поля E существует с конструирования и равна нулю, пока оператор не выполнился; метод, объявленный ниже переменной, законно читает её, а вызов этого метода выше объявления читает нуль (`int: r f()` / `int: x 5` / `fn: f` читает `x` → выход 0). Литерал и выражение в инициализаторе не различаются по форме. Реализовано в S2 (`f6f213a`); добавить минимальную программу как строку harness.

### Общая последовательность

- [ ] Сначала построить одинаковую Structure/Frame независимо от формы записи. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** блочная/короткая/скобочная формы тел — одно дерево, закреплено goldens `p0_tree_contract` в трёх копиях парсера (`run_parser.py --profile current`); остаток — parity единственного анонимного контейнера (§2 «Исправления P0», Grok -127); галочка после -127.
- [x] Разрешить голову и применить единый приоритет: существующее callable-поле → вызов; существующая non-callable привязка → присваивание; объявление → только конструкцией, явно задающей тип. Неизвестная голова без применимой типизированной объявляющей конструкции — ошибка; вывода типа новой переменной из RHS нет. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** архитектурно так (`l2_head_is_call` перед каждой веткой присваивания), но ни одна фикстура не доказывает «одно имя: callable побеждает присваивание» → тикет -130 (Sonnet): метод `foo` + `foo: 5` в той же области вызывает `foo` (побочный эффект); неверная арность → ошибка вызова, не присваивания; мутант — снять `l2_head_is_call`; одна фикстура закрывает :181/:212/:214/:216. **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** `unit_callable_priority.lm2` — `foo: 5`, `foo(6)`, вертикальная `foo:`/`7` вызывают `foo` (побочный эффект hits/last = 1/5, 2/6, 3/7); мутант (`l2_find_method` в `l2_head_is_call` снят) → путь присваивания, RED.
- [x] Затем по уже выбранной роли выполнить одну физическую операцию: callable invocation, assignment/rebind либо declaration/construction. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** утверждение о форме реализации без собственного наблюдаемого входа/выхода — закрывается мутантом тикета -130 вместе с :181. **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** тем же мутантом -130: при снятом разрешении головы вызов уходит в присваивание — роль выбирается до операции.
- [x] Интерпретаторы должны получать уже различённые операции (`CALL`, `SET`/`ASSIGN`, `PUT`), а не повторно угадывать их по исходной пунктуации. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** `dev/l3_interp` получает предразрешённые виды узлов (`L3_NODE_ASSIGN`, `L3_NODE_IF` = 32, `L3_NODE_WHILE` = 33, `L3_NODE_RETURN`) до обхода (посажено ~`eebb382`/`93cd81c`); вызов/значение решается по виду разрешённой привязки, не по пунктуации.

### Объявление

- [x] Если имя цели отсутствует, а справа синтаксически находится Structure, создать структурную переменную: тип явно задан самой Structure согласно принятому структурному правилу. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** ни кода, ни фикстуры для этого пути не найдено; смысл после правила автора о P0 (2026-09-23): `mystruct()` = `mystruct: ()` = объявление пустой Structure при отсутствующем имени — транслятор пишется после -127 (P0 parity) → тикет -132 (Sonnet) вместе с :191–:193. **Закрыто (Sonnet, FABLE-SONNET-EMPTY-STRUCT-20260923-132, `7cc4310`, harness 244/244):** `l2_empty_struct_decl_shape` (зеркало `Model: fresh` без именованной модели; check — `l2_own_add` + `l2_bind_own`; emit — `lmx_node_new_owned` на активацию, без refs/merge — отказать может только аллокация); `unit_empty_struct_decl.lm2` — три записи (`f()`, `f: ()`, закрытая вертикаль) объявляют пустую Structure; мутант (всегда отказ) → ровно 2/229 RED. Попутно три дефекта: повторная проверка формы на emit не узнавала собственное объявление (исключать существующую строку лишь при `l2_own_decl[existing] != stmt`), `if` без тела в эмиссии, необъявленный `l2_tN`.
- [x] Сохранить явное типизированное объявление: `Model: fresh` и `Model(fresh)` — синонимы. После разрешения головы callable-модель вызывается по общему приоритету; non-callable модель/тип при отсутствующем `fresh` объявляет структуру `fresh` типа `Model`. Полное значение получается общим `merge` модели с пустой Structure. Это следствие строгой типизации и полного построения, а не отдельное правило для имён `Model`/`fresh`. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** синонимия измерена ИСТИННОЙ (одинаковый Frame, одинаковая эмиссия `lmx_merge_owned`), но скобочная запись не закреплена ни одной фикстурой → пара обеих записей с одним чтением seed — в тикете -130. **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** `unit_model_fresh_synonyms.lm2` — обе записи в одной фикстуре, чтение seed 1U через вызов метода для каждой.
- [x] Произвольный RHS не выводит тип. `ping: 7` при отсутствующем `ping` — ошибка; примитив объявляется только с явным типом (`int: ping 7`). **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** `unit_colon_undeclared_refused.lm2` (`f6f213a`), located-отказ.
- [x] Для непримитивного нового значения полностью выделить требуемую память/графовую структуру согласно его конструктору; неизвестный тип — ошибка. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** полное построение известных не-примитивов — фикстуры Model (-105/S2); «неизвестный тип → ошибка» измерено (located «assignment target must be a declared typed mutable value»), но не закреплено → фикстура в тикете -130. **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** `unit_decl_unknown_type_refused.lm2` — `Nope: x` located-отказ 1:1 frame=Nope; построение известных Model — фикстуры -105/S2/-130.
- [x] Для пустой Structure одинаково обработать `mystruct: ()`, пустой вертикальный вариант и вариант с `end: mystruct`. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** сегодня не работает ни одна из трёх форм: `mystruct: ()` уходит в общий отказ присваивания; вертикальная форма блокируется валидатором `lm_p0_validate_nonempty_colon_frames*` — его судьба уже решена автором (§2: отказ только висячему `f:`, явно закрытое пустое тело допустимо, -127); при отказе валидатора транслятор печатает вторую строку «translation failed with no located diagnostic» — тот же класс молчаливой диагностики, что закрыт в -129 ч.1, другое место (взаимодействие P0-отказа и транслятора) → в тикет -132. **Закрыто (Sonnet, FABLE-SONNET-EMPTY-STRUCT-20260923-132, `7cc4310`, harness 244/244):** одно P0-дерево (Grok -127) + один путь объявления; `unit_call_args_refuse_struct.lm2` (-92) переведён на новую норму с пояснением в заголовке.
- [x] Не требовать `KnownModel`, зарезервированное имя или отдельную ветку для пустой Structure. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** зависит от :191; проверяется той же фикстурой -132. **Закрыто (Sonnet, FABLE-SONNET-EMPTY-STRUCT-20260923-132, `7cc4310`, harness 244/244):** ни `KnownModel`, ни зарезервированного имени, ни отдельной ветки: тот же путь, что `Model: fresh`, минус модель.
- [x] Сделать разрешение явного типизированного объявления и его полное построение одинаковыми для блочной, короткой и скобочной форм; не вводить специальные ветки для буквальных имён `Model` или `fresh` и не превращать техническое понижение через `merge` в самостоятельное правило языка. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** короткая и скобочная — одинаковы (см. :188); «блочная» запись объявления переменной существующего типа при попытке `Model:` + тело + `end: Model` попадает в путь определения типа («duplicate named Structure») — блочная запись объявления есть вертикальное тело с одним атомом (`Model:` / `    fresh`), то же P0-дерево; измерить и закрепить в -132, не вводить новой записи. **Закрыто (Sonnet, FABLE-SONNET-EMPTY-STRUCT-20260923-132, `7cc4310`, harness 244/244):** блочная запись объявления существующего типа (`Model:` / `    fresh`, вертикальное тело с одним атомом) — ровно форма `l2_colon_decl_shape`, ноль различий с `Model: fresh` (`unit_typed_decl_vertical.lm2`); `Model:` + поля + `end: Model` — определение типа, отказ «duplicate named Structure» закреплён (`unit_duplicate_named_struct_refused.lm2`); вторая молчаливая строка при P0-отказе устранена: `l2_diag_n` теперь считает и located-строку парсера (`unit_empty_struct_hanging_refused.lm2`: голое `f:` — только P0 error 32); `f()` на существующем не-Structure — located «unsupported body».
- [x] Устранить зависимость `Model: fresh` от порядка/числа методов. В текущем ядре минимальная единица, где `main` является первым и единственным методом, отвергает эту строку как assignment к необъявленной цели, тогда как добавление перед `main` постороннего метода позволяет тому же объявлению пройти. Нормализация declaration должна опираться только на разрешённую голову, отсутствие binding и явно заданный тип, а не на ранее собранный метод. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** `unit_colon_model_decl.lm2` (`f6f213a`) — ровно описанный случай единственного метода, зелёный; варианты «один метод» и «два метода до объявления» транслируются одинаково (перемерено -129 ч.2); зависимость от порядка не воспроизводится.

### Присваивание

- [ ] Если существует non-callable binding, выполнить обычное присваивание с полным admission/`implements` до записи. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** явный статический случай закреплён (`unit_implements_assignment.lm2`, `unit_implements_admission.lm2`, harness :1274/:1282); «каждое обычное присваивание прозрачно проходит admission» во время исполнения — см. :204/:205 (после S3 2b).
- [x] Сохранить единственную проекцию непримитивов: Structure/Array binding уже хранит физическую ссылку на дескриптор/вхождение; assignment/call/return копируют ссылку, C by-value aggregate запрещён. `@binding` добавляет уровень адреса ячейки ссылки (`Lmx *` → `Lmx **`), а не превращает создание Structure в ссылочное объявление. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** сделано в коде (`967acd6`/`27479aa`), доказано `unit_addr_slot_structure_projection.lm2` (общий над именем Structure, пара Other/Model), но фикстура НЕ зарегистрирована в `tools/l2_harness.ps1` → подключить в -130, затем галочка. **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** `unit_addr_slot_structure_projection.lm2` зарегистрирована в harness (перепроверена на 6ca54c9: Model и Other, общий путь).
- [x] Реализовать `@fresh` для Structure binding, созданного `Model: fresh`: разрешать имя через ту же таблицу binding/occurrence, которой принадлежит слот с `Lmx *`, и возвращать адрес именно этой ячейки (`Lmx **`). Текущие `l2_check_addr`/`l2_prep_addr` не находят такой binding и выдают `unresolved name`. Не создавать временную C-переменную, не брать адрес referent и не вводить отдельный адресный механизм. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** `unit_field_path_unit_addr.lm2` (harness :1128), `b76c077`/`9516f91` — S2 сделал `mi = e` для кода уровня единицы, особый случай исчез; перемерено, exit 0 — но строка плана шире (`@fresh` через ту же таблицу binding/occurrence для локального слота): галочка после -130, где и пара `Model(fresh)`. **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** `unit_field_path_unit_addr.lm2` (harness :1128) + `unit_model_fresh_synonyms.lm2`; `@fresh` через ту же таблицу occurrence (S2 `mi = e`).
- [x] Исправить явный тип `@: Model slot`: именованная Structure уже проецируется как `Lmx *`, поэтому один `@` над `Model` физически означает `Lmx **`. Сейчас `l2_ptr_local_ty` ошибочно отправляет имя модели в общий foreign-C-pointer interner, и generated L1 буквально содержит `@: Model slot`. Сохранить логическую identity `Model` для admission отдельно от физической ABI-проекции; не объявлять C aggregate `Model`. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** сделано в коде (`967acd6`/`27479aa`), доказано `unit_addr_slot_structure_projection.lm2` + `unit_addr_entry_name_collision.lm2` + `unit_addr_unknown_type_refused.lm2` — все три не зарегистрированы в harness → подключить в -130, затем галочка. **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** `unit_addr_slot_structure_projection.lm2`, `unit_addr_entry_name_collision.lm2` (Says «1 2 3 4 5»), `unit_addr_unknown_type_refused.lm2` («unknown type» atom=Banana 12:4) — все три в harness, заголовки соответствуют утверждениям.
- [x] Проверить и закрепить уже правильную ABI-часть: результат `merge`, Structure-formal и Structure-return понижаются как `@: Lmx` в L1 / `Lmx *` в C; assignment/call/return копируют тот же физический адрес. Исправление `@` не должно превратить обычное Structure-значение в `Lmx **`. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** `unit_field_path_formal_value.lm2`, `unit_local_model_arg.lm2` (-121 D1/D2, harness, зелёные) используют результат `merge`/Structure-formal как `@: Lmx` повсеместно; изменения эмиссии -121/-129 этот контракт не трогали (полный набор зелёный).
- [x] Считать `ping: 7` присваиванием только когда типизированный non-callable `ping` уже объявлен; «тип ранее фигурировал» означает существующую привязку, а не повторное объявление. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** `unit_colon_undeclared_refused.lm2` + `unit_field_write_from_method.lm2`/`unit_field_write_below_refused.lm2` (`765e70c`): присваивание допустимо только уже объявленному выше имени — и для локала метода, и для поля единицы.
- [ ] Присваивание известного значения всегда семантически выполняет `implements`, даже при кешированных проверенных адресах и оптимизированных C-local slots. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** найдена лишь `unit_colon_graph_update_admission_blocked.lm2` (harness :1228) — трансляционный fail-closed отказ, а не живой runtime-путь; тикет после S3 2b (runtime admission `l2_emit_admit`): положительный — повторное присваивание через один оптимизированный локал в цикле, каждая итерация проходит/не проходит `implements` независимо; мутант — пропуск повторной проверки.
- [ ] При неуспешном admission не менять цель и её `dirty`. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** та же зависимость от S3 2b; отрицательный свидетель — после неуспешного runtime admission значение цели и `dirty` ровно прежние.
- [x] Примитивы записывать как значения; Structure/Array и другие non-callable ссылочные значения — как ссылки согласно их контракту, без неявного `merge`, копии или переподчинения. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** `l2_own_store_value`/`l2_emit_occ_write`: примитивы — `lmx_*_store_known`, Structure/указатели — `lmx_pointer_store_known`, без неявного merge/copy; повсеместно проверено фикстурами own-полей -121/-129.
- [x] Закончить terminal Structure-reference assignment для прямых имён и путей. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** -121 и -129 закрыли по одной комбинации корень×вид, но полнота не доказана → тикет -131 (Sonnet): одна фикстура-перечень по всем комбинациям (прямое имя/путь × own/unit/formal × примитив/Structure), по одному утверждению на комбинацию. **Закрыто (Sonnet, FABLE-SONNET-OWN-LOOKUP-AUDIT-20260923-131 ч.3, `7b64331`, harness 224/224):** `unit_field_path_terminal_checklist.lm2` — по утверждению на каждую комбинацию (прямое имя/путь × own/unit/formal × примитив/Structure) через обычный путь; попутно 6-й пробел того же класса: скан корня-имени own-поля в `l2_path_root` не имел unit-fallback (Structure-типизированное unit-поле как корень пути из другого метода → «unknown field path root») — исправлен зеркалом цикла `l2_own_find`, мутант → ровно 1/220 RED. Подпункт «unsupported terminal Structure assignment-path» (:214, остаток -92): `o\inner: a` — located-отказ «a field path must end at a primitive field», второй молчаливой строки нет (`unit_field_path_struct_rebind_refused.lm2`) — fail-closed, не пробел.
- [ ] **Пробел порядка проверки (Sonnet -131 ч.3, не исправлен — архитектурное решение):** `u_struct\value` в составном выражении (`if: u_struct\value != 66U`) из метода, проверяемого РАНЬШЕ E, даёт located «unresolved name»: `l2_field_path_check`/`l2_path_kind` требуют `l2_own_nsty_get(oi)`, который заполняется лишь когда тело E доходит до своего `Model: fresh` (~:12421), а `l2_parse_unit` проверяет E последним (S2: E — последняя строка); запись и голое чтение проходят через whole-path fast exit `l2_check_fields` (~:12049–:12059), которому nsty не нужен. Варианты: предпроход объявлений перед циклом методов (прецедент `l2_merge_scan`) или ленивый пересчёт nsty по `l2_own_decl[oi]` при промахе `l2_path_kind`. Свидетель в фикстуре обойдён чтением в локал (та же форма, что -120, другой корень). Владелец — Sonnet после -132; выбор варианта — по принципу одного механизма (предпроход, как `l2_merge_scan`), если измерение не покажет иного.
- [x] Унифицировать direct own/local/formal, explicit graph path и unit-level stores через одну семантическую admission boundary, сохраняя допустимые backend-оптимизации после проверки. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** предпосылка подтверждена свежими фактами: три раздельно найденных и раздельно исправленных пробела поиска own-поля за один день (D1 — formal-корень читал объявление; D2 — inline merge писал только ячейку графа; -129 — у `l2_own_find_last` не было fallback `l2_own_find`) → тикет -131: аудит каждого места чтения/записи own-поля в эмиттере — какой поиск (`l2_own_find` / `l2_own_find_last` / ad-hoc seeding formal/path) и совпадают ли они на одном имени; затем исправление одним механизмом. **Закрыто (Sonnet, FABLE-SONNET-OWN-LOOKUP-AUDIT-20260923-131, `246056e`/`3616700`, harness 218/218):** `tools/own_lookup_audit.py` (115 мест, 9 резолверов); `l2_hidden_from` → `l2_own_find(callee)` (чужое поле больше не читается; `l2_own_first` удалён; `unit_dyn_hidden_from_cross_method.lm2` 0 → 222 по активациям, `_undeclared_refused` located; мутант → 111); пять ручных копий формулы own-ячейки сведены к `l2_own_from_expr` (мутант — 100 фикстур разом); инвариант «обе записи» проверен на всех 4 местах `l2_emit_occ_write`; остаток — двойная попытка `l2_own_find_last`/`l2_own_find` в `l2_check_sizeof`/`l2_prep` избыточна (чистка позже).

### Вызов

- [x] Если разрешённая голова является callable-полем, всегда выбирать вызов с высшим приоритетом — независимо от `COMPACT`, colon или блочной формы. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** та же фикстура, что :181 (тикет -130). **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** `unit_callable_priority.lm2` (три формы → вызов).
- [x] Если сигнатура или admission такого вызова не принимают аргументы, выдавать ошибку вызова без fallback к assignment/declaration. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** блочная/скобочная формы — отказ без fallback доказан (`entry_bad_sig.lm2`, `unit_call_args_refuse_named.lm2`); colon-форма не закреплена → в -130: существующий метод `foo` без аргументов, `foo: 5` с телом → located-отказ по арности вызова, не «assignment target». **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** `unit_callable_priority_arity_refused.lm2` — `bar: 5` при `bar` без аргументов → «incompatible entry signature» 14:1 frame=bar (ошибка вызова, не присваивания); блочная/скобочная — `entry_bad_sig.lm2`, `unit_call_args_refuse_named.lm2`.
- [x] Универсальный приоритет по resolved binding (не name/syntax exception и не fallback): callable ⇒ call; существующий non-callable ⇒ assignment; отсутствующее имя ⇒ declaration только когда тип явно задан конструкцией, иначе ошибка. Отдельно исправляется unsupported terminal Structure assignment-path. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** приоритет — тикет -130 (см. :181); подпункт «unsupported terminal Structure assignment-path» — это остаток -92: statement-level `ty40` отказ в `l2_check_body` (тогда :12054/:12059) для присваивания Structure-значения по пути; измеряется и закрывается в -131 вместе с :207. **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** приоритет — `unit_callable_priority*.lm2`; подпункт «unsupported terminal Structure assignment-path» (остаток -92 ty40) — в -131 вместе с :207.
- [x] Голый callable в statement position — частный случай expression statement / discard (см. ниже), не отдельная sugar-ветка. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** покрыто :217 (bodiless descriptor, STAGEC-65) и IMPLEMENT-ссылкой раздела discard (`test2(test) == 1`, native CONTINUE-54 / интерпретатор STAGEC-65); отдельного действия нет.
- [x] Не поддерживать прямое присваивание callable-полю. Его замена допускается только общими механизмами: `merge` структур с `implements`, динамической перегрузкой одноимённым callable вызывающего либо явной передачей callable в дескрипторе аргументов вызывающего. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** архитектурно так (`l2_head_is_call`), отрицательной фикстуры нет → в -130 (`foo: 5` против `foo`, не принимающего аргумент: отказ вызова, не присваивание callable-полю). **Закрыто (Sonnet, FABLE-SONNET-CALLABLE-PRIORITY-20260923-130, `87fb409`, harness 194/194):** `unit_callable_priority_arity_refused.lm2` — `bar: 5` не становится присваиванием callable-полю: отказ вызова; мутант даёт формулировку присваивания → RED.
- [x] Formal of callable signature (`test: f` / `test3: f`) receives a whole callable `@: Lmx` occurrence (AUTHOR-CALLABLE-DESCRIPTOR-IMPLEMENT-20260922-43 / G2 whole-occurrence; routes -41 a-f + descriptor-only -44/46). Passing/using that formal stays on the general occurrence path — not a new bind/registry act. Bodiless `test3` is always a valid descriptor (`l2_m_body=0`, `addr=0`); native proven CONTINUE-54 Stage B; interpreter proven STAGEC-65 (`test2`/`test4` RET-of-ARG nullary activate; `test3` ⇒ `LMX_WALK_NOT_CALLABLE`).
- [ ] Проверять полную сигнатуру, dynamic inputs, admission и checkpoints одинаково для native lowering и интерпретатора. **Аудит A1 §3 (Sonnet, FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 ч.2, по факту на `765e70c`):** общих пар фикстур native/L3 нет — `l2_harness` и `run_l3_selftest` структурно раздельны; не один тикет: разложить по механизмам (сигнатура, dynamic inputs, admission, checkpoints) строками §6 (фаза B2, Grok) и закрывать построчно.

### Исполнение именованной Structure и контракт получателя (автор, 2026-09-23; Codex `f61f029`, запрос FABLE-NAMED-STRUCT-CALL-20260923-01)

Норма (дословно автора — `LMX_blog/2026-09-23.md`; книга `provenance/semantics-book.md` → `docs/LMX_semantics.*` §10/§12/§13): всякая именованная Structure исполняется голым атомом имени, а объявление/построение её не исполняет; `fn` — выражение с одним результатом, `sub` — исполнение без результата, `fm` — результат-структура; голый `return` — выход без значения (допустим и в `sub`, результата не создаёт), `return: value` — значение в теле, допускающем результат; на уровне открытия `return` может закрывать именованную Structure как trailer по общему правилу. `f()`, `f: ()`, закрытая пустая вертикаль — Frame с разрешением головы, как прежде; исполнение обычной Structure голым `f` не делает `f()` вызовом. В позиции аргумента выбирает принимающий контракт: явно объявленный callable-формал получает ссылку на callable-вхождение (сохранить `test2(test) == 1` и descriptor-only `fn`); потребитель результата исполняет callable с возвращаемым значением; callable без результата, включая `sub`, передаётся ссылкой; bodiless `fn` — только интерфейс, тело не выдумывается. Никакого скрытого реестра или fallback.

- [x] Аудит по факту (read-only, анкеры на `cb85dc2`): P0 `l1src/parser.lm1` :637–:659 `lm_p0_trailer_role_accepts_target` — `return` как trailer сегодня принимает целью только `fn` (и читает реестр `trailer.target`): норма — любая именованная Structure; ядро `dev/l2src_sandbox/lmx_call.lm1` :12–:33 `lmx_call_ready` — «callable» = Structure с интерфейсом вызова, различие «с результатом / без результата» не выражено; `lmx_walk.lm1` :766–:804 (op RET) — любое callable-вхождение в возвращаемом значении нульарно активируется, тогда как норма исполняет лишь callable с результатом, а `sub` передаёт ссылкой; транслятор `l2trans.lm1` :9831 (`l2_ns_field_kind`) и :10953–:11155 (`l2_take_named`/`l2_take_eternal`/`l2_int_formal`/`l2_typed_formal`) — контракт формала; `dev/l3_interp/l3_exec.lm1` :196–:285 (`l3_exec_stmt`/`l3_exec_range`/`l3_exec_frame`) — исполнение голого атома и `return`. Выход — таблица «место → соответствует / расходится / отсутствует» с минимальными программами. **Сделано (Opus, FABLE-OPUS-NAMED-STRUCT-AUDIT-20260923-138, база `717a325`): таблица, инвентарь реестров/fallback и план 6 коммитов — `steps/named-struct-audit.md`.** Итог: P0 `return`-trailer — только голова `fn` за мёртвым реестром (fn соответствует, sub расходится, fm отсутствует, обычная S расходится); `lmx_call_ready` — бита «с результатом» нет; walker RET активирует и `sub`; транслятор: `g(m)` передаёт ссылку на вхождение как int (−1287876288 вместо 7), `sub` в значение — указатель как int, trailer `return: m` — ссылка, `return: s` ломает эмиссию (колонка 0), `S()` при существующей S — отказ вместо присваивания, тела именованных Structure не принимают int и операторы; L3/walker: голый callable-оператор молча OK; **голый атом-оператор роняет транслятор (segfault `l2_check_body` :13592)**. Противоречие C1 (какой Frame закрывает `return`: по форме vs только fn/sub/fm) и вопрос Q-138-2 (перезапуск объявлений полей при исполнении) — автору, `q19.md`.
- [ ] P0-гейт: `return` (голый и `return: value`) как trailer закрывает `fn`, `sub`, `fm` и обычную именованную Structure — goldens `tests/parser/current` для четырёх голов; `f()`/`f: ()`/вертикаль — деревья не меняются; голое `f` — атом; реестр `trailer.target` либо явная грамматическая таблица, либо удаляется (не скрытый реестр). **Q19.1 (автор, 2026-09-24, дословно в `LMX_blog/2026-09-24.md`):** `return`-trailer закрывает любую callable-Structure — `fn`/`sub`/`fm` **и обычную именованную Structure** (она callable по норме 2026-09-23); голый `return` — когда возвращаемого значения нет; не-callable структуры `return` не закрываются. Это чтение А аудита; открыт Q19.3 (управляющие головы `else:` и т.п. — явная таблица слов языка в P0 или переписать 9+1 мест резаком `---`). Коммит 3 плана аудита: решённая таблица вместо fallback «fn», удаление 6 заглушек реестра и 3 мест вызова, синхронизация копий/зеркал/пина, `tmp_unitroot_test` удалить или явно исключить; гейт «взрыва» деревьев; мутанты R-any / fn-only.
- [ ] Native-гейт (harness): (а) голое `f` исполняет обычную именованную Structure (не только метод) — побочный эффект наблюдаем; объявление/построение не исполняет; (б) `sub` с голым `return` — выход, результата нет; `sub` в позиции результата — не исполняется, а передаётся ссылкой (admission решает, подходит ли ссылка); (в) потребитель результата исполняет `fn`; (г) callable-типизированный формал получает ссылку — `test2(test) == 1` и descriptor-only `fn` без изменений; (д) прямое исполнение bodiless `fn` — located-отказ, тело не выдумывается; (е) `f()` при существующей обычной Structure `f` — присваивание пустой Structure с admission, при отсутствующей — объявление (см. -132); мутанты на каждую строку. **Коммиты 1–2 плана аудита без решения автора (Opus -139):** голый атом-оператор (fn/sub → нульарный вызов, литерал/поле/формал → discard, обычная S → located-отказ до Q19.2; чинит segfault); контракт получателя (результатный callable в позиции значения исполняется, `sub` — отказ «a callable without a result has no value», trailer `return: m` = путь тела, `return: s` — отказ, `S()` при существующей S — присваивание через admission). Пункт (а) для обычной Structure и `return: v` в её теле — коммит 4, после Q19.2.
- [ ] Интерпретаторы (`lmx_walk` RET, `l3_exec`): те же различения «результат / ссылка» либо явное «unsupported»; parity-строки с native. **Коммит 5 (ядро, после Grok -134):** бит «с результатом» в `LmxCallable`/`LmxMethod` + запрос рядом с `lmx_call_ready`; RET — результатный активируется, без результата — ссылка; голый CALLABLE-оператор в L3/walker — исполнение или явный UNSUPPORTED, plain Structure-оператор — явный UNSUPPORTED.
- [ ] Ни одного name-special, реестра «какие имена исполняются» или fallback: инвентарь после реализации (`rg` по `lm_p0_registry_table_has_rows`, `lmx_call_ready`, спискам имён в трансляторе). **Инвентарь (аудит):** мёртвый реестр P0 (6 заглушек, 3 вызова, `LM_*_REGISTRY` очищаются, но никем не читаются), fallback «fn» в 4 копиях + 3 зеркалах; `l2_unit_role` — явная таблица голов; ветка «имя метода как значение» решает по имени (:12928, :14553); `lmx_call_ready` принимает голый METHOD как переходный fallback (:26–:29). Коммит 6 — гейт инвентаря.
- [ ] Логическое противоречие (например, место, где ссылка на `sub` и исполнение `fn` неразличимы по типу приёмника) — автору минимальной программой по-русски, без изобретённого исключения.

### throws / catch (S1; автор Q11–Q13, 2026-09-23; `LMX_blog/2026-09-23.md`)

- [x] `throws:` — в шапке метода (декларативная голова определения); объявленное имя, не отловленное явно вызывающим (`catch:`) и не перечисленное в его `throws:`, — ошибка трансляции (§14); у точки входа E списка throws нет → необработанное объявленное имя, дошедшее до E, — ошибка трансляции «unhandled throw in entry: <Name>». **Opus, FABLE-OPUS-S1-DECLARED-20260923-135 (`9f299a5`, harness 239/239, build_l2src 250, L3 11/11):** `throws:` — первый элемент тела метода, читается как формалы (`l2_throws_frame/_declared/_name/_pos`); имя k уходит статусом k (1..d), неявные — d+g; `l2_collect_method` отвергает пустой список, не-идентификатор, дубликат, неявное имя («an implicit throw name is not declared», fail-closed), `throws:` не первым/во вложенном блоке/на уровне единицы; статическое правило `l2_check_throws_handled` на каждом ребре вызова (`l2_check_primary`, statement-вызовы, `return: f`): каждое объявленное имя callee перечислено у caller, иначе «unhandled throw: <Name>», у E — «unhandled throw in entry: <Name>» (чтение A по Q13); intern несёт упорядоченные имена (`l2_it_tmi`, `l2_throws_same`: другой порядок — другая сигнатура); S1.4 — `l2_method_sig_compatible` требует тот же упорядоченный список. Строки: 10 отказов (unlisted, entry_unhandled, undeclared, not_first, nested, unit-level, duplicate, implicit, payload, formal) + `unit_s1_declared_links` (пины статусов Oops 1, Nope 2, merge d+1 = 3; отображение 1→2, 2→1), `unit_s1_throws_intern` (sig 5U есть, 6U нет); мутанты: снять статическое правило → RED 2; тождественное отображение → RED 1; intern без имён → RED 1; снять S1.4 → RED 1. **Обязательство S1.3:** runtime-доказательство «объявленная позиция ≠ d+g» возможно только с `catch:` (до S1.3 ни одна программа, чья E достигает объявленного имени, не компилируется).
- [x] Неявные имена операторов без видимого `throws` (сегодня `merge`, в т.ч. внутри `Model: fresh`, и `implements` — отказ runtime admission при связывании нетипизированного графа с объявленным типом; **автор, Q17 = А, 2026-09-23: имена неявных throw выбирает команда**, список — таблица транслятора): бросают, отлов не обязателен; неотловленный — в корень исполняющегося Message, `running = 0`, без результата, код выхода 1; `catch: merge` обрабатывает стандартно. **Opus, FABLE-OPUS-S1-STATUS-20260923-133 (`a3bc1c6`/`1abafa4`, harness 222/222, build_l2src 250, L3 11/11):** S1.1 — таблица неявных имён `l2_implicit_name` (merge = 1, implements = 2) в одном месте; `l2_implicit_status(mi, name) = d + g`; выходы отказа (`l2_emit_implicit_throw`: merge-оператор + аллокация тела результата, `Model: fresh`, три места `l2_emit_admit`) делают `l2_emit_checkpoint`, затем `l2_out_throw[0]: 0; return: d+g`; распространение `l2_emit_propagate` перекодирует k > d_callee; адаптер при ненулевом статусе ставит `l2_message\running: 0`, значения нет, возвращает статус (не 70), C main — выход 1; строки `unit_s1_merge_uncaught`, `unit_s1_merge_uncaught_entry` (факты драйвера `mergefail N`/`stopped 1`/`thrown 1`), `unit_s1_implements_uncaught` (`thrown 2` — различим от merge); мутанты: без `running = 0` → RED 7; implements как merge → RED 5. **Остаток:** `catch: merge`/`catch: implements` — S1.3. **Opus, FABLE-OPUS-S1-CATCH-20260923-136 (`b2667dd`/`b0b4158`, harness 261/261, build_l2src 250, L3 11/11):** `catch: merge ()` / `catch: implements ()` — обработчики неявных имён без параметров (`unit_s1_catch_merge_local` 50 при `mergefail 1`; `unit_s1_catch_implements` 42 — отказ admission письма пойман); неявный бросок и распространение вызова сначала ищут обработчик; неотловленный — в корень (`running = 0`, код 1, S1.1).
- [x] Размещение `catch:` — до вызова (повторный вход в участок), после вызова, или отдельным вложенным блоком; два одноимённых `catch` на одном уровне — ошибка (§14, пример t2 перенесён в книгу). **Opus, FABLE-OPUS-S1-CATCH-20260923-136 (`b2667dd`/`b0b4158`, harness 261/261, build_l2src 250, L3 11/11):** pad на блок: `int: l2_atP 0` / `while: 1` / сегмент j под `if: l2_atP <= j` / обработчик i под `if: l2_atP = i` / `break`; доставка после post-call checkpoint (payload → `l2_plP`, `l2_atP`, `continue`): catch до вызова — повторный вход, после — продолжение; во вложенных циклах — `l2_goP` + break-guard; break/continue пользователя в pad — `l2_ubP`/`l2_ucP`; поиск обработчика — от внутреннего блока наружу до тела метода, соседние блоки не ищутся, свой обработчик исключён (тот же `Name` в своём handler → объемлющий catch); два catch одного имени в блоке — отказ. Строки: `unit_s1_catch_tc` (повторный вход/после вызова/int payload), `unit_s1_catch_t2` (103, книжный пример), `_sibling` (3), `_rethrow` (1011), `_nested_while` (122), `_publish` (7 — checkpoint на выходе, отложенный свидетель S1.1), `_user_break` (105), отказы duplicate/shadow/unhandled-in-sibling; мутанты: без exit checkpoint → RED 1; без исключения своего имени → RED 1; без break-guard → RED 1; доставка в первый pad → бесконечный повторный вход на TC (фокусный прогон). **Обязательство S1.3 выполнено:** `unit_s1_catch_declared_vs_merge` — `Oops` (1) и `merge` (d+1 = 2) доходят до разных обработчиков (27 при `mergefail 1`, 8 без). Попутно: анонимный блок `---` ронял `l2_check_body` (segfault, воспроизводится на e42a846/9f299a5) — теперь вложенное исполняемое тело во всех 9 проходах (`unit_anon_block` 16; мутант → RED 9).
- [x] Payload `throw: Name(args)` → параметры `catch: Name (params)`: позиционный префикс, связывание — обычное присваивание через runtime `implements` (§7); intern сигнатуры несёт имена throws. **Opus, FABLE-OPUS-S1-DECLARED-20260923-135 (`9f299a5`, harness 239/239, build_l2src 250, L3 11/11):** сторона `throw:` сделана: `throw: Name(args)`/`throw: Name` требуют имя в списке («throw of an undeclared name»); аргументы проверяются как фактические вызова; payload — Structure в `l2_program_arena` (ребёнок k — свежая типизированная ячейка аргумента k или сама ссылка на Structure, без копии), затем checkpoint, `l2_out_throw[0]: payload; return: pos`; `throw: Name`/`Name()` — без payload (0); неаллоцируемый payload — 0 (S1.3 отвергает при связывании), отказ store в свежую ячейку — инвариант X1. Объём payload S1.2 (D2, расширить в S1.3 по строкам catch): по одному значению на аргумент (имя, числовой литерал, вызов) типов int/size_t/unsigned/ulong/Structure; char, текстовые/char-array пути, многополевые выражения (`a + 1`) — отказ «unsupported throw payload value» (книжный `throw: GreetingSkip(cfg\greeting)` пока отвергается). Строка `unit_s1_declared_payload` (ссылка на Structure + size_t-ячейка, own-поле опубликовано до броска). Попутно исправлен дефект: `return: f` в теле для callable на throw-канале эмитировался через `l2_emit_nullary_call` только с (node, self) — gcc отказывал; теперь как в `l2_prep`, бросок проходит (`unit_s1_return_callable_throw_abi` + `_fails`). Сторона `catch:` — S1.3. **Opus, FABLE-OPUS-S1-CATCH-20260923-136 (`b2667dd`/`b0b4158`, harness 261/261, build_l2src 250, L3 11/11):** параметры catch — own-поля, хостимые catch (как переменная for); связывание в начале обработчика: короткий payload → `implements`; примитив — после `lmx_domain_type` (несоответствие → `implements`); Structure — runtime `implements` через `l2_emit_admit`; все связывания — обычные записи own-полей; отказы: параметры у merge/implements, тип параметра вне int/size_t/unsigned/ulong/named Structure, имя параметра, совпадающее с видимым формалом/методом/own/unit-полем. **Остаток (S1.5):** char/текст/многополевые payload и char/text параметры catch (книжный `throw: GreetingSkip(cfg\greeting)` как текст — пока отказ; порт t2 использует size_t; именованная Structure не принимает `int:`-поле — только size_t/char); `lmx_call0`-диспетчеризация throw-канала через callable-формал с plain ABI; библиотечные обёртки с plain ABI; descriptor не объявляет throws; один слот payload на pad (`l2_plP`) — корректно, т.к. связывание в начале handler; интерпретаторы — явное unsupported.
- [x] Понижение (native): статус-возврат + существующий out-параметр `l2_out_throw`; одна нумерация статусов на метод — объявленные 1..d, затем неявные d+1..; без отдельного «abort»-статуса; `assert` — своим маршрутом; landing pads структурно, без правки L1/пина; интерпретатор — явное «unsupported» до своей реализации. **Opus, FABLE-OPUS-S1-STATUS-20260923-133 (`a3bc1c6`/`1abafa4`, harness 222/222, build_l2src 250, L3 11/11):** нумерация одна на метод (0; объявленные 1..d, d = 0 до S1.2; неявные d+g); отдельного abort-статуса на канале нет — X1 `l2_emit_invariant`: 24 проверки формы `R: merge:` (71..91), все места `l2_fail_return` (функция удалена), пропавшая ячейка/отказ store тела результата, профиль qualified-операнда, result-bind store, ячейка `Model: fresh` — печатают `lmx: invariant: <what>` и `c.abort()` (решение D-a: `lmx_msg_poll_abort` возвращает 1 лишь при `success != 0`, потому «печать и продолжить» доставляло бы в никуда; остановка одного Message — маршрут assert, вне S1); перепись возвратов: ненулевой статус в телах методов — только d+g. Попутно исправлен дефект: trailer `return: X` у throw-ABI метода эмитировался как СТАТУС (`unit_s1_trailer_value`, entry 5, stopped 0; мутант → RED). **Остаток:** landing pads — S1.3; null-path bails (`l2_emit_path_bail`, `l2_emit_path_load`) и пролог `if: self = 0` по-прежнему возвращают статус 0 с нулевым значением; диагностики checkpoint/nextMessage/rebinding печатают и продолжают; ловушка драйвера покрывает `lmx_merge_owned`, не `lmx_merge_profiles_owned`. **Opus, FABLE-OPUS-S1-CATCH-20260923-136 (`b2667dd`/`b0b4158`, harness 261/261, build_l2src 250, L3 11/11):** landing pads структурно (`while: 1` + `l2_atP`), без правки L1/пина; нумерация и X1 — S1.1/S1.2 выше; `assert` — своим маршрутом (вне S1); интерпретаторы — явное «unsupported» (`lmx_walk.h.lm1`, `l3_exec.lm1`). Остатки S1.5 — в :213.
- [ ] Источники нормы: L2/L3-спека и `lingvamyxa_prev/Lingvamyxa_spec.txt` + старые примеры; `l1.txt` (спека L1) — наш промежуточный язык, не норма L2. **Дефект книги (Opus -136, к Codex):** §14 пример t2 ставит каждый вызов `checkedGreeting(...)` ВНУТРЬ обработчика, тогда как в оригинале с табуляцией (`lingvamyxa_prev/tests/t2.lmx`) вызов стоит на уровне catch, после обработчика; порт `unit_s1_catch_t2` следует оригиналу.
- [ ] **Остатки после S1.2 (Opus, измерено, не сделано):** callable на throw-канале, диспетчеризуемый через `lmx_call0` (callable-формал), вызывается с plain ABI — вопрос диспетчеризации ядра (S1.4 здесь — только совместимость); descriptor без шапки не может объявить throws; trailer колонки 0 `return: m` вычисляет `m` как ЗНАЧЕНИЕ (callable-вхождение), а `return: m` в теле — ВЫЗЫВАЕТ: два чтения одной формы, правило приоритета callable говорит «вызов» (относится к «Исполнение именованной Structure» выше, не S1); `l2_uses_is_control` без вызовов — мёртвый код; библиотечный метод на throw-канале не компилируется (обёртки вызывают с plain ABI; у C API нет throw-канала) — pre-existing; `l2_scan_body` пропускает `throws:` и сканирует только аргументы payload (иначе имена выходов становились нетипизированными динамическими входами — только ALL-CAPS пропускались).

### Expression statement и discard

Авторски утверждено (`LMX-DISCARD-PLAN-20260922-01`). Write-реализация входит в sole-writer ticket `GROK-BOT-C-DOOR-UNIFY-20260922-11` (вместе с унификацией двери `c.*`). Evidence addendum (held RO reviews released into this plan before code):

- [ ] Receiverless последовательность в body — общий expression statement: тот же expression-span checker/evaluator, что и для выражений с назначением результата.
- [ ] **Binding KIND** решает call vs value: method / callable graph field — invoke; typed fnptr value — evaluate/discard **без** вызова. Discard **не** выполняет admission.
- [ ] Голый callable `f` — не исключение и не sugar: одноатомное выражение; в expression-statement consumer callable-first вызывает с нулём потреблённых аргументов (когда KIND = callable method/field). Typed fnptr atom в value/discard — значение, не auto-call.
- [x] **IMPLEMENT cross-ref (post-Stage1; AUTHOR-CALLABLE-DESCRIPTOR-IMPLEMENT-20260922-43):** same cases as §2 — `test2`/`test4` formals + bare `return: f` (once KIND=CALLABLE, no special return rule); E2E `test2(test)==1`, `test4(test)==1`; direct `test3` → NOT_CALLABLE without inventing a body. Native DONE CONTINUE-54 Stage B; interpreter DONE STAGEC-65 (`lmx_walk` / `lmx_interp`).
- [ ] Голые data вычисляются нормально; неизвестный atom — диагностика `unresolved name` (не AV, не silent skip).
- [ ] Body walker обязан группировать через `l2_expr_span` и **продвигать итератор на span**, а не на одно field/statement.
- [ ] Каждый expression statement вычисляется; его результат отбрасывается.
- [ ] Нативный L2 discard destination — уже существующий правильно типизированный generated `l2_tN`, неиспользуемый после полного выражения. Не добавлять arena container, namespace binding, selector, publication, addressability, fixed ring или ветвление по синтаксической форме.
- [ ] `l2_new_temp` — **int-only** и **не** универсальный typed allocator. Для discard использовать ту же типизацию temp, что уже делает method-call lowering (`size_t` / `ulong` / pointer / …). Void `ret=8` **никогда** не возвращает и не именует незадекларированный `l2_tN`. Обязательные counterexamples: `size_t`, `ulong`, pointer.
- [ ] Один общий `l2_eval_discard` / body dispatch **заменяет обе** существующие ad-hoc ветки (старые якоря ~15741 call-frame и ~15745 `l2_is_known` external/adapter Frame). Не оставлять параллельные discard/call paths.
- [ ] Удалить body-emptiness call heuristic и COMPACT-gated fnptr call contradictions (call vs value только по resolved binding KIND).
- [ ] Тесты: `2+2`, bare data, bare nullary callable + side effect, nested call, unknown name → `unresolved name`, typed fnptr discard без call, void без undeclared temp, `size_t`/`ulong`/pointer discard temps, отсутствие access violation.
- [ ] **Норма (автор через Codex, CODEX-FABLE-HEADLESS-DOC-20260923-01):** допустимые expression statements в исполняемом теле — голое `2`, `2 + 2`, голое `f`, `(f)`, `(2 + 2)`, пустое `()` и эквивалентные анонимные вертикальные структуры; один общий путь парсер → consumer без исключений по синтаксической форме; callable разрешается по KIND (вызов), не-callable вычисляется и отбрасывается; destination отброшенного результата — уже существующий типизированный generated `l2_tN`, чья жизнь заканчивается после полного выражения (не новый буфер и не арена). **Реализация:** транслятор сегодня отвергает голый атом/`()`/скобочную группу как оператор («unsupported body», `l2_check_body` / эмиттер) — принятая норма отделена от нереализованного; закрывать по свидетельствам ниже.
- [ ] **Обязательный свидетель P0 и runtime (автор, дословно в `LMX_blog/2026-09-23.md`):** фикстура с анонимной скобочной структурой в записи с точками-уровнями (точка в начале строки — продолжение уровня, как в `docs/LMX_grammar.ru.md` §12), буквально:
  ```text
  ( f: 1
  . f: 2
  . 2 * 2
  . f
  )
  ```
  — при строгой типизации `f` должен быть явно объявлен в объемлющем контексте (`int: f 0`), тип из литерала `1` не выводится; проверить последовательное исполнение: `f: 1`, затем `f: 2` (присваивания с admission), затем `2 * 2` вычислено и отброшено в `l2_tN`, затем голое `f` вычислено и отброшено (для callable `f` — нульарный вызов); P0-dump той же структуры — одно дерево с вертикальным эквивалентом; native и interpreter — одинаковые результат и побочные эффекты; никакого синтаксически-особого пути.
- [ ] **P0 TRANSPARENCY HOLD:** отдельную P0-прозрачность sole anonymous argument-container / пустого Frame сохранять по `steps/current.md` и §2 выше; не смешивать её с discard expression-statement и не переносить решение о допустимости Frame на translator/receiver по исходной форме.

### Решённый дискриминатор

- [x] Для `f: g`, `f(g)` и вертикального эквивалента существующий callable `f` всегда вызывается; любой failure остаётся ошибкой вызова, assignment-fallback запрещён. Если существующий `f` non-callable, выполняется присваивание. Если `f` отсутствует, сама эта запись ничего не объявляет; требуется отдельная конструкция с явно заданным типом либо структурное правило, где тип задан Structure справа.
- [x] Правило «три формы равноценны» действует *везде в языке*, следовательно включает L1 projection. Текущие ветвления L1 по `LM_P0_FRAME_COLON` для assignment/type routing подлежат аудиту и устранению; stable/self-build corpus входит в будущую отдельную write-партию после аварийного checkpoint.

## 4. Occurrences, repeated fields и `[N]field`

- [ ] Хранить всё тело callable после `child[0]` в лексическом порядке: own fields и Frame-узлы не сворачивать по имени.
- [ ] Вложенные тела `if`/`else`/`while`/`for` — обычные дочерние Structure тела callable, не вложенные callable: у них нет собственного дескриптора и своей активации; их own-поля — hosted (принадлежат телу-хосту), own-поля уровня метода — unhosted, и это различие обязано сохраняться. Нативное исполнение метода остаётся одной функцией.
- [ ] Повторные `arg:` материализовать как отдельные occurrences.
- [ ] Реализовать `test\[0]arg`, `test\[1]arg` и последующие индексы; неуточнённый путь выбирает `[0]` согласно последнему решению автора.
- [ ] Использовать один общий алгоритм `occurrence -> physical slot/path` для native lowering и интерпретатора.
- [ ] Не добавлять второй индекс, runtime-таблицу имён, отдельный журнал или дублированный обход.
- [ ] **Путь от корня (автор, Q9 2026-09-23, `LMX_blog/2026-09-23.md`):** `node\x` — поле `x` лексического родителя callable occurrence (`M.parent`, то есть `[0]x` этого родителя); собственное own-поле occurrence — просто `x`; `for\x` — поле объемлющего `for`-фрейма как Structure. Чтение «`node`/`for` — префикс написания для своих own-полей» (N1) снято: `node` и `for` — обычные корни того же диспетчера путей (-113), не зарезервированные спеллинговые корни. Пример без такого поля у родителя (`x` объявлен внутри `Model`, а не у единицы) — ошибка «unresolved name». Фикстуры `unit_node_path*`, `unit_node_array_paths`, `unit_forj_*` переписать под это чтение; fallback `l2_slash_own`/`l2_slash_for`/`l2_path_own` и спеллинговые ветки массивных helper'ов удалить.
- [ ] Исправить own-таблицы/планы, которые сейчас склеивают occurrences по имени.

## 5. Canonical local cell, selector и sticky dirty

- [ ] Для каждого logical activation-local имени иметь одну стабильную физическую canonical cell.
- [ ] Голое имя и `@name` всегда обращаются к этой canonical cell; выданный адрес не переключается на графовый слот.
- [ ] Любое выполненное взятие адреса `@name` делает logical local sticky до конца активации независимо от момента относительно binding.
- [ ] Само взятие адреса не создаёт graph field и не выбирает destination, пока occurrence не исполнилось.
- [ ] Selector выбирает только текущее occurrence — назначение будущей публикации.
- [ ] Перед переключением selector со старого occurrence на новое обновить старый pending snapshot из canonical cell.
- [ ] Писать в граф только на checkpoint.
- [ ] При sticky и активной цели консервативно публиковать текущее occurrence на каждом наблюдаемом checkpoint, даже без доказанной записи через указатель.
- [ ] Унифицировать это для примитивов, указателей и прочих адресуемых локальных типов без временных адресных копий.

## 6. Матрица тестов объявления, присваивания и вызова

- [ ] На уровне P0 проверить, что parenthesized, short/colon и vertical/block дают **одно и то же P0 дерево** (автор, 2026-09-23, `next_parser_fix.md`): нульарные — `body fields=0` без контейнера; непустые — одинаковые прямые поля после нормализации единственной анонимной обёртки; для любой головы, включая `mystruct()`/`mystruct: ()`; голое `f:` — ошибка 32; не размножать по этим формам ветки или таблицы транслятора и не оставлять позднюю CALL-only распаковку.
- [ ] Семантическую матрицу транслятора запускать над одной нормальной операцией; отдельно проверить одноатомный bare callable как expression statement (callable-first, zero consumed args), не как sugar-ветку форм.
- [ ] Для одной нормализованной операции проверить absent binding, existing binding и path target.
- [ ] Для каждого контекста проверить primitive, Structure-reference, Array/reference и callable в позиции головы/явного аргумента.
- [ ] Сравнивать не только exit code, но и выбранную physical operation, результат, graph identity, `dirty`, admission trace и диагностику.
- [ ] На границе P0 проверить одинаковый нормальный нульарный результат для `f()`, `f: ()` и явно закрытого `f:`/`---`; отдельно закрепить синтаксический отказ P0 для одиночного незавершённого `f:`. Голый callable `f` проверить как expression statement / discard (callable-first, zero consumed args), а не как четвёртую синтаксическую форму Frame и не как отдельное исключение нульарного вызова; соседний голый data-атом вычисляется и отбрасывается, но не автовызывается. **Свидетельство (Grok Bot, FABLE-GROKBOT-P0-SOLE-CONTAINER-20260923-127):** P0-часть закрыта `bc22881` (см. §2 :96/:157/:158); runtime-часть (нульарный вызов из трёх форм + голый `f` как discard) — строкой harness после удаления unwrap в lmx_walk. **Opus, FABLE-OPUS-S1-STATUS-20260923-133 (`a3bc1c6`/`1abafa4`, harness 222/222, build_l2src 250, L3 11/11):** runtime-часть: интерпретатор — `lmx_walk_selftest` (аргументы = дети вызова, без bag-unwrap); native — `unit_callable_priority.lm2` (-130). Остаток: строка parity «лишний Structure-аргумент нульарному callee» native vs walker.
- [ ] Проверить передачу empty Structure/unit как одного именованного значения отдельно от пустого списка аргументов.
- [ ] Проверить, что bare data expression **вычисляется** и его результат **отбрасывается** (discard), но **не вызывается**; bare callable в executable statement position вызывается (expression-statement / callable-first). Согласовано с п. выше про голый data-атом.
- [ ] Строки-свидетели headless expression statements (CODEX-FABLE-HEADLESS-DOC-20260923-01): P0-dump для голого `2`, `2 + 2`, голого `f`, `(f)`, `(2 + 2)`, пустого `()` и их анонимных вертикальных эквивалентов — одно дерево на форму; native и interpreter — результат и побочный эффект наблюдаются (счётчик в callable `f`, значение `f` после `f: 1`/`f: 2`, отброшенный `2 * 2` не оставляет незадекларированного temp; `l2_tN` типизирован и не используется после полного выражения); отдельно строка со скобочной структурой в записи с точками-уровнями `( f: 1` / `. f: 2` / `. 2 * 2` / `. f` / `)` из §3 (P0-ожидание измерить дампом, не утверждать).
- [ ] Проверить terminal Structure-reference rebinding; отдельно проверить отсутствие прямого callable-field assignment и три разрешённых общих пути замены callable.
- [ ] Проверить наблюдаемое выполнение `implements` на каждом известном присваивании, включая identity и empty-uses случаи после решения автора.
- [ ] Проверить повторные occurrences, `[N]field`, canonical address, selector switch и sticky publication.
- [ ] Сохранить тесты `Model: fresh` как свидетельство явного типизированного объявления и дополнить их эквивалентными блочной/скобочной формами; сохранить `ping: 7` как вызов, когда `ping` разрешается в callable-поле, и добавить соседние случаи: существующий типизированный non-callable → assignment; отсутствующий `ping` → ошибка; явное `int: ping 7` → declaration.
- [ ] Добавить минимальную регрессию объявления, где `main` — первый и единственный метод: `Model: fresh` обязано дать тот же declaration/merge, что и в единице с предшествующим посторонним методом.
- [ ] Добавить generated-L1/C регрессии ссылочной проекции: `Model: fresh` создаёт значение `Lmx *`; передача, возврат и assignment сохраняют точное равенство физического адреса; `@fresh` имеет тип `Lmx **`; `@: Model slot` понижается в тот же физический тип; разыменование `slot` даёт исходный `Lmx *`. Запретить в goldens объявления C/L1 aggregate `Model` и адрес временной копии referent.

## 7. Полная модель `implements` и admission

- [ ] **Payload `throw` → параметры `catch` (автор, Q12, 2026-09-23):** связывание — обычное присваивание, верифицируемое стандартным runtime `implements`; на трансляции проверяется лишь частично, оптимизация ограниченная; ошибка типа — стандартный путь отказа admission. Никакой отдельной трансляционной проверки «согласования префикса»; `catch: Name (params)` берёт первые k аргументов `throw: Name(args)` (старый пример t2, §14).

- [ ] **Промежуточный admission нетипизированного графа (S3, решение fable B2, 2026-09-23):** граф без объявленного типа Structure (письмо `nextMessage:`, значение `@: Lmx`), связываемый с объявленным типом — `nextMessage:` в типизированное own-поле, перепривязка такого поля, аргумент типизированного формального параметра, — допускается в точке связывания ядерным обходом `lmx_runtime_implements(l2_program_arena, кандидат, форма, форма)`. Форма — материализованный дочерний узел объявления типа; она же `Consumer`, то есть каждое объявленное поле считается используемым (строже `uses`, никогда не мягче). `0` — отсутствие графа, а не admission. NO/UNKNOWN — неявный отказ по тому же каналу, что у неудачного `merge` (без значения, exit 1); молчаливого связывания нет. ИЗВЕСТНАЯ ГРУБОСТЬ: обход сравнивает домен листа и не спускается в массивы, поэтому `int: []: []: x` и `char: []: []: x` для него неразличимы (оба ARRAY_OF_DESC); строка harness `unit_admit_letter_coarse` фиксирует это и перевернётся с портом. Мера промежуточная — до порта receiving-expression admission ниже.

- [ ] **Порядок обязателен:** не начинать write-порт `implements`, `Consumer/uses`, runtime-валидации и таблиц преобразований до завершения срочных исправлений ядра из разделов 2–6 и их отдельного зелёного checkpoint. До этого разрешены только read-only аудит и подготовка точных границ порта.
- [ ] Портировать реализацию `implements` из `C:\Nyasha_Planet\lingvamyxa_prev`, не ограничиваясь поверхностным сравнением дескрипторов.
- [ ] Портировать гигантскую таблицу преобразования примитивов полностью, сохранив направленность, диапазоны, точность и ошибки.
- [ ] Портировать механизм создания и заполнения таблиц, написанный на L2, и адаптировать только реальные несовместимости нового ядра.
- [ ] Сохранить единый механизм: аналитический `uses(Consumer, bVar)` плюс runtime-валидация принимающего выражения по принятой модели; не вводить параллельный `RuntimeImplements`, скрытый nominal type test или бессрочное разрешение из кеша.
- [ ] Не переносить как норму историческое приближение, где automatic admission подставляет required descriptor вместо настоящего `Consumer`, а текущий LMX-предикат игнорирует `consumer`. После срочных правок реализовать универсальное правило с настоящим `Consumer/uses`; из прежнего проекта полностью перенести направленную таблицу примитивных преобразований и пригодный табличный механизм.
- [ ] Устранить обход admission в own/local/formal/primitive fast paths.
- [ ] Порт receiving-expression admission (или шаг ядерного обхода внутрь элементов ARRAY_OF_DESC) закрывает грубость промежуточного admission S3: внутренний `T` поля `T: []: []: x` сейчас не проверяется.
- [ ] В `l2_admit_implements` и `l2_admit_consumer_uses` сохранить допустимый успешный результат identity/empty-uses, но пропустить его через обязательную семантическую точку вызова `implements`; текущие ранние `return` до вызова нарушают это требование.
- [x] Отделить обязательность семантического вызова admission от его результата: `candidate == required` и пустое `uses` могут дать успех, но fast path не имеет права миновать саму точку `implements`.
- [ ] По коду прежней реализации установить, какие стадии реально выполняются внутри identity/empty случая: аналитический walk, receiver runtime tests или оба. Не добавлять новое наблюдаемое действие по догадке; это исследование реализации, а не открытое разрешение обходить admission.
- [ ] Не считать совпавшие физические адреса, дескрипторы или кеш доказательством admission.

## 7a. Унификация двери `c.*` (`c.puts` / `c.array`)

**FABLE-GROKBOT-RAW-MEMBER-ROOT-AND-7A-CLOSE-20260923-126 part2 (Grok Bot):** §7a measured on 17d54bc (code) with gates harness 177 / build_l2src 250 / L3 11; ticks carry commit+row/rg witnesses; open items name exact remaining gaps. sizeof: block re-confirmed closed via ancestor 6a07f37.

Авторское решение (`GROK-BOT-C-DOOR-UNIFY-20260922-11`) + evidence (`GROK-BOT-C-DOOR-EVIDENCE-20260922-13`) + **AUTHOR CORRECTION** (`AUTHOR-C-RAW-DOOR-20260922-14`). + **AUTHOR-SIZEOF-RECEIVER-20260922-15** / **AUTHOR-SIZEOF-DOCSET-20260922-16** (`sizeof:` ordinary receiver). + **AUTHOR-ARCH-DOCTRINE-20260922-17** + **AUTHOR-SIZEOF-UNHOLD-20260922-18** + **AUTHOR-SIZEOF-OPERATOR-20260922-19** / **AUTHOR-SIZEOF-DESIGN-20260922-20** (`sizeof:` planned receiver-operator — ввести). Норма: `c.*` — **raw door into C**. Нет declared foreign-entity registry, нет header scanning, нет generated dictionary of C entities, нет classification `c.sizeof` как declared/builtin entity kind. Bounded cleanup: raw-C door transparency + remove `c.puts` specials + remove L2 `c.array` special declarator + expression-statement/discard. L1 `c.array` остаётся отдельно owned measurement boundary (не L2-семантика).

### `c.puts` (removable closed cluster)

- [x] Спецификация L2 **не** задаёт name-specific семантику `c.puts`. Ordinary `c.*` call path уже токенизирует quoted arguments.
- [x] Удалить closed special checker/emitter cluster (`l2_simple_puts_main`, исключения в `l2_c_stmt_door`, специальные `frame_head`/`leaf` ветки); `c.puts` → ordinary foreign-call / `c.name` statement routing. (99 PART2: deleted puts helpers; door no longer excludes c.puts.)
- [ ] Проверить derivation stdio include и ordinary `c.name` statement routing после удаления specials. **Gap (measured 17d54bc):** ordinary `c.name` routing OK (`rg l2_simple_puts_main` = 0; harness `entry_puts_*`/`unit_puts_*` GREEN). **stdio.h still unconditional**, not use-derived: `dev/l2src_sandbox/l2trans.lm1:3` + generated emit ~:16007 always emits `"<stdio.h>"`. Remaining: use-derived stdio (or document unconditional as intentional mapping rule).

### Поля C-структур (автор, Q7 = P1, 2026-09-23)

- [x] Путь `\field` на значении, чей тип спелен `c.*`, — сырой C-доступ к члену: эмитится дословно, проверяет C-компилятор; транслятор не знает C-структур. Удалить фиксированные коды типов по имени (`LmP0Text` ty=5, `LmP0IndentStack` 14, `LmP0TrailerRole` 10, `LmP0Diagnostic` 21, `LmP0Document`/`need_p0`, per-name include-флаги в `l2_is_known`; группа B инвентаря `FABLE-GROKBOT-TYPE-NAME-SPECIALS-20260922-111`), мигрировать порт парсера `parser_*_port.lm2` и фикстуры на `c.LmP0*` с сырыми путями; `lm_own_*`/`lm_p0_*`-адаптеры видимы только через `predef`-объявления, не по зашитому списку. **(988d19a raw member path; a2cf69e delete LmP0* ty codes + port/fixture migrate; 1b395ff raw-dispatch root only for `c.*` / foreign≠Lmx; harness `unit_c_member_*` + `unit_raw_root_*`. Residual emit heuristic: `strstr(s0,"IndentStack"|size_t)` arms ~12966 — track under GATE inventory.)**
- [x] 20 puts fixtures сейчас ungated — добавить real witness, обновить diagnostics намеренно, ввести gate coverage. (Gated hello/seq/empty/nl/esc + method_body + main_beside_method; arity refusals deferred to C.)
- [ ] L2-библиотека puts — обычный L2-код **без** `c.`; C puts — внешний C через ту же общую дверь `c.*`. **Gap:** no L2 puts library unit yet; callers still use raw `c.puts` (gated). Bounded ticket: add L2 library without `c.`.

### L2 `c.array` (declarator, not a call)

- [ ] Спецификация L2 **не** определяет семантику `c.array`. Это declarator, не call. Special L2 semantics сейчас: ~3 name tests + одно user-facing lowering. **Gap (17d54bc):** `l2_array_local` still matches `frame_head(stmt, "c.array")` (`l2trans.lm1:5031`); call sites ~3643/~11780/~14507. CORE rejects name-specific `c.array` but translator still accepts the declarator.
- [ ] Удалить `c.array` из L2 docs и L2 translator/input corpus; мигрировать уже общими механизмами L2 либо внешней библиотекой через `c.*`. Не изобретать replacement syntax / hard-coded helper. **Gap:** ~46 `c.array:` decls remain in `l2trans.lm1` (L1 source). L2 user fixtures migrated (17d54bc). Delete `l2_array_local` after L1 HOLD measurement.
- [x] `parser_dump_port.lm2` → owned allocation: явная length **32** (`c.sizeof(pointer)` дал бы 8), allocation failure handling, free, и документированное отклонение от oracle. **(17d54bc:** sandbox+frozen `[]: char buffer 32`; `dump\failed` + `p0_dump_delete`; comment «Deviation from L1 oracle spelling»; `rg c.array` on file = 0.)
- [x] 4 `c.array` fixtures ungated; вместе с puts — **24** affected fixtures, сейчас **zero** gate coverage — закрыть в этой партии. **(17d54bc:** frozen twins → `[]: char`; harness gates `entry_array`/`entry_array_leading_zero`/`entry_nul` + `unit_native_activation` Absent=`c.array`; puts already gated; full harness GREEN 177/177 `build/l2_harness/fable126_part2a`.)

### Raw-C door (`c.*`) — AUTHOR-C-RAW-DOOR-20260922-14

**Норма (overrides -13 entity-kind / sizeof HOLD framing):** `c.*` — сырой дверной проход в C. L2 **не** ведёт registry foreign entities, **не** сканирует headers для словаря имён C, **не** классифицирует `c.sizeof` / `c.puts` как declared/builtin entity kinds.

- [x] Сделать raw-C door **syntax-transparent**: `c.sizeof(...)` понижается в C `sizeof(...)` через общий raw-C door; unevaluated семантика sizeof даёт **C compiler**, не L2 semantic resolution. (123 part2: deleted c.sizeof special + LmP0 name arms; `unit_sizeof_c_door_arena`.)
- [x] `c.puts(...)` проходит тем же raw-C door **без** name-specific L2 checker/emitter.
- [ ] Удалить header scanners / entity dictionaries / name whitelists / special `c.*` semantic classification. Сначала **inventory** точных obsolete scanners/dictionaries. **(Inventory 17d54bc — still present; DeepSeek owns deletion):** `l2_c_header_walk:4168`, `l2_c_header_chain_has:4266`, `l2_c_header_chain_has_typedef:4296`, `l2_predef_has_function:4516`, `l2_predef_has_type:3982`, `l2_predef_has_fnptr:3877`, `l2_foreign_intern:4547`, `l2_c_stmt_door:8624` (syntax-only `c.` prefix; no puts exclusion), `l2_array_local:5021` (c.array declarator). `l2_simple_puts_main` = 0. Gap: deletion + zero residual refs.
- [x] **Ownership/dependency:** DeepSeek ранее был tasked автором удалить эти scanners/dictionaries — **не дублировать и не отклоняться**; зафиксировать ownership/dependency и координировать. Grok Bot не invents параллельный removal path. **(126 part2:** inventory above; no parallel removal by Grok Bot.)
- [x] Preserve только mapping rules, реально нужные чтобы emit valid C; неизвестные механики — label for audit, **не** invent registry. **Kept mapping rules (126 inventory):** (1) c.* token → raw C spelling via l2_c_door / l2_c_stmt_door (prefix c.); (2) foreign intern/slot tables for typed c.Name values (l2_foreign_intern / byvalue) until DeepSeek deletion lands — currently required to emit typed temps; (3) raw member path when root type is c.* (l2_ty_raw_c_members + l2_raw_path; Structure/Lmx roots excluded — 1b395ff); (4) unconditional bootstrap includes currently emitted (stdio.h/stdlib.h/string.h + predef chain) — audit whether use-derived later; (5) l2_emit_ccall ordinary foreign call lowering. **Label for audit/delete (DeepSeek):** header walk/chain/predef_has_* dictionaries.


### `sizeof:` — ввести language receiver-operator (AUTHOR-SIZEOF-DESIGN-20260922-20)

Clarifies / overrides overstatements in -19. Unhold from -18 stands (no global-contract HOLD/OPEN).

- [x] Текущей работе нужен size operation. Вместо отдельного L2 parsing/checking/emission для сырого `c.sizeof` — **выбрать и ввести** обычный language receiver-operator `sizeof:` (123 part2: `l2_emit_sizeof` / `l2_check_sizeof` / `l2_sizeof_name_bytes`).
- [x] До реализации: статус **absent/planned**, не нарушенная заранее обязанность языка и не HOLD в ожидании другого author contract. (superseded: implemented in 123 part2.)
- [x] После введения: `sizeof:` участвует в существующей receiver-operator architecture (как класс `fn:`); **не** model как function/callable.
- [x] `c.sizeof(...)` остаётся валиден **только** как raw C text через uniform дверь `c.*`, где raw C намеренно используется; **zero** `c.sizeof`-specific L2 parser/checker/emitter semantics. (special emit/check/prep deleted; door uneval soft-check only.)
- [x] Не обобщать это решение на суждения о `length` / `capacity` / etc. в рамках этого тикета. (author Q16: sizeof:=bytes, length=count.)
- [x] Инвентаризовать Lmx-side `c.sizeof` → мигрировать на `sizeof:` после введения operator. (tests + port migrated; `l2trans.lm1` kept `c.sizeof(@:)/c.sizeof(c.)` — built by l1trans.)
- [x] Отличить `sizeof:` от Array `length` (и от L3 `size`/`shape`/`rank`) — без переразметки length/capacity в этом тикете. (`unit_sizeof_array_bytes`: bytes=16, length=4.)
- [x] Реализацию оставить **later bounded ticket** после docs baton/ownership order; one-writer + clean-kernel barriers остаются. (done in 123 part2.)
- [x] Evidence (inventory before/with implementation): exact affected symbols (`l2_prep_sizeof_name` / former `c.sizeof` specials), fixtures/tests, harness gates. (deleted prep_sizeof_name + specials; fixtures gated; mutants measured.)
- [x] Acceptance (после введения): `rg` — zero `c.sizeof`-specific semantic branches; receiver-operator dispatch + surface-form equivalence + native+interpreter. (harness 171 GREEN; build_l2src 250; L3 11 suites.)

### L1 `c.array` (separate, unproven — do not merge)

- [ ] **HOLD / separately owned foreign-backend debt:** L1 `[]` и L1 `c.array` имеют разные emitters; `l2trans` ещё эмитит `c.array` в generated L1 (~5 sites). Перед любым scheduling removal — read-only byte-output equivalence measurement + inventory golden/self-build consequences. **Не** смешивать с bounded L2 cleanup. **HOLD report (17d54bc):** emits L1 `c.array:` via `c.fprintf` at **14510**, **16047**, **16182**, **16414**, **16417**. Reason: L1 lowering still needs `c.array:` for tables/buffers; not L2 declarator semantics.

## GATE. Чистое ядро перед самосборкой L2 (blocking)

**Блокирующий GATE** (`GROK-BOT-C-DOOR-UNIFY-20260922-11`). Самосборка / миграция уровней (§8) **не начинается**, пока ядро не чисто и не зафиксирован один точный pushed checkpoint.

Чистое ядро (`clean kernel`) означает одновременно:

- [ ] Нет нарушений [`architectural-placement`](next_core_tasks_dictionary.md#architectural-placement): нет name-special/`allowlist`/header-scanner/hidden-registry/shim/fallback «дырок»; нет лишних полей в базовых Array/Message ради одного потребителя (cleanup debt до закрытия).
- [x] `sizeof:` — **implemented** language receiver-operator (123 part2); zero `c.sizeof`-specific L2 emit/check specials; raw `c.sizeof` through door.
- [ ] Нет L2 special semantics и name-specific веток для `c.puts` / `c.array`; **zero** stale header scanners / C-name dictionaries / name whitelists / special `c.*` semantic classification (raw-C door only).
- [ ] Нет дублирующих старых discard/call путей рядом с новым общим механизмом (`l2_eval_discard` и единый body dispatch).
- [ ] Нет мёртвых helpers, unreachable compatibility shims, устаревших exclusions/comments и противоречивых docs/tests.
- [ ] Нет скрытого fallback, сохраняющего удалённое поведение.
- [ ] Tracked tree и ownership чисты (маркеры сняты после checkpoint; нет чужого WIP на owned paths).
- [ ] Полный supported L2 runtime/generated harness, релевантный L3 runner, `python tools/check_docs.py` и `git diff --check` зелёные на **одном** точном pushed commit.
- [ ] Source inventory: удалённые helpers имеют **ноль** ссылок; выполнен поиск остаточных special-case `c.puts` / `c.array` (translator, docs, fixtures, corpus).
- [ ] Если L1 `c.array` ещё нельзя убрать — это явный отдельно owned blocker / foreign-backend debt, **не** L2-семантика; до author-approved границы **не** заявлять kernel-clean / self-build-ready.

Порядок: сделать raw-C door syntax-transparent + bounded cleanup (`c.puts` specials + L2 `c.array` + expression-statement/discard; §3 / §7a), inventory/remove obsolete scanners/dictionaries (coordinate DeepSeek ownership), закрыть этот GATE (**включая zero stale c.* machinery**) **до** любого шага §8. L1 `c.array` — отдельно owned measurement boundary и не смешивается с L2 cleanup.

## 8. Перенос уровней и самосборка

- [x] **API почты для аргументов (автор, Q10, 2026-09-23):** никакого нового синтаксиса — только общий ресивер почты `nextMessage:` (семантика: следующее принятое письмо текущего Thread, пока не 0; общий механизм `lmx_thread`, не R0-специфичный). Аргументы процесса кладутся **внутрь письма**: поле `mainArgs` — массив строк ровно как пришли из C (`argc`/`argv`); C `main` строит письмо и посылает его обычной почтой в inbox R0; программа читает `m\mainArgs[i]` и `length` обычными путями. Невзятое письмо освобождается при close (свидетель). **Тип письма (автор, Q15, 2026-09-23):** обычная явно объявленная Structure `MainLetter:` с полем `char: []: []: mainArgs` — общее правило `T: []: []: x` (два уровня самостоятельных одномерных массивов, внешний — ссылки на дескрипторы внутренних; не прямоугольный `[][]:`); NUL входит в элементы внутренних массивов; оба уровня в арене письма; никакого контекстного `String`, таблицы исключений для `mainArgs` или временного profile-описания. Зафиксировать `T: []: []: x` в спецификации (сделано, книга §Array) и реализовать его понижение транслятором (объявление поля, конструирование, чтение `m\mainArgs[k][j]`, `length` на обоих уровнях). **Закрыто (Opus, FABLE-OPUS-S3-MAIL-ARGS-20260923-122):** часть 1 `fd7e8fc` — письмо с `mainArgs` строит и посылает C `main` обычной почтой (`lmx_service_post`), `nextMessage:` — общий ресивер `lmx_thread` (`lmx_argv_letter_selftest.lm1`, `unit_next_message_in_method/_loop/_method_first.lm2`), невзятое письмо освобождается при close (P5); часть 2 `82eb1b2` — `T: []: []: x` как поле вида 6 (объявление, конструирование пустого внешнего Array, `R: merge:`-проверка копии), runtime admission нетипизированного графа `l2_emit_admit` через `lmx_runtime_implements` в трёх местах, чтение `m\mainArgs[k][j]` и `length` на обоих уровнях через `MainLetter: char: []: []: mainArgs` (`entry_index`, `entry_strcmp`, `entry_parse_min`, `unit_charpp_return`, `unit_entry_args`, `unit_arr_path_read`, `unit_admit_rebind_read`; отказы `unit_arr_path_*_refused`); мутанты b/m1/r1/r2/s/w — RED; harness 214/214, L3 11/11. Остаток: внутренний `T` поля вида 6 admission не проверяет (§7, порт receiving-expression admission).
- [x] **Граф ребёнка (автор, Q6 = R1, 2026-09-23):** источник графа ребёнка — единица (`.lm3`-файл), по определению independent (`parent = 0`); ядро не проверяет и не чинит; не-independent source — ошибка программы. Selftest: граф R0 и графы детей после open/create имеют `parent = 0`. **Закрыто (Opus, S3 часть 1 `fd7e8fc`):** `lmx_thread_graph_root_selftest.lm1` — граф R0 и граф ребёнка, созданного из independent source, имеют `parent = 0`; ядро не проверяет и не чинит.

- [ ] После стабилизации семантики закончить миграцию поддерживаемого L1-кода в L2; цель — именно перенос реализации, а не добавление неиспользуемых операторов ради покрытия спеки.
- [ ] Перенести динамический контейнер с L1 на L2 как отдельную реализацию, не меняя базовый Array.
- [ ] Довести до полной самосборки L2: L2 должен собирать собственные актуальные единицы, включая parser, translator, runtime tables и `implements`.
- [ ] Не считать одну текущую строку `library-links` в `tools/l2_harness.ps1` свидетельством выполнения L2-библиотеки: она проверяет только линковку/экспорт символов, а `Exit = 0` совпадает с результатом wrapper, который не смог открыть библиотеку. До RUN-gate сначала реализовать успешный `l2_library_open` и реальный вызов экспортированного тела; лишь затем добавлять отличимый ненулевой runtime-result. `unit_lib_pair_a.lm2` и вспомогательный `unit_lib_pair_b.lm2` учитывать как один составной row, а не как два независимо локализуемых результата. Из остальных строк 21 `l2trans-refuses` зелёные именно при ожидаемом отказе и не должны ошибочно считаться runtime-запусками; этот класс честно отличается от двусмысленного `library-links`.
- [ ] Убрать точечные L1-вставки там, где уже существует рабочий L2-эквивалент; оставшиеся вставки перечислять явно с причиной.
- [ ] Затем перенести преимущественно L2-реализацию в L3 и добиться маршрута L1 -> L2 -> L3 без расхождения семантики.
- [ ] Выполнить полную самосборку L3 + L2 с воспроизводимыми входами, budgets, generated sources и доказательствами.

## 9. `myxa_manager` как приложение

- [ ] Определить один воспроизводимый application entrypoint, а не набор разрозненных модулей и тестовых harness.
- [ ] Собрать `myxa_manager` поверх проверенного ядра после L2 self-build gate.
- [ ] Отделить production modules от test-only fixtures и временных adapters.
- [ ] Описать конфигурацию, входы, жизненный цикл Thread/Message, запуск, остановку и ожидаемый результат.
- [ ] Добавить минимальный end-to-end запуск, затем полный воспроизводимый сценарий приложения.
- [ ] Зафиксировать команды сборки/запуска и evidence так, чтобы чистый checkout воспроизводил результат.

## 10. Документация и синхронизация

- [ ] Поддерживать RU/EN грамматику и семантику синхронно.
- [ ] Обновлять L1/L2 спецификации симметрично при каждом изменении механизма ядра.
- [ ] Хранить фактические ограничения реализации в `implementation-notes`, а план и ownership — в `steps/` и этом файле.
- [ ] Сохранять технические уточнения автора в `LMX_blog` дословно и с корректной атрибуцией; не выдавать резюме Codex за авторскую цитату.
- [x] Удалить из документации прежнее ошибочное утверждение, будто пустой анонимный контейнер является одним аргументом receiver. **Свидетельство (2026-09-23):** Codex `e3bc5a1` удалил из `docs/LMX_grammar.*`, `docs/LMX_semantics.*`, `CORE.md` формулировки «контейнер прозрачен при потреблении / ноль полей — ноль аргументов receiver / передать пустую Structure одним аргументом можно лишь в непрозрачной позиции»; текущий текст — нормализация уже в P0, независимо от головы и пустоты (`LMX_grammar.ru.md` §12 :862, :1324, :1775, :2241; `LMX_semantics.en.md` :87, :281; RU симметрично); grep по `docs/`, `CORE.md`, `provenance/` на «один аргумент / one argument / single argument» — остался только нейтральный «Frame has a head and one argument Structure» (:51). Код-сторона (P0 parity) — §2 «Исправления P0», Grok -127.
