# LMX blog — 2026-09-26

Источник: реплика автора в чате fable 2026-09-26 (на вопрос REVIEW dd153e2 п.2 «отказ `sendMessage` на не-Thread — `abort` или перехватываемый бросок»), дословно:

```text
"То есть адресатом взял само принятое письмо m" -- язык типизирован. СЛать письма можно только to Thread. Вы же определяете типы по диапазонам адерсов? Так в чем проблема?
```

Итог: вопроса «abort или throw» нет — это ошибка типа. Адресат `sendMessage: Ref X` — только Thread; тип ссылки известен транслятору статически (письмо `m` из `receiveMessage: m` — анонимная Structure `(отправитель; X)`, не адрес Thread), значит отказ должен быть located на трансляции, а не в рантайме. В рантайме ядро и так различает Thread по диапазону адресов (`lmx_service_post` → `MISSING`); дойти туда с корректно типизированной программой нельзя, поэтому рантайм-отказ остаётся инвариантом (`abort`), а не броском. Заведён D-80 (транслятор проверяет у Ref только «это ссылка», `l2trans.lm1` `l2_rw_send` «a Ref that is not a reference»).

Источник: реплика автора в чате fable 2026-09-26 (подтверждение), дословно:

```text
Это ошибкатипа должна быть
```

Итог: подтверждено — отказ `sendMessage` на адресат, который не Thread, есть ошибка типа на трансляции (D-80), не инвариант рантайма и не бросок.

Источник: реплика автора в чате fable 2026-09-26 (уточнение к D-80), дословно:

```text
Хорошо, у всех структур -- один диапазон Ты прав.Но недолжен проходить implements так как у просто Message нет нужных полей
```

Итог: адресат `sendMessage: Ref X` проверяется не диапазоном адресов (у всех Structure один диапазон), а `implements` (L2 §14 / L3 admission): Structure по ссылке обязана иметь поля Thread; у просто Message (и у письма) нужных полей нет — `implements(Thread)` не проходит, отказ типа. Статически транслятор знает тип Ref и отказывает на трансляции; тот же `implements` — единственная проверка адресата и в рантайме.

Источник: реплика автора в чате fable 2026-09-26 (объём D-80), дословно:

```text
там дальше по плану порт implements-- достаточнодае первого вариант
```

Итог: полный `implements`/admission — §7 порт по плану (`next_core_tasks.md` §7, `grok_next.md` §4 п.9), D-80 его не ждёт: достаточно первого варианта — статическая проверка типа Ref на трансляции (тип письма/пользовательской Structure не Thread → located-отказ), рантайм не меняется.

## Q31, три пункта (автор, чат Grok, 2026-09-26)

Источник: реплика автора, дословно. Это ответ на Q31.

```text
1) можно было написать метод (именно метод хватит ресиверов) puts наконец и положить туда c.puts: @ arg[1][0]  2) По поводу правила "не в call структурах нет L2"-- оно устарело после того как мы утвердили execute(code, data) и я _просил_ fable отразить это в спеке. L2 нет в интерпретируемых методах потому что интерпретатор только L3. 3) Вы не обязаны компилировать все структуры --сейчас не до этих оптимизаций но все таки вам никто не мешает это делать в метод без аргументов и без возвращаемого значения и если именованная структура прямо выполняется из кода то лучше её откомпилировать.
```

Итог: три независимых пункта плана ядра (`next_core_tasks.md`, «Q31, три независимых пункта»). Спека: L2 нет в интерпретируемом теле, потому что интерпретатор только L3.

## Отсечка тикетов (автор, чат Grok, 2026-09-26)

Источник: реплики автора, дословно, по порядку. Первая — оговорка, вторая — правило.

```text
ДА Я ОГОВОРИЛСЯ
```

```text
Отменяй все что старше 24 сентября 10 AM Сан-Пауло
```

Итог: отменяются работы, записанные раньше 2026-09-24 10:00 America/Sao_Paulo. D-36 записан 2026-09-23 22:38 -0300 и остаётся отменённым. D-77 и D-81 моложе и открыты.

## `post` (глава 28, редакция v2): четыре вопроса fable — ответы автора

Вопросы fable (18:33 UTC, после чтения `docs/new_parts/threadMessageAPI.ru.md` v2): где физически живёт таблица ожиданий отправителя; какая идентичность исходного письма переживает границу Thread и его арену; откуда берётся отметка времени отправки; `uint32` в примере против `u32` в таблице конверсий.

Источник: реплика автора, дословно (2026-09-26, ~18:40 UTC).

```text
"где физически живёт таблица ожиданий отправителя (объявленное поле Thread или что заводит post)" -- мне не важно,очевидно где-то в механизмах Thread так как он принимает и сортирует вызовы всвоем потоке. "какая идентичность исходного письма переживает границу Thread и его арену, откуда берётся отметка времени отправки" -- та же самая какая делается при merge -- весь используемый контекст. Далее будем обрубать (ужечто-то Independent обрубаем) но сам принцип: механизм общий с внутренними вызовами. Как будто у нас ссылка откуда-то есть на другой граф. ". "откуда берётся отметка времени отправки" -- с момента начала отправки :))) Строка не важна. "uint32 в примере против u32" -- это вообще не важно, это пример а не спецификация примитивных типов
```

Итог: (1) таблица ожиданий — деталь механизмов Thread (он принимает и сортирует вызовы в своём потоке), место не нормируется; (2) идентичность исходного письма и его контекст переносятся так же, как при `merge`: весь используемый контекст, общий механизм с внутренними вызовами — «как будто есть ссылка на другой граф»; дальнейшее сужение (что-то из Independent уже обрубается) — отдельными решениями, принцип общего механизма не меняется; (3) отметка времени — момент начала отправки; (4) `uint32` в примере — иллюстрация, не спецификация примитивных типов.

## `post` — ресивер, не «примитив» (автор, ~18:50 UTC)

На вопрос fable в `next_core_tasks.md` §8a («`post` — примитив ядра рядом с `sendMessage`/`receiveMessage` или библиотечная Structure…») автор ответил дословно:

```text
"примитив ядра рядом с" -- _ресивер_. Что за примитив?
```

Итог: `post` — ресивер, того же рода, что `sendMessage`, `receiveMessage`, `merge`, `implements`; слова «примитив» в этом ряду нет. Открытым остаётся только, где живёт тело ресивера: нативное в ядре или на L2 поверх почтовых ресиверов.

## Где исполняются обработчики `answer`/`unanswered`/`catch` (автор, ~18:55 UTC)

На вопрос fable (б) в `next_core_tasks.md` §8a автор ответил дословно:

```text
"Обработчики answer/unanswered/catch исполняются на полосе отправителя на его endturn — так я прочитал §7 и T22. Верно?" -- а они больше нигде не могут исполняться и в принципе мне все равно где они исполняются, просто у нас один поток и кроме как в endturn ты это вряд ли куда-то вставишь
```

Итог: место исполнения обработчиков не нормируется; при одном потоке отправителя иного места, кроме его полосы (практически `endturn`), нет. В книге §28.13 п.3 это следствие устройства Thread, не отдельное требование.

## Таблица преобразований и аналитический implements (автор, чат Grok, 2026-09-26)

```text
Таблицу преобразованияи алгорит implements надо взять из предыдущего проекта C:\Nyasha_Planet\lingvamyxa_prev\ (лучше из этой, самособирающаяся проверенная версия в C:\Nyasha_Planet\lingvamyxa_old_worked_version\ )
```

```text
нет,бери из C:\Nyasha_Planet\lingvamyxa_prev\ , стабилная самосборка тебе не нужна -- с ней только сверятся можно если какие-то совсем непонятки
```

```text
аналитический implements тоже нужно взять из C:\Nyasha_Planet\lingvamyxa_prev\
```

Итог: таблица и аналитический `implements` берутся из `lingvamyxa_prev`. Самособирающаяся версия — только сверка, если в prev совсем неясно.

## Ядра на L1 не будет: L1 — промежуточный язык трансляции (автор, ~19:00 UTC)

На вопрос fable (а) в `next_core_tasks.md` §8a («тело `post` — нативное в ядре (тогда селфтест ядра и мутант) или на L2 поверх…») автор ответил дословно:

```text
"нативно в ядре (тогда селфтест ядра и мутант) или на L2 поверх" -- ПОЧИТАЙ ВНИМАТЕЛЬНО ПОСЛЕДНИЕ ПУНКТЫ next_core_tasks.md КАСАТЕЛЬНО САМОСБОРКИ. И изучи принцип абсолютной самосборки  C:\Nyasha_Planet\lingvamyxa_old_worked_version\  на всех платформах. У НАС НЕ БУДЕТ НИКАКОГО ЯДРА НА L1. У НАС БУДЕТ L3 с вставками L2 --- L1 НАДО ЗАТАЧИВАТЬ ПОДПРОМЕЖУТОЧНЫЙ ЯЗЫК ТРАНСЛЯЦИИ С L3/L2 А НЕ ПОД ПОЛНОЦЕННЫЙ ЗАМАСКИРОВАННЫЙ СИ!!!! У НАС НА L1 _НИЧЕГО_ НЕ БУДЕТ НАПИСАНО -- ОН БУДЕТ ГЕНЕРИТЬСЯ НА ЛЕТУ И ВТОРЫМ ПРОХОДОМ ТРАНСЛИРОВАТЬСЯ В СИ
```

Итог: (1) ядра, написанного на L1, не будет — итог самосборки (§8): язык есть L3 со вставками L2; (2) L1 — промежуточный язык трансляции с L3/L2, а не замаскированный Си: на L1 ничего не пишется руками, он генерируется на лету и вторым проходом транслируется в Си; L1 затачивается под роль цели трансляции; (3) деление «нативно в ядре против L2-библиотеки» ложное: тело любого ресивера, включая `post`, — L3/L2; сегодняшние `*.lm1` ядра — переходное состояние до переноса уровней; (4) эталон принципа — абсолютная самосборка прежней версии (`lingvamyxa_old_worked_version`, машина автора) на всех платформах.

## Эталон: проект `lingvamyxa` и коммит `620db861` (автор, ~19:05 UTC)

Автор указал, где смотреть принцип абсолютной самосборки и модель памяти, дословно:

```text
Проект lingvamyxa там же рядом

commit 620db8612c32569c8dd507cca135d5d076144e9f
Author: Mikhail Kravchenko <mtkravchenko@gmail.com>
Date:   Sat Sep 5 12:53:07 2026 -0300

    Structure memory model is a graph, not a tree

    Slots may share a referent; cycles are allowed. ArrayReference remains a
    rectangle, not this graph. Used-tree, Mix placement tree and P0 source tree
    are unchanged.
```

Итог: (1) эталон самосборки — публичный `github.com/michael0xf/lingvamyxa` (`L2_CORE_SELFHOST_HANDOFF_20260911.md` §29 «What full self-hosting must demonstrate», K7; `buildCore.lm0.sh`/`.bat` — переносимый исходный bootstrap из tracked generated C); `lingvamyxa_old_worked_version` — на машине автора, из облака недоступен; (2) модель памяти Structure — граф, не дерево: слоты могут делить референт, циклы допустимы; ArrayReference — прямоугольник, не этот граф; деревья used-tree, Mix и P0 не меняются. Для `post` это и есть «ссылка на другой граф» из ответа автора о контексте письма.

## Ожидание после `unanswered` — политика пользовательского кода (автор, ~19:15 UTC)

На вопрос fable (в) в `next_core_tasks.md` §8a автор ответил дословно:

```text
"(в) После unanswered ожидание живёт до снятия пользователем или завершения Thread, тайм-аут запись не освобождает, поздний ответ доставляется по маршрутам — верно? " -- да это все заточено под юзер-код, это не UI. Код юзера сам решает что делать
```

Итог: да — тайм-аут запись ожидания не освобождает и ничего не отменяет; поздний ответ идёт по маршрутам; всё, что дальше, решает пользовательский код (это не UI-политика). Совпадает с §6 главы 28.

## Роль снимка сгенерированного Си в самосборке (автор, ~19:25 UTC)

Уточнение к записи fable об алгоритме старой самосборки (`next_core_tasks.md` §8), дословно:

```text
в том проекте что самособирался в gitignor лежат несколько Си-файлов ядра "# Lingvamyxa tracked generated source snapshot." . Они были нужны только для миграции на совершенно новую платформу, для такой миграции существовал  buildCore.lm0.sh buildCore.lm0.bat . Он собирает бинарник отдин раз в истории миграции и больше не учествует в самосборках и модификациях ядра (все это разбивается на этапы просто и собирается бинарником собранным из самого языка а не из Си)
```

Итог: tracked снимок сгенерированного Си (`lingvamyxa` `.gitignore` :69 «Lingvamyxa tracked generated source snapshot», восемь файлов `lm1/build/*.lm1.c`: buildCore, finalize, make, own, parser, rest_lmx_http_client, rest_lmx_http_server, trans) и `buildCore.lm0.sh`/`.bat` — только для переезда на совершенно новую платформу: собирают бинарник один раз в истории миграции и больше в самосборках и правках ядра не участвуют; всё остальное разбито на этапы и собирается бинарником, собранным из самого языка, а не из Си.
