# A letter's graph has no static Structure type: nextMessage into a
# Structure-typed binding is a graph rebinding that needs admission, and that
# stays fail-closed exactly as `a: b` does.
Model:
    size_t: value 1U
end: Model
Model: fresh
nextMessage: fresh
if: fresh\value != 1U
    return: 1
return: 0
