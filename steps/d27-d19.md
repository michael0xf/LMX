# D-27 / D-19 (тикет 20260926-01)

STARTED grok/d27-d19 база main 9e368cf. Ядро не меняется.

## D-27

Измерено на l2trans main до правки: `\[0]x` внутри выражения уже читает единственную ячейку (`x: 10` / `x: 20` → 20). `\[1]x` — located «own occurrence index out of range» (фраза c3c «no such occurrence» в коде не жила; одна фраза теперь «no such occurrence», те же два старых свидетеля).

Падение голой строки `\[0]x` после `x: 20`: P0 даёт пять полей-соседей `\ [ 0 ] x`, а `l2_expr_span` кадра `x: 20` принимал следующий `\` за продолжение пути и склеивал кадр с этой строкой. Проверка тогда звала `l2_check_primary` на кадр и отказывала «unknown method» (раньше тот же склей давал «no located diagnostic»).

Правка: `l2_occ_run` / `l2_span_occ_expr` — пять полей одно вхождение, вместе с хвостом `!=`; кадр, за которым `\` — не путь (`for\j` остаётся путь). Промах вхождения — «no such occurrence», в том числе тихий `return: 1` в `l2_prep`.

Свидетели: `unit_occ_local_read` (\[0]x = 20, Entry 7), `unit_occ_local_second_refused` (needle «no such occurrence»). Мутант: вернуть склейку span → «unknown method» на `x: 20`, строка не читает 20.

## D-19

`int: x -1` у именованной Structure отказывался «a named Structure field needs a literal» (`l2_num` / `l2_literal_value` без знака). Own-поле метода минус уже принимало. Один разбор `l2_int_literal_bits`: знак — часть литерала, биты int в `l2_nsf_val`, эмиссия прежняя `%d`. INT_MIN входит, на единицу дальше — «literal not representable as int».

Свидетели: `unit_ns_int_neg` (Model\x + 8 = 7), `unit_lit_range_ns_int_neg_min` (INT_MIN), `unit_lit_range_decl_int_neg_overflow`. Мутант: снять приём знака → поле не -1, Entry не 7.

Спеки L2 §2 / §12 сегодняшнему тексту не противоречат — не переписывались.

## RESULT

grok/d27-d19, база main 9e368cf. Ядро не менялось. Twin `l2src/l2trans.lm1` и новые `tests/` — копия песочницы.

Гейт: build 282/282 (`build/l2src/20260925_224940`), harness 410/410 (`build/l2_harness/20260925_224657`), L3 11/11, имена 69/128, check_docs OK.

Мутант D-27 измерен на дереве до правки span: склейка кадра со следующим `\` даёт «unknown method» на `x: 20`, значение 20 не читается. Мутант D-19 измерен до `l2_int_literal_bits`: `int: x -1` — «a named Structure field needs a literal», Entry не 7. Финальный прогон этих правок не содержит.
