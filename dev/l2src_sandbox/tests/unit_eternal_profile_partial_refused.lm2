# A partial/reordered qualifier chain is not an immutable physical profile and
# must be refused before any generated profiled allocation can be published.
independent:
    immutable:
        (): NotAProfile
            size_t: value 7U
        end: NotAProfile
    end: immutable
end: independent

fn: main () int
    return: 0
end: main
