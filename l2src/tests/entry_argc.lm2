# The process arguments arrive inside a letter (author, Q10): R0 holds it
# when the entry runs, and the entry takes it.
receiveMessage: m
if: m = 0
    sendMessage: exit(exit_code: 1; stdout: ""; stderr: "")
    return
sendMessage: exit(exit_code: 0; stdout: ""; stderr: "")
return
