predef: "mixa_manager/mtk_named.h.lm1"

# Named.java is a pure interface: one method, no default implementation.
# The fnptr + vtable struct in mtk_named.h.lm1 is the L1 contract;
# concrete implementors (Key, Tag, etc.) supply the fnptr bodies in
# their own .lm1 units.
