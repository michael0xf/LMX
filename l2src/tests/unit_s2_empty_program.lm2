# S2: a unit with no statement is an empty program -- its entry executes nothing and returns 0.
# A unit of methods alone is a program too; the library profile is selected, never inferred.
fn: unused () int
return: 1
