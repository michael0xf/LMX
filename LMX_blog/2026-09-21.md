# Технические уточнения автора — 2026-09-21

## Запрос режима после неуспешного такта

Источник: прямая реплика автора в задаче Codex 2026-09-21. Вопросом были три варианта поведения явно запрошенной смены режима после ошибки такта: зафиксировать её, отменить либо оставить ожидающей. Дословно:

```text
делайте "Отменить запрос — мой рекомендуемый вариант.  "
```

## Синхронизация почтовой коллекции

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
фиксите скорей обычным упрощенным (без wait notify потому что некого оповещать при одном потоке и тп ) java-style synchronized который и будет потом при портировании L1->L2->L3. И надо чтобы этот лок остался только в одном месте (не в смысле в одной строчкке а в одном модуле) коллекции почты. Это нормальное решение?
```

## Динамическая ёмкость почтового кольца

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
Давай оставим простой synchronized но сделаем ёмкость кольца не параметром а нормальным динамическим массивом (k =1.5 к примеру) и стартовать с минимальных значений буфера около 2. Потому что для такого общего случая это и делается а не для того чтобы ставить монитор на 256 фиксированных эдементов которые никогда не будут использоваться.
"Lock-free MPSC ring " -сделаем потом  в другой реализации почты
```

## Адрес локального аргумента и одноимённое own-поле

Источник: прямые реплики автора в задаче Codex 2026-09-21. Дословно:

```text
кстати по поводу взятия адреса локальной переменной (много выше обсуждали) -- не нахожу вариантов где бы поведение L2 не соответстовало бы стандартному ожидаемому. Так как у нас любое присвоение ":" становится полем даже если присваивается аргумент (кстати проверь не сломали ли это). А так как аргументы у нас (и явные и скрытые которые берутся из видимости вызывающего и/или лексически) строго локальные то взятие их адреса получается идентичным. Где тут различие в поведении? Не сбудет такого. Если же ты адрес возьмешь перед присвоением то на L1 такое просто нельзя записать так как в Си нельзя объявить второй раз (если у же есть аргумент), а так как L1 это у нас всего лишь промежуточный язык трансляции -- мы можем легко проследить чтобы не было таких коллизий.    Подумай что яупустил
```

Последующее уточнение автора, дословно:

```text
кажется я вспомнил -- взятие адреса перед присвоением ты приводил как пример. Но! у нас же есть dirty-- то есть значение будет таки записано. Тут только если мы решим "вернуть адрес аругмента" что вообще не стандартное поведение и бессмысленно
```

Решение о консервативной постановке `dirty` и следующем этапе реализации, дословно:

```text
"`@argument` операции, способной записать через адрес  " -- значит просто взятие адреса локальной переменной делает ее dirty -- это же легко?

"

постановка `dirty` после потенциальной записи через адрес;

1. запрет escape адреса activation-local;
2. полная проверка охвата типов argument-as-own." -- тогда после завершения текущих этапов переключай кого-то на это. Ядро нужно чтобы функционировало
```

Уточнение о времени жизни `dirty`, дословно:

```text
"поднять `dirty` после возврата.  " -- просто не убирать как для других и все
```

## Независимость argument-as-own от типа

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
вообще я не понимаю как механизмы могут быть реализованы только для части каких-то типов, нужен общий механизм. Какая разница какой там тип?
```

## Область липкого dirty при взятии адреса

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
"если вложенный вызов явно изменит `node\x`  " -- так address-taken _локальное_ `x`  !!! Каким образом оно стало локальным если у нас есть `node\x` ? локальное это то что пришло с аргументами явными или скрытыми и у нас уже правило что это именно так -- оно локально и не меняет источник. Ты наверное слишком расширил правило. Его не надо расширять вообще на все взятия адреса
```

Уточнение временной границы правила, дословно:

```text
то есть взятие адреса только *до присовения-объявления* делает переменную dirty до самого конца, все остальные случаи *включая взятие адреса у аргумента который вообще не присваивался* уже не отслеживаются этим праивом
```

## Единый каскад закрытия Thread

Источник: автор, 2026-09-21.

```text
"закрывает почту R0 до остановки/settle дочерних lanes  " -- что значит закрывает почту? У на обсуждался стандартынй каскадный механизм закрытия Thread -- зачем для  кого-то исключение?
```

Последующее уточнение порядка и границы синхронизации, дословно:

```text
пофиксите пожалуйста вот это:"

1. в текущем R0 гонка возможна лишь потому, что код нарушает порядок: уничтожает почту до `settle/stop`;
2. у обычных Thread сейчас другая проблема: почта вообще не уничтожается, поэтому имеется утечка, но этой use-after-free гонки пока нет."

учитывая это:

" 1. если стандартный каскад сначала останавливает и доводит связанные Thread до безопасной границы, гонки нет;

2. никакие `state/inflight`, refcount или lock в сервисе адресов не нужны и противоречат правилу «единственный lock — внутри почтовой коллекции»;"
```

Уточнение о распределении каскада между уровнями, дословно:

```text
"а повторный вызов затем запрещён, поэтому текущий API физически не может довести отложенный каскад до конца.  " -- какой повторный вызов? Родитель не закрывает внуков --- он ждет закрытия ребенка и так по цепочке
```

## Верхний каркас R0 и аварийная граница закрытия

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
1) сверху R0 стоит тн "заглушка" но она должна работать по тому же принципу и там единственное что отличается -- она не проверяет родителя так как родителя нет. Это даже не заглушка а просто каркас для одного типа элементов в WorlWideMix (и он уже есть в модели на Java но мы его не переносим потому что нам нужно будет переписать эту модель на Message-механизм). И здесь понятно что  такая заглушка должна иметь отдельный timeout на ожидание закрытия всех потомков -- если они не справились то надо закрывать OS процесс чтобы OS разобрался сам.
```

## Граница синхронизации почты

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
2) проверь что синхронизация "внутри почты" затрагивает теперь только коллекции L3 Thread, а унаследованные от Message атомарные флаги и остальное добро-- не затрагивает
```

## Размещение сортировочных сервисов

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
Думаю сортировщиков на узлах сделаем не на втором круге внутри приложения, а снаружи в WorldWideMix -- там как раз задержки большие и примерно понятно где сортировщики в дереве
```

## Отсутствие числовой раскладки и фиксированного числа детей R0

Источник: прямые реплики автора в задаче Codex 2026-09-21. Дословно:

```text
нет никаких чисел в модели никогда не было
```

Техническое уточнение единственного источника состава детей и обхода планировщика, дословно:

```text
""ссылки на дочерние Message в графе;  "  -- только это надо оставить.  Планировщик должен пользоваться этим динамическим массивом ссылок. Никаких вообще "8,6, 4,3" и тому подобной ахинеи!!!!
"физическая ссылка на массив заранее известных описателей методов. Через него программа находит постоянные дескрипторы методов по физическим ссылкам.  " -- в чем проблема положить этот и другой подобные массивы нормально в корень чтобы передавать ССЫЛКУ а не чертов индекс( в дополнение к ссылке)? У нас типизация по диапазонам адресов -- все подобные массивы должны лежать там даже если по каким-то причинам что-то в этом массиве используется прямо
Это ясно или ест какие-то вопросы?
```

## Родитель R0 и единообразное размещение постоянных массивов

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
"`R0` — обычный `LmxThread`, только без родителя.  " -- ОН У НАС С РОДИТЕЛЕМ-ЗАГЛУШКОЙ!!!!!
"корень хранит физическую ссылку на соответствующий массив;  " -- посмотрите можно ли передать все эти массивы в арену R0 где лежат все эти типизированные массивы. На всякий случай чтобы было однообразно. ТЫ понял или есть вопросы?
```

## Обычные возможности R0 и явно передаваемое содержимое арены

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
лишние функции в родителя R0-заглушку не помещайте, L3 Thread по дефолту  должен уметь все то что умеет R0, просто на данном этапе он у себя в арене хранит *то что явно передается* порождаемым lmx_message и/или lmx_thread
```

## Граница сервиса доставки

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
то есть не тот почтовый сервис который смотрит-читает свои ящики, а именно сервис доставки
```

## Владение начальными и поздно подключёнными неизменяемыми ветвями

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
то есть тот факт что мы подложили в R0 все неизменяемые независимые ветки происходит только из того факта что он сам порождает все дочерние объекты и поэтому ему удобно передавать то что изначально лексически зафиксировано. Но в дальнейшем может быть ситуация когда к нам придет какой-то dll-подобный модуль -- это понятно или есть уточннения?
```

## Контекстная роль ресивера и развилка объявления/обновления

Источник: прямые реплики автора в задаче Codex 2026-09-21. Дословно:

```text
Я понял в чем запутался, у нас конструкция:

env: field # 1) если нет field то объявление поля field типа env с (динамического либо лексического) с полным ее озданием
env: field # 2) если field есть то попытка присваивания переменной env значения, причем "env" должна уже быть объявлена с заранее известнм типом так как у нас типизированный язык
Запутался я потому что у нас выше были чуть сбивающие с толку примеры
fn: test(int: arg)
. arg: 7 # так можно только потому что тип arg известен заранее

fn: test()
. arg: 7 # так нельзя !!!

\=====
Проверь так ли этои если не так то давай фиксить
```

Уточнение общего принципа, дословно:

```text
Да, у нас это записано было прямо в начале спеки -- роль ресивера определяется контекстом
```

Уточнение единственного механизма создания ветви и ошибок обновления, дословно:

```text
да и
env: field
если field-- новая переменная по правилам должен делать полный merge с обходом того что используется (мы писали уже этот обход, возможноон сохранился в пердыдущих проектах L1 Lingvamyxa или даже в этом), никакого другого механизма создания ветки кроме как merge у нас нет.
env: field
если же field уже существует то мы должны проверить тип env и если env вообще не существует (нет типа) или const -- ошибка
```

Окончательная симметричная формулировка и отличие присваивания от `merge`, дословно:

```text
env: field #объявляется переменная field с типом env, туда делается полный merge env (в данном случае с просто копирование так как второй аргумент merge пуст)\
field: env2 #если env2 существует то начинаем полный implements и тольо потом присваиваем , причеем _это присваивание ссылки, не merge_ \
field: env2 #если env2 НЕ существует то ситуация как в env: field -- все банально
```

Уточнение динамического выбора операнда и явного лексического пути, дословно:

```text
это также означает что мы на этапе трансляции конечно же не знаем что точно лежит в env (у нас динамическое связывание доминирует над лексическим) *поэтому для передачи конкретного объекта у нас заведено node\env*
вот и вспомнили способ передать independent: const: immutable: env
```

Уточнение зарезервированного `node`, дословно:

```text
node у нас -- зарезервированное слово в языке (это точно вносили в прошлую спеку и просмотри есть ли в новой). node это дословно тот самый Lmx * node
```

Исправление значения `node`, дословно:

```text
"обозначает саму вызываемую Structure, не её родителя, и его нельзя объявить, затенить, перепривязать или подменить динамическим значением. `node\field` начинает обход прямо от этой Structure.  " -- кажется мы нарвались на баг. node у нас ссылается на родителя!!! Видимо я один раз дал неверный пример
```

Уточнение о синтаксисе собственных полей, дословно:

```text
Это чисто система директорий "." "..". Суть не только в спецификации а в реализации. Надо срочно придумывать синтаксис для обращения к собственным полям. (а зачем, что за абсурд?) Кажется не надо. Просто спеку поправить и посомотреть что реаизация корректная
```

Уточнение смысла примера с родительским полем, дословно:

```text
Все же я приводил пример с изменнием родительского поля как контрпример того что прямо упоминанием оно не меняется (в реализации оно -- скрытый аргумент). Если я употребил "к  своим полям" это просто русский язык у меня такой кривой
```

## Ненормативность L1 для исходной семантики

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
L1 документ  не составлял -- это творчество агентов которым я сказал что L1 это промежуточный язык трансляции. А так как он промежуточный и вообще не должен использоваться -- там допустимы вариации
```

Последующее уточнение границы уровней, дословно:

```text
Да это было изначальное правило -- L1 это любая удобная промежуточная семантика трансляции L2 в Си. Там же нет графа , dirty load и так далее
```

## Скрытый аргумент и own-поле при голом присваивании

Источник: прямые реплики автора в задаче Codex 2026-09-21. Дословно:

```text
"`field: value` тогда меняет только значение текущей активации и не записывает в родительский граф;  " --тут еще смешалось то что у нас field оказался собственным полем. Так как для явных аргументов мы проговаривали это правило  (присваивание значения аругменту в теле создает поле уже на этапе трансляции самим своим фактом)  а для скрытых аргументов не проговорили. И получилась вот такая путаница. Но скрытые аргументы обрабатываем по общему принципу.Изменил-- получи поле еще на этапе трансляции.
```

Уточнение контекстной роли двоеточия и условий создания поля, дословно:

```text
"само наличие присваивания `field: value` обнаруживается транслятором и создаёт own-поле `field` для этого тела;  " -- и важно чтобы агент не забыл что ":" контекстно меняет свое значение но это можно отследить во время трансляции.
 `field: value` field стал полем *только потому что в явных или скрытых аргументах был указан его тип* и также только потому что value был в namespace на момент трансляции
если же одно из этих условий не выполнено -- никакого поля не появляется, а появляется ошибка трансляции. У нас нет нетипизированных полей и мы не присваиваем неизвестные значения.
\===
Я ничего не упуcтил ?
```

Исправление смысла слова «локальный», дословно:

```text
"«присваивание динамическому входу остаётся локальным». Это уже неверно при наличии в исходнике голого `x: …`: сам факт такой строки резервирует own-поле тела, а исполнение строки активирует привязку и `dirty`  " -- почему неверно? Оно и осталось локальным. Мы же не поменяли поле вызывающего!!!! И родительское тоже не поменяли!!!
```

## Зарезервированный `node` и родитель структуры

Источник: прямые реплики автора в задаче Codex 2026-09-21. Дословно:

```text
тут ещё нюанс надо обдумать с этим node (после ткущих аварийных правок ядра):
Зарезервированный "node"ссылается на лексическое пространство над-методом,а не на структуру на которую указывает текущий "node" Lmx * node (проверь по коду)
-- так как у нас любой if и любой вложенная лексическая структура тоже имеет Lmx * node (проверь по коду)
Поэтому надо подумать не стоит ли что-то переименоваать, а то агенты запутаются. К примеру в Lmx структуре сделать Lmx * parent;

Все усложенения, которые мне приходили в голову, в связи с этим parent, я выкинул из головы как лишние -- за переход по parent отвечает лексическая и динамическая видимость
```

Уточнение термина, дословно:

```text
не лексическим родителем!!!! А лексическим над-методом!
```

Уточнение неизменности этого значения во вложенных структурах, дословно:

```text
лексически там может быть любая под-структура. Но node это *над *метод
```

Уточнение статуса имени `parent`, дословно:

```text
"parent" -- не зарезервированное слово! Это просто дежурная перменная в рабочем графе
```

## Композиция L3 Thread и Message

Источник: прямая реплика автора в задаче Codex 2026-09-21. Дословно:

```text
Но вот это нормальна конструкция была
"struct: LmxThread
   @: LmxMsg message
"
а как ты будешь подставлять LmxThread вместо LmxMsg и тп если там будет больше композиций чем один LmxMsg?
```

Окончательное уточнение представления, дословно:

```text
В общем извини что я тебя сбил -- делай именно так как сейчас сказал -- "`@: LmxThread` и `@: t\message` имеют одинаковый физический адрес  ". Я на самом деле просто испугался что сейчас вы начнете туда все сваливать как когда-то в Message, но в данном случае именно это изменение видимо более лаконичное
```

Точная форма записи, дословно:

```yaml
struct: LmxThread
    LmxMsg: message
    @: LmxPost mail
    @: LmxSchedule slot
    ...
end: LmxThread
```
