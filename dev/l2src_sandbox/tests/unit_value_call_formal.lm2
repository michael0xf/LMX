# A callable formal named where a value is consumed gives its callable's result too: f holds
# test's occurrence (a reference -- passing it runs nothing) and f + f runs it twice: 2.
fn: test () int
    return: 1
end: test

fn: twice (test: f) int
    return: f + f
end: twice

return: twice(test)
