# A fixed Array field declares its count, so a literal index is proven against
# it at translation, as an own array's is: 3 is past `[]: int buf 3`.
Holder:
    []: int buf 3
end: Holder

Holder: a
if: a\buf[3] != 0
    return: 1
return: 0
