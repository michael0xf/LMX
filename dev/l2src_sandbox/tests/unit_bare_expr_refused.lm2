# P0 gives a statement line of several atoms as that many fields: `y + 1` is one expression
# statement, not a bare atom, and is not supported yet -- refused by name (it crashed the
# translator).
int: y 0
y + 1
return: y
