# LMX blog — 2026-09-25

Источник: реплика автора в чате fable 2026-09-25 (ответ на `q23.md`), дословно:

```text
K1 -- да, сделайте нормальный итератор (в спеке есть раздел по yield--может поможет?)  K2- да; K3-a (как Erlang)
```

Итог: K1 — выборочное взятие из ящика делать как нормальный ИТЕРАТОР (старая спека: `fn: words () iterator(Text)` + `yield:`, потребление `each: word in words`); K2 — да (host-корень теми же механизмами); K3 = а — как в Erlang: отправитель кладётся первым полем письма (`{self(), Msg}`), письмо = (ссылка на отправителя; полезная Structure).

Источник: реплика автора в чате fable 2026-09-25 (ответ на K4 и Q1 из `q23.md`), дословно:

```text
K4 - a; Q1 -- положите пока одну настройку к примеру ту которую невозможно найти в Си (к примеру рабочую директорию)
```

Итог: K4 = (а) — транспорт опубликованного outbox в inbox адресатов на границе хода (модель 28); Q1 — в платформенные настройки host пока кладётся одна: то, чего нет в C, например рабочая директория процесса (host получает её от ОС и передаёт R0 общей ссылкой вечным профилем).

Источник: реплика автора в чате fable 2026-09-25 (ответ на `q24.md`), дословно:

```text
Q24 - A -- на этот механизмуже вся next_core_tasks.md переписана,мы только с тобой недавно поменяли на lastIndex по умолчанию (было 0)
```

Итог: Q24 = A — повторное типизированное объявление одного имени в одном теле — второе вхождение (`[0]`, `[1]`, неуточнённое — последнее); отказ «duplicate declaration» (самодельное правило 4 из S2 -112) отменяется; план уже написан на механизме вхождений, изменился только выбор по умолчанию (`[0]` → последнее).

Источник: две реплики автора в чате fable 2026-09-25 (уточнение Q24 = A), дословно:

```text
видимо я не до конца понял вопрос Q24. Если мы объявим (int: x 1; char: x 2) #какое поведение намечается по Q24 A?
```

```text
главное чтобытак можно было сделать и  (int: x 1; char: x (x + 2)) читался правильно
```

Итог: повторное объявление одного имени в одном теле допустимо и с другим типом — это новое вхождение (`[0]x` — int-ячейка, `[1]x` — char-ячейка); внутри тела голое имя в выражении означает последнее вхождение, объявленное ДО этого оператора: инициализатор `char: x (x + 2)` читает предыдущее `int: x 1` → 3, после чего появляется новое вхождение; снаружи `test\x` = последнее вхождение (char, 3).

Источник: следующая реплика автора в чате fable 2026-09-25 (к Q24), дословно:

```text
главное разрулить это в трансляции так как в Си такое невозможно прямо а у нас в next_core_tasks.md намечается портирование и расширение абсолютной конвертации с валидацией и оно  будет подставляться только так в интерпретаторе
```

Итог: повторное объявление одного имени с другим типом разруливает ТРАНСЛЯТОР (в C так нельзя): каждое вхождение — своя C-ячейка/локал; значение инициализатора другого типа (`char: x (x + 2)` при int-`x`) проходит через абсолютную конвертацию с валидацией — порт и расширение конвертеров из `lingvamyxa_prev` (план §7, вместе с `implements`); в интерпретаторе подстановка идёт только так же (конвертер — явный приёмник, выбираемый по merge-ключу). Не C-каст.

Источник: реплика автора в чате fable 2026-09-25 (merge методов), дословно:

```text
А мерж методов мы же можем разрешить только породить в итоге интерпретируемое нечто? (в поле 0 просто не будет нативной ссылки, останется один дескриптор и тот -- смерженный)
```

Итог (чтение координатора, подтверждение ниже): merge методов допустим при условии, что результат — интерпретируемая callable-Structure: в поле 0 результата остаётся один дескриптор без нативного адреса (`addr = 0` → walker), заголовок/сигнатура — по правилу Q21 = A (формалы операнда переопределяют одноимённые формалы модели, новые дописываются), тела — op-деревья: ops модели адресуют слоты как были, ops операнда перенумеруются по той же карте позиций (переопределённые поля → слоты модели, новые → дописанные), исполняются по порядку. Нативного кода у результата нет; прежний СТОП «методы не мержатся» превращается в «мерж методов = интерпретируемый результат».
