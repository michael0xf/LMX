# FABLE-SONNET-ADDRESS-TICKETS-20260922-97 Ticket A negative fixture.
#
# A bare (non c.-prefixed) type name in address-slot position that is
# neither a hardcoded primitive nor an l2_ns_find-registered Structure must
# be refused with one precise diagnostic before gcc, not silently accepted
# into the (removed) generic foreign-pointer fallthrough.
Model:
    size_t: tag 0U
end: Model

@: Model slot
@: Banana other_slot
return: 0
