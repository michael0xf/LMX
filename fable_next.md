# fable: продолжение после перезапуска

Записано 2026-09-20 по просьбе автора в момент остановки. Сессия `fable` (лаунчер `C:\claude\fable.bat`, cwd при старте — `C:\Nyasha_Planet`, не проект).

## 0. Вход

1. Прочитать `C:\Nyasha_Planet\LMX\READ.ME`, затем `steps/current.md` (они меняются другими агентами — читать заново).
2. Команды проекта запускать из `C:\Nyasha_Planet\LMX`.
3. Связь: `SendMessage(to="lmx_uds")`; `deepseek` и `openrouter` по запросам Grok не трогать.

## 1. Что сделано в этой сессии

- Подтверждение чтения READ.ME/steps и транспортный `ACK FABLE-20260920-035657` — отправлены `lmx_uds`.
- Ответ на `GROK-INTERP-20260920` отправлен `lmx_uds` (msg_id 118dc977-49f9-4d05-9d3a-7904bed37c5c). В нём я предлагал префикс слотов `[1]=HEAD`, `[2]=RET` у вызываемой Structure. **Это предложение перекрыто** (см. §2) и должно быть отозвано в следующем ответе.
- Файлы проекта не правились, коммитов не было. Единственный созданный файл — этот.

## 2. Решения автора, полученные в этой сессии (обязательны)

Я задал автору три вопроса по ответу GROK-INTERP; ниже его ответы дословно. Первые два уже в `LMX_blog/2026-09-20.md` (см. §4), третий — см. пометку под ним.

1. Ответ на мой открытый вопрос о привязке имён в узлах кода:

```text
имена в коде нкак не привязывать -- мы это устали убирать. Интерпретируется бинарный граф и там нет никаикх имен. У нас есть таблица адрес->имя но ее _не использовать_!
```

2. Ответ на мой вопрос «Устраивает ли, что код копируется вместе с каждой копией вызываемой Structure.» (автор процитировал этот вопрос вставкой и ответил):

```text
-- зачем копировать тело каждый раз? Достаточно стека обычного
```

3. Ответ на мой вопрос «Примете ли вы сам префикс `[1]`, `[2]`» (слоты HEAD и RET сразу после METHOD). Технический фрагмент реплики автора, выдержка; поручение записать это в этот файл опущено:

```text
на третий я grok-у дал инструкици (он уже сделал структуру которая несет в себе все эти дескрпиторы и она будет лежать в 0 вместо Method)
```

То есть вопрос закрыт самим автором, а не только пересказом `lmx_uds`: префикс `[1],[2]` не принимается; в слоте 0 вместо METHOD лежит одна структура, несущая все эти дескрипторы (в песочнице это `LmxCallable {addr, sig, header, body, return_arg}`, коммиты `179ae05`, `e7dd42b`). Инструкции по ней автор дал Grok напрямую — их текста у меня нет, у Grok не переспрашивать через пересказ, а читать его коммиты и `dev/graph_interp/README.md`. В `LMX_blog/2026-09-20.md` эта реплика занесена мной записью «Дескрипторы вызываемой Structure в слоте 0», коммит `bfa8c0c`, в `origin/main`; см. §4.

Следствия для моей работы (это мои выводы, не слова автора):
- В узлах кода нет написаний/имён: никаких `Array<char>` с головой Frame или атомом-именем. Мой пункт P4 (транслитерация LmP0 со спеллингами) и открытый вопрос O1 (привязка имя→слот) из ответа GROK-INTERP — отозвать. Таблицу адрес→имя интерпретатор не использует.
- Тело (код) общее и не копируется с копиями M; состояние активации — на обычном стеке (ср. `Lingvamyxa_spec.txt` 21.7, строки ~15959-15963: «The C call stack is the sole implicit history of method activations»). Мой O2 снят, а P7(a) (слот тела у Frame указывает на конкретный хост H) при общем коде неверен: общий код не может держать указатели в хранилище конкретной копии; адресация собственного состояния — относительно зарезервированного `node`, по позиции, как у нативного кода (`l2trans.lm1:7739-7746`, `14074-14083`).
- Слот 0 → структура с дескрипторами вместо METHOD (ответ 3): мои P1/P2 (префикс `[1],[2]`) отозвать. Нативные индексы собственных полей и хостов (`l2trans.lm1:7728, 7788, 7830`) и `len` у M при этом не меняются. `lmx_uds` передавал то же раньше: «богатый `LmxCallable` в слоте 0 вместо растягивания METHOD или четвёртого поля Lmx».
- Все три моих вопроса закрыты; новых вопросов к автору по этой теме на момент остановки нет.

## 3. ЗАКРЫТО: ответ на GROK-CALLABLE-20260920

**Статус (2026-09-20, после перезапуска):** ответ отправлен `lmx_uds`, msg_id `e0b3f699-e77d-4e0a-9733-700735f98962`, с `ACK GROK-CALLABLE-20260920 fable`. Перед отправкой прочитан `e7dd42b`: терминал копирования (`lmx_graph_copy_owned.lm1:188-197`, корень `:454-455`), `!= c.LMX_CALL_OK` (`lmx_interp.lm1:16`) и `return_arg: @: Lmx` (`lmx.h.lm1:186`) уже исправлены и из находок убраны. В ответе отозваны P1/P2, P4, P7(a), O1, O2. Оставлены открытыми: приведение без проверки вида в `lmx_call_method` (`lmx_call.lm1:35-38`); запись CALLABLE никто не создаёт и правки `e7dd42b` тестом не покрыты; `l2trans.lm1:14427-14439` всё ещё кладёт METHOD и пишет в `ARRAY_OF_METHOD` (`@@: LmxMethod`); «проверить» — классификация терминала таблицей исходной арены при копии уже из дочерней арены (`lmx_graph_copy_owned.h.lm1:24-29`, `.lm1:245-249`); `info\body = 0` → NO_GRAPH мешает форме без тела с трейлером. Файлы проекта не правились. Ниже — исходные заметки, оставлены как были.

Запрос пришёл от `lmx_uds` (бриф Grok, 5 вопросов) — оценить уже реализованный `LmxCallable` в слоте 0. Код: `C:\Nyasha_Planet\LMX\dev\l2src_sandbox`, коммит `179ae05` (смотреть его, не корневой `l2src`). Границы реле: файлы не править, не коммитить; ФАКТ отделять от ПРЕДЛОЖЕНИЯ, ссылки файл:строка; ответ — `SendMessage(to="lmx_uds")` с кодом `GROK-CALLABLE-20260920`, в конце строка `ACK GROK-CALLABLE-20260920 fable`. (На момент записи ответ не был отправлен; теперь отправлен — см. статус выше.)

Собранные ФАКТЫ (пути относительно `dev/l2src_sandbox/`, проверить номера строк перед отправкой):
- `struct: LmxCallable {addr, sig, header, body, return_arg}` — `lmx.h.lm1:181-187`; домен `LMX_DOMAIN_CALLABLE 30`, вид `LMX_DOMAIN_KIND_CALLABLE 16` — `lmx_implements.h.lm1:72, 85`. `lmx_domain_kind` = `lmx_range_classify` (`lmx_implements.lm1:28-29`).
- `lmx_call_ready` принимает CALLABLE или старый METHOD (`lmx_call.lm1:25-31`); `lmx_call_callable` (`45-52`); `lmx_call0` берёт `info\addr`, иначе `method\addr` (`54-80`).
- `lmx_call_method` (`lmx_call.lm1:34-37`) приводит слот 0 к `@: LmxMethod` без проверки вида: для LmxCallable работает только из-за совпадения начала записи `{addr, sig}`; возвращённый адрес не классифицируется как METHOD. `lmx_interp_method` это наследует.
- **Q5, главный конфликт.** Копировщик: терминал — только `LMX_KIND_METHOD` (`lmx_graph_copy_owned.lm1:188-193`, `247-249`); значение вида 16 не NONE/METHOD/STRUCT/PRIMITIVE/ARRAY и падает в `\status: LMX_GRAPH_COPY_INVALID` (`385-387`). В копировщике и merge нет ни одного упоминания `LMX_DOMAIN_KIND_*` (grep: 0). Значит, любой merge и любое создание дочернего Message, дошедшие до M с LmxCallable в слоте 0, сейчас завершаются COPY_INVALID. Корневая проверка (`~447`) отказывает только METHOD: адрес вида CALLABLE как корень копии был бы прочитан как заголовок Lmx. Операнды merge проверяются на `LMX_KIND_STRUCT` (`lmx_merge_owned.lm1:63, 71, 80`) — тут конфликта нет.
- Конструктора LmxCallable нет (LmxCallable встречается только в `lmx.h`, `lmx_call.*`, `lmx_interp.*`); `l2trans.lm1:14427-14442` по-прежнему кладёт в слот 0 `lmx_method_new_owned` и дублирует запись в корневой массив `ARRAY_OF_METHOD`. Сейчас запись домена CALLABLE может создать только тест, регистрирующий домен вручную (`lmx_domain_register`, `lmx_implements.h.lm1:89`).
- GC конфликта не даёт: «собственные записи ядра» сканируются пословно как кандидаты в адреса (`lmx_gc.lm1:80-89`), то есть header/body/return_arg удерживаются.
- Позиционный `lmx_implements` (`lmx_implements.lm1:141-151`): для вида METHOD сравнивает сигнатуры, для CALLABLE уйдёт в ветку сравнения только типов домена. Модуль автор признал невалидным и подлежащим переписыванию (`LMX_blog/2026-09-20.md`), поэтому лишь отметить.
- Перевёрнутая проверка не исправлена и в 179ae05: `lmx_interp.lm1:16-17` — `lmx_interp_callable(...) = 0` → `LMX_INTERP_INVALID`, хотя 0 = `LMX_CALL_OK` (`lmx_call.h.lm1:41`). Любая валидная вызываемая Structure отвергается.
- `sig` ничем не декодируется («DISPATCH contract, not a call contract», `lmx_call.h.lm1:22-38`), так что арность/типы формалов интерпретатор может взять только из `header`.
- Нативный поиск return: `frame\trailer\body` при спеллинге `return` (`l2trans.lm1:1928-1933`); порядок: вычислить поля → чекпоинт → return; void требует 0 полей (`1935-1971`). Тело трейлера в P0 — всегда `LmP0Structure` (`l1src/p0.h.lm1:103-107`).
- Спека: слот 0 описан как METHOD `{addr,sig}`, общий, сохраняется при копировании (`Lingvamyxa_spec.txt:15716-15735`); 21.5.1 — `15821-15888`; 21.6 порядок чекпоинтов — `15907-15926`.

Намеченные ответы (ПРЕДЛОЖЕНИЯ, сформулировать и пометить):
1. Да, слот 0 → LmxCallable лучше моего префикса: нативные индексы (`l2trans.lm1:7728, 7788, 7830`) и `len` у M не меняются, Lmx закрыт, запись одна на метод и общая для копий — это и есть «тело не копируется». Префикс `[1],[2]` отозвать.
2. Поля оставить. Предложить: (а) начало записи сделать структурно совместимым с LmxMethod (вложить `LmxMethod` первым членом либо явно зафиксировать общий префикс), чтобы приведение в `lmx_call_method` не было совпадением; (б) `return_arg` типизировать `@: Lmx` (0 = нет трейлера, 0 полей = void): у нативного пути это всегда Structure из n полей, вычисляемых как одно выражение (`l2trans.lm1:1965`), одиночным значением оно в общем случае быть не может.
3. Разделять, не клонировать (спека 21.5 + реплика автора №2). Следствие: LmxCallable — терминал копирования, а всё, что достижимо из header/body/return_arg, неизменяемо и не содержит указателей в хранилище конкретной копии M; собственные поля и хосты адресуются от `node` по позиции. Проверить (не утверждать): как адрес METHOD классифицируется в арене дочернего Message и покрывает ли тот же механизм домен CALLABLE и код, на который он ссылается.
4. `info: lmx_call_callable(arena, M)`, `ret: info\return_arg` — одно чтение, без обхода дерева: построитель графа один раз разрешил `frame\trailer`. Встроенный `return:` внутри тела — обычный узел-оператор. При выходе из тела (или если тела нет — форма `fn: ten () int` + `return:`) — вычислить поля ret как одно выражение → dirty-only чекпоинт в прямом порядке полей → возврат. Состояние активации — на обычном стеке интерпретатора; ret потребляется как значения, хостом не является (21.5.1).
5. Да, конфликт есть (см. факт про COPY_INVALID): добавить вид CALLABLE в `lmx_copy_is_terminal` и в ветку терминала `lmx_copy_value`, отказ на корне рядом с `~447`; решить судьбу корневого массива `ARRAY_OF_METHOD`.

В этом же ответе явно отозвать из GROK-INTERP: P1/P2 (префикс), P4 (спеллинги в узлах кода), P7(a), O1, O2 — со ссылкой на две реплики автора из §2.

## 3a. ЗАКРЫТО: GROK-CALLABLE-RESUME / REPOLL-20260920

Тот же бриф Grok пришёл дважды (RESUME, затем REPOLL — «живая копия»). Ответ по пунктам 1-4 отправлен `lmx_uds` один раз: msg_id `61f916d9-fab6-4ab4-ab7d-5f0b0308729a` (факты R1-R11, предложения S1-S7), затем ACK под кодом REPOLL — msg_id `989a776b-3c06-4d04-9212-61b86b01fce3`. Ничего не запускалось, файлы проекта не правились. Главное новое: (R1) графа кода в ядре нет вообще — `info.body` нечем наполнять, это вопрос представления; (R4/R5) ранний `return:` делает eval → poll → checkpoint → return (`l2trans.lm1:13664-13680`), трейлер — без poll (`:1950-1971`); (R7) конструктор `LmxCallable` — одна строка через `lmx_arena_take` по образцу `lmx_method_new_owned` (`lmx_value_owned.lm1:41-47`); (R8) корневой массив типизирован `ARRAY_OF_METHOD` (`l2trans.lm1:14427`); (R10) запись слота 0 не классифицируется ареной дочернего Message (`lmx_child.lm1:34-36`, `lmx_range.lm1:175-195`) → копия из дочерней арены даст UNSUPPORTED, а `lmx_call_ready(child_arena, M)` — NOT_CALLABLE; то же для METHOD (DEBT в `l2trans.lm1:14439`). Если `lmx_uds` скажет, что 61f916d9 не дошёл (SendMessage отметил «новая сессия под прежним именем») — переслать целиком.

## 3b. Прокрутка окна lmx_uds — ЗАКРЫТО автором

**Статус (2026-09-20):** автор сообщил: «прокрутку у него починили … больше ничгео не надо делать». Как именно починили — мне не известно; автора об этом больше не спрашивать, `claude.bat` и реле не трогать. Ниже — моя прежняя диагностика; её вывод «правкой bat не чинится» тем самым не подтверждён.

Причина найдена: в `claude.exe` 2.1.278 для `CLAUDE_CODE_SESSION_KIND==="bg"` fullscreen форсирован до проверки `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN` (`function Ua`, причина `"bg_forced_on"`), поэтому правка `C:\claude\claude.bat`/`attach.bat` бесполезна. `fable` прокручивается (автор подтвердил). Автору предложено: (1) оставить `--bg`, прокрутка внутри TUI (PgUp/PgDn/колесо); (2) переписать `claude.bat` на обычную интерактивную сессию (`--resume` + тот же `--messaging-socket-path`, обновить `pid` в `uds-state.json`), цена — реле живёт только при открытом окне. Без ответа автора текущую bg-сессию не останавливать. `lmx_uds` извещён (msg_id `ccb6fb59-9a4a-4e5a-b813-5a5c3fbcb07d`).

## 3c. ИДЁТ: интерпретатор — прямое поручение автора (2026-09-20)

Автор в сессии `fable`: «продолжайте работу по написанию интерпретатора, codex чуть подправил парсер сейчас в dev». С этого момента я не только советчик Grok, а пишу код — но только в СВОИХ новых файлах; `lmx_interp.*`, `lmx.h.lm1`, `lmx_call.*`, `lmx_value_owned.*`, копировщик и тесты Grok не трогаю (Grok активно коммитит в те же каталоги, модель менялась трижды за час).

Действующая модель (Grok, коммиты `c1b07b3`, `a47bb5e`): `LmxCallable {@: LmxMethod method; @: Lmx header}`; тело — сама M после слота 0, по порядку, все `return:` там; узел тела — Structure с записью `LmxOp {code}` (домен 31, вид 17, терминал копирования) в слоте 0, операнды дальше; `LMX_OP_RETURN 1`, `LMX_OP_CALL 2` в `lmx_interp.h.lm1`.

Моё (коммит `87b3374`, в `origin/main`): `dev/l2src_sandbox/lmx_walk.h.lm1`, `lmx_walk.lm1`, `tests/lmx_walk_selftest.lm1` (26 проверок, проходят, собрано на месте), `dev/graph_interp/BODY_NODES.md` (коды 3..14, открытые вопросы, рецепт сборки). Срез на `int`: загрузка собственных полей на входе, позиционная адресация от `node`, dirty-only чекпоинт перед вызовом и выходом, один порядок выхода, рекурсия на стеке C, входы, копия M исполняется без изменений. `l2trans` узлов тела не выдаёт — графы в тесте собраны руками.

Сборка: scratch-каталог со ссылками `l2src` → `dev/l2src_sandbox`, `l1src` → `C:\Nyasha_Planet\L1\dev\l2src_sandbox\l1src`; `C:\Nyasha_Planet\L1\bin\l1trans.exe --unit-root`; все `*.h.lm1` → `<out>/<каталог>/<имя>.lm1.h`; линковать `l2_libc.lm1`. Ловушки L1: `value\field` всегда `->` (вложение структуры по значению требует указателя-локала); присваивание глобалу с заглавным именем (`A: @ x`) транслятор читает как объявление типа.

Отправлено `lmx_uds`: возражения по `GROK-CALLABLE-APPLIED` (msg `03dce961-…`), ответ `GROK-INTERP-WALK` с исполненной пробой ошибки «результат callee как статус обхода» (`lmx_interp.lm1:79-80`) и предложением звать `lmx_walk_run` из `lmx_interp_run` (msg `8359786a-853e-4201-ab56-c9bb27f492cc`). Ответа Grok на предложение ещё нет.

**Поправка автора, пришедшая через Grok (`GROK-L3-ONLY-20260920`):** интерпретатор исполняет только L3 — не низкоуровневые операции L2 и не `c.*`; обход по `LmxOp` — черновик носителя; имя принимающего выражения L3 и состав глав v1 запрошены у Codex (`GROK-L3-SUBSET-20260920`). Сделано: в `BODY_NODES.md` коды 3–14 помечены временным набором для проверки механизма, перенос их в `lmx_interp.h.lm1` снят (коммит `9c11ccc`, в `origin/main`); `lmx_uds` извещён (msg `813ebc8c-de1e-47d2-be3b-f2c92c00f19a`). Механизм активации от набора кодов не зависит и остаётся. **До названного подмножества L3 набор кодов и типы не расширять**; вопрос автору о кодах узлов из моего прошлого отчёта тем самым снят, остаётся вопрос о порядке детей M.

**Ответ автора о порядке полей (2026-09-20, в этой сессии):** «каких детей? Поля Lmx задаются лексически и в графе ничего менять не нужно, это при трансляции в нативный код вы можете как что угодно писать» и «то есть порядок строго лексический полей Lmx». Занесено в `LMX_blog/2026-09-20.md` («Поля Lmx лексические, граф не перекладывается»), вопрос 1 в `BODY_NODES.md` закрыт, коммит `47671a8` в `origin/main`; память `lmx-graph-is-lexical`. Вопросов к автору больше нет. Говорить «поля», не «дети».

**Сведённый контракт `LMX-L3-FABLE-20260920-06`** (от `lmx_uds` по поручению Codex; A — подмножество от `openrouter`, B — контракт допуска от `lmx_uds`). Выполнено в моих файлах, коммит `2bd3ea0` в `origin/main`: узел с непрочитанным кодом `LmxOp` → `LMX_WALK_UNSUPPORTED` на входе (раньше молча пропускался как данные — это была ошибка); чекпоинт в лексическом порядке полей (`LmxWalkOwn.position`, `lmx_walk_before`); `lmx_walk_enter`/`lmx_walk_leave`; раннер `python tools/run_walk_selftest.py` → `lmx_walk: 32 checks`, свидетельства в `dev/l2src_sandbox/build/walk/`. `lmx_uds` извещён (msg `2665635f-1f18-4fe2-9803-78993e243e63`). НЕ делал: границу допуска §5 (нет значений-ссылок, кандидата нечем подать входом; выбирать представление нельзя). Вопросы B1–B7, A2 (poll), A3 (трейлер без тела) записаны в `BODY_NODES.md` как невыбранные — их ведут Codex/Grok, автору я их не задавал. Владение: `dev/l3_interp` — Grok (L3 receiver, N1–N10 по `V1.md`), сборка ядра — DeepSeek, мой участок — `lmx_walk.*`, его тест, `BODY_NODES.md`, `tools/run_walk_selftest.py`. План и замечания Codex — `steps/l3-receiver.md`.

**Решение автора об идентичности узла (`LMX-GRAPH-IDENTITY-20260920-08`, через Codex/`lmx_uds`; дословно в `LMX_blog/2026-09-20.md`, «Приоритет ссылок в исполняемом графе»):** максимум физических ссылок; числовые коды только где ссылка невозможна; текст нигде. Применено, коммит `6c4d70d` в `origin/main`, `lmx_walk: 36 checks`: одна общая запись `LmxOp` на роль (`lmx_walk_roles_open`; роль — адрес записи, `code` — лишь дискриминатор после разрешения ссылки); поля — `[own|at, holder, slot]`, `[put, holder, slot, value]` (держатель по ссылке, число только номер слота: хранилище слотов не значение графа); вызов — `[call, M, arg…]` только по ссылке; числовые пути убраны; лексический порядок собственных полей — обходом собственного дерева M (`lmx_walk_order`, ребёнок принадлежит дереву при `child\node = S`); поле чужого вхождения как own → INVALID. `lmx_uds` извещён (msg `c36c4caa-ad60-453f-a62a-e61d7752e73d`), с его прочтением «срез не задет» я не согласился и объяснил почему. Память: добавить правило о приоритете ссылок.

**Хранилище активации (`09F` → поправки `10`, `11`, `12`, `13`; всё через `lmx_uds` от Codex).** Сначала поручили убрать кучу из `lmx_walk` (calloc `found`/`owns` в `lmx_walk_enter`, calloc `argv` в `lmx_walk_call`), допустив VLA; затем автор уточнил: буквальный «всё на стеке» НЕ требование; требования — (1) M/тело/граф не копируются на входе, (2) локальность/загрузка/dirty как у нативного, (3) скорость примерно не хуже, (4) всё потом перепишут на L2, поэтому только стандартные механизмы: VLA, TLS, inline-буфер, walker-специфичный аллокатор — ЗАПРЕЩЕНЫ как финал; `lmx_arena_mark/revert` отклонён (зернист по блокам, revert = free). Мой незакоммиченный черновик заголовка с VLA снят (`git checkout`), в Git ничего про VLA нет; рабочее дерево по моим файлам = `6c4d70d`, 36 проверок проходят. **Правок репозитория не делать до согласования проекта.**

Замер (только в scratch: `vA`/`vC`/`vE`/`vP`, `make_variants.py`, `lmx_walk_bench.lm1`, gcc -O2): calloc (A) / VLA-нижняя граница (C) / курсор высокой воды над стандартным пулом (E) неразличимы в шуме ±10 %: counter ≈ 0.85–1.1 мкс на активацию, summer ≈ 2.0–2.3 мкс. Настоящая цена входа — 113 вызовов `lmx_range_classify` на активацию из трёх узлов: моё `lmx_walk_op` классифицирует и ребёнка, и слот 0, а вход проходит тело 4 раза. Проект и цифры отправлены (msg `7e6ec8d8-62b8-4b74-9be7-a8cc93ad2343`): общий `LmxScratch {arena, cells, at, used}` над одним `LmxPool` в отдельной `lmx_arena_open_simple`, take через `lmx_pool_take_n`, mark/rewind = (кусок, used), владелец — рабочий/ход, новая пара доменов 18/32 (файл не мой); намерение по сигнатуре `lmx_walk_run(arena, scratch, node, args, nargs, result)`; шов `LmxThreadDispatch(t)` (коммит `6f060c6`, файлы lmx_thread не трогать) несёт только Thread — требования к адаптеру названы, не выдуманы. Важнее стора — предложение (а) распознавать роль сравнением ссылки, без классификации; (б) список используемых собственных полей в лексическом порядке — статическое свойство M, его должен класть транслятор в `header` дескриптора. Жду решения Codex/автора.

**Решение `LMX-ACTIVATION-PLAN-20260920-18` (Codex): одобрены все три моих предложения; за мной три работы из четырёх — ВЫПОЛНЕНЫ.** (A) общий стор: коммит `0fe54cd` — `dev/l2src_sandbox/lmx_scratch.h.lm1`, `lmx_scratch.lm1`, `tests/lmx_scratch_selftest.lm1` (55 проверок), домены `LMX_DOMAIN_SCRATCH 32` / `LMX_DOMAIN_KIND_SCRATCH 18` в `lmx_implements.h.lm1`, ключ `--test` у `tools/run_walk_selftest.py`. Два факта против наброска: простая арена (`lmx_arena_open_simple`) не растёт дальше первого куска → арена контекста обычная; `lmx_pool_take_n` оставляет `chunk_capacity` поднятым при неудаче роста → стор возвращает шаг (по существу дефект пула, сообщён). (B)+интеграция: коммит `17a6b18` — `LmxWalkContext {arena, scratch, roles}`, `lmx_walk_run(context, node, args, nargs, result)`, calloc/free убраны, роль распознаётся сравнением адреса с общими записями ролей (`lmx_walk_role`), `code` не читается, частная запись с номером роли отклоняется; `lmx_walk: 45 checks`. Замер после B: классификаций counter 113→47, summer 3553→1300, looper 3336→351; время −20…30 % там, где доминирует тело; вход в тривиальное тело не ускорился (четыре прохода по телу на активацию) — это снимет только план активации (C), раскладку которого выбирает Grok; я её не выдумываю, вход перепишу на «загрузить из плана», когда она появится. Контрольная точка отправлена `lmx_uds` (msg `e2d8183f-8948-4482-bbbb-d432deac9999`). Scratch-каталог со стендами (`vA`/`vC`/`vE`/`vF`/`vP`, `lmx_walk_bench.lm1`, `make_variants.py`) — вне репозитория, в сессионном scratchpad; при новой сессии его не будет.

**Дефект пула (`LMX-POOL-GROWTH-ROLLBACK-20260920-19`):** Codex подтвердил мой факт 2 как ошибку `lmx_pool_take_n` (`lmx_pool.lm1:226-231`) и отдал исправление deepseek (владелец ядра) с фокусным случаем в `lmx_pool_selftest.lm1`; я только аудирую, в `lmx_pool.*` не захожу. МОЙ ХВОСТ: после его коммита снять обход — строку `s\cells\chunk_capacity: s\step` в холодной ветке `lmx_scratch_take` с комментарием и абзац о NOMEM в `lmx_scratch.h.lm1` (поле `step` оставить) — и прогнать оба раннера (должно остаться 55 и 45; проверку держит существующий тест `pp1\chunk_capacity = 8U` после двух NOMEM). Условия отправлены `lmx_uds` (msg `bcf6119c-8015-4e6a-9f2d-151ea0d141e0`). С deepseek напрямую не общаться — через `lmx_uds`.

**Обход снят (`LMX-SCRATCH-WORKAROUND-REMOVE-20260920-20`) — ХВОСТ ЗАКРЫТ.** deepseek исправил пул (`e513e05`), я удалил строку `s\cells\chunk_capacity: s\step` и переписал абзац о NOMEM; коммит `14b84b2` в `origin/main`, раннеры 55 и 45 зелёные; `lmx_uds` извещён (msg `c043260f-14ee-4ee9-8532-d92d9be3b754`). Урок: после удаления мой тест упал, но неверной оказалась МОЯ проверка (`chunk_capacity = 8U`), а не исправление пула — обход сбрасывал шаг к 8 и маскировал, что успешные крупные запросы уже подняли шаг пула до 36 (замерено: 36 до отказов, 36 после); проверка теперь `= was_step`. Наблюдение передано владельцу пула: на успешном пути `lmx_pool_take_n` оставляет `chunk_capacity` поднятым навсегда, хотя `lmx_pool.h.lm1` говорит «that one chunk». Ещё: мои тесты собираются одной единицей (тела через predef); гейт ядра deepseek линкует с объектами модулей — там те же predef дадут `multiple definition`; если мои тесты включат в гейт, подстроить predef.

**Аудит ABI плана активации (`LMX-ACTIVATION-PLAN-ABI-AUDIT-20260920-28`, только чтение) — ОТПРАВЛЕН** (msg `4be2e781-a81a-4c49-89e0-8c5ddeb2676e`). Документ Grok: `steps/l3-activation-plan.md` (`4f8f52b`). Мои выводы как владельца потребителя: (1) `lmx_plan_build(arena, plan_roles, M, …)` как написано не реализуем — нужны и таблица ролей тела, и раскладка операндов каждой роли (она только в `lmx_walk_scan_node`); поправка — разделить: общая единица `lmx_plan.*` (open/entry/check/publish/count/slot_known, без `lmx_walk.h`) и `lmx_walk_prepare(context, plan_roles, M)` у владельца ролей = мой нынешний анализ входа, один раз; (2) правило: исполняемая форма вхождения неизменна, пока в слоте 0 данный `LmxCallable`; меняться могут только значения; изменение формы одного вхождения = свежий `LmxCallable {method, header=0}` в ЕГО слоте 0, общий дескриптор не трогать; validate не на пути исполнения, дайджест не нужен; (3) роли плана — выделенная пара `LMX_DOMAIN_ROLE 33` / `KIND 19`, шаг с указатель, терминал копирования; (4) правило одной арены: P, записи, индексы и plan_roles — в арене записи `LmxCallable`, publish проверяет классификацией; (5) входу нужно ровно N и адрес СЛОТА i-й записи; все четыре прохода уходят при трёх условиях — разрешение без классификации и без `arena` (`*_known`), возвращать слот, а не ячейку, и правило (2); остаются `lmx_call_ready` (2 классификации) и одна на поле тела при исполнении; ожидание для counter: 47 → 5; (6) документ ошибочно называет `l2_layout_owns` авторитетом порядка — он не лексический. МОЁ НА БУДУЩЕЕ: `lmx_walk_prepare` и вход на плане — когда общая единица плана появится принадлежащим коммитом; добавить тест порядка `own a; while { own t }; own b` → a, t, b.

**Ревизия плана принята, потребитель на удержании (`LMX-PLAN-CONSUMER-HOLD-20260920-31F`).** Grok Bot внёс все мои поправки в `steps/l3-activation-plan.md` (`db1dfa6`): разделение общей единицы и скана тела, правило неизменяемости без дайджеста, пара `LMX_DOMAIN_ROLE 33`/`KIND 19`, одна арена, лексический порядок, `lmx_plan_build_from_paths` / `lmx_plan_publish(arena, callable, P)` / `lmx_plan_slot_known(M, P, i)` → адрес СЛОТА / `lmx_plan_entry_count(P)`. ACK отправлен (msg `b7ef0eea-4abf-434c-ab79-7e22954cced7`), блокеров нет, шесть уточнений. **НЕ ПРАВИТЬ `lmx_walk.h.lm1`, `lmx_walk.lm1`, `tests/lmx_walk_selftest.lm1`, пока общая единица `lmx_plan.*` не появится принадлежащим коммитом (её пишет Grok).** Когда появится — моё: `lmx_walk_prepare(context, descriptor_arena, plan_roles, M)` (идемпотентен при `header != 0`; план в арене ДЕСКРИПТОРА, не `context\arena`), `plan_roles` в `LmxWalkContext` (вход сверяет `P[0] == ROLE_PLAN_ROOT` сравнением адреса), вход на плане: РОВНО две классификации (вид M и вид слота 0 спросить самому — `lmx_call_callable` стоит три, в паре с `lmx_call_ready` пять), цикл по N через `lmx_plan_slot_known`, срез стора под N, `found` исчезает; холодный путь `header = 0` → NO_GRAPH/UNSUPPORTED; тесты B, C, E, F, G, H, I из §12 документа (G — моя сверка плана со сканом тела, общая `validate_shape` тела не знает). Правило на будущее: записи `ROLE_PLAN_ENTRY` — только путь; типизированные поля придут с НОВОЙ ролью записи. Другим участникам по этой теме не писать.

**Ворота завершения интерпретатора (`LMX-L3-COMPLETION-GATE-20260920-36F`) — МОЯ ЧАСТЬ СДЕЛАНА, две партии в `origin/main`.** Никакой миграции L1→L2→L3 до закрытия ворот. (1) `5a1a4ae` — общая единица `lmx_plan.h.lm1` / `lmx_plan.lm1` + `tests/lmx_plan_selftest.lm1` (47 проверок, без `lmx_walk`), домены `LMX_DOMAIN_ROLE 33` / `KIND_ROLE 19` в `lmx_implements.h.lm1`, `KIND_ROLE` — терминал в `lmx_graph_copy_owned.lm1` (одна вставка в чужой файл, по разрешению Codex и ABI). (2) `a99f442` — `lmx_walk_prepare(context, descriptor_arena, plan_roles, M)`, `lmx_walk_plan_check`, вход по плану: ровно ДВА вопроса к арене, все вопросы через `lmx_walk_kind` со счётчиком `LmxWalkContext.classified`; `plan_roles` в контексте; тело без плана → UNSUPPORTED; `lmx_walk: 85 checks`. `удержание 31F` снято этим поручением. Замер вперемежку: counter ≈1100–1240 → ≈238–253 нс, summer(20) ≈58–63 тыс. → ≈8,2–8,6 тыс. нс, looper в шуме (остаток — один вопрос на поле тела за итерацию; план этого не снимает). Два «неопределённых» примитива из §7 документа — существующие `lmx_arena_ref_struct` / `lmx_arena_ref_cell` (правка названий в тексте, документ — Grok Bot). ПОПРАВКА к моему отчёту 36F (он устарел уже в момент отправки — я не перепроверил `git status` перед текстом): исправление return у Grok вошло в `c0601b3` (12:44, за минуту до моего `573baa1`), привязка к Thread — `0f98a93` (13:04), его 14 файлов закоммичены; проверяет это исполнением Codex. Моё наблюдение по `c0601b3`, переданное Codex: значение return несут ГЛОБАЛЫ единицы `l3_last_int` / `l3_has_ret` (и `l3_subject`) в `dev/l3_interp/l3_exec.lm1:12-14` — общие на процесс, то есть не реентерабельны при рекурсии и гонка при нескольких Message на разных потоках; у `lmx_walk` результат идёт через out-параметр и кадр активации. Дальше по моему участку: типы сверх `int` (через НОВУЮ роль записи плана), позиции шагов тела в плане — только по отдельному решению.

**Запрос `43F` (Codex): достаточен ли мой интерфейс, чтобы Grok заменил глобалы `l3_subject`/`l3_last_int`/`l3_has_ret` и `calloc(32)`/`L3_OWN_CAP` в `dev/l3_interp/l3_exec.lm1` — ОТВЕЧЕНО** (msg `1a892199-ceff-47aa-a08d-0bcd9513888f`, только чтение). Записи собственных полей: да, полностью — `lmx_scratch_mark/take/rewind` + `lmx_plan_entry_count` / `lmx_plan_slot_known`; но им нужен СВОЙ сканер-получатель (мой `lmx_walk_prepare` знает операнды только моих ролей). Значение return и subject — не стор и не план, а поля записи активации потребителя, передаваемой указателем (как `LmxWalkFrame`); примитива L2 не недостаёт. Недостаёт вне L2: (Н1) места для контекста исполнения в состоянии Thread — шов `l3_thread_dispatch(t)` взять стор/`plan_roles` неоткуда, и приёмника результата хода нет (владелец `lmx_thread`); (Н2) значений-ссылок во входах моего среза (subject — это вход, а у меня только `int`). МОЙ ВОЗМОЖНЫЙ ХВОСТ, по слову: вывести наружу признак «return со значением» вторым out-параметром `lmx_walk_run` — сейчас void и значение снаружи неразличимы.

**Ответ автора о результате хода (2026-09-20, в этой сессии):** «у нас все общение идет черз Message и это работает. Тот кто что-то соощает пишет Message». Занесено в `LMX_blog/2026-09-20.md` («Общение только через Message»). Следствие: приёмника значения return в состоянии Thread НЕ нужно — моё требование из `43F` (вторая половина Н1) СНЯТО; от `lmx_thread` остаётся нужен только доступ диспетчера к контексту исполнения (стор, роли). Значение return существенно лишь для прямых вызывающих (юнит-тесты допуска), не для шва Thread. Память: `lmx-communication-is-message`. Следом автор: «зачем тебе какой-то доступ елси ты можуешь все спросить у другого потока сообщением?» — требование сужено ещё раз: РОЛИ доступа не требуют вовсе (известны при трансляции, неизменяемы, живут в R0, узел уже указывает на запись, интерпретатор сравнивает адрес с константой; в контекст я клал их только из-за тестов, собирающих роли на ходу); СТОР — не общение между потоками, а собственная рабочая память исполняющего потока (аналог машинного стека), спросить её не у кого. Остаётся чисто механическое, внутри ядра: `dispatch(t)` должен найти память СВОЕГО потока без глобальной переменной процесса — владелец `lmx_thread`. Автора об этом больше не спрашивать.

**Аудит `LMX-ETERNAL-R0-20260920-44F` (только чтение) — ОТПРАВЛЕН** (msg `e6006db9-e05f-42c0-b322-062227e3b167`). Адрес постоянного хранилища R0 в арене источника классифицируется NONE; мест отказа пять: `lmx_copy_is_terminal` (`lmx_graph_copy_owned.lm1:187-201`) — единственное место предиката терминала; фиксация `node` (`:506-513`) даёт INVALID вопреки собственному комментарию `:493-494` «at an eternal parent's own address» — нужна отдельная ветка; корень-терминал сейчас даёт «OK и out = 0» (молчаливый ноль, верно и для добавленного мной `KIND_ROLE`); merge `:63/:71/:80` — там нужен ВИД постоянного адреса, не только «да/нет»; `lmx_call_ready` `:15/:27-30/:50/:64` и мой `lmx_walk_descriptor`. GC (`lmx_gc.lm1:45-52`) уже не ходит в чужие адреса — R0 нельзя делать видимым присоединением интервалов к индексу Message. Предложены тесты T1–T11 (главные: чужой адрес по-прежнему UNSUPPORTED; изменяемая Structure той же формы копируется). T12 (M + LmxCallable + план + роли в постоянном хранилище, копия M в арене дочернего Message исполняется по общему плану) — СДЕЛАН в `FABLE-R0-SUBJECT-20260920-1935`, см. ниже; `lmx_walk_descriptor` отдельного предиката не потребовал — `lmx_range_classify` сам отвечает за привязанное хранилище.

**Признак значения наружу — СДЕЛАНО (`827f125`).** `lmx_walk_run(context, node, args, nargs, result, valued)`: `valued` = 1 при return со значением, 0 при void/падении с конца; любой приёмник может быть 0. Сделано до адаптера Grok `l3_thread_dispatch → lmx_walk_run`, пока внешних вызывающих нет (в `dev/mixa_sandbox/l2src/` лежит СТАРАЯ копия моих файлов — стейджинг deepseek, не вызывающий; обновится при его следующей синхронизации). Состояние стыковки: Grok принял решение B, мой обход не ослабляется, он переводит свои фикстуры на общие записи ролей и пишет адаптер, мои файлы не копирует; `ebac510` — его исполнитель уже на моём `lmx_scratch` (второй потребитель). Предел Н2 (входы только `int`, subject входом не передать) остаётся; наименьший кандидат, если решат: входы как адреса значений графа (`@@: void`), чтение `*_known`, без классификации — НЕ выбирал. Мелочь на будущее: в `git commit -m` не ставить обратные кавычки — bash съел слово в сообщении `827f125`.

**Значения-ссылки и входы-Structure (`FABLE-L3-VALUE-INPUT-NOW-20260920`, Codex) — СДЕЛАНО, `ea6e108` в `origin/main`.** Предел Н2 снят: значение активации — всегда физическая ссылка (`@: void`) на Structure, ячейку графа или ячейку-приёмник потребителя; кода типа нет (вычисленное целое = тождество ссылки с приёмником; вид ссылки в граф — классификация, спрашивается только на публичной границе входа и в новой роли `of [of, значение, слот]`); void = нет ссылки + `present`. Граница: `struct LmxWalkValue {ref, number, present}`, `lmx_walk_run(context, node, @@: void args, nargs, @: LmxWalkValue out)` (out можно 0), внутренний `lmx_walk_activate(..., dest, result, present)`. Целые — destination-passing; ссылка в стор, уходящая через `ret` из `arg`/`call`, копируется 4 байтами в приёмник вызывающего (`lmx_walk_leaving`), ссылка в граф уходит как есть; входы вызываемой — срез стора (n ссылок + n ячеек). Проверено: `lmx_walk: 104 checks`, `lmx_plan` 47, `lmx_scratch` 55, `check_docs` OK, `git diff --check` чисто, полный гейт `tools/build_l2src.ps1 -Run` → GREEN 221 (`build/l2src/20260920_192435`; мои файлы в гейт входят, в момент прогона в песочнице лежала чужая незакоммиченная правка `l2trans.lm1`). Результат отправлен (msg `4fe3d764-3f6f-4b22-9f7e-9c74aed60d65`). Пределы: арифметика и own — только `int`, int-ность операнда не проверяется (доказательство производителя); own-ссылок нет; subject из постоянного хранилища не тестировал — взять вместе с T12 из `44F`. Адаптеру Grok: `args[0] = subject`, `out = 0`. Коммит-сообщения писать через `git commit -F файл` — без обратных кавычек в `-m`.

**Subject в постоянном хранилище R0, вечная ветвь, сборка мусора (`FABLE-R0-SUBJECT-20260920-1935`, Codex; вместе с T12 аудита `44F`) — СДЕЛАНО, `ba247c4` в `origin/main`.** Новый тест `dev/l2src_sandbox/tests/lmx_walk_perm_selftest.lm1` (86 проверок): в хранилище (`lmx_perm_open`) — subject `[7, 35]` как `args[0]`, Structure с полем-subject, записи ролей тела и плана, каждый общий `LmxCallable` с планом, два вечных вхождения; в двух привязанных Message — вхождения с собственным состоянием и копии; второй рантайм рядом. Доказано: физическое тождество (поле, сам subject, через вложенный вызов, поле поля, `at` с держателем из хранилища — адреса хранилища), ни одной ячейки и ни одного байта в хранилище за прогоны; классификация STRUCT / PRIMITIVE INT / OP / ROLE / CALLABLE из обоих Message и NONE из другого рантайма; чужой subject и чужое вхождение — `INVALID`, ничего не исполнилось; копия M в дочернем Message исполняется БЕЗ подготовки по общему плану за те же 7 вопросов; вечное вхождение копируется в себя; две сборки над Message и сборка «над хранилищем» не меняют байтов хранилища (инструмент сначала показан зрячим), после сборок всё исполняется. Правки единиц: `lmx_plan` — правило одного ДОМА вместо одной арены (`lmx_plan_home`: привязанное хранилище, сама арена или 0; публикация требует дом плана = дом дескриптора; роли — того же дома или хранилища); `lmx_walk_prepare` строит план в `descriptor_arena`, публикует через арену контекста, сверяет арену плана с домом дескриптора ДО построения (отказ ничего не оставляет); `put` с держателем в хранилище и `set` собственного поля с держателем в хранилище — `INVALID` при подготовке (план не выдаётся, тело не исполняется), чтение не запрещено; в активацию не добавлено ни одного вопроса. Пределы записаны в `lmx_walk.h.lm1` и `BODY_NODES.md` как ТЕКУЩИЕ неподдерживаемые, не как финальная семантика L3: арифметика и own только `int`, own-ссылок нет; текста и кодов типа при значении нет. Проверено: `lmx_walk` 104, `lmx_plan` 49 (+2 случая дома), `lmx_scratch` 55, `lmx_walk_perm` 86, `check_docs` OK, `git diff --check` чисто; мутации: без отказа записи — 17 падений из 86, без правила дома в publish — 3, без вопроса на границе — 3, без ранней сверки — 1. ПОЛНЫЙ ГЕЙТ: НЕ ЗАВЕРШЁН. tools/build_l2src.ps1 -Run не стартует (пин транслятора не совпадает, см. ниже); прогон в режиме -Translator (пин не проверяется) оттранслировал и скомпилировал все единицы, включая мои, и был убит хостом Claude Code из-за нехватки памяти на машине, когда шёл чужой зависший lmx_app_lanes_mail_selftest; хост запретил перезапускать самому — перезапуск только по просьбе. Вместо гейта поштучно собраны потребители: lmx_interp_walk 3, lmx_callable 6, l3_01..l3_04 и l3_recipes — ok; l3_thread_bind не транслируется из-за чужой незакоммиченной правки dev/l3_interp/l3_thread.lm1:15 (struct в файле тела). Причина оговорки про пин: `bin/l1trans.exe` пересобирает другая сессия (в дереве чужие незакоммиченные `l1src/l1trans.lm1`, `lm1/build/l1trans.lm1.c`); за время задачи бинарник менялся дважды (`7a8cc766…` → `ea686824…`), `L1_PIN.txt` говорит `de02d23c…`, совпадающего бинарника на диске нет. Пин и бинарник НЕ трогал; гейт гонял документированным режимом `-Translator <копия в scratchpad>` (пин не проверяется, в отчёте помечено). ГЕЙТ ПОТОМ ЗАКРЫТ CODEX: после обновления пина (`L1_PIN.txt` = `ac0ee289…`, совпадает с `bin/l1trans.exe`) Codex сам прогнал закреплённый `tools/build_l2src.ps1 -Run` на `ba247c4` — 223 цели, без падений, exit 0, свидетельства `build/l2src/20260920_200540` (в них `lmx_walk_perm: 86 checks`; проверено мной по логам). Доставка до Codex проверена: `python claude_chat/codex_inbound.py status` показывает его ход «Принял через новый обратный маршрут: ba247c4 … 223/223»; пункт про `lmx_interp.lm1:33` он выдал Grok отдельным тикетом `GROK-LMX-INTERP-PLAN-HOME-20260920-01`. ОБРАТНЫЙ МАРШРУТ (`work_chat/README.md`, коммит `2100d63`): отвечать Codex через `SendMessage(to="lmx_uds", message="REPLY <REQUEST_ID> …полный ответ…")`, на английском; последний шаг (`codex_inbound.py send`) делает `lmx_uds`; ответ в своём окне доставкой не считается; ПОПРАВКА CODEX (`FABLE-REPLY-VERIFICATION-20260920-01`, ответа не требовала): `codex_inbound.py status` показывает лишь привязку моста к текущей задаче Codex и её состояние и НЕ доказывает доставку конкретного ответа; подтверждение по запросу — сообщение с тем же REQUEST_ID у Codex или его явное коррелированное подтверждение; по одному `status` ответ доставленным не считать. Три моих блокера закрыты (`lmx_uds`): пин закрыт коммитом `4128140` вместе с бинарником; гейт пройден (выше); «unsupported L1 form» в `l3_thread.lm1:15` не воспроизводится — чужая незакоммиченная правка исчезла; `lmx_app_lanes_mail_selftest` в закреплённом гейте отработал с exit 0 — не зависание, а нагрузка машины в момент, когда хост убил мой прогон. Память: `lmx-codex-tickets-via-uds`. Итоговый отчёт отправлен `lmx_uds` (msg `4cbeb420-cec3-49f8-b066-81b807e7440b`, промежуточный — `b149cac8-1aed-47cb-af12-3f93d08f30af`); гейт перезапускать только по просьбе. Потребителю `lmx_interp.lm1:33` предложено передавать `lmx_plan_home(context-арена, дескриптор)` как арену дескриптора — иначе дескриптор из хранилища теперь даёт `INVALID`. Чего тест не касается: массива удержания R0 (`lmx_root_eternal`) — это незаконченная половина `lmx_uds` (эмиссия вечных ветвей в хранилище есть с `6f5d7ed`, удержание не подключено), хранилище в тесте собрано руками.

**Липкий dirty аргумента, адрес которого взят до привязки; без белого списка типов (`FABLE-L2-ARG-ADDRESS-DIRTY-20260921-67`, Codex, по «FINAL CONSOLIDATED CORRECTION») — СДЕЛАНО, `3021212` в `origin/main`, итог отправлен в `lmx_uds` (msg `06f27875-73df-4860-90f8-f54cf8f8eddc`), тяжёлый слот ОТПУЩЕН; приёмки Codex на момент записи ещё нет.** Семь путей: `dev/l2src_sandbox/l2trans.lm1`, новые `tests/unit_arg_addr_{sticky,types,pointer}.lm2`, `tools/l2_harness.ps1`, `docs/implementation-notes.{ru,en}.md` (абзац о старом белом списке). Не тронуты: оба `L1_PIN.txt`, `-Translator`, корневые `l1src`/`l2src`, ядро, семантика графовых own, правило побега адреса. ПРАВИЛО КАК СДЕЛАНО: три флага активации `l2_q<i>_early/_bound/_sticky` есть ТОЛЬКО у методов, тело которых пишет `@: x` для связанного параметра (статический обход `l2_addr_scan`, мемо `l2_addr_memo`), поэтому все прочие методы порождаются байт-в-байт как раньше. `@x` при несвязанном `x` ставит `early` (`l2_emit_addr_mark` в ветке параметра `l2_prep_addr`); первая ИСПОЛНИВШАЯСЯ связывающая строка решает `sticky` (`l2_emit_bind_mark`); чекпоинт проверяет `dirty || sticky` и сбрасывает только `dirty`. Сам адрес не изменён: `@ l2_p<m>_<k>`. РЕШЕНИЕ, КОТОРОЕ НАДО ПОМНИТЬ: консолидированный текст говорит «первое исполненное присваивание-как-объявление», а `docs/L2_spec_ru.md:170` — «исполненное ОБЪЯВЛЕНИЕ или присваивание-объявление». Сначала сделал только присваивания — спецификация главнее, теперь связывают ОБА, решает то, что исполнилось первым; случай E (`E 6 100`) это закрепляет. Если Codex скажет «только присваивания» — это одна строка в `l2trans` и одна ожидаемая строка. ТИПЫ: семикратный `ty = 0 || 1 || 2 || 36` убран; `l2_own_ty_of_param` отображает код ФОРМАЛА в код ХРАНЕНИЯ own (разные числовые пространства: unsigned 34 → 3, указатель r → 1000 + r через `l2_pointer_depth`), `l2_bind_own` сравнивает через неё. Публикация — ОДНА эмиссия по дескриптору (`l2_own_store_helper`, `l2_own_store_rebinds` — только char, `l2_own_store_value`); тип без ячейки там — ОШИБКА `own field type has no graph cell to publish to`, а не пропущенная ветка. RED СНАЧАЛА (неизменённый транслятор): `A2 6 100`, `A3 9 100`, `A4 9 200`, `D1 6 100`; unsigned в объявленной форме ОТВЕРГНУТ («incompatible entry signature»), в форме присваивания не связан; указатель не связан вовсе. ПОСЛЕ: `A1 6 6|A2 6 6|A3 9 9|A4 9 9|B 5|C1 4 4|C2 9 4|C3 9 100|D1 6 6|D0 5 100|E 6 100`; типы `U 51 51`, `Z 71/71`, `L 81/81`; указатель связан с общей указательной ячейкой, собирается и исполняется. A2 и A4 идут БЕЗ адресного вызова перед ними, а между ними и прошлой публикацией стоит явная запись в граф — это и отличает липкость от «поднять dirty заново после возврата». МУТАНТЫ: «порядок не важен» → `C2 9 9, C3 9 9, D0 5 5, E 6 6` (убит четырьмя проверками); «липкий без привязки» → exit 139 на первом чекпоинте `never_bound` — УБИТ ПАДЕНИЕМ, НЕ ПРОВЕРКОЙ, и проверкой сделать не смог (поле от одного присваивания законным L2 через `node\x` не читается, а объявленное поле связывается объявлением) — в итоге сказано прямо. Этот же мутант нашёл дыру в МОЕЙ первой версии: липкое поле публиковалось через незагруженный `from` — теперь метка привязки сама грузит ячейку тем же текстом, что и присваивание. КОНТРОЛИ СТРОК: обвязка с прежним транслятором — RED 3 из 18 (ровно новые строки); с мутантом порядка — RED 1 (`line 8 is "C2 9 9", expected "C2 9 4"`, замер до появления случая E). В `l2_harness.ps1` у строк появился `Says` — точное сравнение строк вывода по порядку. 280 СТАРЫХ ФИКСТУР старым/новым транслятором: 209/209 транслируются, 280/280 журналов диагностик байт-в-байт, ровно ОДИН вывод отличается — `unit_addr_arg` (+15/−3), единственная фикстура с `@: n` на связанном параметре. ПРОГОНЫ: `l2_harness` GREEN 18 (`build\l2_harness\20260921_065344`); закреплённый гейт `build_l2src.ps1 -Run` GREEN 231, 231 OK / 0 FAIL / 0 SKIP, 60 самотестов `ran, exit 0` (`build\l2src\20260921_065403`), застейдженный `l2trans.lm1` SHA `0e59c66110822a04…` = закоммиченный файл; `check_docs` 0; `git diff --check` 0. Запросов разрешений не было. НЕ СДЕЛАНО, сказано в итоге: (1) скрытые/динамические входы механизмом покрыты (`l2_param_find`/`l2_param_name` охватывают их), но ФИКСТУРЫ на адрес динамического входа нет — законной формы L2 не нашёл; (2) типы формалов без ячейки остаются простыми локалами молча: коды, которых нет в `l2_pointer_depth` (4/5 `LmP0Text`, 6/7 `L2ImmutQuery`, 10, 14/15, 28 `LmxRoot`, 30 `const @unsigned`, чужие структуры по значению) — это и есть «точный блокер представления»; большинство из них в C — указатели, общая указательная ячейка взяла бы их, если расширить `l2_pointer_depth`, но она общая с путями разыменования и приведения — не расширял; (3) формалов-массивов в L2 нет, а пропуск `l2_own_is_array` в чекпоинте касается графовых own-массивов — вне тикета. НАЙДЕНО ПО ДОРОГЕ, не чинил, записано в `implementation-notes`: явная ЗАПИСЬ `node\x: v` разрешает `x` в ПЕРВОМ методе с таким полем (поэтому поля фикстуры зовутся na..ne); чекпоинт любого `char` own приводит через `uchar`, которого сгенерированная программа не определяет — такие программы никогда не собирались (char поэтому нет в СОБИРАЕМОЙ матрице); вызываемый с двумя формалами не принимает фактический `@:`; `node\x` не читает поле, созданное одним присваиванием. ГРАБЛИ ИНСТРУМЕНТА (снова): патчи с обратной косой чертой писать ФАЙЛАМИ через Write, не heredoc; после приватной пересборки смотреть `translate=0 gcc=0` — иначе молча исполняется СТАРЫЙ exe. Scratch: `<scratchpad>\n67\`.

**Доказательная правка почтового теста L3 (`FABLE-L3-MAIL-PROOF-20260921-63`, Codex; мои два пробела из -59 + находка Grok -60 о росте кольца) — СДЕЛАНО, `a3c24b9` в `origin/main`, ровно один путь `dev/l3_interp/tests/l3_mail_prim_selftest.lm1` (36 → 62 проверки).** (1) Результат `take` теперь НАБЛЮДАЕТСЯ по адресу: обход этого среза не умеет присваивать ссылку (own и put — int, `lmx_walk.lm1` PUT делает `lmx_int_store_known`), а путь Thread отбрасывает верхний `ret` (out = 0), поэтому результат уходит операндом во второй узел PRIM к тестовому примитиву `sink_letter` — `[prim, sink, [prim, take, lane]]`; приёмник запоминает АДРЕС, тест сверяет его с доставленным письмом (не значение, прочитанное по ссылке, — его воспроизвела бы верная копия). (2) Отмена запроса: до проваленного хода `current = INTERPRETED && requested = NATIVE` (инструмент первым), после — две проверки: текущий INTERPRETED и `lmx_thread_requested_mode = INTERPRETED`; вводящая в заблуждение метка убрана. (3) Три разных письма из арены через `lmx_thread_mail_accept` (кольцо открывается на `LMX_POST_INBOX_MIN_CAPACITY = 2`, третье заставляет расти; посылка `3U > MIN_CAPACITY` сама проверяется), три хода взятия — FIFO по адресу ПИСЬМА, ни одно поле кольца не читается; взятие из пустого ящика — хороший ход с отсутствующей ссылкой. (4) Страж владельца `l3_thread.lm1:57-58` ОСТАВЛЕН и теперь ДОСТИГАЕТСЯ: тестовая запись с настоящим `l3_prim_mail_count` и ЧУЖИМ владельцем `t2`. МОЯ ОШИБКА ПО ХОДУ, пойманная мутантом: с ячейкой полосы в арене вызывающего отрицательный случай проходил И БЕЗ стража (62 из 62) — отказывала следующая проверка `l3_prim_lane`, классифицирующая операнд через арену ВЛАДЕЛЬЦА; ячейку полосы надо брать из ЧУЖОЙ арены — тогда без стража ход OK и own = 1, то есть ход t прочёл ящик t2. Причина записана комментарием в тесте. Проверено на закоммиченном `e111ce0` + мой файл (в рабочем дереве в это время лежали чужие незакоммиченные заголовки `lmx_post` — первый зелёный прогон по рабочему дереву я доказательством не счёл): набор 62 проверки, 20 из 20; все 10 наборов (6, 20, 6, 6, 20, 28, 6, 48, 45, 62); бюджет 62/64 ×4; `check_docs` 0; `diff --check` 0; после сдвига HEAD на `e5382cd` (в L3-значимых путях только 8 строк комментариев) набор и бюджет перепрогнаны на `a3c24b9` — зелёные. МУТАНТЫ (ни один не трогает тест), все убиты: take отдаёт 0 → 3 падения; свежая запись → 4; проваленный ход сохраняет запрос → 14, ПЕРВОЙ падает сама проверка отмены; страж снят → 2; рост переворачивает порядок → 2; рост теряет старшее письмо → 1; записи в арене → 13; нативный откат → 5; нет отказа второму Thread → 26. Отчёт — `lmx_uds`, msg `85ccfc18-2a09-4871-b703-d971fc794053`. ПРОЦЕСС (`FABLE-PERMISSION-HANDLING-20260921-73`): подтвердил с границей — тикет задаёт ОБЪЁМ, а не разрешение в моей сессии; запросы разрешений отвечает только пользователь; перед действием, способным вызвать запрос, сообщать `lmx_uds` команду / цель / пути; при блокировке — сразу BLOCKED с точным текстом.

**Повторная приёмка почтовых примитивов L3 после починки Grok (`FABLE-L3-MAIL-PRIM-ACCEPTANCE-20260921-59`, Codex, только чтение) — СДАНО: путь принят (PASS), два пробела в ТЕСТЕ названы.** Срез `8c294f5` (`f8b868f` — t2 в своей арене, `INVALID` без дизъюнкции, проба границы, проверки дома; `d08d0cf` — почтовый TU закреплён в бюджете на 62; `3136a0a` — реентерабельный монитор inbox ТОЛЬКО в `lmx_post.lm1`, решение автора «упрощённый java-style synchronized без wait/notify, лок в одном месте»). Снимок `git archive` в scratchpad `n59/`; чужие незакоммиченные правки `lmx_post` / спецификаций в рабочем дереве не читал и не трогал. Набор: `l3_mail_prim: 36 checks`, exit 0, 20 из 20; ДЛИНА ПУТИ проверена, а не принята на веру (предупреждение DeepSeek -57 о падениях при длинном пути exe): тот же exe на путях 89 / 118 / 141 / 167 / 223 символа — 60 из 60 зелёных, этот набор к длине пути не чувствителен. Бюджет GREEN, 4 единицы по 62; `check_docs` 0; `diff --check` 0. ЧЕК-ЛИСТ с мутантами: (1) физическая идентичность — PASS: реестр и записи в хранилище (`l3_thread.lm1:177,180,183`), тест `:269-271` спрашивает диапазоны; мутант «записи в арене» → 4 из 36, первыми прямые проверки. Подделка — простая Structure, отвергается видом домена; настоящая запись примитива по другому адресу исполнилась бы — так задумано ядром. (2) повторная привязка того же Thread — PASS (`:275`; перерегистрация на каждой привязке → 2 падения). (3) атомарный отказ второму Thread того же хранилища — PASS (`l3_thread.lm1:164-167`, присваивание с `:168`; мутанты 14 и 15 падений); путь L3 своего лока не добавляет — монитор остаётся в `lmx_post.lm1`; но `l3_thread_bind` — несинхронизированная проверка-потом-присваивание над семью глобалами процесса, при работающем менеджере две полосы гонялись бы, и по правилу автора лок туда нельзя: привязка должна быть актом настройки на одной полосе, нигде это не записано (по чтению). (4) без текста / числового переключателя / нативного отката / копии — ЧАСТИЧНО: первые три PASS (откат → ровно 4 проверки `native_hits`), «БЕЗ КОПИИ» НЕ ДОКАЗАНО: `mk_take` ставит PRIM на уровне оператора, результат взятия отбрасывается; мутанты «take отдаёт 0» и «take отдаёт СВЕЖУЮ запись вместо письма» выживают 36/36. Код верен (`:102 \out: letter`), но тест не заметит обратного. Исправление: потребить результат take и сверить с адресом доставленного письма. Там же: страж владельца `l3_thread.lm1:57` не достижим ни одним тестом (мутант выживает). (5) отмена запроса режима проваленным ходом — PASS с оговоркой: механизм `lmx_thread_mode_end` (на FAILED `requested_mode: current_mode`), проба — после проваленного хода текущий 2, ЗАПРОШЕННЫЙ 2; мутант «запрос остаётся» ловится 6 проверками, но НЕ проверкой `:302` «failed turn cancelled the native request» — она читает ТЕКУЩИЙ режим, который ожидающий запрос не меняет; `:302` ловит лишь обратный мутант («проваленный ход фиксирует запрос», 8 падений). Есть публичный читатель `lmx_thread_requested_mode` (`lmx_thread.h.lm1:182`) — прямая проверка в одну строку. Перенятые приёмы проверены по существу: −1 в хелпере, проба границы УТВЕРЖДАЕТСЯ трижды, `mk_bad_arity` исполняется, хороший ход после проваленного, отладочных печатей нет, проверка `LMX_TYPE_INT` на полосу с отрицательным случаем. Отчёт — `lmx_uds`, msg `02d3f32a-1e82-4c83-b35e-776c0301fa5c`.

**Инвариант «Thread ↔ арена» (`FABLE-THREAD-ARENA-INVARIANT-AUDIT-20260921-56`, Codex, только чтение; моя заметка ядру из -46, выделенная в работу) — СДАНО.** Вердикт -46 принят, Grok чинит почтовый набор тикетом -55 (его патч не дублировать). ИНВАРИАНТ: каждая запись `LmxThread` / `LmxMsg` / `LmxPost` / `LmxSchedule`, взятая из арены, принадлежит ОДНОМУ Thread, чья запись расписания называет эту арену своей полосой; второй Thread получает свою арену, привязанную к тому же хранилищу. ОН ЗАПИСАН, а не только наблюдается: `docs/LMX_semantics.ru.md:751` (§21) «Каждый Message … владеет одной логической ареной изменяемых данных», `:791` (§23) «Одна арена имеет одну полосу записи. Обработчик, локальное управление, планировщик и служебные данные Message изменяются на его полосе», `:807` (§24) новый исполняющийся Message — «и его отдельную арену», `:759` / `:761` — в конце такта собирается «арена владельца», «сбор не обходит чужую изменяемую арену» (EN — те же строки); в заголовках ядра: `lmx_child.h.lm1:6-8`, `lmx_schedule.h.lm1:31-37`, `lmx_turn.h.lm1:10,15`. Вариант «корни GC включают состояние всех Thread» отвергнут не только ценой — он узаконил бы две полосы записи на одной арене вопреки §23. СОБЛЮДАЕТСЯ построением: `lmx_child.lm1:47,60` (своя `child_arena`, в арене родителя только резервация `:102`), `lmx_root.lm1:73,156,161,174`. НЕ ПРОВЕРЯЕТСЯ публичными примитивами: `lmx_thread_attach`, `lmx_post_init`, `lmx_schedule_init` («The band is written HERE and nowhere else» — и пишется безусловно); у `struct LmxArena` нет поля владельца, проверять сегодня нечем. НАРУШАЕТ одно место в дереве (обзор всех `.lm1` под `dev/`, по аргументам вызовов, приблизительный): `l3_mail_prim_selftest.lm1`; `lmx_domain_selftest.lm1` (+ копия mixa) кладёт 3 ящика и 2 записи расписания на одну арену, но это локальные по значению и сборка там не идёт — заденет только отказ при построении. ПОЧЕМУ НАРУШЕНИЕ ОПАСНО (отладчик): SIGSEGV в `lmx_gc_block_live`, `pool = &t2->mail->nodes`, содержимое `0xfeeefeee…` (заполнение освобождённой кучи Windows); цепочка: `lmx_post_init` → `lmx_pool_open` → `lmx_arena_array` кладёт в `arena\arrays` АДРЕС дескриптора пула, ВСТРОЕННОГО в ячейку `LmxPost`; `lmx_gc_mark` укореняет только `t\mail` собирающего Thread; чужая ячейка сметается, инвентарь хранит адрес внутри неё. При инварианте невозможно: единственный ящик арены — ящик владельца, он корень. ВОСПРОИЗВЕДЕНИЕ ТОЛЬКО НА ЯДРЕ (без L3, нативные ходы, 20 запусков): два Thread в ОДНОЙ арене — 20 из 20 падений, и ПЕРВЫЙ ход t2 = `LMX_THREAD_STOPPED` в 20 из 20 (сборка t1 смела запись Message у t2, `running = 0`) — второе, независимое подтверждение, что STOPPED из -46 есть вид сметённой записи, а не состояние жизненного цикла; два Thread в СВОИХ аренах — 20 из 20 exit 0, по три хода OK, после сборок ящик t2 принимает письмо, счёт 1. ФОРМА РЕГРЕССИИ (без UB): одно хранилище, две арены, по Thread на арену; свидетель по диапазону адресов `t1\slot\arena != t2\slot\arena`, `lmx_range_pool(t1\slot\arena, t2) = 0` и `…, t2\mail) = 0` + зеркально; поведение — три хода каждому, затем accept / count = 1. Расклад с общей ареной тестом НЕ писать: это UB, и утверждение о нём — как раз то, как STOPPED попал в «свойства». ГДЕ ЧИНИТЬ: нарушение — только почтовый набор (-55); ядру для корректности ничего не нужно; НЕОБЯЗАТЕЛЬНОЕ усиление `LMX-BAND-SINGLE-OWNER` — отказ в `lmx_schedule_init` (единственный акт, пишущий полосу) через поле владельца полосы в `LmxArena`; цена: смена раскладки записи ядра, правка `lmx_domain_selftest` (+ mixa), имён типов в заголовках не добавляет (бюджет L3 остаётся 62); не прототипировал — решение владельца ядра. МОЯ ЛОВУШКА, третий раз за сутки: `\n` и `\a` внутри heredoc инструмента Bash портят патч (получается реальный перевод строки / символ BEL) — приватные патчи с обратной косой чертой писать ТОЛЬКО файлом через Write. Отчёт — `lmx_uds`, msg `73e3af0c-d21b-4086-a62b-12d6bc4df3da`; свидетельства — scratchpad `n56/`.

**Приёмка почтовых примитивов L3 Grok (`FABLE-L3-MAIL-PRIMITIVE-ACCEPT-20260921-46`, Codex, только чтение) — СДАНО, вердикт FAIL.** Коммит `3898af9` (`l3_thread.lm1`, `l3_thread.h.lm1`, новый `tests/l3_mail_prim_selftest.lm1`, регистрация в раннере); аудит на снимке `git archive 5e2ccd1` в scratchpad (`n46/`), входы побайтно равны `3898af9`. ГЛАВНОЕ: набор НЕ ИСПОЛНЯЕТСЯ — segfault (exit `3221225477` = `0xC0000005`) в 20 из 20 запусков, и СОБСТВЕННЫЙ исполняемый файл Grok с записанным `selftest_exit 0` / «27 checks» / `impostor st=2` (`dev/l3_interp/build/l3_mail_prim_selftest/`, SHA `e60b4e8e…`) падает так же, 20 из 20; исходники отличаются только концами строк. Зелёный прогон был одной удачей неопределённого поведения. gdb (приватная сборка `-g`): SIGSEGV в `lmx_gc_block_live` (`chunk->mark`) ← `lmx_gc_sweep` ← `lmx_gc_collect` ← `lmx_turn_end_status` ← `lmx_thread_turn` ← `run_graph` — сборка на границе ПЕРВОГО интерпретируемого хода. ПРИЧИНА, подтверждена вариантом в одну строку: тест строит второй Thread В АРЕНЕ ПЕРВОГО (`mk_thread(a, dummy, @ t2)`, `:289`); записи t2 никем из корней t не достижимы и сметаются, а пул его почтового ящика остался в инвентаре арены с записью chunk в сметённой памяти. Дать t2 СВОЮ арену, привязанную к тому же хранилищу, — 10 из 10 exit 0, 27 проверок (в моём `l3_thread_bind_selftest` лишние Thread именно поэтому в своих аренах). Замечание ядру (не этот тикет): два Thread в одной арене ничем не отвергаются. ОСТАЛЬНЫЕ КОМАНДЫ: раннер по умолчанию — 9 наборов зелёные (6, 20, 6, 6, 20, 28, 6, 48, 45), почтовый падает, итог ненулевой, НЕ «all 10 suites ok»; бюджет GREEN 62×3 (у самого почтового TU тоже 62, но в `EXPECT` его нет); `check_docs` 0; `diff --check` 0. ПУНКТЫ (по чтению кода и мутантам на исправленной основе — это свидетельство о коде, не о закоммиченном наборе): (1) роли/записи в хранилище — PASS, тонко: для ролей есть проверка дома по диапазону, для записей примитивов только вид домена; мутант «реестр и записи в арене» ловится лишь поздним поведением (2 из 27); (2) владение Thread — PASS (владелец 0 → 6 падений); (3) атомарный отказ второму Thread — код PASS (отказ после присваивания → 9, без отказа → 10), свидетельство в закоммиченном виде FAIL: именно его построение роняет набор; следствие — отвязки нет, ОДИН интерпретируемый Thread на хранилище навсегда; (4) без имён / числового переключателя / нативного отката — PASS (откат → ровно «impostor did not call native»), ДЕФЕКТ по чтению: `l3_prim_lane` (`:64-66`) проверяет `KIND_PRIMITIVE` и читает `lmx_int_value_known` без проверки `LMX_TYPE_INT` (в незакоммиченной версии времён -33 проверка была); (5) бизнес-ноль ≠ сбой — PASS (ноль как сбой → 2 падения); мутант «ноль не записывается» выживает 27/27 — эквивалентный: временная ячейка потребителя предобнулена; (6) граница жизненного цикла — FAIL как заявлено: `LMX_THREAD_STOPPED` возвращает только `lmx_thread_enter` (`running = 0` / `success != 0` / DONE), то есть при st=2 ход ВООБЩЕ НЕ ВОШЁЛ и подделка не исполнялась; на безопасной основе измерено: подделка ПОСЛЕ удачного взятия — st=1, граница отработала (проба 1 → 0), inbox 0, mode 2, running=1; ДО взятия — st=1; неверная арность после взятия и после подделки — st=1; ХОРОШИЙ ход после двух проваленных — st=0, running=1; `impostor st=1` в 10 из 10. Зависимости от порядка нет, «проваленный ход, потом OK на том же объекте» выполняется; st=2 — так выглядит испорченное состояние Thread. Проверка `:327` принимает `INVALID || STOPPED` — дизъюнкция, подогнанная под наблюдение. ПЕРЕНЯТО НОМИНАЛЬНО: `probe` / `probe_before` заведены и нигде не проверяются; `mk_bad_arity` определён и не вызывается; −1 в `run_graph` перенят верно; в коммите осталось ~20 отладочных печатей. Предложен тикет `L3-MAIL-PRIM-SUITE-REPAIR` (тест + проверка типа в `l3_prim_lane` + закрепить почтовый набор в `EXPECT`), приёмка — 20 запусков из 20 зелёные. УРОК ДЛЯ МЕНЯ: исполняемый файл «зелёного» прогона стоит перезапускать много раз — один зелёный запуск ничего не значит при UB. Отчёт — `lmx_uds`, msg `72c63370-8cf2-4d5c-b697-3929a996ace8`.

**Сброс бюджета имён типов L3 (`FABLE-L3-TYPE-BUDGET-SHED-20260921-36`, Codex; исполнение моего анализа -33, заморозка L3 снята только для меня) — СДЕЛАНО, `aa47a8e` в `origin/main`, девять путей.** (1) `l3_thread.h.lm1` больше не подключает `l3_recv.h.lm1`: из 27 его символов (3 struct, 16 define, 8 прототипов) ни один не встречается в `l3_thread.h.lm1` / `l3_thread.lm1`; −4 имени. (2) Четыре проверки `l3_admit` (три «provisional helper» + «analytic OK without tests is not admission»), хелперы `mk_path1` / `mk_recv` и predef `l3_admit.lm1` ушли из N10 в НОВЫЙ `tests/l3_admit_selftest.lm1` (без Thread, 6 проверок). Положительный допуск сегодня возможен — `l3_04_selftest.lm1:154` уже его проверяет, но тестом «только return», который допускает ЛЮБОГО кандидата; новый контроль — тест `EXPECT_INT` по используемому пути: кандидат с 32 допущен, ТОТ ЖЕ ресивер отказывает кандидату с 64. Пересечение с `l3_04` доложено Codex (слияние — его решение). (3) Три проверки хода с подделкой ждали `LMX_THREAD_OK`; после `4ab2877` — `LMX_THREAD_INVALID`. Две ловушки закрыты: `LMX_THREAD_INVALID = 1`, и собственные отказы `run_graph` тоже возвращали 1 — теперь −1; INVALID возвращает и ход, который НЕ ВОШЁЛ, поэтому добавлен свидетель «граница отработала»: int-ячейка без ссылок PRIMITIVE до хода и NONE после. (4) `tools/l3_type_budget.py`: число имён спрашивает у закреплённого транслятора (приём с заголовком-подкладкой, имён = 64 − K), байты считает модель, счёты обязаны совпасть; падает при ЛЮБОМ отклонении от 62 (раньше обрыва), при отказе транслятора, при расхождении модели, при байтах у предела; всё во временном каталоге; входит в `run_l3_selftest.py` без `--test`. В `EXPECT` только три единицы у края (admit-набор измерен — 25 — и не закреплён намеренно). (5) Итог, все exit 0: по умолчанию `l3_01` 6, `l3_02` 20, `l3_03` 6, `l3_04` 6, `l3_recipes` 20, `l3_thread_bind` 28, `l3_admit` 6 → `all 7 suites ok; type budget ok (3 units)`; `l3_n9_walk` 48 (46 + 2 свидетеля границы), `l3_n10_walk` 45 (48 − 4 + 1; Codex ждал 46 и 44 — расхождение объяснено, не подогнано); бюджет 62/62/62; исполняемые L2 из последнего гейта (`build/l2src/20260921_004243`, собраны из `4ab2877`, ядро с тех пор не менялось): `lmx_primitive` 34, `lmx_primitive_thread` 27, `lmx_thread_turn` 57; `check_docs` 0, `diff --check` 0. Закреплённый гейт не запускал (L3 не собирает). (6) Правило для авторов ядра — в `dev/README.md` (единственный путь вне `dev/l3_interp` + `tools`, доложено), подробности — `dev/l3_interp/README.md`, раздел «Бюджет имён типов». КОНТРОЛИ (приватно, все красные как задумано): +1 тип в заголовке → 63, RED, пока единица ещё транслируется; +3 или возвращённый predef → «over the cliff» с числом модели; статус снова отброшен → ровно проверки статуса (N9 2, N10 1); граница пропущена → ровно свидетели границы (2 и 1); `l3_run_tests` не исполняет тесты → ровно проверка кандидата с 64; ядро, читающее код роли, → N10 5 из 45, изолированный путь Thread 2 из 43. ОСТАЛОСЬ: N9 и N10 по-прежнему вне `ALL_TESTS` (тикет регистрировал только admit) — их замыкания бюджет проверяет, сами проверки идут только по имени; запас 2 — отсрочка; 24 из 62 имён — typedef'ы API-таблиц, встроенных в записи ядра по значению. ИНЦИДЕНТ: во время тикета Grok раскрыл таймер-наблюдатель, раз в 5 минут делавший `git checkout HEAD -- dev/l3_interp` (он и стёр чужой WIP в 01:00:37); по предупреждению `lmx_uds` я скопировал незакоммиченные файлы в scratchpad (`n36/backup_011346`), потерь нет, наблюдатель для этого пути отключён. Отчёт — `lmx_uds`, msg `b6f54cca-9ab4-4d64-8217-a9c0fcc67a77`.

**Предел имён типов l1trans и наборы L3 (`FABLE-L3-PREDEF-CAP-ANALYSIS-20260921-33`, Codex, только чтение) — СДАНО.** После `4ab2877` (deepseek: примитивы L2, девятый `predef: "l2src/lmx_primitive.h.lm1"` в `lmx_walk.lm1`) все три набора с Thread (`l3_thread_bind`, `l3_n9_walk`, `l3_n10_walk`) не транслируются: `l2src/lmx_manager.h.lm1:45:1: too many header type names`; закреплённый гейт L2 этого не видит — он `dev/l3_interp` не собирает. ИЗМЕРЕНО НА ЗАКРЕПЛЁННОМ БИНАРНИКЕ (приём: приватная единица predef'ит замыкание + заголовок из K фиктивных `fnptr:`; имён = 64 − K): сработал лимит ЧИСЛА (64), не байтов (вся таблица 845 из 4096); ДО `4ab2877` TU был заполнен РОВНО 64 из 64 (запас 0, а не 2), после — 66. Модель таблицы (`closure.py`: обход predef в глубину, единица один раз, struct/enum/fnptr/foreign/type в заголовках, голое имя predef — относительно включающего файла, BOM) совпала с транслятором на обоих деревьях и даёт 65-м именем именно `LmxManagerRound` `:45`. Семантика (`l1src/l1trans.lm1`): `l1_hdr_type_add` `:5520`, сброс раз на транслируемую единицу `:6790`, накопление по всему замыканию; тела единиц struct/fnptr объявлять не могут. Состав 66: `lmx_thread.h` 14, `lmx.h` 11, `lmx_manager.h` 9, `lmx_post.h` 7, `lmx_walk.h` 4, `l3_recv.h` 3, далее по 2 и 1; 24 из 66 — typedef'ы API-таблиц, которые используют только своя единица и `lmx_domain_selftest`, но `LmxManagerApi: api` — поле ПО ЗНАЧЕНИЮ в `struct LmxManager`, дёшево не вынести (решение владельца модели, не предлагал). ВАРИАНТЫ СО СТОРОНЫ WALK по арифметике: убрать predef целиком — 64/64/64, запас 0; обратный вызов в `LmxWalkContext` с новым fnptr — 65, не влезает; «инверсия владения» — 66 и struct всё равно нужен; аксессоры с непрозрачной записью — в ОДНОМ TU раннера L3 тело `lmx_primitive.lm1` возвращает оба имени. Единственная форма, проходящая все ограничения, — вынос в отдельную единицу `lmx_walk_prim.lm1` (+ прототип `lmx_walk_eternal`), но одно-TU сборкам тогда нужна заглушка вне `dev/l2src_sandbox` (иначе «multiple definition» резолвера гейта, `build_l2src.ps1:315-328`), и это НЕДОЛГОВЕЧНО: как только `l3_thread` линкует примитивы, оба имени возвращаются. НЕ рекомендовал как исправление. РЕКОМЕНДОВАНО И ИЗМЕРЕНО на приватной копии `bc9ff76` — сброс на стороне L3, девятый predef остаётся: (i) `l3_thread.h.lm1` predef'ит `l3_recv.h.lm1` и ничего из него не использует (−4: `L3Frame`, `L3Result`, `L3Seen`, `LmxOwn`); (ii) N10 один тянет `l3_admit.lm1` ради 4 проверок — вынести их в отдельный набор без Thread. После обоих: счёт транслятора 62 и 62, запас 2 (с примитивами в `l3_thread` или без — walk уже несёт оба имени). ВСКРЫТО: как только предел снят, `l3_thread_bind` 28/28, а N9 даёт 2 падения из 46 и N10 1 из 44 — все «…turn still ends (dispatch status is dropped)»: `4ab2877` изменил `lmx_thread_turn` (статус тела сохраняется, проваленное тело → `LMX_THREAD_INVALID`), то есть за «122 незапускаемыми проверками» прятались 3 уже НЕВЕРНЫЕ; новое поведение лучше, проверки надо перевести на `= c.LMX_THREAD_INVALID`; мой комментарий в `l3_thread.lm1` («returned LMX_THREAD_OK and did nothing») стал историей. Предложен тикет `L3-TYPE-BUDGET-SHED` (только `dev/l3_interp`) + запись счёта имён на TU как регрессия + правило: изменение ядра, добавляющее тип в заголовок, обязано прогонять наборы L3. ДИСЦИПЛИНА: все прогоны — из `git archive` снимков в scratchpad (`n33/`), в `dev/l3_interp` не писал даже свидетельств. ПОПУТНО: `lmx_uds` спросил всех о семи неопознанных изменённых файлах в `dev/l3_interp` (работа по почтовым примитивам); я ответил отказом с доказательствами (мои единственные правки там — `bde9df8`; к 01:02 дерево снова чистое, у всех семи один mtime 01:00:37 — кто-то их восстановил, не я; кто-то в это же время гонял `l3_mail_prim_selftest`). Отчёт — `lmx_uds`, msg `9966ab28-50ec-41d8-94f7-05d24ec211f6`; ответ о владении — msg `e714a338-514a-45c8-95d2-74b907138733`.

**Записи ролей L3 — в привязанном постоянном хранилище (`FABLE-L3-THREAD-ROLES-IN-STORE-20260921-21`, Codex; исполнение моего аудита -14, владение четырьмя файлами Codex отдал мне) — СДЕЛАНО, `bde9df8` в `origin/main`, ровно четыре пути: `dev/l3_interp/l3_thread.lm1`, `tests/l3_thread_bind_selftest.lm1`, `tests/l3_n9_walk_selftest.lm1`, `tests/l3_n10_walk_selftest.lm1`; один проход и ОДИН коммит, чтобы ни одно закоммиченное состояние не держало одновременно роли в хранилище и обнуляющий обходной приём (с несобираемым хранилищем он тёк бы набором ролей за ход).** Единица: `store: lmx_perm_store_of(arena)`; `store = 0` → INVALID; `l3_bound_store != 0 && != store` → INVALID; ОБА отказа стоят ДО `l3_bound_t: t` — отказанная привязка ничего не присваивает и не отвязывает привязанный поток (прежний код присваивал `l3_bound_t` первым); таблицы открываются в `store` один раз; новый глобал `l3_bound_store`, `predef: "l2src/lmx_perm.h.lm1"`, комментарий в заголовке; диспетч не менялся. Тесты привязывают и закрывают хранилище, обнуление и перепривязка удалены из N9 и N10. Доказательства в тестах: дом таблиц и ЗАПИСИ = хранилище по диапазону адресов (`lmx_range_home = @store && lmx_range_pool = 0`), те же таблица / RET / SET через два настоящих хода, тот же граф публикует 7 на втором ходу при `native_hits = 1`, повторная привязка даёт те же адреса, отказ без хранилища (до и после удачной привязки) и с чужим хранилищем (привязанный поток сохраняет таблицы). Результаты: `l3_thread_bind` 14 → 28, `l3_n9_walk` 42 → 46, `l3_n10_walk` 34 → 48, все exit 0; шесть наборов по умолчанию ok; `check_docs` 0; `diff --check` 0. Закреплённый гейт L2 НЕ запускал намеренно — он `dev/l3_interp` не собирает (так и велел Codex). МУТАЦИИ (приватно, меняется только `l3_thread.lm1`), все убиты: прежняя единица против новых тестов — 10 из 28 / 5 из 46 / 7 из 48 (в N9 падают и три ПОВЕДЕНЧЕСКИЕ проверки if / while / return — без обходного приёма старая единица строит следующие программы на мёртвых ролях); присваивание до отказа — 2; нет отказа чужому хранилищу — 3; переоткрытие на каждой привязке — 3; арена без хранилища откатывается на арену — 14. Все восемь мутантов ревью -16 на ПРАВЛЕНОМ N10 дают те же исходы (проверка живости кандидата теперь `:406`, по-прежнему падает на до-ходовом `cand`). ПОПРАВКА К МОИМ -10 / -14: я писал, что причина обходного приёма «нигде не названа» — неверно для N9: в его `run_graph` был двухстрочный комментарий («End-turn GC can collect the roles table…»), копия в N10 его потеряла, а я искал по .md и читал только N10; автор N9 причину знал, не были измерены следствие (молчаливый второй ход) и правильный дом. ОГРЕХ ПРОЦЕССА: коммит сначала создан с заглушкой вместо сообщения, заменён `git commit --amend` ДО первого push; опубликован только `bde9df8`. Команды с `\n` внутри heredoc инструмента Bash превращают `\\n` в настоящий перевод строки — строковые литералы с `\n` вносить инструментом Edit. ПРЕДЕЛЫ (не трогал): глобалы процесса с одним привязанным потоком; `l3_bound_store` не очищается при закрытии хранилища; при сбое `lmx_plan_roles_open` после удачного `lmx_walk_roles_open` открытая таблица сохраняется, привязка даёт INVALID. Решение Codex по -16: N10 Slice 1 ФИНАЛЕН в заявленном объёме; «mutation review should be the default for future negative-proof tests»; Slice 2 запрещён до независимого ревью -21. Отчёт — `lmx_uds`, msg `1347549a-c4ca-4eac-8f8e-8f356b7e7639`; мутанты — scratchpad `n21/`, `n21b/`.

**Финальное ревью N10 slice 1 после исправления Grok (`FABLE-L3-N10-SLICE1-FINAL-20260921-16`, Codex, только чтение) — СДАНО, вердикт PASS: на `b138a9f+6e1a9eb` все четыре требуемых различения ПОКАЗАНЫ мутацией.** Всё собрано из закоммиченного файла (`dev/l3_interp` + `dev/l2src_sandbox` идентичны `6e1a9eb`, тест `fa5806bc…`), свежий каталог scratchpad `n16/`, восемь приватных вариантов по одной правке; настоящий прогон — `l3_n10_walk: 34 checks`, exit 0; `check_docs` 0, `diff --check` 0. (1) Субъект: метод теперь `SET own <- OF(ARG 0, 0)`; первый субъект = чужая Structure → 1 из 34 (`:330`), первый вызов без аргументов → 2 (`:329`, `:330`), второй без аргументов → 2 (`:333`, `:334`), «другой» субъект = тот же кандидат → 1 (`:334`); против `b138a9f` первые два давали 28/28. (2) Подделка с кодом SET: настоящее ядро 34/34; мутация «роль узнаётся и по коду» → 4 из 34 (`:358`, `:359`, `:360`, `:362`). ПРОВЕРЕННАЯ СВЯЗКА: под мутацией прямой `lmx_interp_run` (`:359`) уже публикует 7 в тот же граф, поэтому `:362` сам по себе ничего бы не значил — убрал оба прямых вызова, и ход Thread первым исполняет подделку → 1 из 32, ровно `:362`: свидетель на пути Thread различает САМ. (3) Кандидат жив: `live` создаётся после последнего хода; проба — `live` вид 6, до-ходовые `cand` / `cand64` вид 0; проверка, направленная на `cand`, падает (`:371`). Предел: «вид != NONE» ловит мёртвого кандидата, только пока ячейка не переиспользована; живым его делает ПОРЯДОК создания. (4) Метки: `:32`, `:301`, `:308`, `:309` — «provisional helper … not final implements»; ничто не заявляет финальной семантики; остаток — при зелёном прогоне печатается только `34 checks`. НЕ ВХОДИТ В PASS: обходной приём с ролями всё ещё в файле (`:221-223`) — это -14 / -21; у `l3_admit` нет положительного контроля и он исполняет через `l3_exec`; `native_hits = 1` прочитан, не мутирован. Отчёт — `lmx_uds`, msg `82cee2ef-f1ad-4ab0-ab7c-22c30b4a1fb8`.

**Срок жизни записей ролей L3 (`FABLE-L3-ROLE-LIFETIME-AUDIT-20260921-14`, Codex, только чтение; мои находки A/B из ревью -10, выделенные в отдельную работу) — СДАНО: причина найдена, исправление измерено на приватной копии, блокирующего решения по слотам R0 НЕТ.** Вердикт -10 Codex принял (`b138a9f` остаётся предварительным, Grok получил тикет на исправление; в рабочем дереве лежит его незакоммиченный ` M dev/l3_interp/tests/l3_n10_walk_selftest.lm1` — не трогать, номера строк ниже даны по `b138a9f`). ПРИЧИНА: `l3_thread_bind` (`dev/l3_interp/l3_thread.lm1:72-91`) открывает обе таблицы в арене Message потока (`arena: t\slot\arena`; `:84` `lmx_walk_roles_open(arena)`, `:87` `lmx_plan_roles_open(arena)`), а единственная ссылка на них — пара глобальных переменных единицы (`:14-15`); корни сборщика на границе хода (`lmx_gc_mark`, `lmx_gc.lm1:136-186`: t, message, mail, slot, `message\graph`, узлы почты и кольцо входящих, цепочка детей) глобалов не знают, а из графа на ТАБЛИЦУ ничто не ссылается (кадры держат отдельные записи через child[0]) — каждый ход `lmx_turn_end` → `lmx_gc_collect` (`lmx_turn.lm1:147`) сметает таблицы. `l3_thread_bind` открывает только при нуле в глобале, поэтому тесты СНАРУЖИ обнуляют глобалы и перепривязывают (`l3_n9_walk_selftest.lm1:249-250`, `l3_n10_walk_selftest.lm1:207-208`). НОВЫЙ КОНТРОЛЬ: тест `b138a9f` только без этих строк — после хода 1 роли вид 0, ход 2 того же графа возвращает `LMX_THREAD_OK` (статус диспетча отброшен), own-ячейка остаётся 0 — интерпретатор МОЛЧА ничего не сделал; затем 4 падения из 28. ПРАВИЛЬНЫЙ ДОМ УЖЕ ЕСТЬ: привязанное постоянное хранилище арены — `lmx_perm.h.lm1:3-40` (R0 владеет, GC не посещает, принадлежность диапазонам адресов И ЕСТЬ тип `independent: const: immutable`), `lmx_walk.h.lm1:134-136` прямо говорит, что записи ролей живут в хранилище, `tests/lmx_walk_perm_selftest.lm1:357-358` это уже доказывает (роли в хранилище, два привязанных Message, 86 проверок в гейте), `lmx_plan.lm1:159-167` допускает роли хранилища; доступ — `lmx_perm_store_of(t\slot\arena)`, нового пути не нужно. Слова автора — о ветвях `independent: const: immutable` в R0 (`LMX_blog/2026-09-20.md`); что записи ролей относятся к этому классу — утверждение заголовка ядра и моё, помечено в отчёте как вывод. ИЗМЕРЕННОЕ ИСПРАВЛЕНИЕ (приватно: обе таблицы в `lmx_perm_store_of(arena)`, отказ для арены без хранилища; тест привязывает хранилище и не перепривязывает): после хода 1 те же роли, вид 6, не в пуле арены, `lmx_range_home(arena, roles)` = хранилище; ход 2 ТОГО ЖЕ графа — own-ячейка снова 7, `native_hits` 1; все 28 исходных проверок проходят; единицы ядра не меняются. СЛОТЫ R0 НЕ БЛОКИРУЮТ: граф R0 не читается и не пишется, слот unit (слот 10 / INIT-ONCE, у автора) и `lmx_root_eternal_enter` (`fc9dcfa`, только проект, НЕ использован) не нужны; открытым остаётся лишь конечное состояние — роли как вечные константы, выданные `l2trans` и занесённые в удержание R0. НЕ СКРЫТО: глобалы остаются глобалами процесса с одним привязанным потоком (привязка второго делает диспетч первого INVALID — было и раньше); раз хранилище не собирается, набор ролей открывать ОДИН раз на хранилище — оставленный обходной приём тёк бы набором ролей за ход, поэтому его удаление входит в исправление; `l3_plan.lm1:40` открывает роли плана в арене на вызов (путь `l3_exec`), через ход не держит — не затронут. ПРЕДЛОЖЕННЫЙ ТИКЕТ `L3-THREAD-ROLES-IN-STORE`, ровно 4 файла в `dev/l3_interp`, ПОСЛЕ исправления Grok: `l3_thread.lm1` (таблицы в `store`, `store = 0` → INVALID до присваиваний, новый глобал `l3_bound_store`, привязка с другим хранилищем → INVALID, `predef: "l2src/lmx_perm.h.lm1"`), `tests/l3_thread_bind_selftest.lm1`, `tests/l3_n9_walk_selftest.lm1`, `tests/l3_n10_walk_selftest.lm1` (ни один сегодня хранилище не привязывает; добавить open/bind/close, в N9/N10 удалить обнуление и перепривязку). Регрессия без UB: дом ролей = хранилище по диапазону адресов (`lmx_range_home`, `lmx_range_pool = 0`), отказ без хранилища, адреса таблицы и записей RET/SET неизменны через настоящий ход, ДВА ХОДА одного графа (7, сброс, снова 7), повторная привязка даёт тот же адрес; отрицательный контроль — сегодняшний `l3_thread.lm1`. Приёмка: три набора + шесть наборов по умолчанию `run_l3_selftest.py`, `check_docs`, `diff --check`; закреплённый гейт L2 `dev/l3_interp` не собирает — свидетельством не является. Отчёт — `lmx_uds`, msg `20e53067-1db8-48d9-ab27-d6e966d8058e`; приватные варианты — scratchpad `n10/{fixstore,nofix}` + `patch_lifetime.py`.

**Ревью N10 slice 1 Grok (`FABLE-L3-N10-SLICE1-REVIEW-20260921-10`, Codex, только чтение) — СДАНО, вердикт FAIL (узкий): коммит `b138a9f` (`dev/l3_interp/tests/l3_n10_walk_selftest.lm1`, 28 проверок, exit 0 — воспроизведено) чистый и зелёный, но 2 из 6 утверждений его проверки НЕ доказывают.** Метод: приватные копии unitroot раннера во временном каталоге, по одной правке на вариант, те же транслятор и флаги gcc; дерево не трогал, гейт не запускал. (1) Идентичность роли по адресу — ДОКАЗАНО, но ровно двумя проверками статуса подделки `:328`, `:329`: ядро (`lmx_walk_role`, `lmx_walk.lm1:85`) сравнивает АДРЕС и ничего не разыменовывает; под приватной мутацией «роль узнаётся ещё и по коду в записи OP» падают именно они (`L3_NODE_RETURN = 1 = LMX_WALK_OP_RET`, поэтому подделка несёт содержимое настоящей записи RET по другому адресу); положительные `:295`, `:310` — тавтологии построения. (2) Кандидат как `args[0]` — НЕ ДОКАЗАНО: метод теста — `RET` без операнда (`:175`), субъект никто не читает; с чужим `args[0]` и вовсе без аргументов — те же 28/28; на пути Thread `l3_thread_dispatch` передаёт `args[0] = message.graph`, кандидат и Thread в одной проверке не встречаются. Исправление: метод должен ПРОЧИТАТЬ `args[0]` (`OP_ARG` / `OP_AT`) и опубликовать поле кандидата в own-ячейку (ждать 32) + отрицательный случай. (3) `native_hits` остаётся 1 — ДОКАЗАНО (`:313`, `:333` после настоящих ходов с `native_noop`; в `lmx_thread_turn` отката нет; `:305` пуста как свидетельство). (4) Подделка: отказ доказан вне Thread, «состояние не изменено» на пути Thread — НЕ ДОКАЗАНО: подделка с кодом RET оставляет ячейку 0 и при отказе, и при принятии, под мутацией `:330`–`:333` проходят. Исправление в одну строку, измерено: `plant_fake(m, 2U, c.LMX_WALK_OP_SET)` — настоящее ядро 28/28, мутированное 4 падения (`:328`, `:329`, `:330`, `:332`). (5) Отказ при пустых тестах — ДОКАЗАНО узко (`tests\len < 1` в `l3_run_tests`), оговорки: `cand` к этому моменту СОБРАН на границе хода (вид 6 → 0, измерено) и висит, на этом пути не разыменовывается; нет положительного контроля (`l3_admit` нигде не даёт `L3_OK`); `l3_run_tests` исполняет через `l3_exec_run_on` (L3_NODE), то есть сам `l3_admit` НЕ на пути Thread → interp → walk. (6) INT-индексные пути — скрытого утверждения нормы нет (оговорка `:32`, метки «analytic», `:337`), но `:281`, `:288`, `:289` закрепляют помощник как обязательный зелёный; не блокирует. ВНЕ ШЕСТИ, было ещё в N9 (`l3_n9_walk_selftest.lm1:249-250`), нигде в .md не записано: `run_graph` (`:207-209`) СНАРУЖИ обнуляет глобальные `l3_bound_roles` / `l3_bound_plan` единицы `l3_thread.lm1` и перепривязывает после каждого хода — сборка на границе хода освобождает Structure ролей (адрес: вид 6 → 4, ячейка переиспользована; новые записи другие), то есть привязанные роли `l3_thread` висят после одного хода; измеренное следствие — ТОТ ЖЕ граф на втором ходу под новыми ролями даёт `lmx_interp_run = INVALID`; «общие» записи ролей общие только в пределах хода. `check_docs` OK, `diff --check` чисто. Отчёт отправлен `lmx_uds` (msg `4fe3bc4b-051d-451b-9d2e-378aa6a88608`); приватные варианты — в scratchpad сессии `n10/{plain,mutA,probeB,setimp_real,setimp_mut,rerun,notcand,noargs}` (исчезнут вместе с сессией, воспроизводятся по описанию выше). УРОК: «no edits» в тикете ревью относится к ПРЕДМЕТУ ревью, а не к этой записке — её вести всегда.

**Задвоенный формал `node` в ABI throw — АУДИТ (`FABLE-UNIT-MERGE-DUP-PARAM-AUDIT-20260920-04`, Codex, только чтение) И ПРАВКА (`FABLE-L2TRANS-THROW-FORMAL-20260920-05`, Codex) — СДЕЛАНО, `876040a` в `origin/main` (ровно три пути: `l2trans.lm1`, `harness/l2_eternal_driver.lm1`, `tools/l2_harness.ps1`; третий файл Codex принял явно: для `Args[0]=0` массив существует с `len=0`, утверждения об удержании / сборке / втором ходе не исполняются и не появляются — проверено по журналам запуска).** `l2trans.lm1` до правки `74b35a00f0c8aa50…`, после `7c96f4ed1dd97898…`. Закреплённый гейт `tools/build_l2src.ps1 -Run` на рабочем дереве поверх `916c10b` (гейт берёт исходники из `dev/l2src_sandbox`, staged `l2trans.lm1` = `7c96f4ed…`, побайтно мой незакоммиченный файл): GREEN, 224 цели, 224 OK / 0 FAIL / 0 SKIP, exit 0, 56 selftest `ran, exit 0`, свидетельства `build/l2src/20260920_231624` — те же итоги, что у базового прогона `lmx_uds` `20260920_225925` (гейт харнесс не запускает и `harness/` не сканирует, поэтому счёт и не должен был вырасти). Тяжёлый слот возвращён `lmx_uds`; после коммита освобождена Phase B у deepseek. Эта записка — отдельным коммитом заметок, по слову Codex. `l2src/l2trans.lm1` в этом тикете НЕ синхронизировать (слово Codex). Аудит: `l2trans` и `l1trans` на `unit_merge_in_method.lm2` молчат (exit 0, ничего не печатают), падает только `gcc` — 6 ошибок `redefinition of parameter 'node'`. Причина — `l2_emit_sig` в `dev/l2src_sandbox/l2trans.lm1`: первый зарезервированный формал `" (@: Lmx node"` и скрытый формал исполняющего Message `"; @: Lmx node"` под `l2_m_throws[mi] != 0` — оба фиксированные литералы эмиттера, с исходными именами / merge / локальностью не связано (формалы источника идут как `l2_p{mi}_{j}`). Общий дефект всего ABI throw: из 209 транслируемых фикстур сигнатуру throw имеют ровно 3 (`unit_merge_in_method`, `unit_throwing_callable`, `unit_recursion`), задвоены 3/3. Исторически формал назывался `@: LmxMsg process_message` (`FABLE_GRAPH_ABI.txt:782`), литерал `node` уже есть в бэкапе 18.09. Правка: скрытый формал — `l2_msg`; проброс в `l2_prep` печатает `, l2_msg` из метода (`mi >= 0`; замыкание `l2_throws_close` гарантирует, что вызывающий сам throw) и `, node` из входа (`mi < 0`); `l2_out_throw[0]: node` НЕ тронут — что должно быть полезной нагрузкой сбоя, открытый вопрос модели (Codex согласен), строки харнесса пинят его «как есть»; скрытый формал не удалён, порядок ABI / `METHOD.sig` / интернирование не менялись. Третий файл сверх названных: `dev/l2src_sandbox/harness/l2_eternal_driver.lm1` принимает ожидаемое число корней 0 (массив есть и пуст, фазы сборки и второго хода не идут) — у двух фикстур из трёх нет вечных ветвей, а замыкание ядра для линковки несёт только драйвер. `tools/l2_harness.ps1`: три строки `eternal-runs` с запретом обоих видов дубля (`Lmx node; @: Lmx node`, `l2_p2_0; @: Lmx node`) и требованием сигнатур, проброса `l2_msg` из метода и `node` из входа. Измерено: 280 фикстур старым/новым `l2trans` — 209/209 транслируются, все 280 журналов диагностик побайтно те же, отличаются ровно 3 вывода (8 / 3 / 2 строки); поштучно старый L1 → `gcc -c` exit 1 (6 / 2 / 2 ошибки, все — дубль), новый → exit 0; харнесс GREEN, 15 целей, `build/l2_harness/20260920_231434`; `check_docs` OK, `git diff --check` чисто. ЧЕГО ЗАПУСК НЕ ДОКАЗЫВАЕТ: `lmx_thread_dispatch_native` отбрасывает результат входа, поэтому `return: 81`, статус throw 70 и коды формы merge 71..80 немы, процесс выходит с 0 в любом случае. Приватной пробой (scratchpad: `-Dlmx_root_open=l2_probe_root_open`, обёртка входа печатает его результат) измерено, что сам сгенерированный вход вернул 0 во всех трёх программах — сбоя времени выполнения после первой в истории компиляции не обнаружено; в дерево проба не внесена. `l2src/l2trans.lm1` (не sandbox) несёт тот же литерал в строке 11766 и НЕ правился — решает владелец.

**Акт занесения вечной ветви в удержание R0 — АУДИТ И ПРОЕКТ (`FABLE-ROOT-ETERNAL-ENTER-AUDIT-20260920-03`, Codex, только чтение) — СДАНО, записка `dev/graph_interp/ROOT_ETERNAL_ENTER.md`, `fc9dcfa`; исходники не правились.** Факт: ядро принимает в массив удержания ЛЮБУЮ ссылку — его заполняют сырым `lmx_arena_ref_store`, единственная проверка (ёмкость = счёт) стоит в сгенерированном коде, чей `return: 1` теряется в `lmx_thread_dispatch_native`. Проект: `fn: lmx_root_eternal_enter (@: LmxRoot root; size_t: index; @: Lmx branch) int`; проверки по порядку, все до единственной записи: корень открыт → `INVALID` 1; индекс в ёмкости → `LMX_ROOT_ETERNAL_INDEX` 3; Structure ЖИВОГО хранилища ЭТОГО корня по адресу (`lmx_range_home(@ rootrena, branch) = @ root\perm` и вид `STRUCT`) → `..._FOREIGN` 4; `branch
ode = 0` → `..._PARENTED` 5; запись свободна и корень ещё не занесён → `..._TAKEN` 6. Отказ атомарен: запись одна и последняя. Замкнутость ветви по ссылкам намеренно НЕ проверяется (обход самоссылающейся ветви требовал бы записи пометок в хранилище). Отделено: слот unit и хранение результата входа акту не нужны; от `INIT-ONCE` зависит только МЕСТО ВЫЗОВА (акту нужен `@: LmxRoot`, у сегодняшнего входа есть лишь граф) — акт и тесты ядра сажаются независимо, перевод сгенерированного кода идёт вместе с INIT-ONCE. Вариант «от графа» отвергнут. Приёмочные случаи в записке: подделка из арены Message, член с `node != 0`, не-Structure, чужой корень, снятая роль, закрытый корень, три вида дубля, ёмкость/индекс, нули, законная ссылка из ребёнка + сборки; мутации по одной проверке. Состояние гейта на этот момент: последний полный зелёный — `a9a377f` (у `lmx_uds`), поверх него без гейта лежат `ca7ae68`, `f2533c0`, `ab7a770`; свободной памяти 2.6 ГБ, Codex запретил всем запускать гейт.

**Общий резолвер диапазонов `lmx_range_home` (`FABLE-RANGE-HOME-COMMON-20260920-02`, Codex) — СДЕЛАНО, `f2533c0` в `origin/main`; ПОЛНЫЙ ГЕЙТ L2 ОЖИДАЕТ.** На этот тикет Codex передал мне файлы ядра `lmx_range.lm1`, `lmx_graph_copy_owned.lm1` и прототип в `lmx_arena.h.lm1` (владелец `lmx_range` — deepseek — предложение поддержал). Публичная функция одна: `lmx_range_home(arena, p)` — сама арена, её ЖИВОЕ привязанное хранилище или 0; строится на строго локальном `lmx_range_pool`, заданном по очереди двум таблицам; вида не называет, флагов значения не читает. Правило живости записано ОДИН раз — во внутренней `lmx_range_store`; через неё идут `lmx_range_home` и три отката `classify` / `type_of` / `stride`. Откаты НЕ вызывают `lmx_range_home` намеренно (иначе 4 бисекции на адрес хранилища вместо 2 на пути каждого входа интерпретатора). В копировщике все четыре решения идут через `lmx_copy_in_store` → `lmx_range_home`, сырых чтений `\perm` не осталось. `lmx_plan_home` — переадресация (его называет `lmx_interp.lm1:40`; удалить, когда тот перейдёт). `lmx_range_pool` и GC не тронуты. Закрыт реальный пробел: `lmx_perm_close` сначала снимает роль, потом освобождает — в промежутке индекс цел, и старый копировщик считал адреса хранилища терминалами; после освобождения был прав случайно. Тест `lmx_range_kinds_selftest` — 196 проверок (было 177): спрашивает `lmx_range_home` напрямую, новая фаза «роль снята рукой, ничего не освобождено»; ОТРИЦАТЕЛЬНЫЙ КОНТРОЛЬ — тот же тест с копировщиком из `ca7ae68` падает ровно на ней. Мутации на приватных копиях: каждый откат — 14 падений (как до сведения), единое правило живости — 71, `lmx_range_home` без хранилища — 25. Поштучно, всё exit 0: kinds 196, walk_perm 86, walk 104, plan 49, scratch 55, interp_walk 10, callable 6, merge 39, arena attach 20; с отдельным объектом `lmx_perm` (то, что в гейте делает резолвер; скрипт `link_with_perm.py` в scratchpad): perm 60, perm_child 20, child 50, gc 40, root 90; шесть наборов `dev/l3_interp` ok; `check_docs` OK, `diff --check` чисто. ГЕЙТ НЕ ЗАПУСКАЛСЯ: свободно 2.9 ГБ — уровень, на котором хост этой ночью убил три тяжёлых прогона; запустить `tools/build_l2src.ps1 -Run`, когда памяти будет с запасом (мой успешный прогон шёл при 3.9 ГБ) и нет других тяжёлых процессов. Припаркованная `build/fable_parked/range_home/` тем самым исполнена и больше не нужна.

**Квалификатор как тип по диапазону адресов (`FABLE-ADDR-RANGE-KINDS-20260920-01`, Codex) — СДЕЛАНО в пределах своего владения, `ca7ae68` в `origin/main`.** Новый тест `dev/l2src_sandbox/tests/lmx_range_kinds_selftest.lm1` (177 проверок, 14 видов): ТАБЛИЦА, строка = один обычный конструктор, выполненный в хранилище и в привязанном Message (Structure, слоты ссылок, ячейки int/size_t/unsigned, METHOD, LmxCallable, op, роль плана, массив и его хранение, массив ссылок и его хранение, char). На строку: тот же вид/тип/шаг из обоих Message, что из хранилища; у значения Message тот же вид и тип (квалификатор не второй код вида — отличается только ответившая таблица); другой рантайм молчит; pool локален; дом = хранилище против Message; значение хранилища любого вида — терминал копирования только из своего рантайма; один раз: корень и вложенный член одного вида (`node = 0` — факт Structure), ЗАКРЫТОЕ хранилище не отвечает ни на один вопрос. Мутации на приватной копии (чужой `lmx_range.lm1` не тронут): без отката stride / type_of / classify — по 14 падений ровно одного свойства. В моих единицах знание «как хранилище привязано к арене» сведено в ОДНО место — `lmx_plan_home`: `lmx_walk_eternal` спрашивает его, проверка дома ролей больше не читает флаг; поведение то же (walk 104, plan 49, scratch 55, walk_perm 86 и по-прежнему 17 падений при мутации предиката, interp_walk 10). Записка с планом: `dev/graph_interp/ADDR_RANGE_KINDS.md` (механизм, все читатели квалификатора в ядре и в `l2trans` с file:line, соответствие правилам автора, расхождения, вопрос по ABI). Гейт и харнесс НЕ запускал: шёл прогон `lmx_uds`, память на машине низкая (последние зелёные у `lmx_uds`: гейт 223/223 `20260920_213703`, харнесс 12/12 `20260920_214130`, оба на `a9a377f`, то есть ДО моего нового теста). РАСХОЖДЕНИЕ 1: вопрос «адрес принадлежит живому привязанному хранилищу» записан от руки в 8 местах (`lmx_range.lm1` ×3, `lmx_graph_copy_owned.lm1` ×4 — без проверки живости, у меня ×1). deepseek — ВЛАДЕЛЕЦ `lmx_range.lm1` — предложение одной функции общего механизма поддержал и разрешил делать («не жди меня: делай»); копировщик — срез Codex (44D), править только с ним. Правка ПРИПАРКОВАНА вне Git в `build/fable_parked/range_home/` (`apply_range_home.py`, `try_private.py`, `README.txt`): `lmx_range_store` (правило живости один раз) + `lmx_range_home`, три отката через них, две строки прототипов в `lmx_arena.h.lm1`, `lmx_plan_home` — переадресация; вариант `--copier` меняет три положительные записи копировщика. Приватно проверено с `--copier` и без: kinds 177, walk_perm 86, plan 49, walk 104. Не проверено: `lmx_perm_selftest` и всё, чему нужен резолвер гейта. ЧТОБЫ ПОСАДИТЬ: свободное окно для закреплённого гейта, слово владельцев о строках заголовка и о копировщике. ЛОВУШКА: `try_private.py` берёт исходники из каталога свидетельств раннера — после мутационного прогона там лежит МУТИРОВАННАЯ копия, сначала обычный прогон. Вопрос автору по ABI (передан Codex): тип задаёт КАЖДЫЙ из ресиверов `independent:` / `const:` / `immutable:` или только полная цепочка? Сегодня диапазон один и тип один, транслятор отвергает ресивер в одиночку; самостоятельным `const:` / `immutable:` понадобились бы свои диапазоны. Владение: `dev/l3_interp` теперь у openrouter (N9), мои `lmx_walk.*` / `lmx_plan.*` ему править запрещено — недостающий символ придёт ко мне вопросом.

**ОТКРЫТО, НА УДЕРЖАНИИ: однократное построение против повторяемого тела хода (`FABLE-L2-ETERNAL-INIT-ONCE-20260920-01`, Codex; моя находка A).** Граница в эмиттере: `l2trans.lm1:14636` (fputs комментария «Closed-unit devirtualization») — всё до неё в `l2_program_entry` есть построение, после — тело исходного main. ФАКТ, который решил дело: `lmx_turn_end` (`lmx_turn.lm1:147`) собирает арену R0 на каждой границе хода от `message\graph`, а unit ни к чему не прикреплён — измерено пробой: свежая ячейка `size_t` и свежая Structure арены R0 без ссылки из графа R0 до сборки вид 1 / 6, после — 0 / 0, арена 26→22 блока. Значит, unit обязан быть ДОСТИЖИМ из графа R0, иначе тело второго хода читает освобождённую память. Мой выбор «дочерний слот 0 графа R0» Codex ОТКЛОНИЛ (HOLD): слоты 0..7 по ABI — дети; автору задан вопрос о выделенном слоте 10 для unit (`LMX_ROOT_SLOTS` 10→11, явный доступ ядра). `lmx_root.*` не мой. НЕ реализовывать и НЕ коммитить эмиттер с числом; отвечать Codex только при новом блокере. Независимая от номера слота работа СДЕЛАНА и лежит ВНЕ Git в `build/fable_parked/init_once/` (см. `README.txt` там): правка эмиттера `edit_l2t2_placeholder.py` (новая `l2_program_init` — построение + укоренение unit + `return: 0`, вызывается и проверяется обёрткой после `lmx_root_open` до первого хода; `l2_program_entry` — только тело: unit читается из графа, `l2_b%d` главного тела восстанавливаются по слотам `l2_for_uchild` от unit или от объемлющего тела; слот печатается СИМВОЛОМ `c.LMX_ROOT_UNIT_SLOT`, без числа; то же в библиотечной обёртке; `TODO` про `l2_library_unit` оставлен, потому что `LmxRoot` там локальная — иначе висячий корень), расширенный драйвер (`mk_driver2.py`: перехват `lmx_root_turn` и `lmx_msg_poll_escape`, фазы «до первого хода / после первого / после сборки / после второго хода», всё сверяется с состоянием после построения), приватный раннер, замороженные до-правочные программы для отрицательного контроля. Приватно измерено: до правки — exit 1, 8 FAIL, в т.ч. «второй ход САМ ничего не построил в хранилище» (ячейки 6→9) и «каждая запись держит корень» до первого хода; после правки (значение слота только в командной строке приватной сборки) — 8/8 exit 0, 55–87 проверок, второй ход: ячейки 6→6 и 19→19, корни на месте, тело опросило один раз; приватная фикстура с вложенными телами main — 63; сбой построения (слот, которого у сегодняшнего графа нет) — main возвращает 1, ход не запускается, корень закрыт один раз; 280 фикстур — те же 209/71, все 280 диагностик побайтно те же, 32 программы идентичны, в 177 изменённых текст тела входа НЕ изменился. ДОДЕЛАТЬ, когда ядро назовёт слот: проверить, что `lmx_root.h.lm1` определяет `LMX_ROOT_UNIT_SLOT` (или сменить один печатаемый символ), применить правку к дереву, заменить драйвер, добавить в `tools/l2_harness.ps1` два переименования и строку отрицательного контроля, закоммитить замороженную до-правочную программу рядом с драйвером, прогнать харнесс, `check_docs`, `git diff --check`, закреплённый гейт. Факт L1: каждой сгенерированной fn нужен свой блок `external:`.

**Удержание вечных ветвей транслятора в R0 (`FABLE-L2-R0-RETENTION-20260920-01`, Codex; половина `lmx_uds`, переданная мне) — СДЕЛАНО, `ea21c94` в `origin/main`.** Файлы: `dev/l2src_sandbox/l2trans.lm1` (эмиттер `l2_emit_unit` и `l2_emit_library_wrappers`), `tools/l2_harness.ps1` (новый вид строки `eternal-runs`), новый `dev/l2src_sandbox/harness/l2_eternal_driver.lm1`. Проводка: обе формы программы печатают `lmx_root_open(..., 5000U, <l2_ebr_n>U, 0U)` — СЧЁТ корней (`l2_ns_eternal[k]` — индекс, не счёт), ёмкость методов не трогал; вход берёт массив одним чтением ссылки из графа, который ему передают (`lmx_arena_ref_struct(node, c.LMX_ROOT_ETERNAL_SLOT)` — это граф R0, `lmx_thread_dispatch_native` зовёт `entry(t\message\graph)`), проверяет ёмкость = счёт; каждый корень кладётся в свою запись `lmx_arena_ref_store` и читается обратно; корень остаётся `node = 0`; вложенные члены записей не получают; строка долга 49U убрана. Без текста, кодов типа, второго реестра, особого случая GC и правок ядра. Свидетельства на СГЕНЕРИРОВАННЫХ программах без общего резолвера линковки: драйвер несёт замыкание ядра (predef тел), сгенерированный C компилируется без изменений с `-Dmain=l2_generated_main -Dlmx_root_close=l2_driver_root_close`, драйвер смотрит на настоящий корень после хода и до закрытия. Харнесс `powershell -File tools/l2_harness.ps1` — GREEN, 12 целей: branch 40 проверок, two 46, many (70 корней) 52, shape 58, xref 52, array_empty / array_field / merge_site по 34. Отрицательный контроль: программа, сгенерированная ДО правки, под тем же драйвером падает на ёмкости (exit 1). Регрессия по всем 280 `tests/*.lm2` старым и новым `l2trans`: те же 209 транслируются и 71 отказ, все 280 диагностик побайтно те же, 168 программ отличаются одной новой локальной, 32 идентичны, 9 (с вечными ветвями) — ровно ожидаемыми строками. Гейт: закреплённый пином tools/build_l2src.ps1 -Run — GREEN, 223 цели, exit 0, свидетельства build/l2src/20260920_203723 (в него входит unit l2trans); финальный харнесс — build/l2_harness/20260920_204349; check_docs OK, git diff --check чисто. НАЙДЕНО И НЕ ПРАВЛЕНО (передано Codex): (1) повторный `lmx_root_turn` заново исполняет сгенерированный вход — ветви строятся в несобираемом хранилище ещё раз и пере-публикуются НОВЫЕ корни (ячейки хранилища 6→9, 10→16, 282→492, 19→34, 15→26); сгенерированный main делает один ход, поэтому пока латентно; (2) `lmx_thread_dispatch_native` отбрасывает результат входа — собственные `return: 1` входа немы; (3) `unit_merge_in_method.lm2` не компилируется и раньше (`l2_m0(Lmx * node, Lmx * node, ...`); (4) в `dev/l2src_sandbox/LMX_ETERNAL_STORE.txt` (документ `lmx_uds`) строки 28, 177, 196, 241 про `0U, 0U` и «не в массиве удержания» устарели — правит владелец.

**Явный выбор режима (`LMX-EXPLICIT-MODE-SELECT-20260920-15`, автор):** режим исполнения запрашивается только явной операцией L2, никогда не выводится из наличия METHOD/тела; мой срез уже так делает (NO_GRAPH, без отката). ACK отправлен.

Следующие шаги (пересмотрены поправкой выше): (1) дождаться подмножества L3 от Codex и слова Grok по стыковке; (2) расширить срез типами (`size_t`, `char`, указатели) и `header`; (3) главный недостающий кусок — выдача узлов тела в `l2trans` теми же индексами, что у нативного кода (`l2_own_mslot`), это файл Grok — согласовать. Вопросы автору, которые стоит задать коротко: принимает ли он набор кодов из `BODY_NODES.md`; исходный порядок детей M против нынешней нативной раскладки «поля, затем хосты».

## 4. Архив реплик автора — ЗАКРЫТО

Первые две реплики из §2 занесены другим агентом в `LMX_blog/2026-09-20.md` (записи «Бинарный граф без имён» и «Тело метода общее, активация на стеке»), коммит `89a740a`, в `origin/main`. Текст сверен со §2 — дословно совпадает; `lmx_uds` об этом извещён. Третью (слот 0 вместо METHOD) занёс я: запись «Дескрипторы вызываемой Structure в слоте 0», коммит `bfa8c0c`, `check_docs.py` перед коммитом прошёл, в коммит вошёл только этот файл. Повторно ничего не заносить.

Осталось в силе общее предупреждение: в рабочем дереве лежат чужие незакоммиченные правки (`steps/current.md`, `chat_config.md`, `claude_chat/*`, `work_chat/README.md` и др.) — не коммитить чужое; перед своим коммитом — `python tools/check_docs.py`.

**К §3:** после моей остановки Grok выложил `e7dd42b` «dev: treat LmxCallable as a copy terminal; fix interp OK test» — по заголовку он закрыл обе мои находки (терминал копирования и перевёрнутую проверку). Перед отправкой ответа на GROK-CALLABLE прочитать `git show e7dd42b` и убрать из ответа то, что уже исправлено; проверить, закрыт ли отказ на корне копии (`lmx_graph_copy_owned.lm1`, ~447) и судьба корневого массива `ARRAY_OF_METHOD` в `l2trans.lm1:14427-14442`.

## 5. Прокрутка окна вверх (поручение автора)

Автор: окна `deepseek`/`openrouter` прокручиваются вверх, у `fable` и `lmx_uds` — нет. Причина: в `C:\deepseek\deepseek.bat:15` и `C:\openrouter\openrouter.bat:13` стоит `set CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1` (переменная есть в `claude.exe`), в `C:\claude\fable.bat` её не было.

- Сделано: строка добавлена в `C:\claude\fable.bat` после `setlocal`. После перезапуска спросить автора, работает ли прокрутка; если нет — запасной кандидат `CLAUDE_CODE_ALT_SCREEN_FULL_REPAINT` (тоже есть в бинарнике, поведение не проверено).
- `lmx_uds` — это `--bg`-сессия, окно автора — клиент `claude attach` (`claude_chat/uds.py:150-157, 190-197`, цепочка `C:\claude\claude.bat` → `claude_chat/attach.bat`). `lmx_uds` добавил переменную в `claude_chat/attach.bat` (коммит `0166daa`, в `origin/main`); в `cmd_start` не ставил — экран рисует клиент attach. Учитывает ли её `claude attach` — не проверено: увидит автор при следующем detach/attach через `C:\claude\claude.bat`.

## 6. Память сессии

В память (`C:\Users\mtkra\.claude\projects\C--Nyasha-Planet\memory\`) записаны `lmx-project-entry.md` (где проект, порядок чтения, имена сессий) и `lmx-no-names-in-graph.md` (граф бинарный, имён нет, таблицу адрес→имя не использовать; тело метода общее, активации на обычном стеке). Они подгружаются через `MEMORY.md` автоматически; этот файл — источник подробностей по незавершённой работе.
