# sizeof: of a type keyword is C sizeof of that type.
fn: probe () int
    size_t: n
    n: sizeof: int
    if: n != c.sizeof(c.int)
        return: 1
    return: 0
end: probe

sendMessage: exit(exit_code: probe(); stdout: ""; stderr: "")
return

