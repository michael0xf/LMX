# Исследование семантики L3 — 2026-09-20

## Поручение и разделение работы

Codex продолжает парные `docs/LMX_semantics.*.md`. Grok принял LMX-L1-L2-20260920: полная переработка четырёх спецификаций L1/L2 и перенос стабильного кода после консультации с DeepSeek. Его собственный отчёт — `steps/l1-l2-migration.md`. Общие dirty-файлы моста и `steps/current.md` не включать в коммиты Codex этой работы.

Новая семантика строится по понятиям, с одним основным разделом на правило и устойчивыми якорями. Старые описания не имеют автоматического приоритета над авторским вступлением. Автор подтвердил это в [уточнении от 20 сентября](../LMX_blog/2026-09-20.md).

## Срез источников

- `C:/Nyasha_Planet/L1/Lingvamyxa_spec.txt`: SHA-256 `bdb1a6411a47f1cfc9721c1ff002a001cb7fd19cd087ce15fb398f085cc71b53`, 16391 строка.
- `C:/Nyasha_Planet/lingvamyxa/Lingvamyxa_spec.txt`: SHA-256 `d4bb1e70c9d660b185ec084ed4c636ce75bc08dd100786c754401017cade221c`, 15828 строк.
- Оба хеша повторно проверены 20 сентября и совпадают с [сравнением источников](../provenance/spec-comparison.json). Старый файл использован для проверки различий; содержимое нового не считается безусловной нормой.
- HEAD проекта L1 при начале исследования: `b2a7c98aa59aaf9654f2180e6f2a149e58aa48ee`. Другие агенты продолжают работу; это наблюдаемый HEAD, а не утверждение о чистом или стабильном снимке всего дерева.
- `L1/L2_core_18-09-2026-description.md` прямо указывает источником `L1/dev/l2src_sandbox/l2src`. Корневой `L1/l2src` представляет другую стадию; нельзя смешивать их реализации.

## Карта перенесённых правил

| Основной раздел L3 | Основание | Сопоставление с кодом / предел проверки |
| --- | --- | --- |
| `values` | Обе спеки §2, §6.5; актуальная грамматика пустого тела | `L1/l2src/lmx.h.lm1`: `Lmx`, `LmxArrayDesc`, `LmxMethod`; это проверка представления прежнего среза |
| `identity` | Обе спеки §9.1.3, §19.29.2; новая §2 | Различие лексического родства и владения сохранено; реализация `independent` этим исследованием не подтверждается |
| `fields` | Обе спеки §2.3, §21.11 | `Lmx` фиксирует число полей; разрешение повторных имён транслятором ещё не проверено |
| `references` | Обе спеки §21.4, §2.5.3 | `L1/l2src/lmx_ref.lm1`: `lmx_ref_rebind_local`, `lmx_ref_write_through`, `lmx_ref_set_field` различают перепривязку и запись |
| `composition` | Обе спеки §2.3, §9.1.4 | `L1/l2src/lmx_merge_owned.lm1` подтверждает наличие отдельного merge и проверки вечных операндов; полная корректность обхода не проверена |
| `admission` | Дословное вступление LMX и уточнение автора 20 сентября | Старые §2.1/§2.5 не устанавливают обязательный запуск тестов для всякого кандидата; наличие полной реализации запросили у DeepSeek |
| `execution` | Обе спеки §0, §7.1, §21.7–8 | `LmxMethod` отделён от вызываемой структуры в исследованном заголовке; полнота трансляции вызовов не утверждается |
| `publication` | Обе спеки §21.5–6; описание ядра от 18.09 §1a | В sandbox `lmx_dirty.lm1`: `lmx_dirty_mark`, `lmx_dirty_checkpoint` записывают только dirty-ячейки; автоматическая расстановка границ транслятором не проверена |
| `addressing` | Уточнение автора от 20.09 и вступление LMX | В sandbox `lmx_deliver.lm1`: `lmx_deliver_step` принимает физические адреса Thread. Маршрут по предкам требует отдельной проверки; сетевой транспорт не исследован |

Таблица связывает новую семантику с источниками, но не является отчётом о полной конформности ядра. ABI, алгоритмы выделения памяти и исторические реплики не перенесены в текст L3. Проверки ядра не запускались: в этой части работы изменена документация.

## Расхождения и вопросы

1. **Приоритет модели — решено автором.** Обязательная runtime-валидация из вступления сохраняется. Старые частичные проверки совместимости не подменяют её. Условия кэширования результатов, повторной проверки после изменения состояния и конкретное представление тестов пока не определены.
2. **Масштабы адресации — решено автором.** Спека из L1 §2/§19.29.7 описывает цепочку индексов; описание ядра от 18.09 §5–6 — физические адреса Thread. Автор уточнил: промежуточный уровень использует физические адреса, цепочка индексов относится к WorldWideMix начиная с третьего уровня. Это разграничение внесено в `addressing`; алгоритмы маршрутизации не объявлены проверенными.
3. **Срез реализации — уточняет Grok у DeepSeek.** Корневой `l2src/lmx_owned_ranges.lm1` содержит линейный `lmx_owned_ranges_find` и связные списки, тогда как новая §2 требует единую таблицу с бинарным поиском. Описание от 18.09 и sandbox уже называют `LmxArena.index`. Это может быть различием стадий, а не дефектом стабильной ветки; не объявлять результатом аудита текущего ядра до выбора среза.
4. **Полнота проверок кандидатов — запрос DeepSeek L3-CORE-20260920.** В sandbox прочитаны `lmx_implements.h.lm1` и `lmx_implements.lm1`: `lmx_implements` сравнивает kind/type и не читает consumer; `lmx_runtime_implements` вызывает обход, который возвращает YES/NO/UNKNOWN. Это не выполнение юнит-тестов принимающего выражения. Наличие остальных стадий в трансляторе уточняется у DeepSeek.
5. **Именованные пути против позиций — вопрос автору отправлен.** `lmx_implements_walk` берёт один `index` из consumer и читает позиции с этим индексом у обоих операндов. Старые §2.1 исключают сравнение порядка полей и требуют используемые именованные пути. Без подтверждённого преобразования путей в согласованные позиции нельзя считать этот обход реализацией всего старого контракта. Также код трактует любой METHOD на используемом пути как вызываемый, тогда как §2.1 различает перенос и вызов; это дополнительное ограничение данного обхода.

## Ответ DeepSeek и границы проверки

Промежуточный ответ L3-CORE-20260920 получен через `lmx_uds` в 07:16:57 UTC. Текущая сессия DeepSeek уточнила, что занимается портом mixa_manager, а ядро разрабатывала прежняя сессия l1-98, отсутствующая в текущем реестре. Поэтому её ответы о ядре — исследование исходников, а не свидетельство автора реализации. DeepSeek подтвердил расхождение копий l2src и сообщил о зелёном гейте 200 мишеней в sandbox; Codex этот прогон не повторял. Ответ о полной реализации проверок кандидатов остаётся в работе у DeepSeek.

В sandbox дополнительно просмотрен `lmx_range.lm1`: индекс арены действительно хранит интервалы, указывающие на дескрипторы массивов, а корневая версия `lmx_owned_ranges.lm1` использует связные списки. Различие стадий подтверждается самими файлами; переносить корневые списки в новую норму L3 оснований нет.

Первый проход `python tools/check_docs.py` после наполнения RU/EN успешен: сохранено вступление, согласовано число абзацев, проверены ссылки, 28 парных разделов грамматики, 229 исходных выдержек и 615 импортированных файлов. Это проверка документации, не проверка семантической полноты или реализации ядра.

## Продолжение

После ответов сопоставить текущий стабильный код с моделью и подробнее описать динамические входы; затем Message, ходы, доставку, неизменяемость и вечные ветви. Не закреплять старые детали без проверки более поздних авторских решений. Следующие слои: управление и исключения, модели/таблицы, межмашинные сообщения. Объём исходников не позволяет считать десять начальных разделов полной спецификацией L3.
