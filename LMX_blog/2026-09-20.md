# Уточнения автора о языке: 2026-09-20

## Приоритет развивающейся модели

Источник: ответ автора в задаче Codex о продолжении семантики L3. Вопрос касался обязательной runtime-валидации кандидатов из авторского вступления и отличающегося описания валидаторов в прежних спецификациях. Ответ дословно:

```text
да вступление в приоритете --я меняю и дополняю модель в процессе
```

Ответ устанавливает приоритет вступления. Условия повторного использования результатов тестов этим ответом не определены.

## Масштабы адресации

Источник: последующий ответ автора в той же задаче на вопрос о физической и иерархической адресации. Дословный технический фрагмент; последующее указание об оформлении спецификации опущено:

```text
вступление объясняет новые изменения -- на промежуточном уровне адресация по физическим адресам в памяти, цепочка индексов это механизм WorldWideMix начиная с третьего уровня (во вступлении написано).
```

## Проверка по дереву и интерпретатор тестов

Источник: следующее сообщение автора в той же задаче. Дословно, с техническим экранированием пути как в сообщении:

```text
да, аналитическая проверка должна быть как была в C:\Nyasha\_Planet\ lingvamyxa\_prev по дереву, текущая реализация вообще невалидная (она где-то используется?) Далее там будет интерпретатор на графе который будет гонять юнит-тесты
```

## Общая классификация типов арены

Источник: последующее сообщение автора в той же задаче. Дословный технический фрагмент:

```text
классификацию адресов почтовый сервис должен вести по общему механизму классификации типов который есть в арене (должен быть)
```

## Единственный механизм валидации

Источник: последующее сообщение автора. Дословный технический фрагмент; организационное поручение вести общий план опущено:

```text
implements? Да его надополностью переписывать сначала как был в C:\Nyasha\_Planet\ lingvamyxa\_prev  а потом после написания интерпретатора ещё и в ивде валидации юниттестами (все параграфы про валидацию замени в новой спеке этим механизмом кроме него ничего не будет).
```

## Вход интерпретатора

Источник: следующее уточнение автора, дословно:

```text
то есть промежуточный этап там интерпретатор графа (не текста)
```

## Смысл взятия адреса

Источник: уточнение автора в той же задаче о различии адреса данных и адреса рабочей копии. Дословно:

```text
"а в L2 для собственного поля — адрес данных в графе, не его рабочей копии. В документации разделю эти случаи и отдельно отмечу ограничения транслятора, чтобы неполная реализация не выглядела правилом языка.  " -- да но в реальности кто-то берет адрес локальной переменной?  Иначе семантика получается идентичной -- тебе нужен адрес элемента массива и ты его получаешь. Так ведь?
```

## Индексы L2 и L3 и адрес элемента

Источник: следующий ответ автора в той же задаче. Дословно:

```text
"после проверки индекс  " -- почему после проверки индекса? В L2 не должно быть в итоге никаикх проверок индекса, если они есть то надо перенсти в L3 и только там оставить. "`@array[i]`  " это абсолютно коорректная конструкция, просто мы для L2 берем не адрес локальной переменной а адрес значения в графе и далее работает с ним как в Си. Дескриптор массива только в L3 будет проверяться (иначе зачем вообще L2 нужен?)
```

## Бинарный граф без имён

Источник: реплика автора в сессии `fable` 2026-09-20, в ответ на предложение привязывать имена в узлах кода интерпретатора. Дословно:

```text
имена в коде нкак не привязывать -- мы это устали убирать. Интерпретируется бинарный граф и там нет никаикх имен. У нас есть таблица адрес->имя но ее _не использовать_!
```

## Тело метода общее, активация на стеке

Источник: следующая реплика автора в той же сессии на вопрос, копируется ли код вместе с каждой копией вызываемой Structure. Дословно:

```text
-- зачем копировать тело каждый раз? Достаточно стека обычного
```

## Дескрипторы вызываемой Structure в слоте 0

Источник: последующая реплика автора в той же сессии `fable`. «Третий» — третий из вопросов, заданных автору в этой сессии: принимается ли размещение ссылок на заголовок метода и на аргументы `return:` в отдельных слотах `[1]`, `[2]` после METHOD. Дословный технический фрагмент, выдержка; поручение о рабочих записях опущено. Сами инструкции, данные автором Grok, в эту запись не входят:

```text
на третий я grok-у дал инструкици (он уже сделал структуру которая несет в себе все эти дескрпиторы и она будет лежать в 0 вместо Method)
```

## Тело по порядку, return в телах

Источник: реплика автора в сессии Grok 2026-09-20 о раскладке вызываемого графа. Дословно:

```text
по порядку лежит _тело_ . заголовок вы положили в дескриптор. Но return понятно что должен лежать в телах так как их вообще может быт много
```

Следующая реплика автора в той же сессии, дословно:

```text
пускай в дескрипторе лежит ссылка дескритор нативного метода и дерево дескриптора само, а return и все остальное в базовом дереве тела по порядку
```

## L1 — стадия трансляции, не исходники графа

Источник: реплика автора в сессии Grok 2026-09-20. Дословно:

```text
lm1 постепенно надо будет переписывать на lm2 так что L1 не носитель графа а промежуточная стадия трансляции -- в итоге его не должно остаться в исходниках
```

## Поля Lmx лексические, граф не перекладывается

Источник: реплика автора в сессии `fable` 2026-09-20. Вопрос Fable был о порядке полей вызываемой Structure M после слота 0: означает ли «тело — M после слота 0 по порядку» исходный порядок, где поля и операторы перемежаются, при том что транслятор сейчас кладёт сначала собственные поля, затем структуры управляющих тел, и должны ли индексы интерпретатора и нативного кода ставиться одним проходом. Дословно:

```text
каких детей? Поля Lmx задаются лексически и в графе ничего менять не нужно, это при трансляции в нативный код вы можете как что угодно писать
```

Следующая реплика автора в той же сессии, дословно:

```text
то есть порядок строго лексический полей Lmx
```

## Приоритет ссылок в исполняемом графе

Источник: реплика автора в задаче Codex 2026-09-20, в ответ на вопрос о представлении ролей `return`, `call`, `if`, `while` и внутренних числовых тегах интерпретатора. Дословно:

```text
приоритет такой
1\) максимум физических ссылок и
2\) числовые коды везе где нельзя физическую ссылку
3\) текст нигде не нужен
Это ответ?
```

## Активация метода и режим исполнения L3 Thread

Источник: следующая реплика автора в задаче Codex 2026-09-20. Дословно:

```text
проконтролируй что при входе в метод интерпретатор не делает его копию в памяти и ограничивается обычным стеком локальных переменных (тот же признак локальности что унативного кода, те же load и dirty), также нужно записать и реализовать что один L3 Message выполняется по-прежнему только в одном потокe, то есть:
1\) либо нативно
2\)либо интерпретатором
и последнее два -- не "общий режим" а выбранный в endturn режим L3 Thread
```

## Практический критерий хранения активации интерпретатора

Источник: следующая реплика автора в задаче Codex 2026-09-20. Дословно:

```text
"`lmx_walk` сейчас выделяет массивы activation-local `owns/found` через `calloc`; это не чистый обычный стек;  " -- тут главное чтобы работало примерно не хуже
```

## Переносимость реализации интерпретатора в L2

Источник: следующая реплика автора в задаче Codex 2026-09-20. Дословно:

```text
и не забывайте что потом весь это код придется на L2 переписывать -- если вы сразу не заюзаете стандартных механизмов то может быть сложно
```

## Явный выбор режима исполнения

Источник: следующая реплика автора в задаче Codex 2026-09-20. Дословно:

```text
А вот механизм выбора режима исполнения нужно сделать явным потому что мы потом его будем привязывать к implements и тому подобному
```

## Хранение ветвей independent: const: immutable в R0

Источник: реплика автора в задаче Codex 2026-09-20. Дословный технический фрагмент; организационное поручение агентам опущено:

```text
independent: const: immutable: ветки в проекте -- где хранятся (если хранятся) и как оптимизируются. Мы на прошлой неделе обсуждали что они должны храниться в Root Thread ("R0"), так как всегда известны на этапе сборки, сборщик мусора на аренах их не трогает хотя указатели на них могут быть вдругих Message.
```

## Общение только через Message

Источник: реплика автора в сессии `fable` 2026-09-20. Fable сообщил, что шов интерпретируемого исполнения `l3_thread_dispatch(t)` получает только Thread, возвращает один код статуса, и в состоянии Thread нет приёмника для значения, которое вернул метод верхнего уровня хода; затем спросил автора, возвращает ли такой метод значение кому-нибудь. Дословно:

```text
у нас все общение идет черз Message и это работает. Тот кто что-то соощает пишет Message
```

Следующая реплика автора в той же сессии — на слова Fable, что при таком правиле интерпретатору от Thread достаточно «одного доступа к стору и ролям». Дословно:

```text
зачем тебе какой-то доступ елси ты можуешь все спросить у другого потока сообщением?
```

## Значение independent

Источник: реплика автора в задаче Codex 2026-09-20. Дословно:

```text
independent у нас означает что node=0
```

## Квалификаторы как тип, определяемый по диапазону адресов

Источник: следующая реплика автора в задаче Codex 2026-09-20. Дословно:

```text
и не забывайте про общий механизм address range -- все типы можно определять только по вхождениюадреса, не городите гигантский код для простого определения independnt: const: immutable тоже. Эти ресиверы задают неки тип
```
