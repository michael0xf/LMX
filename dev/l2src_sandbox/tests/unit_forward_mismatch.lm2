# Same-unit name with a later definition whose parameter types do not match
# the forward. Bound on translator Structures, not source lines.
fn: later (int: x) int
fn: later (int: x; int: y) int
    return: x + y
end: later
fn: main () int
    return: later(1, 2)
end: main
