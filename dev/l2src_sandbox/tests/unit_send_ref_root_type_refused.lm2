# FABLE-SONNET-SEND-REF-20260925-172 commit 3: `sendMessage: Ref X` at the walked root refuses a
# Ref that is not a reference (l2_rw_send's l2_rw_fields_ty check, ty < 1000) -- measured directly:
# a plain int own field named as Ref is refused at the exact statement, not silently accepted or
# misrouted into the payload.
int: n
n: 5
sendMessage: n exit(exit_code: 0; stdout: ""; stderr: "")
return
