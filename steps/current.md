# LMX documentation-first restart

## Аварийная коррекция ядра от 2026-09-21

Рабочий `dev/l2src_sandbox` приведён к принятой модели: `LmxMsg message` является первым by-value членом `LmxThread`, поэтому адреса Thread и его Message-префикса совпадают; отдельный `LmxMsg` не открывает хвост Thread. Текущий код базового Array ещё имеет `{len, data}`; принят целевой ABI `VoidArray {size_t size, void *data}`, встроенный по значению **первым** в `Lmx {array, parent}` как действительный массив дочерних ссылок с backing в диапазоне арены. Отдельный псевдоним `LmxArrayDesc` в цели не нужен. Перенос кода ещё не выполнен. Ни нынешний, ни целевой базовый Array не получает `capacity`/grow-семантику. Динамическое членство детей реализует отдельный List: его нынешний открытый дескриптор использует пару `{len, data}`, отличается `KIND_LIST`, а ёмкость хранится во внутреннем префиксе backing. Тип `LmxListDesc` полностью удалён из заголовков; поля и API Thread держат непрозрачную физическую ссылку. Нет фиксированных root-слотов, `LmxLink` или второй очереди членства менеджера. R0 остаётся обычным Thread под структурным Thread-родителем.

Аварийная правка Array/List сохранена и опубликована точным checkpoint `7419470`: L2 harness `60/60` без FAIL (`build/l2_harness/20260922_073250`), L3 type budget возвращён к `62/64` и `818/4096`, `check_docs` зелёный. Две последующие комментарные правки сохранены checkpoint `f0afb6a`. Checkpoint `f858cea` убрал зависимость решения «вызов или не вызов» от короткой формы и оставил разрешение головы источником решения; L2 harness зелёный `65/65` (`build/l2_harness/20260922_075940`). Входящая почта публикует под единственным monitor пару физического адреса и donor-арены, а арену присоединяет только принимающий owner при `take`. Отказ закрытия сохраняет pending-пару; settle закрывает почту до unregister/remove/attach/release. Продвижение в стабильный `l2src` выполняется отдельной проверяемой партией.

Актуальный совместный план Codex/Grok: [implementation-plan.md](implementation-plan.md). Поручение от 20.09 о полной замене `implements` и последующей валидации интерпретатором графа расширяет прежний этап documentation-first; последовательность и распределение работ указаны в плане.

- Start from `docs/LMX_semantics.ru.md` / `docs/LMX_semantics.en.md` and the user's current instructions. Do not import old Message/arena/runtime designs as new requirements.
- Keep RU/EN documents synchronized in meaning, order, examples, and cross-references. Preserve the author's Russian semantic opening verbatim.
- Grammar source: `provenance/grammar.json`; render using `python tools/build_docs.py`. Preserve copied examples and their source ranges.
- Author correction 2026-09-23: `f()`, `f: ()`, and explicitly closed `f:` + `---` must already be one P0 Frame/body shape (zero fields); the sole anonymous argument-container is transparent generally, including nonempty bodies. Bare `f:` remains invalid; bare `f` is an executable atom and has the same nullary effect when callable. The 9c166ff/current goldens encode the superseded unequal shapes. See `next_parser_fix.md` for code audit and correction route; do not apply CALL-only transparency or a special declaration exception.
- Imported tests and goldens are historical evidence. Do not rewrite them to hide a disagreement. Current language expectations are separate under `tests/parser/current`.
- Work only in this LMX repository unless the user explicitly requests edits to another project. The initial work is documentation and parser-test migration, not kernel implementation.
- Before committing, run `python tools/check_docs.py`. Report parser conformance separately from historical-regression agreement.

## Текущий план и состояние

- Все правила языка универсальны в пределах своей области. Никаких специальных случаев по имени, типу, форме записи или удобству backend не вводить; предполагаемое исключение либо противоречие немедленно выносить автору с минимальным примером и точными местами кода.
- Рабочие инструкции и планы хранить в `steps/`, поддерживать актуальными; в спецификациях оставлять описание языка.
- Разделение спецификаций: `LMX_semantics.*.md` — семантика L3; `L2_spec_*.md` — специальные низкоуровневые операции L2 и симметричное точное описание ядра на уровне L2; `L1_spec_*.md` — язык L1, перевод в C99 и все существенные детали реализации ядра.
- Для наполнения L1 изучить прежнюю `L1_spec.txt`, текущий проект L1 и все разделы «реализация на Си» прежней `Lingvamyxa_spec.txt`. Код сопоставлять с решениями автора: расхождения фиксировать, не превращать случайное поведение реализации в норму.
- Описание каждого механизма ядра вести симметрично в L2 и L1, со стабильными якорями и взаимными ссылками на соответствующие параграфы. Существенные детали не оставлять только в L1. Эти пары служат руководством для транслятора L2 и порта L1 → L2; при изменении механизма обновлять обе пары RU/EN. Наличие и соответствие текущего транслятора DeepSeek проверить по исходникам, не считать установленным.
- Корпус тестов парсера перенесён: 615 исходных файлов. Исторические прогоны: 131 случай на каждом из двух прежних парсеров. Это не подтверждение соответствия новой грамматике.
- Пустое вертикальное тело: текущая `lm_p0_validate_nonempty_colon_frames*` ошибочно отвергает пустой Frame. Исправление должно восстановить прозрачность sole anonymous argument-container из `lingvamyxa_prev/Lingvamyxa_spec.txt` §4.0.1 и одинаковое нульарное поведение скобочной, короткой и вертикальной форм.
- При дальнейшей работе с P0 сверять детальное поведение Mix-меток, `{#...}` и границ строк с исторической реализацией и тестами; не выдумывать отсутствующие правила.
- После завершённых изменений проверять документацию, делать коммит и push в публичный репозиторий `michael0xf/LMX`.

## Стиль записи тел: `end: <name>`, trailer `return:` и закрытие лестницы (автор, 2026-09-23)

Обязательно для всех агентов при написании фикстур, примеров и генерируемого кода; дословная реплика автора — `LMX_blog/2026-09-23.md`.

1. Форма

```text
fn: firstArg (MainLetter: m) @: char
    return: @ m\mainArgs[1][0]
end: firstArg
```

   допустима по правилам парсера, но это **плохой стиль**: `end: <name>` служит для закрытия **длинных** тел. В таком коротком теле `return:` может быть и **trailer'ом** — то есть стоять вместо `end:`:

```text
fn: firstArg (MainLetter: m) @: char
return: @ m\mainArgs[1][0]
```

2. Парсер **не требует последовательного закрытия каждого уровня**: требуется только финальное закрытие лестницы отступов (`---`, trailer или `end:` на нужном уровне). Это единственное отличие от Python. Не вставлять «закрывающие» `---`/`end:` на каждом промежуточном уровне ради симметрии.

Следствие для инструментов: генераторы фикстур и `tools/l2_s2_migrate_main.py` должны предпочитать trailer-форму для коротких тел и не добавлять `end:` там, где тело закрывает trailer.

## Дефекты — отдельный список, чинить сразу (автор, 2026-09-24)

Правило автора: «дефекты вносите в отдельный список и поправляйте сразу как находите». Список — `steps/defects.md` (дата, анкер/минимальная программа, владелец, статус; закрытые остаются с SHA). Найденный дефект не ждёт стадии плана: координатор выдаёт малый тикет немедленно; пункты `next_core_tasks.md` — нормы и стадии, не дефекты.

## Облако (с 2026-09-25 по git): гейт и тикеты

fable, Opus и Sonnet работают в облачных сессиях; на машине автора — Grok CLI (`grok_next.md`). Единственный общий носитель — git. В облаке собираются l1trans (из seed `lm1/build/l1trans.lm1.c`), l2trans и `python tools/build_l2src.py` (портативный двойник `build_l2src.ps1`; 174/230 на main — красные только модули с `<windows.h>` и их замыкание); harness eternal-runs и L3 11/11 — только на машине до решения по POSIX-двойнику (Q10, `fable_next.md` §7). С ~15:40 UTC 25.09: код — Grok CLI и будущие субагенты; стратегия и базовые документы — автор с ChatGPT, результат в git; fable ревьюит и код, и документы в `steps/review-log.md`. С ~10:25 UTC 25.09 проект ведёт Grok CLI сам по `grok_next.md` (очередь §4 там), fable — ревьюер (`steps/review-log.md`); история тикетов дня — `steps/tickets-20260925.md`; прежний протокол облачных сессий — `steps/cloud-protocol.md`.

Ведущий проекта (Grok CLI или любой будущий агент) **ревью fable не ждёт**: посадил в main — сам берёт следующий пункт `next_core_tasks.md` или тикет `steps/tickets/`; замечания ревью и BLOCK закрываются следующей посадкой. fable не ждёт ANSWER и ведёт раунды дальше (каждые 15 минут; тикет — после получаса тишины). Мелкие шаги с git — не повод ждать друг друга (автор, 2026-09-26 11:20 UTC; полный текст — `grok_next.md` §0, `steps/tickets/README.md`).

## Свидетель не может быть пустым (правило координатора, 2026-09-24)

Фикстура-свидетель обязана иметь положительный наблюдаемый результат, который не может дать молчаливый выход (`return: 0` из E при null-cell bail давал «успех» любой фикстуре — D-15 прошёл, не выполнив ни одной проверки): ненулевое значение entry на успех (например 7) или строка `Says`, а «0» — только как отказ. Перед регистрацией строки harness — прогнать инвертированные проверки (должны падать).

## Источники спецификации и журнал автора

- `L1_spec_*.md` — максимально структурированный нормативный контракт L1 и его понижения, выведенный из исследования исходников, а не конспект реплик автора. Исследуемый коммит, локальные изменения, подтверждения конкретными файлами и проверками, ограничения реализации и задания разработчикам фиксировать в `docs/implementation-notes.*.md` и `steps/`, а не в спецификации. Не объявлять исследование завершённым до его выполнения.
- В `LMX_blog/` сохранять только технические уточнения автора о языке, прямо влияющие на спецификацию, по правилам отбора и цитирования в `READ.ME`. Организационные сообщения не архивировать.
- Журнал очищен от организационных сообщений и обсуждения моста. Сохранён технический фрагмент о флагах рукопожатия; это не полный сборник прежних уточнений языка.
- Утверждения автора сопоставлять с кодом. При расхождении записывать отдельно фактическое поведение реализации, намерение автора и подтверждающие источники; не додумывать промежуточный механизм и не скрывать расхождение.
- Раздел о флагах рукопожатия удалён из L1/L2 как преждевременно внесённый без исследования кода. Исходная реплика сохранена в `LMX_blog/2026-09-19.md`. При исследовании проверить размещение флагов, реальные операции и порядок памяти, значение слов «без синхронизации»; не выбирать эти детали самостоятельно.
- Сохраняется требование симметричных описаний ядра L1/L2 с взаимными ссылками. Заполнять их по результатам исследования, а не добавлением разрозненных цитат.

## Codex закрыт (2026-09-26)

Codex нет. `codex_inbound.py`, пайп `codex-browser-use` и поручения «вести Codex» не используются. D-17 отменён. Откроем заново, когда Codex снова будет. Ниже по файлу — прежние проверки канала; это не действующая инструкция.

## Единая инструкция по общению агентов

- Начинать с [work_chat/README.md](../work_chat/README.md): все сочетания агентов и команды. Исправление по пользователю: BOT-REVERSE-84261 получил ACK лишь после ручного пинга Bot. Позднейший BOT-AUTOREPLY-20260919-204004 получил ACK с подтверждением ответа по самому webhook без нового пинга пользователя; один автоматический цикл подтверждён. Текст handler для routine подготовлен в инструкции, но ещё не применён.

- Новый вход в существующую задачу Codex: [CODEX_INBOUND.md](../claude_chat/CODEX_INBOUND.md). Активная доставка INBOUND-ACTIVE-52916 и пробуждение из idle CODEX-IDLE-WAKE-68143 проверены. Второй тест отправил отдельный локальный процесс; Grok может вызывать тот же клиент, но его отправка ещё не проверена.

- Постоянные имена сессий Claude заданы в самих лаунчерах флагом `--name`: `lmx_uds` (реле), `fable` (`C:\claude\fable.bat`, Fable, effort xhigh), `deepseek` (`C:\deepseek\deepseek.bat`) и `openrouter` (`C:\openrouter\openrouter.bat`). Таблица с моделями и рабочими каталогами — в [work_chat/README.md](../work_chat/README.md). Прежние производные имена `l1-c9`, `l1-91`, `l1-98` устарели и не должны попадать в инструкции; имя действует со следующего запуска своего лаунчера, а сами лаунчеры лежат вне репозитория.

- Для Codex и Grok использовать [chat_config.md](../chat_config.md): все каналы, настройка, discovery, отправка и чтение ответов. `python claude_chat/chat_status.py peers` показывает текущие адреса, `read --name lmx_uds --contains CODE` читает только текст из родной истории. Старые исследования не являются текущими инструкциями.

## Мост Claude Code

- Проверен автоматический обмен Codex → lmx_uds → действующая l1-c9 → lmx_uds: `ACK AUTO-L1C9-58319`. Пользовательский `crossSessionInbound: accept` подхватился без перезапуска; ручной ввод и подтверждение в тесте не использовались. Область настройки и порядок работы: [claude_chat/README.md](../claude_chat/README.md). Не путать с прямым входящим каналом в Codex или Grok.

## Прямой канал Grok — актуальный результат

- Проверен прямой ACP-клиент [claude_chat/grok.py](../claude_chat/grok.py): две связанные реплики с Grok 4.6, встречный вопрос Grok и ответ Codex, один session ID при переподключении. Инструкция и точные границы — [GROK_DIRECT.md](../claude_chat/GROK_DIRECT.md).
- Для работы новой задачи Codex использовать `python claude_chat/grok.py ask "..."`: сохраняется контекст управляемой беседы `grok_direct`. Это новая беседа настоящего Grok, не прежний открытый терминальный чат пользователя.
- Для инициативного сообщения в простаивающий Codex использовать codex_inbound.py; пробуждение проверено. Grok Bot не проверен. Связь с Claude lmx_uds — отдельный механизм.

## Подключение именно к прежнему активному Grok

- Проверен двусторонний обмен с исходной сессией Grok CLI **grok**: ACK ACTIVE-GROK-93281, встречный вопрос, ответ Codex и ACTIVE-CONSOLE-CONFIRMED с прежним кодом. Доставка прямо в консоль этой сессии, ответы из её родной истории. Без новых бесед и без Claude. Не путать с Grok Bot.
- Клиент и ограничения: [GROK_ACTIVE.md](../claude_chat/GROK_ACTIVE.md). Последний повторный запуск клиента отказался отправлять в непустую строку ввода; не стирать пользовательский ввод и не обходить эту проверку. Правка сопоставления при автодополнении пути пока не проверена полным новым циклом.

- Подтверждён настроенный auto-ACK Grok Bot: BOT-ARMED-20260919-204214 вернулся без ручного пинга, в той же беседе. Пользователь уточнил, что предыдущий handoff был внутренним действием Bot; его нельзя считать вмешательством пользователя.
