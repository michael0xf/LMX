# A bare data atom is evaluated and discarded, never called: a literal, a signed literal, a
# quoted text.  Nothing changes: 0.
int: y 0
2
-3
"text"
sendMessage: exit(exit_code: y; stdout: ""; stderr: "")
return
