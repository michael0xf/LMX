# Тикет fable 2026-09-26-02 — D-20: строковые значения программы как `LmxCharArray` с точной `len`, без NUL (час молчания)

Статус: OPEN. Написан по правилу `steps/tickets/README.md`: последний коммит Grok на main — `9f57351` (ANSWER 90aba4b-1) в 03:13 UTC, к 04:15 UTC ни коммита, ни ANSWER, ни TAKEN. Следующий пункт очереди по `grok_next.md` §4 п.7 и ANSWER acc05c7-1 («Дальше — D-20») — D-20.

## Норма

Автор (2026-09-24; `next_core_tasks.md` §1 строка «Решение координатора по BLOCKER -134», `steps/defects.md` D-18/D-20): строковое значение — `LmxCharArray {size_t len; char *data}` с точной `len` и без NUL; один тип на элемент char; дублёров типа нет. C-строка с NUL — форма двери `c.*` (`fopen`, `strcmp`, `puts`), не форма значения в графе.

## Где сегодня NUL (измерить перед правкой, записать в план)

1. **Письмо `mainArgs` строит ядро, не транслятор:** `dev/l2src_sandbox/lmx_root.lm1` ~:1064 («argv[i] with its NUL»), контракт в `lmx_root.h.lm1` :289–:290. Элемент `m\mainArgs[i]` сегодня — char Array длиной `strlen(argv[i]) + 1`. Фикстура `entry_index.lm2` (root-pending) в комментарии строк 1–2 прямо опирается на этот NUL (`c.puts: @ m\mainArgs[1][0]`).
2. **Транслятор:** литерал текста в письме (`l2trans.lm1` ~:19867) уже берёт `t\length - 2U` — проверить, что копия данных не дописывает NUL; `l2_tok_text` ~:14810 (`dest[t\length]: 0`), `l2_fmt_l1_string` ~:2786, unquote-помощники путей — это **внутренние C-буферы транслятора и L1-эмиссия**, не значения программы: в срез не входят, но строку D-20 в `steps/defects.md` переписать, разделив «значения программы» и «буферы транслятора / дверь `c.*`».

## Сделать (первый срез)

- **Ядро:** элемент письма `mainArgs` — `LmxCharArray`/char Array с `len = strlen(argv[i])`, без NUL. Правка ядра ⇒ селфтест ядра + мутант обязательны: письмо из argv `ok` даёт элемент `len` 2 и `data[1] = 'k'`; мутант «+1» — RED. Комментарии `lmx_root.h.lm1` :289–:290 и `lmx_root.lm1` :28/:1064 — по новому факту. Тело платформо-независимое; POSIX-число снимет fable.
- **Свидетель L2 (ненулевой):** строка harness с `Argv = @('ok')`: `length(m\mainArgs[1])` = 2 → Entry 7 (сегодня измерить: ожидаемо 3); мутант — не 7. Форма чтения — та, что уже бежит (`entry_argc_if`, `entry_dyn_array_index`), без спец-имён и второго пути.
- **Дверь `c.*`:** `c.puts: @ m\mainArgs[1][0]` и `c.strcmp` над элементом письма (`entry_index`, `entry_strcmp`, обе root-pending) — C API ждёт NUL. Норма: у двери явная копия/конвертер с NUL (по образцу `l2_conversion_refused_msg` — не тихо), хранение NUL в графе — нет. Факты этих строк менять только с обоснованием; если корень не walkable по другой причине — root-pending остаётся, причина в комментарии.
- **Спеки:** L2 (RU/EN, раздел про Array, строка «`LmxCharArray {size_t len; char *data}` — дескриптор строкового литерала с точной `len` и без NUL») уже держит норму; L3 §Array (`docs/LMX_semantics.ru.md` :88 / `.en.md` :88) говорил «каждая строка — свой массив `char` с завершающим NUL» — противоречие, fable поправил в источнике `provenance/semantics-book.md` (RU/EN одной правкой, книга перестроена `tools/build_semantics.py`) вместе с этим тикетом; `CORE.md` про NUL молчит. Правятся комментарии кода (`lmx_root.h.lm1` :289–:290, `lmx_root.lm1` :28/:1064, `entry_index.lm2` :1–:2) и `steps/`.

## Общее

Доктрина §0: без спец-имён, одна форма на нативный и walk-пути, позиции и ссылки прежде чисел. Гейт машины (четыре числа), twin `l2src/` включая `tests/`, ПОСАЖЕНО в `next_core_tasks.md` §3, D-20 в `steps/defects.md` — FIXED с F-номером для среза (остаток, если есть, — отдельной строкой с точными местами).

Критерий готовности: `length(m\mainArgs[1])` для argv `ok` равно 2 нативно и под ручкой; селфтест ядра с мутантом; harness зелёный; в графе у строковых значений программы нет NUL.

Дальше — продолжать `next_core_tasks.md`.

TAKEN grok/d20-char-array@765e760 2026-09-26 04:35 UTC
DONE 3f8ea41 2026-09-26 04:47 UTC: build 282/282 (`build/l2src/20260926_013800`), harness 412/412 (`build/l2_harness/20260926_014339`), L3 11/11, имена 69/128, check_docs OK. D-20 F-76: `length(m\mainArgs[1])` для argv `ok` = 2, Entry 7. Мутант `+ 1` — RED сценарий C. Остаток — D-81.
