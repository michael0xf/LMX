# LMX blog — 2026-09-25

Источник: реплика автора в чате fable 2026-09-25 (ответ на `q23.md`), дословно:

```text
K1 -- да, сделайте нормальный итератор (в спеке есть раздел по yield--может поможет?)  K2- да; K3-a (как Erlang)
```

Итог: K1 — выборочное взятие из ящика делать как нормальный ИТЕРАТОР (старая спека: `fn: words () iterator(Text)` + `yield:`, потребление `each: word in words`); K2 — да (host-корень теми же механизмами); K3 = а — как в Erlang: отправитель кладётся первым полем письма (`{self(), Msg}`), письмо = (ссылка на отправителя; полезная Structure).

Источник: реплика автора в чате fable 2026-09-25 (ответ на K4 и Q1 из `q23.md`), дословно:

```text
K4 - a; Q1 -- положите пока одну настройку к примеру ту которую невозможно найти в Си (к примеру рабочую директорию)
```

Итог: K4 = (а) — транспорт опубликованного outbox в inbox адресатов на границе хода (модель 28); Q1 — в платформенные настройки host пока кладётся одна: то, чего нет в C, например рабочая директория процесса (host получает её от ОС и передаёт R0 общей ссылкой вечным профилем).

Источник: реплика автора в чате fable 2026-09-25 (ответ на `q24.md`), дословно:

```text
Q24 - A -- на этот механизмуже вся next_core_tasks.md переписана,мы только с тобой недавно поменяли на lastIndex по умолчанию (было 0)
```

Итог: Q24 = A — повторное типизированное объявление одного имени в одном теле — второе вхождение (`[0]`, `[1]`, неуточнённое — последнее); отказ «duplicate declaration» (самодельное правило 4 из S2 -112) отменяется; план уже написан на механизме вхождений, изменился только выбор по умолчанию (`[0]` → последнее).

Источник: две реплики автора в чате fable 2026-09-25 (уточнение Q24 = A), дословно:

```text
видимо я не до конца понял вопрос Q24. Если мы объявим (int: x 1; char: x 2) #какое поведение намечается по Q24 A?
```

```text
главное чтобытак можно было сделать и  (int: x 1; char: x (x + 2)) читался правильно
```

Итог: повторное объявление одного имени в одном теле допустимо и с другим типом — это новое вхождение (`[0]x` — int-ячейка, `[1]x` — char-ячейка); внутри тела голое имя в выражении означает последнее вхождение, объявленное ДО этого оператора: инициализатор `char: x (x + 2)` читает предыдущее `int: x 1` → 3, после чего появляется новое вхождение; снаружи `test\x` = последнее вхождение (char, 3).

Источник: следующая реплика автора в чате fable 2026-09-25 (к Q24), дословно:

```text
главное разрулить это в трансляции так как в Си такое невозможно прямо а у нас в next_core_tasks.md намечается портирование и расширение абсолютной конвертации с валидацией и оно  будет подставляться только так в интерпретаторе
```

Итог: повторное объявление одного имени с другим типом разруливает ТРАНСЛЯТОР (в C так нельзя): каждое вхождение — своя C-ячейка/локал; значение инициализатора другого типа (`char: x (x + 2)` при int-`x`) проходит через абсолютную конвертацию с валидацией — порт и расширение конвертеров из `lingvamyxa_prev` (план §7, вместе с `implements`); в интерпретаторе подстановка идёт только так же (конвертер — явный приёмник, выбираемый по merge-ключу). Не C-каст.

Источник: реплика автора в чате fable 2026-09-25 (merge методов), дословно:

```text
А мерж методов мы же можем разрешить только породить в итоге интерпретируемое нечто? (в поле 0 просто не будет нативной ссылки, останется один дескриптор и тот -- смерженный)
```

Итог (чтение координатора, подтверждение ниже): merge методов допустим при условии, что результат — интерпретируемая callable-Structure: в поле 0 результата остаётся один дескриптор без нативного адреса (`addr = 0` → walker), заголовок/сигнатура — по правилу Q21 = A (формалы операнда переопределяют одноимённые формалы модели, новые дописываются), тела — op-деревья: ops модели адресуют слоты как были, ops операнда перенумеруются по той же карте позиций (переопределённые поля → слоты модели, новые → дописанные), исполняются по порядку. Нативного кода у результата нет; прежний СТОП «методы не мержатся» превращается в «мерж методов = интерпретируемый результат».

Источник: реплика автора в чате fable 2026-09-25 (подтверждение чтения о merge методов), дословно:

```text
да, подтверждаю, вноси в план next_core_tasks.md и желательнов спеки пока Codex на отдыхе
```

Итог: чтение подтверждено — merge методов допустим, результат интерпретируемый (один дескриптор без нативного адреса, формалы и тела по правилу Q21 = A, ops по слотам); вносится в план и в спеки (книга, L2-спека).

Источник: вопрос автора в чате fable 2026-09-25 (merge методов и сигнатур как частичное применение), дословно:

```text
По поводу исполнения именованных структур и одобренного плана широкого мержа в том числе методов и сигнатур:
haskell:
 add :: Int -> Int -> Int
 add x y = x + y
 add5 = add 5

возможно ли на LMX сделать так:

fn: add (int: x; int y)
 . return: x + y fn:
fn: add5 (int: x) int merge(y: 5; add) #сигнатура add пропадёт

И обратное:

 fn: add5 (int: x)
 . return: x + 5
 add: merge(add5, fn: add (int: y) int (return: x + y)) 

?
```

Ответ координатора (чтение по принятым правилам Q21 = А + «merge методов = интерпретируемый результат»): прямое — да: `fn: add5 (int: x) int merge(add; y: 5)` — модель `add` (первый операнд), `y: 5` переопределяет слот формала `y` значением (формал становится связанным полем), объявленная снаружи сигнатура `(int: x) int` — заголовок результата (сигнатура `add` пропадает), `x` тела `add` разрешается транслятором в формал `x` результата; результат интерпретируется (дескриптор без нативного адреса). Порядок операндов существенен: модель — первая, `merge(y: 5; add)` сделало бы моделью `(y: 5)` и переопределило бы поле `y` несвязанным формалом `add`. Обратное — нет в предложенной форме: тела — op-деревья, дописываемые по порядку; `return: x + 5` модели `add5` исполняется первым, и приписанное тело `return: x + y` не достигается — получится `add5` с лишним формалом `y`. Переопределения тела merge не делает (переопределяются поля, операции дописываются).

Источник: реплика автора в чате fable 2026-09-25 (на предложение «тело операнда заменяет тело модели»), дословно:

```text
Надо подумать что тогда сломается. И что еще починиться если тело операнда заменяет тело модели
```

Итог: анализ — `steps/merge-body-replace.md` (чинится: обратный пример, ловушка мёртвого тела, один принцип специализации; ломается/оговорки: последовательная композиция через merge, не-callable операнд сохраняет тело модели, инициализаторы модели не исполняются, catch-площадки уходят с телом). Решение — за автором.

Источник: реплика автора в чате fable 2026-09-25 (алгоритм merge функций/процедур и простых Structure), дословно:

```text
давай только эту оговорку обсудим " (с оговоркой: не-callable операнд тела не несёт, тело модели остаётся; поля модели не переинициализируются — значения из экземпляра) " -- я предполагаю такой алгоритм мержа функций и процедур друг с другом и с простыми структурами:  1) структуры аргументов самих ресиверов fn и sub мержаться друг с другом по такому же принципу -- как "args". То есть дескрипторы мержаться как структуры. Тип "return" -- тоже. То есть если отсутствует именованный аргумент (к пример у sub нет типа return) то мержится как бы с пустой структурой анологичная по смыслу струткура. Потом body мержаться по тому же правилу. Но для структур сделать не односторонний запрет а чтобы merge(add; y: 5) делал  функцию смержив только body add и  "y: 5". А вот merge(y: 5; add) делал обертку вокруг add. Почему, какое тут общее правило -- не знаюно как будто каждая последующа структура хоть и заменяет поля но как бы мержится внутрь. Ты не можешь смержить две директории так чтобы одна поддиректория вдруг оказалась внешней. Плюс это разрешает сиутацию с возвратом внутреннего метода по return -- вокруг него делается с помощью merge такая же обертка
```

Итог (чтение координатора, к подтверждению): callable = Structure из именованных частей `args`, `return`, `body`; простая Structure = только `body`-часть; merge — попарно по частям, рекурсивно, на каждом уровне по Q21 = А (одноимённое — в слот модели, новое — в конец), отсутствующая часть = пустая Structure того же смысла; модель — внешняя рамка, каждый следующий операнд мержится ВНУТРЬ (поддиректория не становится внешней): `merge(add; y: 5)` — `y: 5` уходит в body add (специализация; заголовок — объявленный снаружи), `merge(y: 5; add)` — обёртка: Structure с полем `y` получает части add и становится callable; так же строится обёртка вокруг возвращаемого вложенного метода. Открыт один пункт — безымянные операторы body при merge двух callable.

Источник: четыре реплики автора в чате fable 2026-09-25 (обёртка вокруг вложенного метода, приведение по объявленному типу), дословно:

```text
Понял в чем проблема -- вот это не читается как метод " ( n: 5; fn: addN (int: x) int; return: x + n ) " Да?
```

```text
Но так как тип "return: " известен _мы можем привести -- у нас все конвертируется_ Зато вот это " fn: add (int: x; int y) . return: x + y fn: / fn: add5 (int: x) int merge(y: 5; add) #сигнатура add пропадёт " и решается само собой. Так?
```

```text
то есть в возврат внутреннего метода делает просто два мержа вместо одного -- и все, да?
```

```text
то есть даже один мерж с тремя аргументами
```

```text
в общем  тело операнда заменяет тело модели — вноси в план и спеки и запиши все что обсудили тут везде в план и в спеки
```

Итог (правило merge, окончательно): (1) callable = именованные части `args`, `return`, `body`; любая Structure (именованная или безымянная) = `body`; отсутствующая часть = пустая; (2) merge попарно по частям, рекурсивно, на каждом уровне Q21 = А (одноимённое поле операнда → слот модели, новое — в конец); (3) модель — внешняя рамка, поздние операнды мержатся внутрь: `merge(add; y: 5)` — `y` в body add (специализация), `merge((n: 5); addN)` — Structure с полем `n` и вложенным методом `addN` (как поддиректория; сама не callable: поле 0 — `n`); (4) безымянные операторы `body`: операторы операнда ЗАМЕНЯЮТ операторы модели (как содержимое файла в git); (5) приведение по объявленному callable-типу (заголовок `fn: add5 (int: x) int merge(...)`, результат `fn` у `makeAdder`, callable-формал) — это ещё один merge с методом как моделью: `merge(add; {y: 5}; заголовок)` — формалы, которых нет в объявленной сигнатуре, связываются одноимёнными полями, объявленный заголовок переопределяет `args`/`return` («сигнатура add пропадает»); (6) возврат вложенного метода = один merge с тремя операндами `merge(addN; {используемые значения}; объявленный заголовок)`; результат — интерпретируемый callable (дескриптор без нативного адреса), ops перенумерованы картой позиций; пример §7 (`add5: 1` → 6) сохраняется.

Источник: текст автора в чате fable 2026-09-25 с просьбой «внеси пожалуйста этот текст в спеку docs/LMX_semantics.*.md потому что он будет влиять на портирование конверсий которые заложены в план уже», дословно (английский оригинал автора):

```text
LMX has no process-global built-in type environment, conversion registry, generic-instantiation state, or implicit system table. Type descriptions, symbolic type bindings, conversion relations, and the expressions implementing those conversions are ordinary explicitly supplied LMX data.

An executing Message has one type-and-conversion context for its graph. That context is initialized from Structures explicitly supplied to the Message when it is constructed. The initial root may provide the application's initial type descriptions, symbolic bindings, conversion tables, and related receiving expressions, but a child Message does not inherit them merely because it was created by that root or by another Message.

If a Message requires such data, the creating operation must explicitly supply the corresponding Structures or references under the ordinary Message-construction rules.

Conceptually:

Root
    type descriptions
    symbolic type bindings
    conversion relations
    converter callables

        ↓ explicitly supplied

Message M
    application graph
    Message-local type bindings
    Message-local conversion context

The word "common" in this context means common to one concrete Message, not global to the process.

### Stable symbolic type bindings

A symbolic type name may be bound once in the type context of a Message and then used consistently throughout that Message.

For example, a Message may contain the bindings:

a -> int
b -> String

Every use of `a` in that Message resolves through the same Message-local binding unless an explicit language operation constructs a different Message-local context.

The binding is not repeatedly inferred or re-instantiated for each function call.

Thus, within one Message:

fn: identity (a: x) a
return: x

uses the same binding of `a` at every occurrence of `a`.

If this Message binds:

a -> int

then its effective contract is:

fn: identity (int: x) int
return: x

Another Message may receive a different binding:

a -> String

without changing the first Message.

A symbolic type name therefore represents a stable relation inside one Message, not a process-global type variable and not a fresh generic parameter instantiated independently at every call site.

This rule prevents separate parts of one executing graph from silently assigning different meanings to the same symbolic type name.

### Genericity is the default structural case

LMX does not require a separate generic, template, or type-parameter instantiation construct in order for an expression to accept structurally different candidates.

An expression states only the paths, values, callable operations, result contracts, and other properties that its Consumer actually requires. Candidate admission determines whether those requirements can be satisfied.

Consequently, structural generality is the ordinary case. Additional type information narrows the accepted domain; it does not activate genericity.

A symbolic binding such as `a` is required only when several positions must refer to one stable Message-local type relation.

For example:

fn: identity (a: x) a
return: x

states that the input and result use the same Message-local type binding.

By contrast, an expression that merely consumes whatever fields and operations it requires does not need an artificial type variable solely to declare itself generic.

### Message-local conversion table

The conversion table used by a Message is likewise explicit Message-local data.

It is not a hidden global registry and is not implicitly shared by all Messages.

Conceptually:

Message M
    type bindings:
        a -> int
        b -> String

    conversions:
        int -> String
        String -> int
        Meter -> Foot
        Foot -> Meter
        ...

The actual representation remains ordinary LMX Structures, descriptions, relations, Tables, and callable expressions. This notation only illustrates their role.

A conversion is available to an operation only when the concrete Message can reach the corresponding explicitly supplied description/relation/converter under the ordinary graph rules.

Absence of a conversion from the Message context does not trigger a process-wide search and does not cause the runtime to invent one.

### Conversion and candidate consumption

When a Consumer requires a value of description T, a candidate need not originate with an identical primitive or semantic description if the Message-local conversion context explicitly provides a valid path accepted by the Consumer's rules.

The ordinary sequence is:

candidate value
    ↓
Consumer requirement
    ↓
explicitly available Message-local conversion
    ↓
conversion contract and range validation
    ↓
formed value satisfying the destination requirement

Converters remain ordinary receiving expressions. Analytical checking does not execute them. When execution actually requires the conversion, the selected converter is executed under its declared contract.

Range, representation, unit, precision, and other semantic constraints belong to the applicable descriptions and conversion contracts.

A successful structural admission therefore never authorizes unchecked reinterpretation of primitive storage.

### Units and semantic quantities

Units of measurement require no separate built-in kernel mechanism.

A Message may explicitly receive descriptions and conversion relations for semantic quantities such as:

Meter
Foot
Second
Kilogram

If the Message-local context contains an admitted conversion such as:

Foot -> Meter

then an operation requiring Meter may consume a value described as Foot through that ordinary conversion mechanism.

The conversion remains subject to its normal contracts, including range, numeric representation, precision, and any unit-specific conditions.

The kernel therefore needs no privileged list of physical units. Unit conversion is one application of the same description-and-conversion mechanism used for other values.

### Callable arguments and results

The same Message-local conversion context applies when forming callable arguments and consuming callable results.

For a selected callable:

actual value
    ↓
ordinary Message-local conversion if required
    ↓
formed argument
    ↓
exact selected callable argument contract

and on return:

callable result
    ↓
ordinary Message-local conversion if required
    ↓
Consumer's required result contract

This permits a callable to be used when its original primitive descriptions differ from the Consumer's descriptions, provided that the concrete Message contains the required explicit conversions and all conversion contracts succeed.

For example, a Consumer may conceptually require:

int -> int

while the selected callable has:

String -> decimal

if this Message explicitly provides and admits:

int -> String
decimal -> int

The resulting call is still fully typed. The arguments presented to the selected callable must satisfy its actual descriptor after formation, and the value presented to the result Consumer must satisfy the Consumer's requirement after result conversion.

### Message boundary

Type bindings and conversion availability do not silently cross a Message boundary.

If Message A contains:

a -> int

and a conversion:

String -> int

Message B does not obtain either merely because A creates B, sends to B, or is B's parent.

They become available to B only if the relevant Structures or references are explicitly supplied under the applicable Message creation or delivery contract.

Therefore two Messages may intentionally execute the same reusable expression under different type-and-conversion contexts while each Message remains internally consistent.

Example:

Message A:
    a -> int

Message B:
    a -> decimal

The source expression:

fn: identity (a: x) a
return: x

may consequently have different concrete bindings in A and B, but within either Message the binding remains stable.

### No hidden system state

The implementation must not introduce any of the following as an alternative semantic source: a process-global conversion registry; an implicit universal type-variable environment; automatic inheritance of the root's conversion context; per-call re-instantiation of an already Message-bound symbolic type; a hidden search across conversion providers not reachable from the Message; built-in privileged knowledge of units or semantic quantity classes.

An implementation may cache resolved bindings or conversion selections as an optimization, but the observable result must be identical to resolving the explicitly supplied Message-local data.

The semantic source of a binding or conversion is always ordinary reachable LMX state.
```

Итог: новая глава книги `conversion-context` («Message-local type bindings and conversion context»): EN — текст автора, RU — перевод; влияет на порт конверсий (§7 плана): контекст типов и преобразований — локальные данные Message, явно переданные при создании; символические привязки типов устойчивы внутри Message; обобщённость — обычный структурный случай; таблица преобразований — явные данные Message; конвертеры — обычные приёмники; единицы — тот же механизм; граница Message не пересекается молча; никакого скрытого глобального состояния.

Источник: реплика автора в чате fable 2026-09-25 (после утренней интеграции), дословно:

```text
поправь пожалуйста CORE.md и связаныне спеки -- там зачем-то написано что L3 интерпретируется. Он у нас компилируется по  таким же правилам как L2.Там прсото правило что L3 _может_ интерпертироваться а L2 -- нет
```

Итог: норма — L3 компилируется по тем же правилам, что и L2; единственное различие: построенный граф L3 *может* исполняться интерпретатором, L2-операции — нет. Исполнение корневого тела интерпретатором (решение 2026-09-24 «пользовательский код по дефолту интерпретируемый») остаётся умолчанием реализации текущего тулчейна, не правилом языка. Поправлены: CORE.md §1 (схема сборки, абзац), §3 (у Thread нет режима — путь вызова по дескриптору; `request_mode` к удалению), книга §scope и §thread (RU/EN), L2-спека §1 и §Thread, план §3.

Источник: ответ автора на Q25 в чате fable 2026-09-25, дословно:

```text
разумеется Payload потому что мы не можем получить письмо не присоединив его арену и не сделва ему success=1 и итоговое running=0.У него уже конверт использован -- зачем он нужен? Но кстати так как тип который возвращает receiveMessage: уже известен -- нам не надо объявлять отдельно тип m. По тому же принципу мы объявляем все остальное, у нас нет такого что ресиверы требуеют заранее типизированных аргуменов! Model: var # в спеке написано что при неизветсном var копирует в него (делает merge себя с пустой структурой). Поэтому вы написали нельзя сказать что неправильно так как так можно  -- ресивер может как принимать аргумент так и возвращать значение, в том числе неявно возвращать как "int: i 2" Но вот сам ресивер вы написали длинновато. "int: i 2" не требует объялять int заранее :))) Тут конечно тип как бы явно не просматривается но _это ресивер_ а ресиверы у нас делают что хотят -- по определению операторов.
```

Итог (Q25 = payload): в форме `MainLetter: m` … `receiveMessage: m` поле `m` получает **payload** письма, допущенный по `MainLetter`; конверт (запись Message: арена присоединена, `success = 1`, `running = 0`) после приёма использован и не хранится. Ресивер `receiveMessage: m` сам объявляет `m` типом своего результата (как `int: i 2` не требует объявлять `int` заранее) — предварительное `MainLetter: m` допустимо (ресивер может и принять аргумент, и вернуть значение; `Model: var` при неизвестном `var` копирует в него — merge с пустой Structure), но это длинная запись; ресиверы делают что хотят по определению операторов. Первое поле письма — ссылка на отправителя (K3 = а) — остаётся в формах `receiveMessage: m` / `receiveMessage: m Model`, где `m` — письмо, объявленное ресивером.

Источник: ответ автора на Q26 в чате fable 2026-09-25, дословно:

```text
Q26: у нас есть ссылки "@" вы помните? Заодно их проверите. Это ответ?
```

Итог (Q26): присваивание Structure в Structure-типизированное поле по пути — без отказа, по виду поля: own-поле `Inner: inner` держит значение → `o\inner: a` = merge на месте (Q21 = А: одноимённые поля `a` в слоты `inner`, новые дописываются; идентичность `inner` сохраняется; позднейшие записи в `a` через `o\inner` не видны); поле-ссылка `@: Inner inner` держит адрес → `o\inner: @ a` перепривязывает слот на `a` (записи в `a` видны через `o\inner`). Ссылки `@` (kind 10/11, Sonnet -172) проверяются тем же свидетелем. Строки `unit_field_path_struct_rebind_refused` / `unit_matrix_path_struct_rebind_refused` перестают быть отказами.

Источник: уточнение автора к Q26 в чате fable 2026-09-25, дословно:

```text
Там не банальная логика потому что ссылка на структуру полчается двойным указателем _но это разруливалось уже в стром проекте_ и надо чтобы сейчас разруливалось
```

Итог: ссылка на Structure — двойной указатель (own-поле Structure-типа держит pointer-ячейку → Structure; `@` от такого поля — адрес ячейки; D-53 OPEN); в старом проекте (`lingvamyxa_prev`) это уже разруливалось — повторить его решение, не изобретать: Opus -183 (follow-up Q26) сначала находит место в `lingvamyxa_prev` и цитирует его в записке, затем строит свидетель для own-поля (merge на месте) и для поля-ссылки (`o\inner: @ a`, чтение через двойную косвенность).

Источник: второе уточнение автора к Q26, дословно:

```text
двойной там получается при трансляции а в коде это одинарный
```

Итог: в исходнике LMX ссылка на Structure одинарная (`@: Inner inner`, `o\inner: @ a`, чтение `o\inner\value`); двойной указатель возникает только в трансляции (Structure-поле представлено pointer-ячейкой, `@` от него — адрес ячейки), и лишний уровень косвенности снимает транслятор незаметно для программы — как в `lingvamyxa_prev`. D-53 — дефект трансляции, не языка.

Источник: ответ автора на Q26.2 и на объяснение «одной формы привязок Structure» в чате fable 2026-09-25, дословно:

```text
ну самой конечно структуры,а чего ещё?
```

Итог (Q26.2 = B): `@` от любой Structure-привязки (`Model: m`, own-поле, результат merge, принятое письмо) — адрес самой Structure; поле-ссылка `@: Inner inner` держит адрес Structure (один deref при чтении), перепривязка `o\inner: @ a` кладёт в слот ссылку, которую держит `a`. Правило prev «@x — адрес привязки (адрес адреса)» не переносится: адрес слота внутри Structure копир отвергает (CHILDREN), pointer-ячейка дублируется при копировании (проба Opus -183). Следствие для реализации (инженерное, не языковое): одна форма — слот держит адрес Structure прямо (`PUT_REF`, Grok -186); pointer-ячейки для Structure-привязок (`Model: m`, Structure-поля корня, `m` после take) переводятся на прямые слоты отдельным тикетом; D-53 закрывается этим.

Источник: реплика автора о промежуточных (pointer-) ячейках, дословно:

```text
так промежуточные ячейки же существуют длятогочтобы понятьчто лежит в промежуточнойячейке :))))
```

Итог: промежуточная ячейка между слотом и Structure не нужна — что лежит по адресу, говорит классификация адреса по диапазонам арены; ячейка законна только там, где она сама значение (число). Подтверждение «одной формы» привязок Structure (прямые слоты, `PUT_REF`).

Источник: реплика автора о ссылках на Structure в графе, дословно:

```text
Просто ты же не можешь в data * void положитьпрямую ссылку на структуру. А то она будет считаться структурой. Ты можешь положить ссылку на ссылку. Но после прочтения это будет просто ссылка на структуру.ТАк же?
```

Итог (правило представления): адрес Structure прямо в слоте = эта Structure — РЕБЁНОК (own-поле; копир входит, admission видит Structure). ССЫЛКА (`@: Inner inner`) = в слоте ссылка на ссылку (pointer-ячейка): по классификации адреса видно, что это ссылка, а не ребёнок; копир в pointee не спускается (общий); после прочтения — просто ссылка на Structure: `o\inner\value` — один шаг, `@ inner` — адрес самой Structure, не ячейки (D-53 — дефект чтения). Поправка к «одной форме» (fable): прямые слоты и `PUT_REF` — только для own-привязок (`Model: m`, Structure-поля корня, результат merge); ссылочные поля — pointer-ячейки. Следствие для -172 к.2 (Sonnet): `@: T name` в именованных Structure как прямой слот — неверно (стал «ребёнком»), вернуть pointer-ячейку (D-52); D-56 чинить со стороны письма: поле `sender` — ссылка (pointer-ячейка), не сырой адрес Message в слоте — ждёт подтверждения автора (ссылка на Message — та же форма?).

Источник: ответ автора на вопрос «ссылка на Message (sender в письме, `@: LmxMsg`) — та же форма ссылки на ссылку?», дословно:

```text
разумеется, в L2 -- так.
```

Итог: в L2 ссылка на Message — та же форма, что ссылка на Structure: pointer-ячейка (ссылка на ссылку), чтение даёт ссылку на Message. Следствия: письмо `(sender; payload)` держит `sender` как pointer-ячейку, не сырой адрес Message в слоте (-173 правится); поля `@: T name` в именованных Structure — pointer-ячейки (D-52), а не прямые слоты (-172 к.2 откатывается в этой части); D-56 чинится со стороны письма (kind прототипа и письма совпадают).

Источник: ответ автора на Q26.1 («own-поле `o\inner: a` — новое вхождение или запись в ту же Structure?»), дословно:

```text
просто эти операции для интерпретатора вы куда бдете складывать? Они как "if"или "for", а новым вхождением inner они разумеется не являются
```

Итог (Q26.1): `o\inner: a` — ОПЕРАТОР тела (как `if`/`for`), лежит в op-дереве интерпретатора; новым вхождением `inner` не является — пишет в ту же самую Structure (merge на месте, идентичность `inner` сохраняется, ранее взятые ссылки видят новые значения). Новое вхождение создаёт только объявление (Q24: `int: x 1; char: x 2`), не присваивание по пути. Для ядра: n-операндный merge-примитив walker’а нужен в форме «merge в цель» (into), а не только «свежий результат».

Источник: реплика автора о вхождениях и рабочих копиях в чате fable 2026-09-25, дословно:

```text
вообще я понял тебя -- давай уберем все эти вхождения и заодно код быстрей станет. int: i 5 это целая операция которыуюты наверное слева где-то хранишь в интерператоре? Не надо клади уж ге она лежит -- в то поле. Все эти dirty -load видимо надо убрать
```

Итог (ждёт уточнения): значение живёт в самом поле графа — `int: i 5` пишет 5 в ячейку поля `i`; рабочие копии «сбоку» (walker: `LmxWalkOwn.value`/`.ref` + publish; нативный код: локалы `l2_qN` с dirty-load) убираются — чтение и запись прямо в ячейку. Вопрос fable: остаётся ли нумерация вхождений `\[N]` как вид на поля графа (второе объявление — второе поле на своём лексическом месте; Q22.2/Q24 стоят), а убирается только обвязка вхождений, — или каждое присваивание перезаписывает одно поле и `\[N]` уходит.

Источник: две реплики автора следом (dirty-load и рабочие копии), дословно:

```text
поигрались и хватит --вырезай весь код load-dirty и храни там в ячейках нормальный граф для интерператора
```

```text
да прямо там. Зачем тебе рабочие копии?Заводи стек и в нем работай
```

Итог: правило представления исполнения — значения полей живут в ячейках графа (и для интерпретатора, и для нативного кода), рабочих копий нет, dirty-load вырезается; состояние активации (аргументы, временные, результат) — стек. План §3 «Ячейки — это граф»: Grok -188 (walker), Opus -189 (нативный транслятор).

Источник: обсуждение автора с fable 2026-09-25 (ПАУЗА: решения нет, связанные изменения не начаты) — реплики автора по порядку, дословно:

```text
стойнадо подумать
НЕТ пока пауза потому что моя последняя инструкция была "поигарлись и хватит" всмысле убрать граф данных?
Я имел в вид B но надо его продумать. Что там в итоге будет лежать. И как к этому обращаться.
Пока не стартуем просто подумаем. Любая активация вроде должна работать с локальным снимком. Но локальный снимок всего дерева метода -- дорого.
а M\for\y  тоже ячейка в плоском кадре,безделения на подструктуры?
а я понял  S -- не объявитьлокальную переменную
и в S и в F пропадают все типизации по адресу вхождения в типизированых массивах арены?
А что в графе есть? Ты же исполняешь этот граф. И "for"хранишь, expression -- хранишь,а "int: " не хранишь?
тут вопрос только в том как обеспечить  доступ к данным.Потому что у нас фактически граф с примитивами на листьях и эти примитивы не именованы. То есть по ситуации. int: x (3 + y) это безымянное "3"
суть втом что присваивание (3 + y) --> 7 поменяет метод.А у нас нативный есть и он не меняется
Это модель класса обычного :)) Надо пока думать -- не начинай связанных изменений
```

Ход обсуждения (без решения): автор имел в виду (B) — одно дерево, а не «два дерева + рабочие копии»; пройдены: S (объявления в теле метода = поля M в графе, общие для активаций — «не объявить локальную переменную») и F (ячейки объявлений — в кадре активации; кадр должен лежать в арене, иначе теряется тип по адресу); тип в (B) — ссылка на запись ресивера в голове узла (`[→int, ячейка, initializer]`), а не только пул по адресу; доступ к данным — по месту (узел → операнд), имена только в трансляторе; присваивание `x: 7` — узел-запись в ячейку объявления, новое вхождение — только новое объявление; ключевое замечание автора: запись в ячейку внутри дерева тела МЕНЯЕТ МЕТОД, а нативный метод неизменен → тело = неизменный код, значения — в экземпляре (кадр активации / юнит / Structure); автор: «это модель обычного класса» — думать дальше. Grok -188 и Opus -189 на HOLD; -185 принят, не гейтится.

Источник: реплики автора в чате fable 2026-09-25 после паузы (снятие «убрать граф данных», разделение code/data, предложение `ыыы.md` → `q27.md`), в порядке поступления, дословно:

```text
нет,я не шел к второму. Мне нравилась текущая конструкция и я неподумав бросил убирай потому что мне показалось там бесопорядок. Если мы просто отделим Lmx void * data структуру от исполняемого кода то порядка же станет больше?
У нас сейчас практически все может выполняться. Даже одиночное число в строке -- так же? Можем ли мы оставить этот общий странный принцип но при этом навести порядок: структура, сделанная callable, всегда получает одним из аргументов другую структуру которую считает данными?
то есть суть в странном принципе --они теоретически могут меняться местами
а обдумай вот это предложение
C:\Nyasha_Planet\ыыы.md
Как модельи можноли обойтись вообще без явного ресивера  execute(code,data) ?
```

Итог: «ячейки — это граф / убрать dirty-load» снято самим автором — он не шёл ко второму дереву, а хотел порядка: отделить данные (`Lmx *data`) от исполняемого кода. Принцип «всё исполняемо» сохраняется: Structure не имеет врождённой роли; callable всегда получает другую Structure, которую считает данными; роли теоретически меняются местами (та же Structure — код в позиции головы, данные в позиции аргумента). Предложение `ыыы.md` (архив — `q27.md`): пара исполнения (code, data) из двух обычных `Lmx *`, без `execute`-receiver'а в языке, без третьего context-графа.

Источник: выбор автора из вариантов fable (вопрос предложения — исходная точка `node`) и поручение, дословно (после обрыва сессии fable — восстановление контекста):

```text
вот это я выбрал
вот здесь иходное предложение ыыы.md которые мы уточнили моимвыбором из твоих предоженных вариантов
вот это всё нужно сформулироватьв спеки и в план next_core_tasks.md
```

Выбранный текст (fable, вариант «данные вкладываются как код»):

```text
данные вкладываются так же, как код. Экземпляр данных вызываемого создаётся полем данных лексического родителя, в слоте, отвечающем этому callable (как сейчас occurrence метода — поле юнита). Тогда node\x = data\parent\x — статическая цепочка Pascal'я, но это цепочка parent графа данных, ничего третьего. Побочный подарок: слот родителя держит экземпляр последней активации → M\x снаружи и липкие фикстуры -67 работают, а «копия на входе / запись на выходе» (21.5/21.6) становится «свежий экземпляр в слот» — без dirty и без перекладывания байтов. Рекурсия: каждая активация — свой экземпляр, слот показывает последний.
```

Итог (решение автора, модель принята): исполнение = пара (code, data); роль — позиция, не свойство Structure. Код неизменен (дескриптор, args/return, тело операторов, литералы); данные — обычная Structure объявленных полей тела в лексическом порядке (прототип данных метода; вложенные тела — вложенные Structure полей, `M\for\y` — тот же путь; формалы — поля). Активация: свежий экземпляр прототипа в слоте данных лексического родителя, отвечающем этому callable; аргументы — в его слоты; код пишет прямо в переданные данные. `node\x = data\parent\x`; слот держит экземпляр последней активации (`M\x` снаружи, липкие фикстуры -67); рекурсия — свой экземпляр на активацию. Рабочие копии, dirty, load, checkpoint (21.5/21.6), sticky-selector исчезают. ABI `(node, self)` = `(data\parent, data)`. Merge callable: args/return свои, данные попарно в слот модели (Q21 = А), код — код последнего операнда с телом (`add5: merge(y: 5; add)` → тот же `Cadd`, `Dadd5 = merge(Dadd; y: 5)`). `Model: fresh` = экземпляр прототипа. Открыто Q27.1: результат — поле `return` данных или значение активации. Записано: `next_core_tasks.md` §3 «Пара исполнения code/data», L2 §10/§11, L3 §11/§12, `CORE.md` §3/§9. Тикеты -188 (Grok, HOLD ACK, WIP `13b27d4`) и -189 (Opus) переформулированы в code/data split, на HOLD до «go» автора.

Источник: реплики автора о результате и об основании модели (чат fable 2026-09-25, после записи `b2a8b24`), дословно:

```text
как в данных может быть поле return ?
return же что-то  исполнеяемое
" «заводи стек и в нём работай»," -- да причем мои слова эти, с того момента как вы взяли  в работу тикет который я потом остановил мы обсуждали новую модель.Она же вообще должна отличаться: Никакого третьего “callable context graph”. Если для выполнения чего-либо недостаточно code и data плюс обычной машинной activation с аргументами/locals, значит надо сначала выяснить, почему.
конечно одобряю если мы только что обсудили модель где структура подается данных и исполняемая то какой return в данных -- не понимаю
```

Итог (поправка к предыдущему итогу; Q27.1 снят): основание модели — ограничение `q27.md`: в исполнении есть ровно код, данные и обычная машинная активация (аргументы, локалы, возвращаемое значение); реплика «заводи стек» относилась к остановленному тикету и основанием не является. `return` — оператор кода; поля результата в данных нет: результат — обычный возврат машинной активации (`fn` — значение, `fm` — ссылка на Structure результата, `sub`/именованная Structure — ничего), локальная ячейка результата для `square: x * x` — обычный локал. Формальные аргументы — аргументы машинной активации, а не поля данных; полем данных аргумент становится только по правилу L3 §12 (голое присваивание ему в теле) или связыванием через merge (`y: 5` у `add5`; связанный формал берёт значение из данных, сигнатура результата merge выводится, машинное тело одно). Слот родителя показывает последний созданный экземпляр; результат через слот не читается. Исправлено в `next_core_tasks.md` §3, `q27.md`, L2 §10, L3 §11/§12, `CORE.md` §3.

Источник: предложение автора о нативной реализации как поле `Lmx` (чат fable 2026-09-25, после «go»), дословно:

```text
по поводу компиляции -- можно не нулевым полем а дополнительным полем в Lmx (после parent к примеру) положить прямуюссылку на нативный метод (если он есть) -- так структурыостанутся единообразными совершенно (родную сигнатуру клади соответственно обычным полем графа и она сама граф. Принимаешь предложение?
"родное поле графа" тут в void * data, а ссылку на нативную реализацию предлагаю отделить так как она -- не лексический элемент. И положить после parent в конец Lmx
```

Итог (принято): нативная реализация тела — слово `native` в конце записи `Lmx` после `parent` (`{array, parent, native}`), прямая ссылка на вход C, нуль, если нативной формы нет; это не лексический элемент и не поле графа. Сигнатура — обычное лексическое поле графа в `data`, сама Structure. Уходят `child[0]`-дескриптор, `LmxCallable`, `LmxMethod {addr, sig}`, слово `sig`, записи ролей `lmx_plan`, переходный METHOD-ABI и классификация `child[0]` в `lmx_call`: исполнение Structure в позиции головы выбирается по слову (непустое — нативный вход, нуль — walker). Копир переносит слово дословно; merge берёт слово от последнего операнда с телом, сигнатура выводится как граф. Слово есть у каждого узла одинаково, без различия ролей (см. поправку автора ниже); доктрина §0 плана остаётся в силе — разовый рефакторинг автора. Записано: `next_core_tasks.md` §0 п.5 и §3 п.8 (+ замер в -188/-189 к.1), L2 §2/§5/§11 ru/en, `CORE.md` §2/§3/§9.

Источник: поправки автора к моей формулировке о слове `native` и о доктрине, затем о рантайм-построении кода (чат fable 2026-09-25), дословно:

```text
"Это слово в каждом узле Lmx, включая экземпляры данных, у которых нативного кода не бывает никогда. Плата за единообразие: один указатель на узел. Я считаю её оправданной, но это ваша цена." -- КАКИХ ДАННЫХ?!!!!!!!!!!
В последнем плане явно сказано что надо все структуры сделать универсальными и выбирать на месте кто выполняется а кто данные!!!! Без всяких пометок "callable"!!!!!
--доктрина остается в силе потому что она для вас - -чтобы вы не добавляли. Сейчас мы сделали рефакторинг и добавили.
то есть понятно что ты компилируешь не все подряд _но это на структуру структур не влияет так как у нас есть интерпретация и можем интерпретировать любую структуру если захотим_
мы должны уметь прямо в коде заполнить пустую структуру или имещуюся и отправить интерпретатору.После проверки мы сможем отправить Message трансялтору и получить обратно ответ с путем к dll или что-то в этом роде
**Никакого третьего “callable context graph”. Если для выполнения чего-либо недостаточно Lmx-структуры в роли `code` и Lmx-структуры в роли `data` плюс обычной машинной activation с аргументами/locals, значит надо сначала выяснить, почему.
```

Итог (поправка моей ошибки): «экземпляров данных» как вида Structure не существует — роль (код/данные) выбирается на месте исполнения, пометок «callable» нет; слово `native` есть у каждого узла одинаково и означает только «есть ли нативная форма тела»: нуль — интерпретируется, а интерпретировать можно любую Structure; нативная форма необязательна и на устройство Structure не влияет. Доктрина §0 остаётся в силе для агентов; `Lmx.native` — разовый рефакторинг автора, не прецедент. Цель (без тикета): программа строит код в рантайме — заполняет пустую или существующую Structure и отправляет интерпретатору; после проверки та же Structure уходит письмом транслятору как сервису, ответ — путь к dll (или подобному), из которого заполняется слово `native`. Записано: `next_core_tasks.md` §0 п.5, §3 п.8–9; `CORE.md` §2 таблица; блог-итог выше исправлен. Попутно сняты остатки старой модели, найденные Opus (-189 к.1): L3 §12 (публикация, `dirty`, трасса рекурсии, пример `for` — теперь `9 9`), L3 §3 `self` = экземпляр данных, L3 §10 защита без «публикации рабочих полей», `CORE.md` §3.1 `@fresh` = адрес самой Structure (Q26.2).

Источник: напоминание автора о правиле корня и компиляции (чат fable 2026-09-25, после восстановления сессии), дословно:

```text
вспомни правило которые мы вводили до случайного выхода твоего из сессии -- выполнение начинается в исходной структуре из файла без всяких main с первой строчки -- и любая такая структура в принципе теперь компилируется (мы убрали запрет который происходил от кривой модеи предыдущей)но разумеется merge двух таких структур не создает сейчас нативного выполняющейся структуры -- после него у результата таких структур будет варинат только интерпретации _но вложенные методы сохраняют свои нативные сигнатуры так как они переносятся целиком и этот механизм реализован прямо сейчас у вас (или планировался я уже не знаю)_
```

Итог: (1) исполнение начинается в исходной Structure файла, без `main`, с первой строки — записано (план §3 «Пользовательский код интерпретируется по умолчанию», L3 §1), реализовано (корень `.lm2` — op-дерево walker’а, Opus -159 к.2b); (2) запрет «нативное исполнение только внутри методов» снят — любая Structure, включая корень файла, в принципе компилируется, нативная форма тела — слово `Lmx.native`, интерпретация корня — умолчание текущего тулчейна; пункт (2) нормы в плане переписан, нативная форма корня — без тикета; (3) merge двух Structure даёт результат с пустым `native` — только интерпретация; вложенные методы переносятся целиком и сохраняют свои слова `native`: в спеке L3 §20 (переформулировано с «дескриптора без адреса» на слово `native`), реализовано сегодня в части копира (указатели на нативную реализацию сохраняются при копировании графа), а merge по частям (§4: аргументы/результат/тело, операторы операнда заменяют) — план, не посажен; в паре code/data это -188/-189.

Источник: реплика автора о снятии правила «интерпретируется по умолчанию» и о старте с пустыми данными (чат fable 2026-09-25), дословно:

```text
"пользовательский код интерпретируется
  по умолчанию. " -- теперь нам в слюбом случае нужна вторая структура как данные и поэтому такое правило придется убрать.Осталось одно правило: интерпретируется если не заполнено новое поле в Lmx со ссылкой на нативный метод. Очеивеидно что сначала выполняется с пустой структурой данных и потом уже только получает receiveMessage -- эту ситуацию обдумай как разрулить потому что он будет воспроизводиться
```

Итог: правило «пользовательский код интерпретируется по умолчанию» (2026-09-24) снято; единственное правило выбора способа исполнения — слово `Lmx.native`: пусто — интерпретация, заполнено — нативный вызов (план §3, норма переписана; `implements` получает интерпретацию через merge, у результата которого `native` пусто). Любое исполнение, включая корень файла, — пара (code, data); данные корня создаёт host как свежий экземпляр прототипа файла. Ситуация «сначала пустые данные, потом `receiveMessage`» вынесена на обдумывание (предложение fable — в чате, ждёт автора).

Источник: реплики автора о едином графе в обеих ролях и ответы на Q28/Q29 (чат fable 2026-09-25), дословно:

```text
" Данные R0 это его собственный граф Message" -- а код его это что? Я теперь предлагаю разрешить в качестве и кода и данных один и тот граф
Q29-- одна ячейка там где объявление; Q28 -- второе
```

Итог: одному графу разрешено стоять в обеих позициях пары исполнения: код R0 — та же Structure файла, что и его данные (R0, R0); обычный вызов исполняет `M` над его собственным графом (M, M), и `M\x` снаружи читает поля самого `M`. «Код неизменен» уточнено: исполнение никогда не пишет в узлы операторов, литералы и слово `native` — только в объявленные поля. Q28 = второе: свежий экземпляр прототипа создаётся только там, где транслятор не может исключить повторный вход (рекурсия в статическом графе вызовов, динамический вызов), или при явно переданных данных; он живёт в кадре и снаружи не виден; в цикле вызовов мусора нет. Следствие: поле с неисполненным объявлением хранит значение прошлой активации (пробная строка Opus → 77). Q29 = одна ячейка там, где объявление: повторное голое присваивание пишет в ту же ячейку и вхождением не является, `[N]name` нумерует объявления; закрытый пункт §4 «повторные `arg:` — отдельные occurrences» и фикстуры `unit_occ_arg_slots`/`unit_occ_root_named`/`unit_occ_snapshot_selector` пересматриваются в -189 c3. Записано: план §3 п.1–4/7, норма (1), тикеты -188/-189, §4; L2 §10 ru/en; L3 §4/§11/§12; `CORE.md` §3/§3.1.
