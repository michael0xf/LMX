# Карта закрытия `next_core_tasks.md`

Цель (автор, 2026-09-23): закрыть весь документ `next_core_tasks.md`. Состояние на `fb028e7`: **159 открытых** пунктов, 41 закрытый. Эта карта — рабочий порядок закрытия и владельцы; сами пункты и их галочки живут в плане, галочка ставится только по свидетельству (фикстура/гейт/коммит/измерение), никогда «по прозе».

## Протокол закрытия одного пункта

1. Владелец измеряет пункт на текущем `origin/main` (не по памяти): фикстура/строка harness/selftest/коммит, которые его доказывают, или точное место, где он не выполнен.
2. Выполнен → `[x]` + в конце пункта: коммит и свидетельство (строка harness / selftest / измерение). Не выполнен и bounded → тикет с положительным и отрицательным случаем, мутантом и гейтом. Не выполнен и требует правила → вопрос автору с минимальной программой (`q*.md`).
3. Правки плана — отдельный docs-коммит по точному пути (`next_core_tasks.md`), интегрирует координатор; параллельные писатели плана не работают.
4. Пункты-процессы (§0 :12-14, §10 :392-395) — не задачи, а правила; после закрытия задач они переносятся в `READ.ME`/`steps/current.md`, из плана удаляются.

## Фазы и владельцы

| Фаза | Разделы плана | Владелец | Когда |
| --- | --- | --- | --- |
| A1 | §2 Норма / Исправления P0 / Известные места; §3 Общая последовательность, Объявление, Присваивание, Вызов, Expression statement и discard — аудит по факту + галочки + bounded-тикеты на остаток | Sonnet | после -121, D2 |
| A2 | §7a `c.puts` остаток, `c.array` (declarator), Raw-C door, `sizeof:` (10 пунктов), L1 `c.array` HOLD | Grok Bot | после -123 P2 |
| A3 | §3 throws/catch (S1) — реализация по Q11–Q14 | Opus | **ЗАКРЫТА 2026-09-23** (S1.1 `1abafa4`, S1.2 `9f299a5`, S1.3 `b0b4158`); остатки S1.5 в :213 |
| B1 | §4 Occurrences/`[N]field` и §5 Canonical cell/selector/sticky — один общий алгоритм occurrence → physical path для native и интерпретатора; дизайн, затем запись | Opus (дизайн), писатель по дизайну | **ЗАКРЫТА 2026-09-24 по аудиту -142** (алгоритм один — на трансляции; интерпретатор позиционный; дизайн не нужен); остаток `test\[N]arg` — Sonnet -146 |
| B2 | §6 Матрица тестов — строки harness по каждому пункту, включая сравнение physical operation / graph identity / dirty / admission trace | Grok Bot | **ЗАКРЫТА 2026-09-24** (`b2dd090`, 16 фикстур `unit_matrix_*`); остаток — dirty/admission trace после порта implements |
| C | GATE (10 пунктов): инвентарь остатков name-special/shim/fallback/dead helpers, чистое дерево, полный набор гейтов на одном SHA, checkpoint + push | Grok Bot (инвентарь) + координатор (checkpoint) | после A2, A3, B1, B2 |
| D | §7 implements/admission — порт из `lingvamyxa_prev` (реализация, таблица преобразований, L2-механизм таблиц, `uses(Consumer, bVar)` + runtime), снятие fast-path обходов | Opus (дизайн/порт), Sonnet (admission в трансляторе) | только после C (порядок обязателен, :284) |
| E | §8 Перенос уровней и самосборка: миграция L1→L2 (List, parser port, translator, runtime tables, implements), полная самосборка L2, затем L3 + L2 | все трое по манифесту миграции | после D |
| F | §9 `myxa_manager` как приложение: один entrypoint, production/test разделение, lifecycle, end-to-end из чистого checkout | Opus | после E |
| G | §10 Документация: RU/EN синхронно, L1/L2 симметрично, implementation-notes, блог, удаление ошибочных утверждений; перенос правил-процессов из плана | координатор | по ходу и в конце |

## Открытые пункты по разделам

На `fb028e7`: 159 открытых / 41 закрытый. На `cfc7f41` (2026-09-23 ~12:30): 148 открытых / 55 закрытых; на `d70341c` (~15:30): 145 / 63 (§1 +5 пунктов VoidArray); на `42766fb` (~17:00): 131 / 77; на `edd3f8f` (~18:30): 137 / 79 (+6 пунктов «исполнение именованной Structure»); на `e42a846` (~21:40): 142 / 81 (+7 пунктов §1 «Дескрипторы динамических массивов»); на `c52557f` (~23:10): 138 / 87; на `717a325` (~23:50): 134 / 91; на `95fc32a` (24.09 ~03:30): 127 / 104; на `c64b616` (~06:30): 103 / 128; на `e4266d8` (~09:30): 90 / 141; на `71695c5` (~10:30): 94 / 141 (+4 пункта правила merge); на `2bbe8a0`+ (~12:30): 95 / 141 (+1 P2 c.sizeof); на `23f7a96` (~13:30): 99 / 141 (+4 «интерпретация по умолчанию»); на `0b6322b`+ (~17:15): 98 / 142 (P2 закрыт); на `655f2de` (~20:15): 95 / 145 (-155 закрыт, -156 посажен); на `9387371` (~23:55): **95 открытых / 147 закрытых** — §0 3 · остатки аудита 1 · §2 Норма 8 / P0 6 / Известные места 2 · §3 1 + Общая последовательность 4 + Объявление 8 + Присваивание 11 + Вызов 6 + throws/catch 6 + Expression statement 14 · §4 8 · §5 9 · §6 15 · §7 11 · §7a puts 1 + поля C 1 + c.array 2 + raw door 1 + sizeof 0 + L1 c.array 1 · GATE 9 · §8 9 · §9 6 · §10 5. Рост в §2/§3/§6 — новые пункты нормы (P0 sole-container parity, headless expression statements), не регресс.

## Ход по фазам (журнал)

- 2026-09-23 ~11:00 — A2 (Grok Bot, -123 P2 `6a07f37`, -126 `1b395ff`/`17d54bc`/`a3ffe7c`): `sizeof:` закрыт полностью; §7a галочки по свидетельствам (raw member только для типа `c.*`, dump port owned allocation, 4 фикстуры c.array под гейтом). Остаток §7a (5 пунктов) — пробелы, а не недомер: stdio include безусловный, нет L2-библиотеки `puts` без `c.`, `l2_array_local` всё ещё принимает `c.array` (~46 объявлений в l2trans), сканеры (DeepSeek), L1 `c.array` HOLD.
- 2026-09-23 ~12:00 — A1-предпосылка: Sonnet -121 (occurrence root, D1, kinds 8/9, 7 фикстур `<Method>\X`) на интегрированном дереве 183/184; вскрыт латентный дефект -105 (inline merge `Model: a` в теле метода обновляет ячейку, но не dirty-load локал → передача `a` в formal даёт 0); fixup в `sonnet/node-root-2` на базе `a3ffe7c`. Grok -127 (P0 sole-container) STARTED. Opus -128 read-only дизайн S1 (A3) во время ожидания S3 2b.
- 2026-09-23 ~13:30 — Sonnet -121 + D2 ПОСАЖЕНЫ `6321bbf`/`7421feb`/`ce323be`/`520b8fe` (гейты на интегрированном дереве: harness 185/185, build_l2src 250/250, L3 11/11, check_docs). Opus -128 принят: дизайн S1 записан в `steps/s1-throws-design.md` (`bdf21e1`), решения X1/X2 — координатором, X3 → `q17.md` автору (имя неявного throw при отказе runtime implements). Opus: S3 2b STARTED на `520b8fe` (opus/s3p2b). Sonnet: -129 — молчаливый отказ трансляции при записи в поле единицы из вложенного метода (находка Opus; fallback ~:15857) + A1-аудит §3 (только отчёт, план правит координатор). Grok -127: предупреждён, что импортированные goldens `tests/parser/imported/03-l1` неизменяемы (check_docs), расхождения — в слое ожиданий.
- 2026-09-23 ~14:00 — Grok -127 BLOCKER PART1: нормализация меняет 9 деревьев исторического корпуса (`f: (a b)` → `body fields=2`; brace_mark_block_string_payload, bracket_bounded_standalone, multiline_paren, operator_atoms, parser_signed_number_call, repeat_head_groups, short_form_body_after_inline_return, t1, t2), а моё требование «previous-l1 131/131» им противоречило. Решение координатора по контракту профиля (`tests/parser/README.*` :28 — «историческая регрессия, не норма»): импортированные байты неизменяемы; расхождения — явная карта `tests/parser/previous-l1/divergences.json` + goldens `tests/parser/previous-l1/goldens/`, `run_parser.py --profile previous-l1` считает «diverged as recorded» отдельно и отказывает устаревшей записи; каждое из 9 обосновать как ровно один снятый уровень единственного контейнера (иначе — дефект нормализации, не расхождение); README RU/EN + `dev/l1src_sandbox/README.md :41`.
- 2026-09-23 ~15:30 — Sonnet -129 ч.1 ПОСАЖЕНА `765e70c` (запись в поле единицы из вложенного метода; located-отказ вместо молчания; harness 187). A1 §3 аудит (Sonnet, ч.2, только отчёт) применён `d70341c`: 8 галочек по свидетельствам (:183, :189, :194, :202, :203, :206, :215 + :180 ждёт -127), остальное — пометки с bounded-тикетами: -130 (приоритет callable, colon-арность, пара `Model(fresh)`, пин неизвестного типа, подключить 3 незарегистрированные addr-фикстуры), -131 (аудит поиска own-поля :208, перечень terminal Structure-reference assignment :207, ty40 :214), -132 (пустая Structure :187/:191–:193 — после -127), :204/:205 — после S3 2b, :218 — разложить по механизмам в §6 (B2). Счётчик: 145 открытых / 63 закрытых. §1: подсекция «Представление VoidArray» (автор через Codex; array первым, без LmxArrayDesc; план, не код).
- 2026-09-23 ~16:30 — Grok -127 ПОСАЖЕН `bc22881`/`2b99163`/`6ca54c9` (P0 parity единственного контейнера; карта расхождений previous-l1: 131 passed, 9 diverged; `l2_call_args` удалён; harness 187, build_l2src 250, L3 11/11). План `0a45ace`: §2 :96/:157/:158 [x], :159 остаток (lmx_walk — Opus после S3 2b; `mystruct()` — Sonnet -132), §6 :292 P0-часть. Follow-up PART1b (тот же ID): Mix-узел `{...}` (флаг на Structure) разворачивать нельзя — одно из 9 расхождений было дефектом нормализации. Промежуточное состояние main между PART1 и PART2 (d70341c) принимало `add: ((1 2))` — Sonnet -130 переезжает на 6ca54c9. Счётчик: 142 открытых / 66 закрытых.
- 2026-09-23 ~17:00 — Sonnet -130 ПОСАЖЕНА `87fb409` (только фикстуры: приоритет callable в трёх формах + мутант по `l2_head_is_call`, colon-арность как ошибка вызова, пара `Model: fresh`/`Model(fresh)`, пин неизвестного типа, три addr-фикстуры в harness; harness 194/194). План `42766fb`: 11 строк §3 закрыты. Счётчик: **131 открытых / 77 закрытых**. Выдан -131 (аудит-скрипт мест поиска own-поля → исправления одним механизмом → перечень terminal Structure-reference assignment + остаток ty40).
- 2026-09-23 ~18:30 — **Q17 = А** (автор: «Q17: A», «имена придумывайте сами»; блог, `cb85dc2`). Opus S3 часть 2 ПОСАЖЕНА `82eb1b2` (поле вида 6 `T: []: []: x`, runtime admission `l2_emit_admit`, `m\mainArgs[k][j]`, `l2_program_completed`; harness 214/214); §8 :398/:399 закрыты (`edd3f8f`). Grok PART1b `e446835` (Mix не разворачивается; previous-l1 131 = 123 + 8 diverged). Opus -133 STARTED: (1) удаление CALL-only unwrap в `lmx_walk`, (2) S1.1 (merge = 1, implements = 2). Sonnet -131 ч.1 `ba913fe` (скрипт `tools/own_lookup_audit.py`: 115 мест, 9 резолверов; БАГ `l2_own_first` — глобальный поиск первой own-строки по имени, `l2_hidden_from` читает чужой метод; 5 ручных копий формулы `l2_emit_own_from`); решение координатора: несвязанный динамический вход разрешается в СОБСТВЕННОЙ таблице callee (mi), иначе located-отказ; ч.2 GO. Новая норма автора (Codex `f61f029`/`843d888`): исполнение именованной Structure голым атомом, `sub` с голым `return`, контракт получателя в позиции аргумента → план §3 :220–:229 (аудит + P0/native/интерпретаторы гейты; расхождение `lmx_walk` RET — Opus после S1.1). Счётчик: **137 открытых / 79 закрытых** (+6 новых пунктов нормы).
- 2026-09-23 ~19:30 — **Q18 отвечен** (дескрипторы динамических массивов: общее правило `{<T>Array array; size_t capacity}`, базовый дескриптор без capacity, отдельный дескриптор на тип элемента в своём массиве арены, void-дескриптор один; раскладка `Lmx` — отдельная работа; режим и очерёдность — координатор; все правки автора — в план). План §1 :64–:72 (`a225cd1`). Выдан Grok -134 (единственный писатель ядра; runUuid 49ab9e59): инвентарь → документы RU/EN → код одним SHA (шесть мест + любые найденные) → гейты (selftest раскладки, rg-гейт capacity, мутанты). Вне ядра (транслятор/парсер/L3) — позже, теми же правилом. Sonnet -131 ч.2: решение координатора — несвязанный динамический вход читает каноническую ячейку callee как есть (0 до первого прогона), без извлечения литерала.
- 2026-09-23 ~21:00 — Sonnet -131 ч.1+2 ПОСАЖЕНЫ `246056e`/`3616700` (harness 218/218): §3 :208 закрыт; ч.3 (перечень terminal Structure-reference assignment :207 + ty40 :214) ещё должна; далее Sonnet -132 (пустая Structure :187/:191–:193 + находки Opus 1–2). Grok -134 BLOCKER по чанку решён (байтовый динамический массив, пул runtime-generic) и ПОДТВЕРЖДЁН автором («оставляйте наиболее общий код», замеров не надо). Автор объяснён по устройству арены: пул на тип, чанки — блоки одного типа, импорт — `owner` в интервале.
- 2026-09-23 ~21:40 — Opus -133 ПОСАЖЕН `a3bc1c6`/`1abafa4` (harness 222/222, build_l2src 250, L3 11/11): (1) `lmx_walk` без CALL-only unwrap (§2 :159 закрыт); (2) S1.1 — одна нумерация статусов (merge = 1, implements = 2), неотловленный throw ставит `running = 0`, X1-инварианты через `l2_emit_invariant` + abort, попутно исправлен trailer-как-статус; решения D-a…D-e приняты. Выдан Opus -135 (S1.2: `throws:` в шапке, `throw: Name(args)`, статическое правило «отловлено или перечислено», «unhandled throw in entry», перекодировка позиций; S1.4 при малом объёме). Счётчик: **142 открытых / 81 закрытых**.
- 2026-09-23 ~22:40 — Sonnet -131 ч.3 ПОСАЖЕНА `7b64331` (перечень terminal Structure-reference assignment; 6-й пробел own-поиска исправлен; ty40 fail-closed; harness 224). Новый пункт §3: пробел порядка проверки (nsty заполняется при проверке E, E — последняя). Opus -135 (S1.2) ПОСАЖЕН `9f299a5` (harness 239): `throws:` в шапке, `throw:` с payload, статическое правило «отловлено или перечислено», у E — отказ (чтение A по Q13; моя runtime-строка в тикете была ошибкой), intern с именами, S1.4 совместимость; §3 :210 закрыт, :213 — сторона throw. Выдан Opus -136 (S1.3 catch pads + обязательство runtime-свидетеля позиций + расширение payload). Sonnet -132 STARTED. Счётчик: **142 открытых / 83 закрытых**.
- 2026-09-23 ~23:10 — Sonnet -132 ПОСАЖЕНА `7cc4310` (пустая Structure тремя записями через путь `Model: fresh` без модели; блочная запись объявления = вертикаль с одним атомом; дубликат типа закреплён; вторая молчаливая строка при P0-отказе устранена; harness 244). §3 :187/:191–:193 закрыты. Выдан Sonnet -137 (предпроход объявлений по образцу `l2_merge_scan` для пробела порядка проверки + освобождение `_g`-обходов; возврат named Structure через обычный `@: Lmx` ABI; присваивание результата predef C-функции через таблицу predef). Счётчик: **138 открытых / 87 закрытых**.
- 2026-09-23 ~23:50 — Opus -136 (S1.3) ПОСАЖЕН `b2667dd`/`b0b4158` (harness 261/261): catch landing pads, связывание payload через runtime implements, порт t2 из книги, runtime-свидетель «объявленная позиция ≠ неявная» (`unit_s1_catch_declared_vs_merge`), попутно segfault на анонимном `---` исправлен. **Фаза A3 (S1) закрыта.** §3 :211–:214 закрыты; остатки S1.5 в :213. Дефект книги (t2: вызов внутри handler) → Codex FABLE-CODEX-BOOK-T2-20260923-01 (pipe Codex снова лежит — очередь у почтальона). Grok -134 RESULT — НЕ посажен: плоские типизированные дескрипторы вместо композиции из фиксированных, чанк `void` вместо byte → переделка. Выдан Opus -138 (read-only аудит стадии «Исполнение именованной Structure» §3 :220–:229 + план коммитов). Счётчик: **134 открытых / 91 закрытых**.
- 2026-09-24 ~00:40 — Opus -138 (аудит стадии «Исполнение именованной Structure») принят: `steps/named-struct-audit.md` (`b814d75`), план коммитов 1–6; C1 (какой Frame закрывает `return`) и Q-138-2 (перезапуск объявлений при исполнении) → автору `q19.md` (рекомендации Б и 2). Выдан Opus -139 (коммиты 1–2: голый атом-оператор — чинит segfault транслятора; контракт получателя — `g(m)` исполняет, `sub` в значение — отказ, trailer `return: m` = путь тела, `S()` при существующей S — присваивание). Grok -134: rework 9a093d9 композиция верна, но 8 fnptr-типов «сброшены» в `@: void` ради таблицы 64 имён l1trans → review 2 (поднять таблицу до 128/8192, перепинить l1trans через self-build); Grok перезапущен после самообновления — рецензия переслана повторно.
- 2026-09-24 ~02:00 — Q19 закрыт целиком (автор): Q19.1 — `return` закрывает любую callable-Structure, включая именованную; голый `return` без значения; Q19.2 = 2 (тело целиком, состояние публикуется в граф, путь `Counter
` не исполняет даже метод; именованная не-`fn` значения не возвращает); Q19.3 = А1 (таблица управляющих голов); объём правки P0 сужен автором до «разрешить голый return как trailer именованной Structure». Grok -134 ПОСАЖЕН `657be19`…`f0837c7` (дескрипторы по правилу Q18, таблица l1trans 128, перепин 5292FC42, все гейты). Sonnet -137 ПОСАЖЕНА `4f50a90`…`790258b` (предпроход объявлений, возврат named Structure, результат predef; harness 265). Новое правило автора: `steps/defects.md` — дефекты отдельным списком, чинить сразу (`d21f3f5`); выданы Grok -140 (D-03/D-04/D-11) и Sonnet -141 (D-15/D-10/D-13). Новая задача автора в плане §7 (`e6dadcd`): формирование аргументов callable до точной сигнатуры (§6/§7.13) и `return` вложенного callable через merge (makeAdder). Opus -139 (к.1 сделан 1d8b45c, к.2 в работе; к.3–4 разблокированы).
- 2026-09-24 ~03:30 — Sonnet -141 D-15 (`967c24d`: `int:`/`unsigned:`/`ulong:` поля именованной Structure) и D-10 (`d321575`: избыточный второй own-поиск удалён, 514 .c байт-идентичны) посажены; D-13 в работе. Автор: `string` = дескриптор `{size_t len; char *data}` с точной len без NUL → имя `LmxCharArray`, дублёр `LmxByteArray` удаляется (Grok -140 первым коммитом); примитив char в пуле — значениями, дескрипторов нет. Закрыты по свидетельствам: §2/§3 семь строк равноценности форм (`c289b77`), §6 пять строк матрицы (`95fc32a`). Выдан Sonnet -142 (A1-аудит §4/§5 + §6 :324 — факты для дизайна B1). Счётчик: **127 открытых / 104 закрытых**.
- 2026-09-24 ~04:10 — Grok -140 ПОСАЖЕН `6bb6b4c`/`78c92f1`/`012e327`/`99de444` (`LmxCharArray`; L3/walker без молчаливого OK; арность в walker; `tmp_unitroot_test` удалён); D-20 (NUL-строки в трансляторе) в списке. Выдан Grok -143 = **фаза B2** (матрица §6: сетка {absent, existing non-callable, callable, path} × {primitive, Structure, Array, callable} в трёх формах, физическая операция/идентичность/dirty/диагностика; пустая Structure одним именованным значением vs пустой список; parity лишнего аргумента) — только фикстуры, дефекты в список.
- 2026-09-24 ~05:30 — Opus -139 к.1–2 ПОСАЖЕНЫ `f33a1a9`/`cbc97c7` (голый атом через один резолвер, segfault устранён; контракт получателя: `g(m)` → 7, `sub` в значение — отказ, `f()` на Structure — присваивание с admission; harness 283). Grok -143 ПОСАЖЕН `b2dd090` (матрица §6, 16 фикстур; D-23 `f(x: ())`). Sonnet -144 ПОСАЖЕНА `43d876c`/`6cc6c9f` (D-21 молчаливая потеря данных при `@ buf[i]`; D-22 узкий отказ адресной арифметики; D-24 — полная арифметика по §18.2 требует вывода типа выражения). Harness 301/301 на `2f8c3f2`. **Фаза B2 закрыта** (§6 :314–:316 [x], :317/:319 частично). Opus — D-08 + коммит 3 (P0 голый `return` trailer именованной Structure) → к.4; Sonnet — -142 аудит §4/§5 (возобновлён); Grok — -145 **фаза C инвентарь** (read-only). Счётчик: **121 → 118 открытых / 113 закрытых**.
- 2026-09-24 ~06:30 — Sonnet -142 (аудит §4/§5) применён `c64b616`: 14 галочек; один алгоритм occurrence → путь существует ровно один раз (трансляция), интерпретатор — по позициям; B1 как дизайн не нужен; остаток — корневая `test\[N]arg` → Sonnet -146 (+ D-25 сироты, проверка 8 «двойных попыток» из инвентаря). Opus к.3 ПОСАЖЕН `c8e5247` (P0: голый `return` закрывает именованную Structure; таблица управляющих голов; перепин l1trans 016DA755; parser current 38/38); D-08 `0ded44c`. Opus к.4 — вопрос раскладки автору `q20.md` (Б: безымянная METHOD-строка транслятора / Д: child 0 = callable у экземпляров, ABI ядра); пока Opus -147 = `l2_eval_discard` (§3 :265/:270). Grok -145 (инвентарь GATE, read-only): 73 VIOLATION / 39 ALLOWED / 96 DEAD (список DEAD ненадёжен — API ядра вызывается из сгенерированного C и по fnptr); выдан Grok -148 (ядро: снять переходный METHOD-fallback в `lmx_call`; уточнить и удалить подтверждённо мёртвое под полным гейтом). **Фаза C открыта.** Счётчик: **103 открытых / 128 закрытых**.
- 2026-09-24 ~09:30 — Посажены: Sonnet -146 к.1/к.2 (`4781bfe`, `298ef1e`: корневая `test\[N]arg`, сироты D-25 закрыты, 8 «двойных попыток» — ложные срабатывания), Grok -148/-150 (`37fc590`, `3f4e5f1`/`7facf77`: 11 мёртвых функций ядра, продьюсеры на дескриптор, `lmx_call` descriptor-only), Opus -147 целиком (`5a77cb2`, `8131ace`, `c12c003`: `l2_eval_discard`, ячейки int/unsigned/ulong-полей (D-15 по-настоящему — свидетель -141 был пустым; правило «свидетель не может быть пустым» в `steps/current.md`), D-05/D-06 на X1). Автор: Q20 = Д, затем **СТОП по merge callable-Structure** («методы таким образом мержить нельзя») — коммит 4 и Q21 заморожены; `q20-next.md` — измерение (скопированный merge метод читает `node`-поля по запечённым индексам исходного родителя, entry 81; D-29) и два прочтения I/II. Выданы: Opus -151 (GATE транслятор: реестр P0, name-тесты, мёртвые функции), Grok -152 (ловушка `lmx_merge_profiles_owned`, `os:`-фикстура, измерение переносимости дескрипторов в арену), Sonnet — D-28 после -146. Счётчик: **90 открытых / 141 закрытых**.
- 2026-09-24 ~10:30 — **Правило автора о merge принято** (план §4 «Merge: правило автора», `71695c5`): простыня + скрытый `offset` (в дескрипторе копии) + `merged\x` = последний occurrence + методы копируются, не мержатся + callable-Structure интерпретируются (тело — op-дерево) + makeAdder через merge метода в Structure + parent всегда переписывается. Q20/Q21 закрыты, стоп снят, коммит 4 заменён. Тикеты: Grok -153 (`offset` в `LmxCallable`, запись при merge, accessor; runUuid af4b989e), Opus -154 после -151 (пролог читает offset, `node\x` от offset, `merged\x` = последний; свидетель `q20-next.md` §2). Далее: интерпретация callable-Structure (эмиссия op-деревьев) и makeAdder. Счётчик: **94 открытых / 141 закрытых** (+4 пункта правила).
- 2026-09-24 ~12:30 — **Codex закрыт (лимиты) — правки доков сделаны мной** (`7e1ba7c`): книга §fields/§10/§composition + CORE §3.1 — неуточнённое имя = ПОСЛЕДНЕЕ вхождение (`[lastIndex]`); merge-абзац (простыня, методы копируются, `offset` в дескрипторе копии, parent переписывается, возвращаемый вложенный метод через merge) + L2-спека §13/§11; правило уровней (Structure — L3, L2 — только в телах методов, L3 ⊂ L2) + **дополнение автора: `@` любой глубины в L3, но только как receiver объявления** (книга §scope + таблица `@`, L2-спека §1, CORE §1); именованная Structure — только голый `return`, путь `Counter\n` не исполняет; §14 t2 по оригиналу (D-14 FIXED, F-32). Посажены на интегрированном дереве: Grok -152 к.1/к.2/к.3 (`3686971`, `914a8c1`, `932e616`: D-07 FIXED F-31 — tap `lmx_merge_profiles_owned` + `unit_s1_merge_profiles_uncaught`; `unit_os_prologue` пинит X1-пролог `l2_emit_os_fn`; `steps/arena-typed-descriptor-measure.md` — ни одно из пяти мест не переносится в typed array арены без смены владения), Opus -151 к.1 (`2bbe8a0`: D-16 FIXED F-33, пин → `F70407F5…`). Гейты: self-build 8/8, parser 42 / 131 (8 diverged), harness 320/320, build_l2src 251, L3 11/11, check_docs, diff --check. Opus -151 Q2 (P0-атом `c.sizeof` — единственное имя-специальное правило C-поверхности P0): P1 в -151, P2 — отдельный тикет после к.2 (план §7a). Счётчик: **95 открытых / 141 закрытых** (+1 P2).
- 2026-09-24 ~13:30 — **Правила автора: пользовательский код интерпретируется по умолчанию; дескриптор без нативного адреса ⇒ интерпретировать** (`23f7a96`, план §3 новая подсекция, блог дословно): корень файла — интерпретируемый L3 даже в `.lm2`; нативно — только внутри методов; флаг режима `lmx_thread` (81 упоминание в 6 файлах, измерено) удаляется; диспетчеризация только по дескриптору (`addr = 0` ⇒ walker). Следствия приняты: S2 «unit = вхождение E» заменяется на «корень = Structure с дескриптором без адреса»; `return: N` в корне отпадает (Q19.2) → свидетель `field K N` по позиции; L2 в корне (~20 фикстур) — в методы. Тикет Grok -156 (ядро: диспетчеризация по `addr`, удаление режима; runUuid f5c539d0) после -153; транслятор (эмиссия op-дерева корня, отказ L2 вне метода) — Opus после -154/-151 к.2; harness `field K N` — вместе с транслятором; `implements` — после порта §7. Дополнение автора к уровням: `@` любой глубины в L3 — только receiver объявления (в книге/спеке/CORE, `7e1ba7c`). Счётчик: **99 открытых / 141 закрытых** (+4 пункта новой подсекции).
- 2026-09-24 ~14:15 — Автор подтвердил оба следствия («да, оба следствия верны, продолжайте по плану»); уточнение: в Structure `return: N` никогда не было, **exit code = финальный `l2_tN`** (последнее вычисленное выражение корневого тела; `1` + голый `return` → 1) — план §3 подсекция поправлена (без `field K N`, свидетель `entry N` остаётся). Посажены Opus -151 к.2/к.3 (`5bc8bce`, `bb43ecd`; интегрированное дерево: L3 11/11 — гейт, который Opus не смог прогнать из-за нехватки памяти, harness 323 = 320 + 3, build 251, check_docs, diff --check): `sizeof:` принимает тип-фрейм (`sizeof(@: T)`, одна таблица `l2_prim_type_word`), оба `c.sizeof`-скипа транслятора удалены (мутант check-скипа RED 1, scan-скип — измерен как ненаблюдаемый), 15 строк Mixa + `library_sizeof_ptr_forms` мигрированы, фикстура `unit_sizeof_type_frame` целиком в методах (по правилу уровней), 13 мёртвых функций + 2 сироты удалены (578 входов транслируются байт-в-байт, 492 функции живы). Opus дальше: -154, затем P2.
- 2026-09-24 ~15:00 — **Q22 автору** (`q22.md`, `f667cc8`+`3f24187`, бипер): Opus нашёл дыру в правиле `offset` — `A: fn: M` сегодня ссылка на unit-метод, `node\tag` в `M` разрешён по раскладке unit, свидетель q20-next §2 проходил по совпадению индексов; чтения I/II/III, рекомендация II (ссылка не копируется, `node` остаётся unit, `R\M()` = 3; `offset` — только для методов, объявленных внутри Structure); Q22.2 — область правила «неуточнённое имя = последнее» (все повторные имена, как записано в книге, или только merge). Посажены Opus -154 к.2 + инвентарь (`7f0d9f4`, `16c90d5`; интегрированное дерево: L3 11/11, harness 325 = 323 + 2, build 251, check_docs, diff --check): `merged\x` = последнее, `merged\[N]x` по операндам с отказом вне диапазона; к.1 держится до Q22 + -153. Grok -153 ещё не доставлен; -156 STARTED (в очереди за -153).
- 2026-09-24 ~15:45 — Посажен Grok -153 (`5214386`; интегрированное дерево: build 252, L3 11/11, harness 325, check_docs, diff --check): `LmxCallable.offset` (size_t), accessor `lmx_callable_offset` (sentinel `(size_t)-1` для отсутствующего child 0), merge пишет offset = сумма ширин предыдущих операндов через COW-клон дескриптора (общий дескриптор сохраняется при offset 0), parent переписывается только когда родитель — копия операнда/тела; selftest `lmx_callable_offset_selftest` (мутант COW RED 3). Ядро D-29 закрыто. Замер Q22 на этом дереве (`q22.md`): программа Q22 — entry 90 (мусор, D-33) до и после; q20-next §2 — было 81, стало X1 «check 78» (D-30: инварианты транслятора vs COW; Opus -154 к.1). Ревью -153: D-31 (дублированный блок COW), D-32 (комментарий accessor’а обещает классификацию, кода нет) → Grok -156 к.0.
- 2026-09-24 ~16:30 — Opus P2 замерен (план §7a, `e7ef416`) → тикет Opus -157 (к.1 одна эмиссия `c.sizeof(unsigned long)` + миграция `unit_ptr_grow`; к.2 удаление P0-атома в трёх копиях + seeds + пин + goldens, свидетель `c.sizeof(x)` над own x → 7). Sonnet: D-28 стартовал (не был начат — ждала назначения); тикет -155 (name-specials транслятора: зашитый список адаптеров в `l2_is_known` :8710–:8713 — галочка §7a ложна, подстрочные type-arms :14856–:14860, `c.array`-декларатор :5037/:5047, безусловный stdio) ранее НЕ был отправлен — отправлен сейчас, старт после D-28. Q22/Q22.2 у автора; Opus -154 к.1 держится.
- 2026-09-24 ~17:15 — Посажен Opus -157 (`abc03ff`, `0b6322b`; **пин L1 → `AC4A2210`**; интегрированное дерево: self-build 8/8, parser 42 / 131 (8 diverged), L3 11/11, build 252, harness 327 = 325 + 2, check_docs, diff --check): P0-атом `c.sizeof` удалён (три копии + двойник порта + драйвер порта, seeds), 155 деревьев меняются, C-выход l1trans байт-в-байт; эмиссия `c.sizeof(unsigned long)` → `c.sizeof(l2_ulong_probe)`; `unit_ptr_grow` → `sizeof(unsigned)` + строка; свидетель `unit_csizeof_operand_lowered`. §7a P2 закрыт. Opus свободен до Q22 (к.1 -154 держится, несёт D-30).
- 2026-09-24 ~18:45 — Посажены записки Opus (`11d4132` root-walk-coverage, `14fcad4` root-walk-trampolines) и решения по «интерпретируемому корню» (план §3: prim-ABI трамплин на каждый метод, nargs-расхождение = X1, статус = собственная S1.1-нумерация callee, walker пропускает поля-объявления; порядок -156 → -158 → -159); Sonnet D-28 посажен (`d762a6f`, `c6cd6cc`; harness 328 = 327 + 1, build 252, L3 11/11): формал первым в `l2_path_root`, вывод типа из имени отвергнут и удалён, 2 фикстуры на `@(Lmx Model)` переписаны — D-28 FIXED (F-34). Sonnet -155 STARTED (к.1 замер: список `l2_is_known` смешанный — часть имён дублирует predef, часть фикстур без predef опирается на список).
- 2026-09-24 ~19:30 — Посажены: Sonnet -155 к.1/к.2 (`fa6e620` замер; `40a3640`: `l2_is_known` = только дверь `c.*` + `l2_predef_has_function`, зашитый список 11 имён удалён, 4 фикстуры получили `predef: "l1src/own.h.lm1"` (новый prototype-only заголовок в песочнице), негативный свидетель `unit_own_missing_predef_refused` («unknown method»; мутант — список восстановлен → строка зелёная = RED), новые находки D-34/D-35); **Grok -156** (`ee7085e` к.0: D-31/D-32 FIXED F-35, D-33 измерен — граница у транслятора; `fc84018` к.1: диспетчеризация по дескриптору, `addr = 0` → walk через хук, значение = последнее выражение; `9782b30` к.2: режим потока удалён, 154 → 0 упоминаний, один ход; интегрированное дерево: build 252, L3 11/11, harness 329, check_docs). Хук `lmx_call_install_walk` — стык компоновки (D-36, позже — прямой вызов). Тикет Grok -158 (G1 ядро: op вызова зовёт нативный дескриптор через prim-ABI, факты 1–3 Opus; G3: walker пропускает поля-объявления; G2/статус к ходу) выдан.
- 2026-09-24 ~20:15 — Посажены Sonnet -155 к.3/к.4 (`e02b987`: подстрочные type-arms + отдельная 4-именная проверка `l2_need_own` с автоинъекцией `prototype:` удалены — измерено, что инъекция дублировала predef-строку unit’а; пин строки `unit_bad_sizeof` (Absent — инъецированная сигнатура), мутант RED; `655f2de`: `l2_array_local` + 5 мест вызова удалены, `c.array:` в L2 — обычный `c.*`-фрейм, 0 фикстур; интегрированное дерево: harness 330, build 252, L3 11/11, check_docs, diff --check). **-155 закрыт:** зашитого списка адаптеров, подстрочных type-arms и L2-декларатора `c.array` в трансляторе нет; `stdio.h` — намеренный маппинг (документировано). Счётчик: **95 открытых / 145 закрытых**. Grok -158 STARTED. Sonnet → D-35.
- 2026-09-24 ~21:00 — Посажен Grok -158 (`ec7b86f` G1, `2873b4f` G3, `a6e90a5` G2; интегрированное дерево: build 252, L3 11/11 (EXPECT 71), harness 330, check_docs, diff --check): walk зовёт нативный `LmxCallable.addr != 0` через prim-ABI и отдаёт ненулевой статус как throw (S1.1), примитивы ядра — по-прежнему `LMX_WALK_PRIMITIVE` (различение по записи дескриптора); поля-объявления пропускаются; статус throw из walked-тела останавливает Message (`running = 0`). Ядро для «интерпретируемого корня» готово; тикет Opus -159 (трамплины → op-дерево корня → срезы) выдаётся. Sonnet -160 (D-35/D-34) выдан.
- 2026-09-24 ~21:45 — Посажен Sonnet -160 (`3a18970` замер, `dc4714e` D-35: результат predef-функции несёт тип из `prototype:`, `77d3e5c` D-34: `c.LmP0*`; интегрированное дерево: harness 331, build 252, L3 11/11) — F-36. Новые дефекты: D-37 (канал walk смешивает ошибки walker’а и номера throw — Opus при чтении -158; → Grok -161: ошибки = X1, канал = только throw), D-38 (`lmx_call0` (node, self)-каст — UB после трамплинов; Opus -159 к.1 вводит `lmx_call_prim` в `lmx_call.lm1`, разрешено), D-39/D-40 (Sonnet: динамический индекс через member-путь; две диагностики на одно правило), D-41 (слово типа → код рассыпано по ~9 местам; Sonnet -162). Opus -159 к.3 (статусы) ждёт -161.
- 2026-09-24 ~22:30 — **Пересмотр автора: Q21 = А, «файл это Structure» (Q22 = II), Q22.2 = A** (`eb20f2c`, `e31776c`, `c8f3c96`; блог дословно): merge — специализация модели (совпадающее поле → слот модели, новые — в конец, раскладка модели не сдвигается, `offset` не нужен); `A: fn: M` — ссылка на вхождение метода файла-Structure, при merge не копируется, `node` не меняется; неуточнённое имя = последнее вхождение — общее правило (и для повторных объявлений в теле). Книга §fields/§10/§composition, L2-спека §11/§13, CORE §1/§3.1 переписаны; план §4 пересмотрен. Отменено: Opus -154 к.1; -153 (`offset`/COW) откатывается тикетом Grok -161 (после Opus -159 к.1: карта позиций от транслятора, ссылка на метод — терминал копира, D-37); далее Opus: эмиссия merge с картой позиций, `test\arg` = последнее. Sonnet -162 к.1 посажен (`a54d0fe`: 9 мест — минимум 7 пространств кодов; к.2 — свернуть две согласованные группы + D-42 `LmxMsgBlock` fall-through, узкие пространства — в GATE-инвентарь).
- 2026-09-24 ~23:55 — Посажены Opus -159 к.1 (`9a66ce6`: prim-ABI трамплин на каждый метод = `addr` дескриптора, `lmx_call_prim`, `lmx_call0` на prim-ABI, 6 динамических мест транслятора → остаток S1.2 закрыт, 24 natives в 18 selftest’ах ядра; D-38 FIXED F-38; harness 332, budget 70) и Sonnet -162 к.2 (`9387371`: две группы кодов типов свёрнуты, D-42 `LmxMsgBlock` fall-through починен, 553 входа байт-в-байт; F-39; -162 закрыт). Решения по -159 к.2: значение хода — поле записи `LmxThread` (`turn_value`, accessor `lmx_thread_turn_value`), не процесс-глобальная заметка; 16 throw-ABI строк — root-pending до -161 (D-37); Grok -161 — после к.2 Opus (один писатель `lmx_call.lm1`/`lmx_walk.lm1`). Счётчик: **95 открытых / 147 закрытых**.
- 2026-09-25 ~00:30 — Посажен Sonnet -163 (`f7d896c`/`d89ef65`: D-40 FIXED F-40, D-39 измерен — 9 мест; harness 333); Sonnet -164 (Q22.2 = A на пути -146: неуточнённый путь = последнее вхождение) выдан. Доки: правило интерпретации по умолчанию в книге §scope и L2-спеке §1 (`9c4f996`). **Автор (вечер): код завершения — через родителя R0** (host-корень принимает `exit(exit_code; stdout; stderr)` или отображает `success` R0; при success = 0 — дамп усыновлённых арен; блог `3f60200`): Opus -159 к.2 припаркован (WIP `99bf213`), к.2h (host-корень) в работе, вопрос автору — написание отправки (`send:`); Grok -161 выдан (откат offset/COW, карта позиций, ссылка-терминал, D-37 + F1/F2/F3 = D-43/D-44/D-45) параллельно с раздельным владением файлами.
- 2026-09-25 ~01:20 — **Ответы автора:** K1 = да (итератор по ящику, старая спека §yield), K2 = да, K3 = а (Erlang, письмо `(sender; payload)`), K4 = а (транспорт outbox на границе хода), Q1 = одна настройка host — рабочая директория (`LMX_blog/2026-09-25.md`); L2-спека §8.1/§9 дописаны (`c2831dc`). Посажены: Grok -161 к.0 (`27e7b01`: откат `offset`/COW, D-30 по построению) и к.1 (`207361e` + whitespace fixup `d674870`: `LmxMergePair`, карта позиций «слот модели ← поле операнда», null = дописывание; авторизованный null-pass в 4 местах эмиссии l2trans + драйверные tap’ы; 554 входа: 308 идентичны, 67 отличаются только null-картой; harness 334, build 252); Sonnet -164 (`f4ac8e5`: 22 места на «последнее вхождение», честный свидетель; находка: повторное типизированное объявление — отказ «duplicate declaration» (правило 4 S2, самодельное) против книги → **Q24** автору; D-47 `l2_own_seg_scan` первое с уровня корня → Sonnet -165); **Opus -159 к.2h** (`da40aa3`: host-корень по форме H1 — `lmx_root_launch`: host = Thread `lmx_root_open`, R0 — ребёнок `lmx_child_reserve_profiled` с result-ячейкой, настройки host (рабочая директория, `lmx_root_os_cwd`) — R0 общей ссылкой (свидетель: тот же адрес, view с домом в арене host), письмо `mainArgs` через service, один ход R0 из manager’а, приём `exit` `(sender; payload)` по модели из собственного inbox, иначе отображение `success`, при 0 — отчёт с графом R0 из result-ячейки + усыновлённые массивы; `lmx_root_host_selftest` 36/36, 8 мутантов RED; build 253, harness 334, L3 11/11). Готово для 2b: `l2_program_build` → `LmxRootProgram`, main → `lmx_root_launch(...)`, `sendMessage: X` родителю = `lmx_service_post(..., lmx_thread_parent(t), (sender; X))`, `entry N` драйвера → код launch. D-46 снят формой H1. Заметка на потом: `lmx_root.h` называет Thread `lmx_root_open` «R0» — под launch это host (правка доков после 2b).
- 2026-09-25 ~02:10 — **Интерпретируемый корень посажен: Opus -159 к.2b** (`5b1a4be` + `1aeb9df`; 188 файлов; harness 334 = eternal-runs 70 / root-pending 122 / refused 117 + 25 прочих, build 254, L3 11/11): корень — op-дерево walker’а под host-запуском (`lmx_root_launch`), `sendMessage: exit(...)` вместо `return: V` (310 хвостов), драйвер читает код launch, `-Werror=implicit-function-declaration` в kflags (первый прогон поймал неявный `lmx_clock_ticks` в библиотечном профиле → fixup). Также посажены Grok -161 к.2 (`2cbe790`/`de368b5`: ссылка на метод — терминал копира, check 77 = тот же адрес, q22 → 3) и D-45 (`bbce046`: поле с parent 0 — объявление). **Автор:** Q24 = A + уточнения (повторное объявление с другим типом — новое вхождение; инициализатор читает предыдущее; разруливает транслятор, разнотипное значение — через конвертацию §7, не C-каст) → D-48 Sonnet -166 к.3; merge методов = интерпретируемый результат (подтверждено; книга/L2-спека/план `f24ce95`). Далее: Grok -161 к.3 (D-37/D-44) → тикеты walker-ops по классам (не-int 51 → бросающие 20 → …); Opus -159 к.3 после -161 к.3; Sonnet -166.
- 2026-09-25 ~03:00 — Посажены: Grok -161 к.3 (`33bb554`→`df7aa21`: D-37/D-44 — ошибки walker’а X1, throw = THROWN+k; F-41; D-29/D-30 закрыты F-42), Sonnet -166 (`afd437a`/`540c7fe`: `receiveMessage`, D-48 повторные объявления, 3 спящих бага), Grok D-49 (`99db452`: запись примитива — терминал копира; F-43), **Opus -159 к.3** (`cb215cf`: бросающие вызовы в корне — 17 из 20 строк, вечные ветви 7 строк, драйвер наблюдает Thrown/Stopped через launch; **harness 337 = 95 eternal-runs / 98 root-pending / 119 refused + 23**). -159 ЗАВЕРШЁН. Далее: Grok D-43 → -167 (typed values, часть A) → часть B → классы (блоки, implements, массивы, catch-роль); Sonnet -168 (`sendMessage` в методах, `Ref X`); Opus — транслятор по классам вслед за ядром.
- 2026-09-25 ~04:00 — Посажены: Grok D-43 (`09854d3`+fixup: пул Post отвязывается при sweep; F-44; -161 закрыт целиком), Sonnet -168 (`569fa09`: `sendMessage` в телах методов через тот же генератор `l2_msend<k>`; `Ref X` — блокер типа отправителя), Opus -169 (`2e8d1fb`: анонимный блок в корне = IF(LIT 1, body); eternal-runs 96), Opus -173 (`dce979e`/`4bcfbab`: письмо `mainArgs` = `(sender; (mainArgs))`, зеркало selftest; форма письма зафиксирована в `steps/mail-wrappers.md` `6ea9960`), Grok -167 часть A (`60c4e54`/`6dea450`: типизированные ячейки walker’а, одно-типные ADD/SUB/LT/EQ, IF/WHILE по любому ненулевому). Решения: отправитель — значение `@: LmxMsg` (адрес Message = адрес Thread), поле-ссылка `@: T name` в именованных Structure — общая ветка (пробел нормы найден Sonnet), модель пользователя описывает payload (поле 1). Выданы: Grok -174 (Structure-поля walker’а, часть B), Opus -175 (типизированные числа в корне, 23 строки), Sonnet -172 (`@: T` поля, `m\[1]` = payload, `sendMessage: Ref X`). main `6dea450`: harness 338, build 256, L3 11/11.
- 2026-09-25 ~05:10 — Посажены: Opus -175 (`828121c`: типизированные числа в корне, литералы по месту, смешанные типы — отказ; eternal-runs 101; замер: класс «не-int» = 28 Structure-полей + 9 typed CALL dest + 1 конвертация), Grok -174 к.0/к.0b/к.1 (`af74bbc` D-50 знаковое LT для int + D-51 ширина size_t — F-45/F-46; `cc39985` строки проб; `6e22cba` DEREF), записка Opus по implements/receiveMessage (`31f8763`), Opus -176 (`2b8d51e`: `implements(...)` в корне — литерал транслятора, приоритет объявленного `receiveMessage`; eternal-runs 110 / root-pending 85 / refused 121; `Letters` снят из 20 строк). Решения: throw допуска аргумента — номер вызывающего на месте вызова (d_caller + 2). main `2b8d51e`: harness 341, build 258, L3 11/11. Очередь Grok: -174 к.2–к.4 → -177 (take-примитив, PUT/EQ null-ссылок) → -171 (catch) → -170 (`&&`/`||`, `* / %`); Sonnet -172 в работе.

## Уже закрыто кодом, но не отмечено (кандидаты первого прохода A1/A2)

- §2: discard data-атома (T2/-99), голый `f` (-92), `LM_P0_FRAME_*` не читаются транслятором (COMPACT 0 reads с 14bdac3). **Не закрыто, а пересмотрено автором 2026-09-23:** равноценность трёх форм — это одно P0-дерево для всех голов (`next_parser_fix.md`, коммиты Codex e3bc5a1/cb9c3ac); аксессор `l2_call_args` (-92) и материализация 9c166ff — временные CALL-only решения, которые после P0 parity удаляются (план §2 «Исправления P0», §6 :265/:270). Фаза A1 включает этот тикет первым.
- §3: приоритет по разрешённой привязке без исключений по имени (S2 `l2_unit_role`), `Model: fresh` как объявление (-105/S2), запрет присваивания callable-полю, universal literal range (-106), типовая позиция (-106/-120), объявление по модели внутри метода (-105).
- §7a: `c.puts` (-99), поля C-структур (-120), `FILE` (-111), включения из директив (-123 P1); `sizeof:` — после -123 P2.
- §8: письмо с аргументами и `receiveMessage:` (бывший `nextMessage:`, переименован Sonnet -166; S3 part 1, fd7e8fc), граф ребёнка R1 (-110 selftest).

Каждый такой пункт всё равно проходит шаг 1 протокола — галочка по свидетельству, не по этому списку.
