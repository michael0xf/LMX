# A leaf handle of the include-reach fixture: it declares one incomplete struct and
# reaches nothing.  The pair used to be mutual under include guards; L1 refuses a
# predef cycle, so the reach is carried by the outer header.
type: L2TestInner struct L2TestInner
