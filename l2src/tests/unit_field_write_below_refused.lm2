# FABLE-SONNET-UNIT-FIELD-WRITE-20260923-129 negative: a unit field declared
# BELOW the method that assigns it is not yet backward-visible (the author's
# norm is backward visibility, never forward) -- refused with a location and
# the name, not silently. l2_own_unit_seen's own l2_vis_ok(l2_own_decl[oi])
# check is what l2_check_body already reads for this; this fixture pins that
# a declaration position AFTER the method is not an exception.
fn: bump () int
    y: 7
    return: 0
end: bump
int: y 5
bump()
return: y
