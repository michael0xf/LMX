# Negative: non-c.* unresolved compact must NOT become a call via the raw door.
# Bare unknown head hits the early unsupported-body gate (not widened to dotted/unknown).
fn: main () int
    bogus()
    return: 0
end: main
