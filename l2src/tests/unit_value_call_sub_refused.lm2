# A sub has no result, so naming it where a value is consumed is refused (plan §3 native gate
# (б)): it is neither called nor passed as a reference.  (Its occurrence reference used to arrive
# as the int.)
int: y 0

sub: s ()
    y: 3
end: s

fn: g (int: x) int
    return: x
end: g

return: g(s)
